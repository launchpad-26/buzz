# RQA gap register — business requirements

The 14 business requirements of the frozen specification, one row each. What the automation is for and who it answers to — the business obligations.
Sibling registers hold the other two classes:
[`br.md`](br.md), [`fr.md`](fr.md), [`nfr.md`](nfr.md).

**This file is the skeleton.** Every requirement of this class has a row, in the
order the specification lists them, with `gap degree`, `root cause`, `evidence`
and `depends on` set to `TBD`. A `TBD` means *not yet evaluated* — it is not a
degree, and it is not a claim that the requirement is met or unmet. The
evaluation lane replaces the four `TBD` cells of each row in place, using the
definitions in [`methodology.md`](../methodology.md); it does not restructure the
table, reorder it, add columns to it, or write a new file.

## Why the register is keyed by requirement ID

The requirement identifier is the only key in this analysis that is stable
across revisions. Files get renamed, split and deleted; clusters and disposition
units are this analysis's own constructs and will change when the estate does;
requirement IDs are fixed by the frozen specification, which
[`methodology.md`](../methodology.md) §1 forbids this analysis to edit. Keying on
the ID gives three properties the analysis depends on:

- **Completeness is checkable.** 14 + 39 + 33 = 86 rows against 86 `### RQA-…`
  headings in
  [`requirements-specification.md`](../../requirements/requirements-specification.md).
  A requirement with no row is a mechanical failure, not a judgement call.
- **A row is addressable.** Any later document, issue or decision cites
  `RQA-BR-NNN` and lands on exactly one row.
- **Re-evaluation is diffable.** Re-running the analysis at a later revision
  updates the same rows, so the diff shows what *changed about the assessment*
  rather than what changed about the document's layout.

## How a row is updated

The register is **updated in place, never superseded by a new document.** There
is one register per requirement class, for the life of this analysis.

1. Re-read the requirement in the frozen specification and the code it concerns
   at the revision being evaluated.
2. Apply the "implements" test ([`methodology.md`](../methodology.md) §2), then set
   `gap degree` (§5), `root cause` (§6) and `evidence` (§3) from what that test
   found. `root cause` is `-` when and only when `gap degree` is `fit`.
3. Set `depends on` to the requirement IDs whose gaps must close before this
   one's can (§9), or `-`. Never a priority, never a sequence number.
4. **Set `revision` to the short SHA the row was evaluated against.** The
   `revision` cell records when this row was last *evaluated*, not when the
   register file was last touched. A row still reading an older SHA than its
   siblings is a row nobody has re-checked — that is the cell's whole purpose,
   so it is only ever bumped by an evaluation that actually re-read the code.
5. Where the evaluation runs into something a person must decide, add
   `PENDING-HUMAN:<short-id>` to `notes` and leave the rest of the row at its
   honest value. An escalation never justifies changing the specification.

Rows are **never deleted**: the frozen specification defines the row set, so a
row disappears only if the specification does, and nothing in this analysis may
edit it. A requirement that turns out to be met needs its degree changed to
`fit`, not its row removed.

## Column meanings

| column | meaning |
| --- | --- |
| `id` | The requirement identifier from the frozen specification. The row's key. |
| `gap degree` | `fit`, `partial gap`, `full gap` or `conflicting`, as defined in [`methodology.md`](../methodology.md) §5. Exactly one, always present. |
| `root cause` | `-` for `fit`; otherwise `not built`, `built against superseded intent`, `built then orphaned` or `built contrary` — [`methodology.md`](../methodology.md) §6. |
| `evidence` | Disposition-unit IDs (`U-<CLUSTER>-NN`) and/or `path:line` citations relative to `launchpad/skills/review-queue-automation/`, at the row's `revision`. Never empty. A `fit` row never rests on a test or a document alone ([`methodology.md`](../methodology.md) §3). |
| `depends on` | Requirement IDs whose gaps must close before this one's can, or `-`. A technical precedence only — [`methodology.md`](../methodology.md) §9. |
| `revision` | Short SHA the row was last evaluated against. `9267b6308` throughout this skeleton. |
| `notes` | Free text; `-` when there is nothing to say. `PENDING-HUMAN:<short-id>` marks an escalation to a person. |

There is deliberately **no priority column**. Every requirement in the frozen
specification is mandatory, so ranking them is a planning decision this analysis
does not make ([`methodology.md`](../methodology.md) §9).

## Row order

Rows follow the order in which the specification lists the requirements of this
class — the order of the `### RQA-BR-NNN` headings in
[`requirements-specification.md`](../../requirements/requirements-specification.md),
which is thematic and **is not** ascending numeric order. This is the order the
mechanical conformance check requires, and it keeps the register readable
alongside the specification it mirrors. **Do not re-sort this table**: a
numerically sorted register fails the check.

## The split-AC rule applies to every row here

A source clause that produced several requirements is satisfied only when
**all** of them are satisfied. Never record or summarise a clause as covered
because one child row reads `fit` while a sibling does not
([`methodology.md`](../methodology.md) §8). Degrees are assigned per requirement;
nothing in this file is a clause-level judgement.

## What this class turned on

Class-specific notes from the evaluation pass, recorded so a later reader can
check the reasoning rather than re-derive it.

- **Two rows are `conflicting`, and they are the same behaviour seen twice.**
  `RQA-BR-010` and `RQA-BR-013` both derive from CL-017, and both are
  contradicted by one mechanism: in live approval mode, any failed gate — including
  the purely mechanical `checks_complete_ok`, `rate_limit_ok` and `audit_writable`
  gates — enqueues a human request, delivers a notification for it, and parks the
  job in `human_approval_pending` with nothing that re-drives it. `RQA-BR-013` is
  the interruption; `RQA-BR-010` is the parking. Each is `conflicting` because a
  reachable execution at this revision produces an outcome its own requirement
  forbids, so satisfying either means that execution stops producing it
  ([`methodology.md`](../methodology.md) §5). Sharing a mechanism is an
  observation about the two rows, not an ordering between them and not a
  dependency edge.
- **One row is `fit`, and it is scoped narrowly.** `RQA-BR-007` is `fit` against
  its own text — an unchanged head queues no second review — and is not a finding
  about `RQA-FR-005`'s stronger evidentiary obligation. `RQA-BR-011` read `fit`
  in the first pass and does not now: its own note conceded that the approval
  record carries no findings, and the justification offered for that — retrieval
  through the ledger — is not implemented, so the row is `partial gap`.
- **The absent finding category is this class's single largest cause.** No
  finding carries a mechanical-versus-substantive category anywhere in the tree,
  which is why `RQA-BR-005` is a `full gap` and why `RQA-BR-002`, `RQA-BR-005`
  and `RQA-BR-006` all record a contract dependency on `RQA-FR-008`.
- **Every dependency edge names one of §9's three categories, one edge at a
  time.** Each entry in a `depends on` cell is defended in its row's `notes` by
  its own sentence naming substrate, authority or contract in those words, so a
  reader can test the reason against that one edge without unpicking a claim
  made about several at once. An edge whose honest reason fits none of the three
  names is inadmissible and was dropped rather than relabelled; delegation of
  satisfaction from one row to another is not one of the three names.
- **Evidence tagged with a BR ID in wave 1 was not inherited unchecked.** Where a
  wave-1 claim carried one of these IDs but did not bear on the requirement's own
  text, this register cites what actually supports the row instead; the
  corrections are listed in this lane's handoff.

## Register

| id | gap degree | root cause | evidence | depends on | revision | notes |
| --- | --- | --- | --- | --- | --- | --- |
| RQA-BR-001 | partial gap | built against superseded intent | This row is satisfied exactly when the rows its fit criterion names are (requirements/requirements-specification.md:136). Trustworthiness is met by reachable code: U-VERDICT-16 (scripts/risk.py:236-262, the 22-gate conjunction) and U-AUTHORITY-06/U-AUTHORITY-07 (scripts/human_cli.py:216-220, a decision cannot be recorded without naming its actor). Auditability is met on the normal path (U-RESILIENCE-06 scripts/ledger.py:121-163, U-RESILIENCE-07 scripts/explain.py:53-81) but not in full: a review runs with no protocol identity when the config carries no policy section (scripts/config.py:30-44, scripts/dispatcher.py:1546-1548) and every ledger-write failure is swallowed silently (U-DISPATCH-20, scripts/dispatcher.py:202-218). Efficiency is not met: the recorded consumption is the pre-panel reservation with no field marking it an estimate (U-RESILIENCE-01 scripts/budget.py:217-230, U-DISPATCH-13 scripts/dispatcher.py:1323-1329). Consistency is this register's RQA-BR-002 row. | RQA-FR-011, RQA-FR-012, RQA-FR-013, RQA-FR-021, RQA-NFR-022, RQA-NFR-028 | 9267b6308 | Four obligations, one per quality; trustworthiness met, the other three unmet. Two of the three unmet (auditability, efficiency) are reachable code implementing an earlier intent — optional policy-as-data pinning, an estimate recorded as consumption — and one (consistency) is `not built`, so the cause is the two-count. Dependency cell, one sentence per edge. RQA-FR-011 is authority: a successful disposition is only permitted once the required-obligation gate FR-011 states exists, so producing one before it exists ships the outcome without the check. RQA-FR-012 is substrate: this row's auditability is read off the reconstruction record FR-012's behaviour produces, and nothing can be read off a record not yet written. RQA-FR-013 is substrate: the approving human and the basis of that approval are record elements FR-013's behaviour writes, and this row's trustworthiness is read off them. RQA-FR-021 is substrate: efficiency is measured against a recorded consumption figure FR-021's behaviour must first produce as a measurement or mark as an estimate, which is the same category this file gives RQA-BR-012's edge to that row. RQA-NFR-028 is substrate: the provenance record auditability rests on is the tamper-evident one NFR-028's behaviour creates. RQA-NFR-022 is authority: NFR-022 is the write-authority control over that provenance record, and an audit may not rest on a record any model output could have written. Three edges the fit criterion names dropped in round 2 as inadmissible: RQA-BR-002, RQA-FR-019 and RQA-FR-020 each restate one of this row's own qualities in functional terms — one consistent definition is consistency, and the two bounds on unrequired work are efficiency — and establish no record, gate or vocabulary this row's behaviour must act on. That the fit criterion names a row is recorded in the evidence cell; delegation of satisfaction is not itself a section 9 reason and produces no edge on its own. |
| RQA-BR-002 | partial gap | not built | Counted against the fit criterion's own six concepts (requirements/requirements-specification.md:149), three are pinned by one shared definition applied identically whatever ran the review and three are not. Pinned: review scope (U-DISPATCH-24, scripts/planner.py:129-137, identical inputs give an identical plan), review completion (U-VERDICT-12, scripts/panel.py:726-751) and final disposition (U-AUTHORITY-03, scripts/approval_evaluate.py:268-288); the execution-mode mapping is total over every registered strategy and supports the latter two rather than being a seventh concept of its own (U-POLICY-09, scripts/modes.py:136-152). Not pinned: the findings taxonomy, which does not exist at all — `grep -rn -e mechanical -e procedural -e creation-time scripts/ schemas/` returns exactly one line, `scripts/planner.py:125:#: mechanically narrower and does not need the deep code questions.`, a comment; required evidence, since validate_verdict checks that the required fields are present and well-typed but not the schema's per-finding minLength or location pattern (U-VERDICT-02, scripts/verdict.py:149-197 against schemas/reviewer-verdict.json:17-23); and blocking conditions, since the same validator never checks the severity enum that the blocking test then treats as an exact membership check (scripts/findings.py:162). | RQA-FR-001, RQA-FR-008 | 9267b6308 | Three obligations met, three unmet, and all three unmet for one reason: no reachable code supplies or enforces the vocabulary, so two reviewers can carry different severity words and different finding categories through the same validator. Both edges are contract: RQA-FR-001 fixes the one published definition covering all six concepts, and RQA-FR-008 fixes the category vocabulary the taxonomy concept is expressed in. Corrected in round 1: the earlier cell counted four pinned against two by listing the execution-mode mapping as a concept and bundling required evidence together with blocking conditions. |
| RQA-BR-004 | partial gap | not built | The threshold itself is a repository rule applied by code, not by a reviewer: the blocking severity set is read from repo-local policy at scripts/dispatcher.py:798-802 and applied at scripts/findings.py:157 and scripts/findings.py:162; corroboration needs two provider families or a cited failing check (U-VERDICT-05, scripts/findings.py:146-197); risk bands are validated policy (U-POLICY-02, scripts/config.py:255-273) scored deterministically as a maximum RPN (U-VERDICT-13, scripts/risk.py:110-131); protected triggers are a deterministic policy match (U-VERDICT-14, scripts/risk.py:138-152); and the action gate is a fixed conjunction (U-AUTHORITY-04, scripts/action_gate.py:63-83). What is missing is the vocabulary that makes the threshold reviewer-independent in practice: nothing enforces the schema's four-value severity enum (schemas/reviewer-verdict.json:19) on a returned verdict (U-VERDICT-02, scripts/verdict.py:149-197), while the blocking test is an exact membership check (scripts/findings.py:162) and the corroboration fingerprint is keyed on severity (scripts/findings.py:54-61). | - | 9267b6308 | Two reviewers reporting one defect as `blocker` and `critical` reach different blocking outcomes and never corroborate each other, so the threshold is in practice still decided by each reviewer's own severity word. Closing this is additive — enforce the declared enum on the reachable validation path — so no prerequisite row is recorded. |
| RQA-BR-005 | full gap | not built | No finding carries a category at all. The verdict schema fixes five finding keys with additionalProperties false and no category among them (schemas/reviewer-verdict.json:17-23); the absence is reproducible — `grep -rn -e mechanical -e procedural -e creation-time scripts/ schemas/` returns exactly one line, `scripts/planner.py:125:#: mechanically narrower and does not need the deep code questions.`, a comment — and this row and RQA-BR-002 cite the same search result. What the record does carry is a corroboration basis, not a category: U-VERDICT-05 (scripts/findings.py:146-197) splits findings into verified and single_family_uncorroborated, and U-RESILIENCE-05 (scripts/advisory.py:71-95) renders that split as two headed sections. U-VERDICT-04 (scripts/findings.py:92-121) keeps only severity, location, evidence and primary_source. A reader of the record can therefore separate corroborated from uncorroborated findings, and cannot separate a mechanical or creation-time finding from a correctness or security one. | RQA-FR-008 | 9267b6308 | No obligation of this row is met, so the degree is full rather than partial: the corroboration basis is a different distinction, not a partial delivery of this one. The single edge is contract: RQA-FR-008 establishes the category vocabulary every finding must carry, and this row is the record-level readability that vocabulary makes possible. |
| RQA-BR-007 | fit | - | Reconciliation creates at most one job per exact repo, number, head and lane key, and an existing row for that key blocks a second (U-QUEUE-01, scripts/queue.py:116, scripts/queue.py:117, scripts/queue.py:118), so requesting review again on an unchanged head queues no second review. The identity that key rests on is a stable hash of the same four inputs (U-RESILIENCE-13, scripts/common.py:471-478). An in-flight PR is not re-claimed concurrently (U-QUEUE-03, scripts/lease.py:73-74; U-QUEUE-04, scripts/dispatcher.py:1699-1700), the advisory post is idempotent per job (U-DISPATCH-18, scripts/dispatcher.py:787-789), and every non-approval mutation is deduplicated by a deterministic mutation id (U-AUTHORITY-09, scripts/github_mutate.py:231-234). Work is abandoned rather than duplicated when the head does move (U-QUEUE-02, scripts/queue.py:159-169). | - | 9267b6308 | Scoped to this row's own text and fit criterion. RQA-FR-005's stronger evidentiary obligation — independently checkable proof that zero reviewer calls ran — is a separate row and is not asserted here. The assurance ladder does re-invoke reviewers on an unchanged revision when it escalates the profile (scripts/panel.py:626-631), which is bounded, policy-driven escalation rather than repeated identical work, and belongs to RQA-FR-019 and RQA-FR-020. |
| RQA-BR-009 | partial gap | not built | A failing automated-review signal from the harness is attributed without a human by reachable code: the coarse disposition vocabulary errors.py defines is returned from panel.py's own branches - TRANSIENT on a genuine timeout, after the same candidate has been retried once (scripts/panel.py:393); PROVIDER_TERMINAL on any other exception, which retires every candidate of that provider family for the run (scripts/panel.py:404); CANDIDATE_TERMINAL on a verdict that fails validation (scripts/panel.py:413, scripts/panel.py:424) - with the family cooldown consulting the first two only (scripts/panel.py:721) and the typed JobBlockingError re-raised to the caller ahead of all of them (scripts/panel.py:383) and refused before any state write by dispatcher.degrade() (U-RESILIENCE-04, scripts/dispatcher.py:442). The fine-grained classify_disposition rule list (scripts/errors.py:94-115) is deliberately NOT part of that credit, corrected on re-review: it is reached only under test, so methodology §2.3 makes it unimplemented from the product's point of view, and an earlier revision of this cell rested part of the met obligation on it. Quoted as the searches returned, run from launchpad/skills/review-queue-automation/: `grep -rn "wrap_error" scripts/ tests/` → its only output is the definition, scripts/errors.py:191, so classify_disposition's sole product caller has itself no caller anywhere; `grep -rn "classify_disposition" scripts/ tests/` → scripts/errors.py:9, :107 and :195 (docstring, definition, and the call inside wrap_error) plus tests/test_repairs.py:384, :386 and :389; `grep -rn "classify_error" scripts/ tests/` → scripts/errors.py:5 and :79 plus tests/test_errors_states.py:15, :28, :33, :38 and :42. Degree and root cause are unaffected by that correction: both rest on the reachable evidence above and on the unmet six-value attribution below. Also corrected 2026-09-08 under the maintainer's ruling on RES04-META-UNREACHED: an earlier revision credited CATEGORY_META (scripts/errors.py:207-231) as what panel.py's loop consumes, which the ruling's consumer search disproves, and the registry is now carried as its own unit, U-RESILIENCE-17, at bin. A failing check is not attributed: checks.py canonicalises a conclusion and nothing more (U-VERDICT-06, scripts/checks.py:47-61, scripts/checks.py:64-85), and no code decides whether a failure belongs to the PR or to its base — `grep -rn -e base_sha -e base_ref -e inherited scripts/` returns exactly one line, `scripts/runners.py:76:    passed explicitly rather than inherited from the caller's cwd.`, unrelated to check attribution. The only place that question is asked is a prompt activity handed to the reviewer model, whose answer lands in prose rather than in a structured attribution (U-DISPATCH-24, scripts/planner.py:231-234). | - | 9267b6308 | The transport and infrastructure attributions the harness produces are a real, complete obligation of this row, which is why the degree is partial. The six-value attribution the requirement names has no implementation anywhere in the tree, established by the quoted null search above. |
| RQA-BR-003 | partial gap | built against superseded intent | Performer and judgement basis are established by reachable code: the runner, model, provider family, capability and effort are written by the machinery into a sidecar outside model-controlled content (U-VERDICT-11, scripts/panel.py:249-263), the built invocation records what actually ran including whether the requested effort was enforceable (U-DISPATCH-25, scripts/runners.py:41-59), the trace vocabulary fixes route_selection, strategy and decision events with a required subset (U-RESILIENCE-08, scripts/logging_otel.py:54-70), and one command reconstructs strategies, routes, evidence, findings, assurance, human events and the final decision from the ledger (U-RESILIENCE-06, scripts/ledger.py:121-163; U-RESILIENCE-07, scripts/explain.py:53-81), with each finding carrying its own basis (U-VERDICT-05, scripts/findings.py:212-221). Which protocol was followed is not: `policy` is absent from the required config keys (scripts/config.py:30-44) and a config without one dispatches unpinned by design — "snapshotting is additive for repos that have not adopted policy-as-data" (scripts/dispatcher.py:1546-1548) — against requirements/requirements-specification.md:371. | - | 9267b6308 | The unpinned path is reachable code implementing an earlier intent in which policy-as-data was optional, which the frozen specification has replaced; that is the cause recorded. A second, distinct weakness in the same obligation set: every ledger-write failure is swallowed by a bare except (U-DISPATCH-20, scripts/dispatcher.py:202-218) and the docstring's claim that failures surface in the JSONL log does not hold at this revision, so a provenance entry can be lost without trace. Wave 1 also tagged this ID on U-DOCS-18, U-DOCS-20, U-DOCS-39 and U-DOCS-46 — retention manifests, local maintenance commands, log redaction and lease node ids — none of which bears on this row's text; those citations are not inherited here. |
| RQA-BR-008 | partial gap | not built | The record does state which asserted defects were verified and which were not, with a per-finding basis, provider families and citation (U-VERDICT-05, scripts/findings.py:146-197, scripts/findings.py:212-221), a defect with no location, evidence or primary source never reaches that stage at all (U-VERDICT-04, scripts/findings.py:92-121), and the split reaches the reader: the CHANGES_REQUESTED body lists only corroborated findings and says the rest were escalated instead (U-DISPATCH-17, scripts/dispatcher.py:1041-1063), the advisory body separates the two and names achieved against required assurance (U-DISPATCH-18, scripts/dispatcher.py:801-831; U-RESILIENCE-05, scripts/advisory.py:63-95), and assurance is computed from evidence completeness rather than from activity volume (U-VERDICT-18, scripts/risk.py:391-412). What is absent is any statement about the PR's own claims and cited evidence: no review obligation carries an evidence state. The absence is structural and reproducible: `grep -rn -i -e verified -e unavailable -e contradictory -e incomplete -e not_verified -e unknown schemas/` returns nothing (exit 1), the reviewer-verdict finding keys are fixed to severity, title, location, evidence and primary_source (schemas/reviewer-verdict.json:17-23), and the only uses of the vocabulary term `contradictory` in the tree are messages about malformed verdict fields, not fields on a review record (`grep -rn -e contradictory scripts/` returns scripts/panel.py:16, scripts/panel.py:348, scripts/panel.py:417, scripts/verdict.py:5 and scripts/verdict.py:195). The schema's missing_evidence is a free-text array (schemas/reviewer-verdict.json:28). | RQA-FR-010 | 9267b6308 | Partial rather than full: the verified-versus-unverified split with its decision basis is a complete obligation of this row and is reachable and published. The single edge is contract: RQA-FR-010's evidence-state vocabulary is the terms in which "which were verified and which were not" can be answered for anything other than the reviewer's own asserted findings. |
| RQA-BR-011 | partial gap | not built | The population is the approvals this system brokers itself; no approval gate consumes a GitHub-side human review (scripts/risk.py:210-234, the 22 gate names). What is preserved is real and durable: the request row carries recommendation, rationale, action and failed_gates keyed to the job (U-AUTHORITY-05, scripts/approval.py:85-155), written with the risk score and band, the protected paths, the failed gates, the assurance profile actually reached and the reviewers that ran (scripts/dispatcher.py:1484-1496); the decision records the deciding actor and reason and cannot be recorded without an actor (scripts/approval.py:233-271; U-AUTHORITY-06, scripts/human_cli.py:216-220); it is refused once the head or policy it was made against moves (scripts/approval.py:183-201); the authorisation is persisted and the resulting review body names the authorising actor (U-AUTHORITY-07, scripts/human_cli.py:147-185); and one decision-kind ledger entry records the disposition, failed gates, risk and profile for the same job (scripts/dispatcher.py:1451). What is not preserved is what the reviewers found: the packet passes findings=[] and ci={} (scripts/dispatcher.py:1493); no finding-kind ledger entry is written on this path, the only such call site being on the request-changes path (scripts/dispatcher.py:925); and the ledger's human kind is declared and rendered but never written by any call site in scripts (scripts/ledger.py:42, scripts/ledger.py:154). | - | 9267b6308 | Corrected in round 1. This row read `fit` on a note conceding the empty findings list and justifying it by retrieval through the ledger; that justification does not hold, so under the ruling the conceded fact decides the row and the claim narrows to what the citations carry. Partial rather than full: the assurance state that justified the approval — failed gates, risk band, assurance profile, reviewer identities, actor and reason — is a complete obligation, is retrievable, and survives the retention purge, which removes artifact bytes only and leaves every ledger, approval and human-request row intact (U-DISPATCH-06, scripts/dispatcher.py:1941-1998). Root cause is `not built` rather than orphaned: only the read side of the human kind exists, and no code anywhere performs the write. Closing this is additive, so no prerequisite row is recorded. |
| RQA-BR-014 | partial gap | not built | The record does demonstrate what was undertaken rather than asking for trust in volume: the plan records a reason for every selected activity and for every omitted one, from observable PR facts and no model judgement (U-DISPATCH-24, scripts/planner.py:183-186, scripts/planner.py:194-247), the risk-class activities are selected by path class with their own reasons (scripts/planner.py:202-219), the evidence bundle fails closed rather than being written partially (U-VERDICT-07, scripts/evidence.py:37-50), achieved assurance is derived from evidence completeness and reviewer completion against a required floor (U-VERDICT-18, scripts/risk.py:391-412), the published body names routes, effort enforcement, achieved against required assurance and unmet gates (U-RESILIENCE-05, scripts/advisory.py:97-119), and one command reconstructs the lot (U-RESILIENCE-06, scripts/ledger.py:121-163). What is missing is the examined-or-not statement per obligation: the plan records selection reasons only and no completion or evidence state (scripts/planner.py:194-247), and the verdict shape carries no per-obligation field (schemas/reviewer-verdict.json:5). | RQA-FR-010 | 9267b6308 | So a reader can confirm which risks the system decided to examine and what assurance it reached, and cannot confirm from the record that each of them actually was examined. The single edge is contract: RQA-FR-010's per-obligation evidence state is the vocabulary in which examination is stated, and it is what turns a plan into a demonstration. |
| RQA-BR-006 | full gap | built then orphaned | No product path applies a remedy. The mechanism is complete — create, commit, push and clean, with a push that refuses force and refuses any branch other than the configured PR head (scripts/worktree.py:102, scripts/worktree.py:170, scripts/worktree.py:195, scripts/worktree.py:229, scripts/worktree.py:213-222) — and nothing under scripts reaches it: the only occurrence of the name in another script is a comment (scripts/runners.py:120), the dispatcher treats the author-triage lane as a generic lane parameter gated by a canary key (scripts/dispatcher.py:2162, scripts/dispatcher.py:1797-1800), and the only importers are two test modules (U-DISPATCH-26, U-DOCS-13). The documentation describes the behaviour as something the system does (SKILL.md:76, references/contracts.md:16, references/classification.md:21), and authority declares a `fix` activity (scripts/authority.py:32-34) that nothing ever performs. Every deterministic defect therefore still needs the contributor to fix it and a further review cycle to clear it. | RQA-FR-008, RQA-NFR-019 | 9267b6308 | Root cause is orphaned rather than not built because the remediation code is present at this revision and unreached, which is methodology section 2.4's worked example. RQA-FR-008 is contract: it fixes the classification vocabulary, and a remedy cannot be applied automatically until a finding is classified as the mechanical, deterministic kind policy may permit fixing. RQA-NFR-019 is authority: it is the bound that confines remediation authority to the categories policy names as mechanical, so wiring this row's automatic fix before that bound exists ships the mutating action without its check. RQA-NFR-031 was considered in round 2 and not added: it is the behavioural ceiling on what policy may call mechanical, and this row's own text already carries `deterministic`, so nothing in it is defined against a vocabulary NFR-031 establishes that RQA-FR-008's edge does not already carry. |
| RQA-BR-010 | conflicting | built contrary | Discovery and dispatch do run without an operator: the timer fires a tick per configured lane (U-QUEUE-08, scripts/scheduled-tick.sh:40, scripts/scheduled-tick.sh:60), the interval is chosen and persisted rather than typed by hand (U-QUEUE-05, scripts/cadence.py:109-116), and a clean PR under live authority reaches completed_auto_approved with no human in the loop (U-DISPATCH-16, scripts/dispatcher.py:631-665). The contradiction is what happens to a PR needing no judgement at all: in live mode evaluate returns human_escalation on ANY failed gate (scripts/approval_evaluate.py:268-288), and the gate set includes purely mechanical ones — checks_complete_ok, rate_limit_ok, audit_writable (scripts/approval_evaluate.py:189-212, scripts/risk.py:210-234) — whereupon the job is transitioned to human_approval_pending and progresses no further (scripts/dispatcher.py:1482-1502). Nothing re-drives it: no second job is created for the same head (scripts/queue.py:116-118), the sweep selects only detected jobs (scripts/dispatcher.py:2140-2164), and recovery deliberately leaves human_approval_pending untouched (U-DISPATCH-07, scripts/dispatcher.py:1868-1878). | - | 9267b6308 | Conflicting on the deciding test: a reachable execution at this revision produces an outcome this requirement forbids — a PR whose failed gate needs no judgement is parked awaiting an owner's decision and progresses no further (scripts/dispatcher.py:1482-1502) — so satisfying the row means that execution stops producing it, which is altering reachable behaviour whatever the shape of the patch. A moved head is not the example: that case is superseded and re-reviewed automatically. Three edges have now been dropped and the cell is empty, which is correct: every citation this degree rests on is reachable code that closes by rerouting its own execution, not by another row's gap closing first. RQA-FR-017 went in round 2 — none of checks_complete_ok, rate_limit_ok or audit_writable is a finding, so FR-017's population does not reach the escalation path this degree is derived from, and the trailing remediation sentence that carried the edge is dropped with it; that observation belongs to RQA-BR-006's row. RQA-BR-006 went in round 1 because CL-010 speaks of a further contributor rather than the owner CL-014 names, and RQA-BR-013 because the two fixes are independent in both directions; the shared CL-017 mechanism is an observation in this file's prose, not an edge and not an ordering. |
| RQA-BR-013 | conflicting | built contrary | In live mode every failed gate raises a human request and delivers a notification for it (scripts/approval_evaluate.py:268-288, scripts/dispatcher.py:1482-1500), and the gate set includes conditions that need no human judgement whatever: a still-pending or failing required check, an exhausted REST budget, an unwritable audit directory (scripts/approval_evaluate.py:189-212, scripts/risk.py:210-234). Delivery makes no distinction: deliver carries no urgency or routing parameter, so a routine gate failure goes out through exactly the same configured transport as a protected-trigger denial (U-AUTHORITY-12, scripts/notify.py:180-196, scripts/notify.py:94-135). The paths that correctly stay silent are the contrast — an evidence-incomplete escalation logs a warning and returns with no delivery (U-DOCS-02, scripts/dispatcher.py:360-369). | - | 9267b6308 | Conflicting on the deciding test: a reachable execution at this revision produces an outcome this requirement forbids — a condition needing no judgement raises a notification to a person — so satisfying the row means that execution stops producing it. The system has no notion of urgency at all, so whether a delivered packet interrupts is left to the operator's transport; that absence is why a mechanical condition and a genuine authority question are indistinguishable at the point of delivery, and it is the reading behind this degree. This row shares source clause CL-017 with RQA-BR-010 and is contradicted by the same mechanism, which is an observation and not a dependency in either direction: either row can close without the other. |
| RQA-BR-012 | partial gap | not built | Review work is bounded to what the policy asks for by reachable code: strategy selection walks a fixed specificity order and does not reach for a higher-cost method the signals do not call for (U-POLICY-08, scripts/strategies.py:137-181), the execution mode is sized from the assurance profile so it can never demand more independent participants than required (U-POLICY-09, scripts/modes.py:155-179), the token, attempt and concurrency ceilings refuse before any spend (U-RESILIENCE-01, scripts/budget.py:396-414, scripts/budget.py:377-394), the fallback ladder leads with subscription tiers before a metered call (U-RESILIENCE-03, scripts/fallback.py:46-60), the sweep is batch-bounded and cadence-gated (U-DISPATCH-08, scripts/dispatcher.py:2140-2164, scripts/dispatcher.py:2323-2372), and loading the timer spends nothing (U-QUEUE-08, scripts/launchd.plist.example:55). The comparison this row's fit criterion turns on cannot be made: what is recorded as consumption is the reservation itself, because runners report no token count, and the stored row carries no field distinguishing that estimate from a measurement (U-RESILIENCE-01, scripts/budget.py:217-230; U-DISPATCH-13, scripts/dispatcher.py:1323-1329). | RQA-FR-021 | 9267b6308 | The fit criterion is explicit that recording consumption without being able to show non-excess against the policy's stated required assurance does not satisfy this row, and nothing in scripts performs that comparison. The single edge is substrate: the comparison acts on a recorded consumption figure that RQA-FR-021's behaviour must first produce as a measurement, or at least mark as not one. |
