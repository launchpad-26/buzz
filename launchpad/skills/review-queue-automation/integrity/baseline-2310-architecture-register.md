# RQA architecture/coherence findings — pre-remediation

Audit target: `4d64f0c4476ed6ab907b69809b96d5c4c2653df6`. These are initial-tree findings captured before any #2310/#2314 remediation. “Disposition candidate” is triage, not a final ownership decision.

| finding | source | check | evidence | classification | disposition candidate | action recommendation | verification | reason | overlap |
|---|---|---|---|---|---|---|---|---|---|
| C-001 — The production composition root cannot grant repository authority | `rqa/cli/composition.py:113-122,332-333`; `rqa/authority/gate.py:95-110,422-439`; `launchpad/skills/review-queue-automation/SKILL.md:144-154`; `launchpad/skills/review-queue-automation/OPERATORS.md:275-284` | Trace the CLI-composed authority client through `grant()` and determine whether a configured repository can ever reach an enabled gate. | `AuthorityClient` carries only GitHub and store dependencies and delegates to the module-level `grant`. That function constructs `Gate` from `_configured_repositories()`, which returns an empty `frozenset` and says real wiring should construct `Gate(repos=...)`. Consequently every composed activity is denied `REPO_NOT_MANAGED`; the operator documents explicitly identify this as #2274. | Genuine production-unwired implementation | `separate-work` if #2274 remains the owner; otherwise `wired` | Establish one validated managed-repository inventory at composition and inject a configured `Gate`; retain fail-closed behavior for absent/invalid inventory. Do not recreate the removed legacy-registry path. | A composed-path integration test must show an enabled managed repository proceeds beyond `REPO_NOT_MANAGED`, while an unmanaged repository still denies. | This is demonstrated call-path behavior, not a design preference: the only production constructor supplies an empty repository set. | Known #2274; deferred-blocker residue from #2188. Likely T1/reference-analysis overlap. Avoid treating it as a new legacy-cutover task. |
| C-002 — One bare `repo: str` represents two incompatible identities with no mapping | `rqa/policy/snapshot.py:62-69`; `rqa/cli/main.py:69-88`; `rqa/intake/tick.py`; `launchpad/skills/review-queue-automation/SKILL.md:38-56`; `launchpad/skills/review-queue-automation/OPERATORS.md:28-29` | Follow the repository argument from onboarding and `repos.json` through policy admission and GitHub inventory. | Onboarding treats `repo` as a filesystem root and writes `<repo>/.rqa/config.json`. Tick configuration and GitHub operations treat it as an `owner/repo` slug, but tick passes that same slug to policy snapshot lookup. No production mapping connects a slug to its checkout. Thus onboarding `/checkout/buzz` does not create the config later read as `launchpad-26/buzz/.rqa/config.json`; it works only under an accidental matching relative-directory layout. | Genuine architecture/data-model incoherence | `separate-work` or `wired`; lead judgement required | Replace the ambiguous string with a managed-repository descriptor such as `{slug, checkout}` or introduce one durable slug-to-checkout mapping consumed by policy and authority. Preserve the slug as the durable job identity. | Onboard an arbitrary checkout path, then tick its unrelated `owner/repo` slug and prove policy reads the same generated config while GitHub receives the slug. | The documented command contracts explicitly assign different domains to the same parameter, and the execution path contains no conversion. | Closely coupled to #2274’s managed-repository source. Likely reference-analysis overlap; potentially larger than a bounded cleanup. |
| C-003 — Two accepted policy keys are validated and documented but behaviorally inert | `rqa/contracts.py:140-144`; `rqa/policy/validate.py:386-411`; `rqa/judgement/judge.py:233-253`; `launchpad/skills/review-queue-automation/architecture/architecture.md:267-269,463-465`; `launchpad/skills/review-queue-automation/architecture/code/P-07-judgement.md`; `launchpad/skills/review-queue-automation/config.example.json` | For every accepted `blocking` field, locate the runtime read that changes judgement or blocking. | Validation requires and materializes `blocking.severities`, `blocking.categories`, and `blocking.corroboration`, but judgement reads only `categories`. Corroboration is hard-coded as two independent families or a cited failed check; the architecture explicitly says repositories do not control it. `severities` likewise has no production consumer. | Ineffective configuration; multiple sources of truth | `removed` unless maintainers explicitly choose a behavior change | Remove `severities` and `corroboration` from the runtime contract, schema/validator, example, operator docs, and fixtures. If configurability is intended, defer that behavior and architecture change to separately accepted work rather than silently wiring it here. | Prove every remaining accepted config key has a runtime consumer; obsolete keys must fail validation and disappear from examples. Existing fixed corroboration tests must remain green. | Retaining knobs whose values cannot affect execution gives operators a false control surface. Wiring them would contradict the current protocol-fixed architecture and change product behavior. | Surviving #2188 integrity criterion (“every remaining config key has a runtime consumer”), but not a request to reenact its file cutover. May overlap T1/reference analysis. |
| C-004 — GitHub writes retain an obsolete duplicate implementation of canonical stable hashing | `rqa/github/writes.py:26-32,134-142`; `rqa/intake/identity.py`; `tests/test_rqa_github_writes.py:274-278`; `launchpad/skills/review-queue-automation/architecture/code/P-09-github-adapter.md` | Compare the dynamic fallback with the canonical identity owner and establish whether the fallback state can occur in the current supported tree. | `_stable_hash` dynamically imports `rqa.intake.identity.stable_hash`, but on `ImportError` computes the same SHA-256 formula locally. The module and test still describe identity as “not yet landed,” although it is present and required by the accepted P-09 composition. The fallback masks a broken canonical import and creates a second implementation that may drift. | Obsolete compatibility layer; duplicate implementation/source of truth | `removed` | Import the canonical `stable_hash` directly; delete the fallback, local hashing dependency, and pre-landing commentary. Update the test to verify use of the canonical owner, not compatibility with a no-longer-supported tree. | Mutation identifiers must remain byte-for-byte stable. A test replacing/patching the canonical helper should be observed by the writer path; breaking the canonical import must fail, not fall back. | Current architecture has one landed owner and no supported deployment without it; the alternate path only preserves a historical assembly wave. | Strong T1/T2 detector overlap because the reference is dynamic. Bounded remediation independent of legacy cutover. |
| C-005 — Final-tree surface tests still accept historical partial assembly states | `tests/test_rqa_supply_surface.py:1-24,111-145,308-319`; `tests/test_rqa_intake_surface.py:1-16,90-103`; `tests/test_rqa_policy_surface.py:1-15,108-120`; `tests/test_rqa_record_surface.py:1-15,160-172`; `tests/test_rqa_intake_lock.py:1-8` | Delete or omit each later-wave module/export and ask whether the exact-surface invariant still fails. | Supply, intake, policy, and record tests use alternative expected sets for earlier and final waves. In the current complete tree, coordinated removal of the later intake modules, policy onboarding, or record explain/migrate surfaces can satisfy the older alternative. The supply spend invariant likewise accepts either the migration writer alone or migration plus the runtime writer, so removing `rqa/supply/spend.py` can pass. Intake lock’s header still says `tick.py` does not exist. | Ineffective invariant guard; stale test architecture | `fixed` | Collapse alternative sets to the single accepted final surface and require both spend writers/current owners. Add the removable modules/exports to the bounded mutation inventory. Refresh stale test descriptions. | Mutations deleting each accepted later-wave module/export or the runtime spend writer must fail the intended invariant test, with the unmutated suite green. | Historical assembly flexibility now permits architectural regression in exactly the artifacts the tests claim to lock. | Direct Task #2313 inventory/runner overlap; coordinate so the invariant owner, not a duplicate test family, carries the mutation proof. |
| C-006 — External record anchors are publish-only, so claimed recovery after loss of local anchors or the whole database is not implemented | `rqa/github/anchor_publisher.py:39-104`; `rqa/record/anchor.py:1-42,145-205`; `rqa/record/verify.py:48,95-184`; `rqa/record/store.py`; `launchpad/decisions/ADR-0066-rqa-record-chain-anchoring-without-a-key.md`; `launchpad/skills/review-queue-automation/architecture/code/P-12-record.md:355-418`; `launchpad/skills/review-queue-automation/OPERATORS.md:158,234-238` | Locate a production reader for the GitHub anchor marker and exercise verification after both the record and local anchor state are removed/rebuilt. | The GitHub adapter can publish an anchor comment, but `ANCHOR_MARKER` has no production reader and record verification reads only `latest_anchor` from the local store. P-12 defines publishing but no fetch/compare operation. Therefore the accepted ADR/operator claim that the externally published head detects deletion of local anchors or a wholly rebuilt record is not executable. Existing tamper mutations retain the local anchor and do not test this claim. | Incomplete integrity architecture; documented capability not wired | `separate-work`; lead judgement required | Add an explicit remote-anchor verification/import path with a stable destination reference, or narrow the ADR and operator claims to the local-anchor threat model. Decide deliberately whether offline `explain` stays local-only and a separate online verification command owns the comparison. | Publish an anchor, delete/rebuild both record and local-anchor storage, then fetch the published anchor and require verification failure. Also cover unavailable/malformed/ambiguous remote anchors fail-closed as designed. | An external write cannot serve as an independent recovery trust root unless some supported path reads and compares it. | Recent #2300/ADR-0066 implementation and likely reference-analysis overlap. Not suitable for an unreviewed bounded cleanup because it changes the operational verification surface. |
| C-007 — Current architecture documents still prescribe superseded HMAC/keychain integrity | `launchpad/skills/review-queue-automation/architecture/architecture.md:67,514,611,654-655`; `architecture/context.md:34,75,90,130`; `architecture/container.md:49,95`; `architecture/code/P-13-revision-reuse.md:178`; `architecture/adr-drafts/ADR-F.md`; `architecture/adr-drafts/README.md`; `launchpad/skills/review-queue-automation/OPERATORS.md:178`; `launchpad/decisions/ADR-0063-rqa-record-provenance-integrity.md`; `launchpad/decisions/ADR-0066-rqa-record-chain-anchoring-without-a-key.md` | Compare present-tense integrity descriptions against the accepted ADR chain and current P-12 implementation. | ADR-0066 supersedes ADR-0063 and the current record code/P-12 uses an unkeyed hash chain plus external anchor. Nevertheless the overview, context, container, reuse contract, draft index, and one operator cross-reference still describe OS-keychain HMAC/unavailable-key segments as current behavior or ADR-0063 as the governing decision. More detailed P-12/ADR-0066 material describes a different system. | Obsolete documentation; competing architecture truths | `fixed` | Update current architecture/context/container/P-13/draft-status/operator references to ADR-0066. Keep ADR-0063 only as a historical superseded decision, with its status unambiguous. | Repository search for HMAC/keychain/ADR-0063 should find only historical and supersession discussion, not present-tense component responsibilities or current test cases. Cross-document integrity descriptions must agree with P-12. | These documents are normative architecture inputs, and their contradictory current-state claims can produce incompatible implementations and runbooks. | Recent ADR-0066/#2300 documentation overlap; bounded documentation remediation. |
| C-008 — The documented and machine-readable command surface omits the shipped `anchor` command | `rqa/cli/main.py:286-289`; `rqa/edges.py` E-17 declarations; `rqa/contracts.py:EDGES["E-17"]`; CLI parser/provider tests; `launchpad/skills/review-queue-automation/SKILL.md:26`; `launchpad/skills/review-queue-automation/architecture/architecture.md` §11; `launchpad/skills/review-queue-automation/OPERATORS.md` anchor section | Compare parser subcommands and handler branches with every normative and machine-checkable E-17 command inventory. | The parser and main dispatch expose seven commands including `anchor`, and the operator runbook/P-12 documents it. SKILL still says there are six commands and contains no anchor section; architecture §11 lists only the earlier six. More importantly, `rqa.edges` declares no E-17 `anchor`, and `contracts.EDGES["E-17"]` omits it, so the supposedly machine-checkable edge inventory certifies an incomplete public command seam. | Stale interface architecture; multiple command inventories; ineffective edge invariant | `fixed` | Add `anchor` to E-17’s architecture and edge contract, SKILL, and command table; update the count/semantics. Add a small invariant comparing parser/dispatch subcommands with the intentionally documented E-17 inventory. | Assert/document the same subcommand set (`tick`, `onboard`, `status`, `pending`, `decide`, `explain`, `anchor`) and ensure each has a production dispatch branch and E-17 declaration. A mutation removing `anchor` from either side must fail. | Operators and implementers currently receive contradictory accounts of the public interface, while the existing edge test only proves agreement between two stale architecture sources. | Recent #2300 overlap; direct T1/T3/surface-invariant overlap. |
| C-009 — Wave-era module/test descriptions contradict the assembled code and its import boundaries | `rqa/protocol/__init__.py:8-13`; `rqa/github/writes.py:26-32`; `tests/test_rqa_github_writes.py:274-278`; `tests/test_rqa_intake_lock.py:1-8`; `rqa/lifecycle/__init__.py:9-13`; `rqa/lifecycle/steps.py:83`; `rqa/record/__init__.py` | Check statements that sibling modules “do not exist,” that parts never import other parts, and that record implementation types stay behind the package front door against current imports. | Protocol says validators do not yet exist while importing one; GitHub writes and its test say intake identity has not landed; intake lock says tick does not exist. Lifecycle says no lifecycle module imports another part, yet steps imports `rqa.record.reader.SQLiteRecordReader`; record says other modules import only its front door and keeps that reader private. Accepted P-02 text separately allows record names, leaving no single truthful boundary statement. | Obsolete implementation documentation; contradictory module-boundary contract | `fixed`, with boundary choice requiring lead judgement | Remove assembly-wave claims. For the reader boundary, either export/document the sanctioned dependency, inject a reader interface/factory, or change the package rule—then make one invariant enforce the chosen boundary. | Search should leave no false “not yet landed/does not exist on this branch” statements. Import-boundary tests must encode and pass against the actual chosen dependency rule. | The contradictions are mechanically demonstrable in the same files; they obscure which direct cross-part imports are violations versus intended exceptions. | Partial T1/T3/source-surface overlap. Reader-boundary choice may overlap reference analysis. |
| C-010 — Public CLI exit codes 2 and 3 have no reachable production path | `rqa/cli/exitcodes.py`; `rqa/cli/main.py`; `launchpad/skills/review-queue-automation/SKILL.md:119-130`; CLI main tests | Enumerate production return sites and exception mappings for each exported exit code. | `NETWORK=2` and `AUTH=3` are public/exported, but main returns only `OK`, `INPUT_ERROR`, or generic `OTHER`; no exception is classified into 2 or 3 and tests exercise only those reachable outcomes. SKILL explicitly acknowledges the gap. | Public definition referenced by documentation/tests but production-unwired | `separate-work` or `removed`; lead judgement required | Either introduce typed network/auth failures and map them at the CLI boundary, or remove the unreachable codes and promises if no authoritative compatibility requirement needs them. Do not infer failure type from message text. | Targeted CLI tests must make each retained code reachable through the composed command path; otherwise exact public-surface tests must reject the dead constants. | An advertised exit contract that can never emit two values is ineffective for automation and fits Feature #2310’s explicit unwired-public-definition criterion. | Direct T1/T2/reference-analysis overlap; ownership depends on whether the original five-code DoD remains authoritative. |
| C-011 — Trace persistence remains a known unwired subsystem | `rqa/record/trace.py`; package exports; `launchpad/skills/review-queue-automation/OPERATORS.md:288-299`; issue snapshot #2188 | Find production callers of `append_trace` and determine whether trace rows can be created by the current lifecycle. | `append_trace` has no production caller and is not part of the active record front door. The operator docs already name the missing wiring as #2273 and retain a legacy script only as reference. | Genuine production-unwired implementation, already tracked | `separate-work` | Leave #2310 bounded remediation out of this subsystem unless #2273 is explicitly folded in; preserve the finding in the completeness register. When owned, wire trace emission from lifecycle decisions rather than reviving the legacy runner. | A composed lifecycle scenario must persist the specified trace and `explain` must consume it; a mutation removing the call must fail. | The current code proves presence without reachability, but the defect already has a named owner and redoing #2188’s legacy cutover would violate scope. | Known #2273 and #2188 CUTOVER residue; direct T1/T2 overlap. |
| C-012 — `rqa.github.testing` is intentionally test-only production-tree code, not dead production behavior | `rqa/github/testing.py`; `launchpad/skills/review-queue-automation/architecture/code/P-09-github-adapter.md` test-helper requirement; credential/network safety tests | Determine whether a production-tree public definition used only by tests is required to keep safety tests runner-neutral. | `NON_TOKEN_CREDENTIAL` and `raising_socket` are used by tests rather than the runtime adapter, but P-09 deliberately places runner-neutral credential/network guards in a non-pytest helper so they remain effective under alternate runners. Their location and usage are therefore intentional evidence infrastructure, not a competing production implementation. | Deliberate exception / expected test support | `retained` with explicit detector classification | Keep the helper. If T1 flags it, encode a narrow path/symbol rationale in the findings or detector allowlist rather than manufacturing a runtime caller or moving it into pytest-only support. | Run the credential and network-denial tests under the repository’s supported non-pytest execution path; detector output should classify rather than fail this bounded exception. | Feature #2310 requires distinguishing genuine use from mere reference; this is genuine test-only use mandated by accepted architecture. | T1 false-positive triage and invariant-test overlap. |
| C-013 — The recorded conformance run still presents two subsequently fixed findings as current blockers | `launchpad/skills/review-queue-automation/TESTING.md:1000-1017,1075-1085,1127-1266`; `rqa/record/explain.py:268-275`; `rqa/authority/gate.py:233-240`; `launchpad/decisions/ADR-0062-rqa-credential-floor-and-ceiling.md:11-28` | Compare each unqualified finding/blocker in the historical conformance report with current code and accepted amendments, using F-3’s explicit resolved banner as the document’s precedent. | F-1 says pre-plan explanation cannot recover three snapshot pins, but `explain_job` now falls back to the last `snapshot` row. F-2 says all six activities remain structurally ungrantable and ADR-0062 is undecided, but the accepted #2250 amendment authorizes scope-checked write attestations and the gate now unions those three attested write capabilities into the check. Many verdict rows still cite F-1/F-2 as current blocks. F-3, by contrast, is clearly marked resolved while retaining historical evidence. | Stale conformance documentation; superseded blocker analysis | `fixed` | Preserve the commit-pinned historical observations, but add prominent resolved/superseded annotations to F-1 and F-2 and qualify downstream verdict rows as results at `b9c98f423`, not current blockers. Link the fixes/amended ADR and identify #2274 as the remaining composed-path block. | A current-reader scan of TESTING must not state that snapshot-pin fallback is absent, that write attestations are under dispute, or that #2274’s repair necessarily leads to six capability denials; historical output remains unchanged and explicitly dated. | The report is intentionally historical, but its own F-3 precedent distinguishes historical evidence from current status; leaving only F-1/F-2 unqualified creates a false present-tense remediation map. | Reference-analysis/documentation overlap; F-1 likely prior fix, F-2 #2250/#2252. C-008 also covers F-5’s stale six-command claim. |

