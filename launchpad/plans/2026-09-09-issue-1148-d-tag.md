# Issue #1148 — document `layers/protocol/d-tag.md`

Issue #1148 (`task: document layers/protocol/d-tag.md`), parent Feature #609. No `Size`
line exists in the issue body — the corpus `type:task` template carries a definition of
done and impacted-components list instead. Dispatch brief caps this at **5 steps**; that
cap governs.

Deliverable: exactly one hand-authored corpus node,
`launchpad/docs/corpus/layers/protocol/d-tag.md`, id `layers-protocol-d-tag`,
`type: layers`, built from `launchpad/docs/corpus/templates/concept.md`.

## ALREADY TRUE (verified against real files and real command output)

Worktree `/home/serina/Launchpad/buzz/__worktrees/task-1148-d-tag`, branch
`task/1148-d-tag`, branched from `origin/launchpad`. `git rev-parse HEAD` inside the
worktree returns `29ca9b189bd3f639ba09c972b57c70538c0860c6`; `git cat-file -e` on that
sha exits 0. That is the provenance revision.

`launchpad/docs/corpus/layers/protocol/` does not exist yet — no `layers/protocol/**`
node appears in the merged-id list (465 merged ids, checked). The whole protocol subtree
is Feature #609's, so **no sibling in this Feature is a legal relationship target**.

Evidence already opened and read (not "a file exists that probably says so"):

- `crates/buzz-core/src/kind.rs` — `PARAM_REPLACEABLE_KIND_MIN/MAX` = 30000/39999;
  `is_parameterized_replaceable`'s doc comment reads "These events are keyed by
  `(pubkey, kind, d_tag)` — the latest `created_at` wins"; `is_replaceable`'s doc
  comment states NIP-33 kinds "use a different replacement key (includes `d`-tag)";
  `KIND_NIP29_GROUP_METADATA/ADMINS/MEMBERS/ROLES` = 39000–39003; `KIND_TEAM_CATALOG`'s
  doc states ingest requires "exactly one non-empty, bounded `d` tag — generic NIP-33
  storage maps a missing `d` to the empty coordinate, which would collapse every team
  into one slot."
- `crates/buzz-db/src/store/event.rs` — `extract_d_tag` returns the **first** `d` tag's
  value, `""` when absent ("Missing d tag → empty string per NIP-33"), and `None` for
  any kind outside 30000–39999; `D_TAG_MAX_LEN = 1024`. Unit tests
  `extract_d_tag_first_d_wins`, `extract_d_tag_missing_becomes_empty_string`,
  `extract_d_tag_empty_value_preserved` cover exactly those.
- `crates/buzz-relay/src/handlers/ingest.rs` — parameterized branch extracts the `d`
  tag, rejects `> D_TAG_MAX_LEN` with "invalid: d tag too long", then calls
  `replace_parameterized_event`. Separately, a long match arm excludes user-owned and
  parameterized-replaceable kinds from `h`-tag channel scoping ("A stray `h` tag must
  not channel-scope them").
- `crates/buzz-db/src/store/replaceable.rs` — the advisory lock key folds the `d` bytes
  in; the head lookup is `WHERE community_id AND kind AND pubkey AND d_tag AND
  deleted_at IS NULL ORDER BY created_at DESC, id ASC LIMIT 1`; `dominated` is
  `created_at < accepted || (created_at == accepted && incoming_id >= accepted_id)`.
- `crates/buzz-relay/src/handlers/req.rs` — `#d` is pushed into SQL only when every kind
  in the filter is parameterized-replaceable, because `d_tag` is NULL for other kinds;
  test `d_tag_pushdown_only_for_nip33_kinds` asserts all five branches.
- `crates/buzz-relay/src/handlers/side_effects.rs` — NIP-09 addressable deletion splits
  the `a` tag on `:` into three, `parts[2]` being the `d` value, then soft-deletes by
  coordinate; `emit_group_discovery_events` pushes `["d", group_id]`.
- `desktop/src-tauri/src/commands/channels/fetch.rs` — `get_channels` step 1 queries
  `{"kinds":[39002],"#p":[my_pubkey]}` and reads each result's `d` tag as the channel id;
  step 2 fetches kind:39000 by `#d`. **No `h` tag anywhere in that chain** — the root
  `CLAUDE.md` claim holds.
- `migrations/0001_initial_schema.sql` / `schema/schema.sql` — `d_tag TEXT` (nullable);
  `idx_events_parameterized` is a **non-unique** partial index; no UNIQUE constraint over
  the coordinate exists on `events`. The one PK shaped like a coordinate is on
  `parameterized_event_watermarks` (`migrations/0007_nip_rs_retention.sql`).
- `docs/nips/NIP-RS.md` — a Buzz-own NIP, openable here, constraining its `d` to
  `read-state:<32 lowercase hex>` and requiring exactly one `d` tag.
- `launchpad/docs/corpus/capabilities/channels/channel-metadata.md` and
  `.../channel-membership.md` — both merged; membership already records the root
  `CLAUDE.md` `d`-vs-`h` sentence, metadata already records `emit_group_discovery_events`
  building the kind:39000 `d` tag. **Link, do not restate.**
- `launchpad/docs/corpus/layers/data/postgres/events-table.md` and `.../indexes.md` —
  both merged and both already own the `d_tag` column and `idx_events_parameterized`.
  **Link, do not restate.**

The upstream NIP-01/NIP-33 texts are **not** in this repository (`docs/nips/` holds only
Buzz's own two-letter NIPs). Every claim about the standard is therefore worded as what a
Buzz source *records* about it, cited to that source.

## Steps

### Step 1 — Create the node file with schema-valid front matter

Write `launchpad/docs/corpus/layers/protocol/d-tag.md` (creating the `protocol/`
directory) with `id: layers-protocol-d-tag`, `type: layers`, `status: draft`,
`origin: launchpad`, `audiences: [agent, developer]`, and the evidence ledger built from
the sources above. Exactly one commit-only FACT: the provenance entry naming
`29ca9b189bd3f639ba09c972b57c70538c0860c6`. Issue-only claims go to `TEAM_KNOWLEDGE` with
`provided_by`.

**Done when:** the file exists, front matter carries only the seven permitted fields, and
every FACT names a source actually opened in this session.

### Step 2 — Write the body to the concept template's required sections

Definition first (one sentence), then the coordinate, the wire-level rules Buzz enforces
(first-`d`-wins, missing → `""`, 1024-byte cap), how `#d` is queried, and Buzz's own uses
(channel ids on 39000–39002, NIP-RS slot ids, team/persona slugs). Use cases, comparison
against the `h` tag, related nodes as `relationships`, and Scope and omissions carrying
both required halves.

**Done when:** each of the concept template's required sections is present; nothing
restates `events-table`, `indexes`, `channel-metadata` or `channel-membership` canonical
content; no sibling #609 subject (parameterized-replaceable events, replaceable events,
event tags generally, the `h` tag) is documented rather than named as a boundary.

### Step 3 — Add relationships, verified against the merged-id list

Grep each candidate id in the merged-ids file before writing it. No #609 sibling.

**Done when:** every `relationships[].target` appears verbatim in
`corpus-merged-ids.txt`, and `git ls-tree -r --name-only origin/launchpad --
launchpad/docs/corpus` contains its file.

### Step 4 — Validate

Run `python3 launchpad/project-intelligence/corpus/validate.py` from the worktree root.

**Done when:** exit status 0.

### Step 5 — Self-review, then stamp and commit

Re-read every evidence entry against its cited source. Then the verify-gate stamp as the
sole foreground command with `timeout: 600000`, ending `OK`; then `git add` + `git commit
-s` in a separate call.

**Done when:** the stamp run ends `OK` and the commit exists. **Do not push. Do not open
a PR.**

## Out of scope

Anything the issue lists as out of scope: a second hand-authored canonical document,
runtime product behaviour, resolving an unresolved ADR, "while here" cleanup. Also out of
scope by boundary: the `a`-tag coordinate form as a subject in its own right (named only
where the `d` value is re-used inside it), and the four sibling subjects above.
