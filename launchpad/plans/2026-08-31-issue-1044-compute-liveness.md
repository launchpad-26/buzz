# Issue #1044 — layers/compute/liveness.md

Stated size: single small document, DoD capped -> cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and `launchpad/docs/corpus/templates/concept.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/layers/compute/liveness.md` does not exist yet (checked: no `layers/` directory at all exists under `launchpad/docs/corpus/` on `origin/launchpad`, via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`). No open PR targets this path. The parent Feature (#611) lists five sibling `layers/compute/*` tasks (#1041 backend-provider, #1042 kubernetes-provider, #1043 lifecycle, #1045 local-agent-compute, #1046 mesh-compute) and one sibling `layers/observability/liveness.md` task (#1138) — none merged, so none are valid `relationships` targets yet.

STEP 1  [independent] Gather evidence for the concept's definition and boundary before drafting: read `docs/remote-agents.md` in full (Abstract, Non-Goals, System Model, §Launchers, Invariants I3/I5, Provider Protocol) for the spec-level claims about presence-as-liveness; cross-check every spec claim against real code — `crates/buzz-pubsub/src/presence.rs` (`PRESENCE_TTL_SECS`), `crates/buzz-core/src/kind.rs` (`KIND_PRESENCE_UPDATE`), `crates/buzz-acp/src/lib.rs` (heartbeat publish/interval/shutdown), `crates/buzz-backend-kubernetes/src/wire.rs` (no status op in the `Request` enum), `desktop/src-tauri/src/managed_agents/reserved_env_keys.rs` (`BUZZ_ACP_NO_PRESENCE`), and one desktop UI call site combining the deployment-bookkeeping axis with relay presence; separately identify the relay's own `/_liveness`/`/_readiness` HTTP probes (`crates/buzz-relay/src/router.rs`, `deploy/charts/buzz/values.yaml`) as the concrete, structurally-different mechanism to draw the boundary against #1138. Record `git rev-parse HEAD` as the provenance revision. ← RUNS HERE
  done when: every claim planned for the node's evidence ledger has been read at its cited source and a specific path (with line numbers where feasible) is in hand, and the recorded revision is captured.

STEP 2  [needs 1] Write the node's front matter against `templates/concept.md`: `id: layers-compute-liveness`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`, one evidence entry per claim from STEP 1 classified FACT (opened and read directly), INFERENCE (reasoned, with `confidence`) or TEAM_KNOWLEDGE (issue-sourced, with `provided_by` — the #1041/#1042/#1043/#1045/#1046/#1138 sibling-scope claims land here since GitHub issues are not openable files), and `relationships: [{type: references, target: architecture-containers-agent-runtime}]` (the one existing merged node whose subject — the `buzz-acp` harness — is the actual mechanism that publishes the presence heartbeat this node documents; no `layers/*` sibling target exists yet, per ALREADY TRUE).
  done when: front matter is present and every array in it is non-empty per the schema's own required-field rules (checked mechanically in STEP 4).

STEP 3  [needs 2] Write the body per `templates/concept.md`'s required sections: a one-sentence definition first (compute liveness for a Buzz agent instance is answered exclusively by relay presence, kind:20001, not by a substrate status query or by deploy bookkeeping), a short Mermaid diagram of the presence flow (harness -> relay event -> desktop reads it), a Background subsection on the M1 design axiom and the 90s->180s TTL history, a Use cases subsection, and a Scope-and-omissions section that (a) names the five sibling `layers/compute/*` tasks and the `layers/observability/liveness.md` sibling by issue number and states why none is a relationship target yet, (b) explicitly excludes the provider protocol shape, the Kubernetes binding, start/stop/auto-stop lifecycle policy, local/mesh substrate specifics, the deploy-state-machine startup classification, and the relay's own process-level `/_liveness` probe, and (c) names what was expected to be verified and could not (e.g. whether issue #3783 cited by the spec is this repo's own issue numbering or upstream's).
  done when: every DoD bullet from the issue body is satisfied by a specific section (definition-first sentence, boundaries/non-goals, links to related implementation/verification and — where they exist — corpus nodes, examples that introduce no second concept).

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root and fix and re-run until it exits 0. Then, as the sole command in its own tool call, run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` to earn the verify-gate stamp, confirm `OK`, and commit with `git commit -s`.
  done when: `validate.py` exits 0 and the unittest run reports `OK`, in that order, before any commit is made.

PARALLEL: none — one file, one task, no independent sub-work to fan out.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 before proposing completion (AGENTS.md's own "Running the check" section and this Feature's acceptance criteria). The verify-gate stamp command in STEP 4 must run as a lone command with no other tool call folded into it, per this repo's gate-matching convention. `review-adjudicate` and the cross-model final review pass are the batch owner's job on the eventual shared PR, not run here.

BUDGET: small — one Markdown file (~150-250 lines of body + a ~20-entry evidence ledger), evidence gathering scoped to `docs/remote-agents.md` plus roughly ten already-identified source files, no code changes, no test changes.

OPEN: Whether `#3783`, which `docs/remote-agents.md` cites as the issue that raised `PRESENCE_TTL_SECS` from 90s to 180s, is a `launchpad-26/buzz` issue or an upstream `block/buzz` issue was not resolved in STEP 1 and is not this task's to resolve — the node records the spec's own claim as a FACT about what the spec states, not as independently confirmed issue history, and flags the gap in scope-and-omissions per STEP 3. Whether `layers/observability/liveness.md` (#1138) will in fact cover the relay's `/_liveness`/`/_readiness` probes is this node's own reasoned placement (its issue body carries no scope detail beyond the file path) and should be revisited once #1138 lands — a builder must not upgrade that placement to a stated fact about #1138's contents.

LEFT OUT: No relationships to any `layers/compute/*` or `layers/observability/*` sibling node — none are merged on `origin/launchpad` yet (ALREADY TRUE). No attempt to document the general provider protocol (#1041), the Kubernetes binding (#1042), start/stop/auto-stop lifecycle (#1043), local-only compute (#1045), mesh/shared compute (#1046), or the relay's own process-level liveness probes (#1138) — each is named only to draw this node's boundary against it, never explained on its own terms. No new corpus template, no schema change, no code change to any liveness-adjacent behavior. No independent verification of `#3783`'s repository or content beyond what `docs/remote-agents.md` itself states.
