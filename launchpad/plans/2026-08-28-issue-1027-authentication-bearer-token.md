# Issue #1027 — layers/authentication/bearer-token.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and the `standards/` batch (identifiers, naming, taxonomy, evidence, etc.) are merged on `origin/launchpad`. `launchpad/docs/corpus/layers/authentication/bearer-token.md` does not exist yet, and no `layers/` directory exists yet in the corpus at all — this is the first `type: layers` node. `launchpad/docs/corpus/templates/concept.md` is merged and matches this issue's DoD tail (define in one sentence, state boundaries/non-goals, link related concepts, examples only to clarify) closely enough to apply directly. Precedent for multi-segment ids under a subdirectory is the already-merged `architecture/**` batch (e.g. `architecture-flows-websocket-authentication`), not the older, smaller-sample `corpus-`-prefixed convention `standards/naming.md` documents — that document's own SHOULD guidance is to follow observed corpus convention, and the larger, more recent batch is the convention actually in force. `architecture/flows/websocket-authentication.md` (merged, id `architecture-flows-websocket-authentication`) explicitly names NIP-98 HTTP Auth (kind:27235) as "not yet in this corpus" and "deserves its own node" — that is this task.

STEP 1  Gather evidence: read `crates/buzz-auth/src/nip98.rs`, `nip98_replay.rs`, `crates/buzz-auth/src/lib.rs` module doc (the Auth-paths table and security invariants), `crates/buzz-core/src/kind.rs` (`KIND_HTTP_AUTH = 27235`, `KIND_AUTH = 22242`'s "carries bearer tokens" comment), `crates/buzz-relay/src/api/bridge.rs` (`verify_bridge_auth`, `require_auth_token` dev-mode fallback), `crates/buzz-relay/src/config.rs` (`BUZZ_REQUIRE_AUTH_TOKEN`), `crates/buzz-relay/src/nip11.rs` ("REST API token toggle" comment), `crates/buzz-relay/src/api/git/transport.rs` and `crates/buzz-relay/src/api/invites.rs` (other NIP-98-gated REST endpoints), and `docs/multi-tenant-relay.md` (which names the pattern "a NIP-98-minted bearer token on REST" directly). Separately confirm the `buzz-db` `api_tokens` table (`crates/buzz-db/src/api_token.rs`) — check whether any relay handler actually calls its lookup functions (`get_api_token_by_hash*`) to verify an incoming request; a repo-wide grep at dispatch time found none, only the multi-tenant conformance test — so the node must state this as a currently-unwired, adjacent scaffold rather than describe it as live authentication behavior. Re-check `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` immediately before finalizing front matter for the true current set of merged nodes (`architecture-flows-websocket-authentication`, `architecture-flows-http-event-submission`, `architecture-flows-git-push` are confirmed present as of dispatch and are candidate `references` targets; re-verify, since siblings in this same batch are merging concurrently). ← RUNS HERE

STEP 2  [needs 1] Write front matter: `id: layers-authentication-bearer-token`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, one evidence entry per claim (FACT for anything opened directly in STEP 1, INFERENCE with `confidence` only where reasoning is required, TEAM_KNOWLEDGE with `provided_by` naming this issue/PRD for DoD-derived requirements), and `relationships: [references -> architecture-flows-websocket-authentication, references -> architecture-flows-http-event-submission, references -> architecture-flows-git-push]` if all three still resolve against `origin/launchpad` when checked in STEP 1; drop any that don't.

STEP 3  [needs 2] Write the body against `templates/concept.md`'s required sections (Definition — the one-sentence term plus scope/boundary; Use cases; optional Comparison against NIP-42 WS auth and against the unwired `api_tokens` DB scaffold; Related resources via the `relationships` above; Scope and omissions naming both what's out of scope and what STEP 1 could not verify). The Definition must explicitly disambiguate two different things the corpus/codebase already calls "bearer token": (a) the NIP-98-signed-event credential presented per-REST-request — this node's subject — and (b) an opaque token that may ride *inside* a NIP-42 AUTH event's content, which is why kind:22242 events are never stored/logged (`buzz-core/src/kind.rs`, `buzz-db/src/lib.rs` crate doc) — a different concept the node names but does not adopt as its own subject, so a future reader doesn't conflate the two.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 5  [needs 4] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the verify-gate stamp, confirm `OK`, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK` before commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here; the PR is opened as a draft and says so explicitly.

BUDGET: small — one document, no code changes; evidence gathering scoped to ~10 named files already located during planning.

OPEN: Whether `api_tokens` (`buzz-db`) is dead code, a future feature awaiting its HTTP surface, or wired somewhere this task's grep missed is not settled by this task — it is reported as an honest gap (checked, not found wired) rather than investigated further, since resolving it is implementation work, not corpus authoring. Whether all three candidate `references` targets still resolve on `origin/launchpad` at commit time must be re-checked in STEP 1/STEP 2, since sibling corpus tasks in the same batch (#1023–#1192) are merging concurrently and the set can shrink or grow between planning and commit.

LEFT OUT: No step-by-step verification walkthrough of `verify_nip98_event` (that is `architecture-flows-http-event-submission`'s and a future NIP-98-flow node's territory, per `concept.md`'s own boundary against procedure/reference content — this node explains what a bearer token is and where the pattern shows up, not how each byte is checked). No edits to `architecture/flows/websocket-authentication.md`'s own "not yet in this corpus" gap note or to any other existing corpus node. No claim about whether `api_tokens` will ever be wired, and no implementation work to wire it.
