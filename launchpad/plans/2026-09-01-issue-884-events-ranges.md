# Issue #884 — corpus node: events/ranges.md

Issue #884, parent Feature #616 ("interface and event contract corpus exists").

Stated size: issue carries no explicit Size label (labels: type:task, area:docs, by:agent); dispatching instructions cap this single-document task -> cap: 5 steps.

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad` at HEAD 650354eab8d41ab6ce1a71de079a6c6d95c69052 (this worktree's own HEAD, checked out directly from `origin/launchpad`). The target file `launchpad/docs/corpus/events/ranges.md` does not exist yet, and no `events/` subdirectory exists yet under `launchpad/docs/corpus/` (`ls` confirms). No corpus node anywhere in the tree currently carries `type: interfaces-events` except two template nodes (`templates/event-kind.md`, `templates/interface.md`) — this will be the corpus's first real `interfaces-events` content instance. A fresh baseline run of `python3 launchpad/project-intelligence/corpus/validate.py` at this revision exits 0 with zero errors (360 non-fatal UNVERIFIED notices, no ERROR lines) — not the 21 pre-existing errors the dispatching instructions described; that gap is closed already at this HEAD and is recorded under OPEN below rather than silently assumed away.

STEP 1 [independent] — Gather evidence: read `crates/buzz-core/src/kind.rs` in full (already done pre-plan: the NIP-01 four-category cross-check helpers `is_ephemeral`, `is_replaceable`, `is_parameterized_replaceable`, the `PARAM_REPLACEABLE_KIND_MIN/MAX` and `EPHEMERAL_KIND_MIN/MAX` constants, and every named-band comment: NIP-29 group admin 9000-9022, NIP-43 relay admin 9030-9033, NIP-IA archival 9035-9036 plus relay-signed 8002-8003/13535, Buzz moderation commands 9040-9044, NIP-51/NIP-65 replaceable lists plus agent profile 10000-19999, ephemeral 20000-29999, NIP-33/NIP-29 addressable 30000-39999, stream messaging 40001-40100, relay-only sidecar 40901-40902, direct messages 41000-41999, product feedback 42000, agent job protocol 43000-43999, member notifications plus agent turn metric 44100-44200, forum 45000-45999, workflow engine 46000-46999, user groups 47000-47999 reserved with no kinds defined, system/admin 48000-48999, media 49000-49999); confirm the pinned NIP-01 commit SHA already used as evidence elsewhere in the merged corpus (`templates/event-kind.md`'s citation to the nostr-protocol/nips repository's 01.md at commit dabfcb2aaecf4fa374eda8b1232ab303a03f60ba) resolves and reuse it rather than re-pinning a second SHA for the same spec. done when: a list of every kind.rs band comment plus its numeric range and NIP-01 category classification has been transcribed into scratch notes, cross-checked against the four boolean helpers.

STEP 2 [needs 1] — Write `launchpad/docs/corpus/events/ranges.md`: front matter (`id: events-ranges` — matching the observed real-content-node convention of a path-joined-by-dashes identifier seen in every merged `architecture/**` node, e.g. `architecture-containers-relay`, `architecture-flows-event-ingestion`, none of which carry a `corpus-` prefix, since that prefix convention is observed only on meta-documents about the corpus itself per `standards/naming.md`; `type: interfaces-events`, the enum's dedicated protocol/event-surface value, matching the precedent stated in `templates/event-kind.md`; `status: draft`; `origin: launchpad`; `audiences: [agent, developer, reviewer]`; no `relationships` field, since this step's own read of the corpus tree finds no merged node whose subject is Nostr event kinds, NIP-01, or a numeric-range convention to target). Body: one table for NIP-01's four canonical numeric categories cross-checked against `kind.rs`'s own helpers and constants, one table for Buzz's authored custom numeric bands (grouped by `kind.rs`'s own section comments, each row labeled with its NIP-01 category and representative kinds), an explicit generated-versus-authored distinction (NIP-01's four bands are protocol-mandated; the decimal-thousand custom bands are Buzz's own unwritten convention, evidenced only by code comments, not a spec), and a scope section stating this node does NOT cover: per-kind tag/content/access-control semantics (owned by future `kind-*.md` instance nodes built from `corpus-template-event-kind`, none of which exist yet), the four access-control set memberships (`AUTHOR_ONLY_KINDS`/`P_GATED_KINDS`/`SHARED_GATED_KINDS`/`RESULT_GATED_KINDS`), and kind-renumbering history recorded as inline `kind.rs` comments (e.g. "V1 used kind:10001 ... wrong"). done when: the file exists with schema-required front-matter fields present and a body containing both tables and the scope/omissions section. RUNS HERE.

STEP 3 [needs 2] — Run `python3 launchpad/project-intelligence/corpus/validate.py` against the full tree; capture the error count and compare it against the 0-error baseline recorded in ALREADY TRUE. Fix and re-run until the new node adds zero additional ERROR lines. done when: the run's error count equals the baseline (0) with the new node present, or every discrepancy is fixed and re-verified until it does.

STEP 4 [needs 3] — Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call; confirm it prints `OK`. done when: the command's own stdout contains `OK` with no failures.

STEP 5 [needs 4] — In a separate tool call from STEP 4's gate command, `git add` both new files and commit with `git commit -s -m "docs(corpus): document event kind numeric ranges (#884)"`. If the commit is rejected for a missing gate stamp, stop and report it as a finding rather than routing around it. done when: `git log -1` shows the new commit with a `Signed-off-by` trailer, or the run stopped and reported the exact rejection reason.

PARALLEL: none — one file, one worktree, no independent workstream to run alongside it.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must not introduce any new ERROR line beyond the 0 recorded in ALREADY TRUE. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit gate in STEP 5. No PR is opened, pushed, or merged in this task — reporting stops at a local commit SHA per the dispatching instructions.

BUDGET: single document, one sitting — no multi-hour scope expected.

OPEN: the dispatching instructions describe "21 KNOWN pre-existing validation errors ... tracked as issue #1951" as the current state of `origin/launchpad`. The fresh baseline run in ALREADY TRUE shows 0 errors at this worktree's HEAD (650354eab8d41ab6ce1a71de079a6c6d95c69052, checked out directly from `origin/launchpad`) — either #1951 has already been fixed upstream since those instructions were written, or the 21-error count referred to a different root or revision. This plan treats "zero new errors introduced by this node" as the actual bar (matching the dispatching instructions' own stated fallback: "that is your bar for 'validation passes,' not a global exit-0" — satisfied a fortiori since global exit-0 already holds). Not re-litigated further; recorded so a reviewer does not mistake the discrepancy for an unnoticed regression.

LEFT OUT: no `relationships` edges (no sibling corpus node's subject overlaps, checked in step 1/step 2, not assumed); no attempt to author any real `kind-*.md` instance node from `corpus-template-event-kind` (that is separate future work, explicitly out of this task's scope per the issue's own "Out of scope" list); no reconciliation of `kind.rs`'s inline migration-history comments into this document (recorded as a named omission in the node's own scope section instead).
