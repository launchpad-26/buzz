# Issue #1076 — layers/data/postgres/backup-boundary.md
Stated size: no `Size` line in the issue → cap: 5 steps (per the overnight batch dispatch brief: "this is one small document")

ALREADY TRUE  (verified against git, not notes)
  `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/datastore.md` and `launchpad/docs/corpus/architecture/containers/postgres.md` (`architecture-containers-postgres`) are merged on `origin/launchpad` (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`).
  `launchpad/docs/corpus/layers/data/postgres/backup-boundary.md` does not exist yet, and neither does the `layers/` subtree at all on `origin/launchpad`.
  Three sibling documents from this same overnight batch (`layers-data-object-storage-minio`, `layers-data-object-storage-role`, `layers-data-authoritative-data`, read directly from their local branches `task/1071-minio`, `task/1073-object-storage-role`, `task/1060-authoritative-data`) all chose `type: layers` over `templates/datastore.md`'s own `type: architecture` suggestion, disclosing the tension in their evidence ledgers; this task follows that precedent rather than re-deciding it.
  `deploy/charts/buzz/README.md`'s `## Backups` section (lines 210-218) and `deploy/charts/buzz/templates/NOTES.txt` (lines 45-47) already state, in this repository, the operator-facing list of five things that must be saved for Buzz to be recoverable, with PostgreSQL named item 2, "the canonical event store." No backup-automation tooling (`pg_dump`, `pg_basebackup`, `wal-g`, `pgbackrest`) or `buzz-admin` subcommand exists anywhere in this repository — confirmed by grep across `.md`/`.yaml`/`.yml`/`.toml`/`.tf` and a `*backup*` filename search, which returned only unrelated desktop client-side Nostr-key-backup files.
  `docs/push-gateway-deployment.md` (line 35, and its "PostgreSQL and replicas" section) documents a second, separate Postgres database for `buzz-push-gateway`, explicitly required to be a dedicated database distinct from the relay's — a real, checked boundary this node should name rather than conflate with the relay's own Postgres.

STEP 1  Confirm no further in-repo backup-related source was missed: re-check `ARCHITECTURE.md` (no backup/retention/RPO/RTO mentions found), `crates/buzz-db/src/migration.rs` (`SCHEMA_DESTRUCTION_LOCK_KEY`, relevant to schema-change safety but not backup itself), and `architecture-containers-postgres.md`'s own scope-and-omissions table, which already names "Production/staging Postgres provisioning and topology" as owned by `squareup/block-coder-tf-stacks` (private, not opened by this task) — the same external-repo boundary this node draws for backup automation specifically.  [independent]
        done when: every fact used in STEP 2's evidence ledger has been read directly from the file it cites, in this worktree, this session.

STEP 2  Write front matter (id `layers-data-postgres-backup-boundary`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, one `relationships` entry: `part-of` → `architecture-containers-postgres`) and the body: what "backup boundary" means for this datastore (Postgres is the authoritative/canonical event store, not a cache or derived projection, per `architecture-containers-postgres.md`); what this repository actually defines about backup (the chart's save-these-five list) versus what it deliberately does not implement (no in-repo backup automation, schedule, or retention policy); the cross-store consistency implication (no point-in-time coordination between the Postgres backup, the S3 bucket, and the Git PVC exists in this repository); tenancy (one shared multi-tenant Postgres instance — a backup/restore is community-boundary-blind, unlike per-community deletion); failure/restore behavior per the chart's own restore notes; and the push-gateway's separate database as an explicit non-subject. Scope-and-omissions section discloses the `type: layers` vs. datastore-template's `type: architecture` tension per `standards/taxonomy.md`'s "say so, don't silently pick" rule, plus what was expected but not verified (whether `squareup/block-coder-tf-stacks` actually runs scheduled backups).  [needs 1]
        done when: the file exists at `launchpad/docs/corpus/layers/data/postgres/backup-boundary.md` and every evidence entry cites a path that exists in this worktree.

STEP 3  Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until it exits 0.  [needs 2]  ← RUNS HERE
        done when: the command's exit code is 0.

STEP 4  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the verification stamp, then commit the plan + document in a separate call.  [needs 3]
        done when: the unittest run reports OK, and `git log -1` on this branch shows a signed-off commit containing both files. No push, no PR — this branch is bundled by a later batch step.

PARALLEL  None. All four steps touch the same one document (or gate it), and this is a single isolated worktree task with no sibling work to fan out to.
GATES     `python3 launchpad/project-intelligence/corpus/validate.py` after STEP 3, must exit 0. `python3 -m unittest discover ...` before the commit in STEP 4, must report OK. `review-adjudicate` and cross-model final review are deferred to the batch owner's later bundling/review pass — not run here. No `qa` explore mode — this is a docs-only change with no runtime interface to exercise.
BUDGET    STEP 2 (drafting the body) is most likely to eat the budget — distinguishing a backup-*boundary* claim (what this repo defines and where its responsibility stops) from a full datastore document's operational-characteristics section requires care not to duplicate `architecture-containers-postgres.md`.
OPEN      Whether `squareup/block-coder-tf-stacks` (private, external) actually implements scheduled Postgres backups is unresolved — named as a gap in the document rather than guessed at.
LEFT OUT  No relationship to sibling `layers/data/postgres/*` documents from this same batch (events-table, migrations, partitions, etc.) — none are merged on `origin/launchpad` yet, so none are legal relationship targets. No implementation or proposal of backup automation — out of scope per the issue's own "Out of scope" section (no runtime product behavior changes). No attempt to resolve the `type: layers` vs. `type: architecture` tension — named and left for a later corpus-wide pass, per precedent.
