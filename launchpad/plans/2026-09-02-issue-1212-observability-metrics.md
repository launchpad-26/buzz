# Issue #1212 — operations/observability/metrics.md

Stated size: issue #1212 carries no explicit Size label; the corpus-batch-author dispatch brief for this Feature #618 batch caps every task at one hand-authored document  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/reference.md`, `launchpad/docs/corpus/layers/observability/metrics.md` (id `layers-observability-metrics`) and `launchpad/docs/corpus/layers/observability/prometheus.md` (id `layers-observability-prometheus`) are merged on `origin/launchpad` (confirmed against `<SCRATCH>/existing-node-ids.txt`). `launchpad/docs/corpus/operations/observability/metrics.md` does not exist yet (confirmed with `ls`). The worktree and branch `task/1212-observability-metrics` already exist and are checked out.

STEP 1  [independent] Gather evidence: grep every crate under `crates/` for `metrics::(counter|gauge|histogram)!` call sites (including multi-line invocations) to build the actual metric-name/type/label catalog; read `crates/buzz-relay/src/metrics.rs` and `crates/buzz-push-gateway/src/metrics.rs` in full for installation/exposition mechanics; read the representative call-site files (`handlers/event.rs`, `handlers/auth.rs`, `handlers/ingest.rs`, `handlers/side_effects.rs`, `connection.rs`, `state.rs`, `storage_sweep.rs`, `main.rs`, `push_runtime.rs`, `subscription.rs`, `tunnel/directory.rs`, `api/git/*.rs`, `api/media.rs`, `api/mod.rs`, `api/bridge.rs`, `api/gifs.rs`, `handlers/command_executor.rs`, `handlers/count.rs`, `handlers/moderation_notices.rs`, `handlers/req.rs`, `buzz-db/src/runtime/observability.rs`, `buzz-db/src/runtime/mod.rs`, `buzz-datastore-tracing/src/lib.rs`, `buzz-relay-mesh/src/lib.rs`) for label sets and multi-tenancy (`community`) cardinality; read the Helm chart's `service.yaml`, `servicemonitor.yaml`, `values.yaml` scrape/port settings and the dev-compose `prometheus.yml`/`compose.dev.yml` scrape target. done when: a deduplicated list of every distinct metric name found in source exists, each backed by at least one opened file, plus the scrape-configuration facts from the chart and dev-compose files. ← RUNS HERE

STEP 2  [needs 1] Write the front matter (schema-valid: id `operations-observability-metrics`, type `operations`, status `draft`, origin `launchpad`, audiences `[operator, developer]`, one commit-only provenance `FACT`, a `references` relationship to `layers-observability-metrics` and `layers-observability-prometheus` — both merged on `origin/launchpad` per `<SCRATCH>/existing-node-ids.txt` — and one evidence entry per grouped claim from STEP 1, each citing the specific files opened for that group) and the body, following `launchpad/docs/corpus/templates/reference.md`'s required sections: a Reference description paragraph; a structured-entries table of every metric name/type/labels found in STEP 1, grouped by subsystem in source order rather than alphabetically; an optional Commands-style scrape-configuration table (ServiceMonitor, Helm values, dev-compose); a Boundary statement naming this node as reference-only (not concept, not how-to, not API reference) and naming the two already-merged `layers/observability` siblings and the in-flight `operations/observability` siblings (logs #1211, traces #1213, alerts #1209, dashboards #1210) without linking the unmerged ones; a Relationships section; and a Scope-and-omissions section carrying both what this node does not cover/who owns it and what was expected but could not be verified. done when: the file exists at `launchpad/docs/corpus/operations/observability/metrics.md` with all required template sections present.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repository root; fix any error it reports (schema violations, unresolved relationship targets, unrecognized citation shapes) and re-run until exit 0. done when: the command exits 0.

STEP 4  [needs 3] Self-review the drafted node against issue #1212's Definition of Done bullet-by-bullet (one hand-authored document, schema-valid front matter, one independently maintainable idea, every substantive claim traceable and classified, links without duplicating canonical content, checked against the recorded revision, validation passes, structured for lookup, generated-vs-authored values labeled, scope-and-omissions present, authoritative source/schema/config linked); fix anything that fails; re-run STEP 3's validate command if the body or ledger changed. done when: every DoD bullet is satisfied or its gap is named in the body's scope-and-omissions section, and `validate.py` still exits 0.

STEP 5  [needs 4] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own Bash call (not chained, no piped output) to earn the commit-gate stamp; confirm `OK`; then, in a separate tool call, `git add -A && git commit -s -m "docs(corpus): document operations metrics reference (#1212)"`. done when: the test command reports `OK` and `git log -1` shows the new commit with a `Signed-off-by` trailer.

PARALLEL: none — single document, single task, no independent sub-work.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 (STEP 3/4). The corpus unittest suite must report `OK`, run unpiped as its own Bash call, before the commit (STEP 5) — this is the mechanism that earns the verify-gate stamp; do not edit the stamp file and do not use `--no-verify` if the gate still refuses. `review-adjudicate` and the cross-model final review pass are the batch owner's to run later against the integrated Feature PR, not run here.

BUDGET: small-to-medium for a single document — no code changes; evidence gathering is a repo-wide grep across `crates/` (~30 files with actual metric call sites) plus half a dozen chart/compose/config files, all read-only.

OPEN: Whether the ~153 metric names this node catalogues should later become a `generated/` artifact derived mechanically from source (per `AGENTS.md`'s note that #1316 owns provenance for generated content) rather than a hand-maintained table that can silently drift after this commit — not decided here; the body states plainly that this is a point-in-time enumeration and names the exact grep command a reader can re-run to refresh it.

LEFT OUT: No claim about logs, traces, alerts, or dashboards content (#1211, #1213, #1209, #1210) — named as sibling reference nodes in the boundary section, not linked, since none is merged on `origin/launchpad` yet. No relationships declared toward those in-flight siblings. No attempt to reconcile or edit `layers/observability/metrics.md` or `layers/observability/prometheus.md` — both are read-only inputs here. No new Prometheus alerting rules, dashboards, or runbook content invented; this node reports only what source already emits and how it is scraped.
