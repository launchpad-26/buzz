# Issue #1180 — layers/security/threat-model.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/threat-model.md` (id `corpus-template-threat-model`), and `launchpad/docs/corpus/architecture/context/buzz-platform.md` (id `architecture-context-buzz-platform`) are merged on `origin/launchpad`; `launchpad/docs/corpus/layers/security/threat-model.md` does not exist yet. No `layers/` directory exists in the corpus yet — this is the first node of that surface.

STEP 1  Gather evidence: read `templates/threat-model.md` in full (STRIDE + OWASP four-phase model, required sections, DFD/Mermaid convention) and open the real relay code backing each threat/mitigation row this node will assert — NIP-42/NIP-98 verification (`crates/buzz-auth/src/nip98.rs`, `crates/buzz-core/src/verification.rs`), tenant/community boundary (`crates/buzz-core/src/tenant.rs`, `crates/buzz-relay/src/tenant.rs`), the p-gate (`crates/buzz-relay/src/handlers/req.rs::p_gated_filters_authorized`, its HTTP call sites in `crates/buzz-relay/src/api/bridge.rs` returning `StatusCode::FORBIDDEN`), the audit hash chain (`crates/buzz-audit/src/hash.rs`, `service.rs::verify_chain`), Blossom media auth (`crates/buzz-media/src/auth.rs`, `crates/buzz-relay/src/api/media.rs`), the AI agent shell/file tools (`crates/buzz-dev-mcp/src/shell.rs`, `str_replace.rs` — no OS sandboxing), workflow webhook auth (`crates/buzz-relay/src/api/bridge.rs::workflow_webhook`, secret-header check), git smart-HTTP auth (`crates/buzz-relay/src/api/git/transport.rs`), and rate limiting (`crates/buzz-auth/src/rate_limit.rs`, fixed-window, documented 2x-burst weakness). Also check `docs/remote-agents.md`'s "Known Defects" section (~line 1581) for agent-harness-adjacent threats already catalogued there, and confirm no `layers/security/residual-risks.md` exists yet (sibling #1174, open, not yet drafted) to avoid duplicating its scope. ← RUNS HERE

STEP 2  [needs 1] Decide and justify `type: layers` in front matter (PRD #602 names "layers" as one of the corpus's own enumerated in-scope surfaces, matching the issue's own target path `layers/security/`, taking precedence over the template's generic `architecture`-by-default guidance — document this deviation explicitly in the node body per the template's own requirement). Write front matter: `id: layers-security-threat-model`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`, one `evidence` entry per substantive claim (commit citation for the recorded revision; FACT entries for every code citation above, opened directly), and `relationships`: `implements` → `corpus-template-threat-model` (merged, real target), `depends-on` → `architecture-context-buzz-platform` (the system this analysis threat-models; merged, real target).

STEP 3  [needs 2] Write the body against the template's required sections: purpose/scope, notation legend, one Mermaid DFD (external entities: Nostr clients, AI agents, relay operator; process: the relay; data stores: Postgres/Redis/S3; trust boundary: the per-host community boundary), a STRIDE threat table (one row per element/trust-boundary crossing, cited to the code opened in STEP 1), a mitigations table using the template's four literal status values (Not Started / Needs Investigation / Mitigated / Not Applicable), review-and-validation, boundary statement (excluding SECURITY.md-style disclosure policy, org-wide control catalog, pen-test reports, and #1174's residual-risks scope), and scope-and-omissions naming what is expected-but-unverified (e.g., no live pen-test evidence, no verification of Redis-backed rate-limiter behavior under real load).

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Re-read the diff against every DoD bullet and re-open every FACT citation to confirm it actually supports its claim.

STEP 5  [needs 4] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the verification stamp, confirm `OK`, then commit (`git commit -s`) in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK` before commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here (self-review only).

BUDGET: medium — one document, no code changes, but a system-wide STRIDE catalogue needs evidence gathered across ~8 crates (buzz-auth, buzz-core, buzz-relay, buzz-audit, buzz-media, buzz-dev-mcp, buzz-workflow, git transport).

OPEN: Whether `type: layers` or `type: architecture` is the better front-matter choice is not settled by any prior merged `layers`-type node (none exists yet) — this node's body states the reasoning (PRD #602's own surface enumeration plus the issue's literal target path) rather than treating it as obviously correct. `depends-on: architecture-context-buzz-platform` targets the whole-platform context node rather than a single container, because the issue explicitly scopes this as "the full threat catalogue... for Buzz" system-wide, distinct from #1174's residual-risks doc — a narrower per-container threat model is left as future decomposition, per the template's own guidance to prefer smaller scopes once container-level nodes need their own dedicated models.

LEFT OUT: No `layers/security/residual-risks.md` content (sibling #1174, separate task, open). No new corpus relationships to sibling `layers/` nodes (none exist yet). No code or config changes — evidence-gathering and documentation only. No live penetration test or dynamic security scan — this is a static, code-evidenced design-time analysis per the template's own boundary section.
