# Issue #1207 — operations/deployment/push-gateway.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/procedure.md`, `launchpad/docs/corpus/architecture/containers/push-gateway.md` (`architecture-containers-push-gateway`), and `launchpad/docs/corpus/architecture/flows/push-notification.md` (`architecture-flows-push-notification`) are present in this worktree (the latter two are `status: draft`, not yet confirmed merged on `origin/launchpad` — checked against `<SCRATCH>/existing-node-ids.txt`, which lists both ids as currently resolving). `launchpad/docs/corpus/operations/deployment/push-gateway.md` does not exist yet (confirmed: `launchpad/docs/corpus/operations` has no tree in this worktree). `docs/push-gateway-deployment.md`, `Dockerfile.push-gateway`, `deploy/charts/buzz-push-gateway/`, and `crates/buzz-push-gateway/` exist and are current (`docs/push-gateway-deployment.md` and `crates/buzz-push-gateway/src/config.rs` share the same last-touching commit, `c432a111c`, confirmed by `git log -1` on each). `.github/workflows/docker.yml`'s `push-gateway-build`/`push-gateway-merge` jobs carry `if: github.repository == 'block/buzz'` with an explicit comment that this fork does not operate the gateway's image-publish lane.

STEP 1  Gather evidence beyond what is already read: confirm `crates/buzz-push-gateway/migrations/0001_push_gateway_authority.sql` table list, `Justfile`'s `cargo nextest run -p buzz-push-gateway` line, `.env.example`'s relay-side `BUZZ_PUSH_ENABLED`/`BUZZ_PUSH_GATEWAY_DELIVERY_URL` entries, and `.github/workflows/push-gateway-helm-chart.yml`'s publish job (no `block/buzz`-only gate, unlike the image jobs) — record exact current variable names, chart value paths, and command text so the node's steps cite real, current commands rather than the container node's now-stale variable names (`BUZZ_PUSH_APP_ATTEST_APP_ID` etc., superseded by `BUZZ_PUSH_DOGFOOD_*` in the same commit that touched both the doc and `config.rs`). ← RUNS HERE

STEP 2  [needs 1] Write the front matter (id `operations-deployment-push-gateway`, type `operations`, status `draft`, origin `launchpad`, audiences `[operator, developer]`, no `relationships` — the only two candidate targets, `architecture-containers-push-gateway` and `architecture-flows-push-notification`, are themselves `status: draft` and not confirmed present on `origin/launchpad`; declaring an edge to either risks the exact "validates in worktree, hard-errors in CI" trap `AGENTS.md` and the naming/linking standards warn about, so they are named in prose instead) and the body, following the procedure template's skeleton: Overview; Before you start (prerequisites: dedicated Postgres database, the two Kubernetes Secrets the chart expects, Apple App Attest root + dogfood APNs cert material, Helm 3.16+); one numbered task sequence to deploy/upgrade (image, secrets, values override, `render.sh`, `helm upgrade --install`, migration-Job verification, readiness/rollout verification, traffic routing, relay-side opt-in) and a second to roll back; See also (naming the container/flow nodes and the deployment doc in prose, not as broken relationships); Boundary; Relationships section stating none declared and why; Scope and omissions carrying two distinct things per `AGENTS.md` step 8 — what the node does not cover (incident response for push-delivery failure, owned by sibling issue #1225; the full variable-by-variable reference, owned by `docs/push-gateway-deployment.md`) and what was expected but could not be verified (whether this fork operates any live push-gateway deployment — no evidence found).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repository root; fix any reported error (schema violations, unresolved citations, non-existent paths) and re-run until exit 0. Treat `UNVERIFIED` notices as expected for the commit-only provenance `FACT` and the container/flow-node `TEAM_KNOWLEDGE` mentions, not as failures to chase.

STEP 4  [needs 3] Self-review the diff against issue #1207's Definition of Done bullet by bullet (see report step 8 of the dispatch brief), then run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own Bash call (no `cd` chained, no piping) to earn the commit-gate stamp, confirm `OK`, then in a separate call run `git add -A && git commit -s -m "docs(corpus): document push gateway deployment (#1207)"`. Do not push and do not open a PR.

PARALLEL: none — single document, single task, no code changes.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must print `OK` and must run as the sole, unpiped command in its own Bash call for the verify-gate to stamp the commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch orchestrator's later integration review — not run here.

BUDGET: small — one corpus document plus this plan file, no source-code changes, evidence gathering scoped to `crates/buzz-push-gateway/` (config, migrations), `docs/push-gateway-deployment.md`, `deploy/charts/buzz-push-gateway/` (chart values, templates, tests), `Dockerfile.push-gateway`, `.github/workflows/docker.yml` and `push-gateway-helm-chart.yml`, `Justfile`, and `.env.example`.

OPEN: Whether `architecture-containers-push-gateway` and `architecture-flows-push-notification` will have merged to `origin/launchpad` by the time this task's own branch integrates — both are `status: draft` in this worktree and their presence in `<SCRATCH>/existing-node-ids.txt` reflects a point-in-time snapshot, not a guarantee at merge time. This plan does not declare `relationships` to either, so the outcome is names-in-prose either way, but a follow-up pass could add typed edges once both are confirmed merged. Whether this fork intends to ever operate a live push-gateway deployment, or whether the correct long-term framing is "documents upstream's mechanism, inherited but unused here," is a product decision outside this task's scope — the node states the observed fact (CI gated off, no evidence of a live deployment) and does not resolve the question.

LEFT OUT: No new corpus node for the NIP-PL wire protocol, the relay's own container, or push-delivery-failure incident response (#1225) — each is out of scope per the brief and per the container/flow nodes' own stated gaps. No change to `docs/push-gateway-deployment.md`, the Helm chart, or any application code — this task is documentation-only. No attempt to actually run `helm upgrade --install` against a live cluster; the procedure's commands are grounded in the chart's own render/lint tests (`deploy/charts/buzz-push-gateway/tests/render.sh`), not in an executed deployment, and this gap is named explicitly in the node's own scope-and-omissions section per the procedure template's evidence expectations.
