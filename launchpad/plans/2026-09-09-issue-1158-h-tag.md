# Plan — issue #1158: document the `h` tag

**Issue:** launchpad-26/buzz#1158 (Feature #609, protocol and networking layer corpus)
**Target:** `launchpad/docs/corpus/layers/protocol/h-tag.md`, node id `layers-protocol-h-tag`
**Template:** `launchpad/docs/corpus/templates/concept.md`
**Worktree:** `__worktrees/task-1158-h-tag`, branch `task/1158-h-tag`
**Provenance revision:** `29ca9b189bd3f639ba09c972b57c70538c0860c6` (`git rev-parse HEAD`
in the worktree; confirmed with `git cat-file -e`, exit 0)

## Shape

One concept node. Subject: the `h` tag — the NIP-29 group tag whose value is a channel
UUID, and the mechanism by which the relay assigns an event to a channel. Not a
reference catalogue of every kind, not a procedure.

## Steps (max 5)

1. **Evidence gathering — done before this plan was written.** Opened
   `crates/buzz-relay/src/handlers/ingest.rs` (`extract_channel_id`,
   `is_global_only_kind`, `requires_h_channel_scope`, the `channel_id` assignment block
   and its tests), `crates/buzz-core/src/filter.rs` (the `#h` fallback and
   `h_tag_fallback_uses_stored_channel_id`), `crates/buzz-sdk/src/builders.rs`
   (`extract_channel_id`), `crates/buzz-core/src/event.rs` (`StoredEvent.channel_id`),
   `crates/buzz-db/src/store/event.rs` (`global_only`), `schema/schema.sql` and
   `migrations/0001_initial_schema.sql` (the `channel_id` column and its indexes),
   `crates/buzz-relay/src/handlers/req.rs` (`#h` extraction on the read path),
   `crates/buzz-core/src/kind.rs`, `AGENTS.md`, and `docs/nips/`.
   *Done when:* every claim the node will make has a source that was actually opened.

2. **Write front matter.** Seven permitted fields per `schema/node.schema.json`;
   `type: layers`, `status: draft`, `origin: launchpad`, audiences `agent` + `developer`.
   One provenance FACT citing `commit 29ca9b18…` and no second commit-only FACT. Every
   other FACT rests on a bare repo-relative path. The absence of upstream NIP text in
   this repository is a tool-result citation in `identifier(args) -> result` shape.
   *Done when:* the ledger has one entry per substantive body claim, classed honestly.

3. **Write relationships.** Only ids verified present in the merged-id list:
   `capabilities-channels-channel`, `architecture-principles-community-is-security-boundary`,
   `implementation-crates-buzz-relay`, `implementation-crates-buzz-core`,
   `verification-contracts-nostr`. No #609 sibling — none is merged.
   *Done when:* each target was grepped out of the merged list before being written.

4. **Write the body** to the concept template's required sections: definition first,
   then how the relay honours it per kind (the three-way split: 23 kinds that require
   an `h` tag, 47 that are forced global, and the derived cases), the read-path
   `#h` fallback, the boundary against the host-derived community (link, do not
   restate), use cases, and a scope-and-omissions section carrying both a
   what-is-not-covered part and a separate expected-but-not-verified part.
   *Done when:* every section the template marks required is present and the node
   restates no merged sibling's canonical content.

5. **Validate, self-review, stamp, commit.** Run
   `python3 launchpad/project-intelligence/corpus/validate.py` to exit 0; re-read every
   evidence entry against its source; then run the corpus test suite as the sole
   foreground command; then `git add` + `git commit -s`. No push, no PR.
   *Done when:* the validator exits 0, the suite ends `OK`, and the commit exists.

## Out of scope

- A second hand-authored corpus node (the `d`, `e`, `p` and tag-vocabulary nodes are
  siblings #1148/#1149/#1162/#1155 and are not mine).
- Any change to runtime behaviour, including the known filter-layer limitation
  `is_global_only_kind`'s doc comment records.
- Restating what `architecture-principles-community-is-security-boundary` or
  `capabilities-channels-channel` already establish.
