# Issue #823 — capabilities/workflows/delay-action.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/architecture/flows/workflow-execution.md` (id `architecture-flows-workflow-execution`) is merged and already documents the whole workflow-run lifecycle (three trigger paths, the shared step loop, owner-authority/write-fence crossings, and generic failure/abort behavior) at the run level. `launchpad/docs/corpus/capabilities/workflows/delay-action.md` does not exist yet, and no `capabilities/` directory is merged on `origin/launchpad` at all yet, so there is no directory-local precedent for `type` beyond the `architecture/flows/*` → `type: architecture` convention that node itself sets. No template named for a single workflow action step exists in `launchpad/docs/corpus/templates/`; the closest is `templates/capability.md` (product-level "what," not one action's runtime contract) and `templates/flow.md` (right-shaped sections, but scoped to a multi-actor scenario, and itself typed `architecture`, not `capabilities`).

STEP 1  Gather evidence on the `Delay` action specifically (not the whole run lifecycle, which the merged flow node already owns): `ActionDef::Delay` schema (`crates/buzz-workflow/src/schema.rs:150-153`), that `resolve_step_templates` does *not* template-resolve `duration` unlike every other action (`crates/buzz-workflow/src/executor.rs:466-468` vs. `:419-465`), the runtime-only 270s cap enforced inside `dispatch_action`'s `Delay` arm rather than at `WorkflowDef::validate()` time (`crates/buzz-workflow/src/executor.rs:734-751`, confirmed absent from `validate()` at `schema.rs:173-280`), `parse_duration_secs`'s h/m/s/plain-number grammar and its unit tests (`executor.rs:786-814`, tests at `:1546-1572`), that `Delay` is excluded from `requires_elevated_authority` (only `CallWebhook` triggers it — `schema.rs:165-169`), and the community serving-write lease `dispatch_action` acquires around *every* action including `Delay` (`executor.rs:551-562`) and holds via `ServingWriteGuard::protect`'s `tokio::select!` race against lease-heartbeat loss for the full sleep duration (`crates/buzz-deletion/src/lib.rs:88-105`) — a race whose window is uniquely large for `Delay` among the six actions. Record `git rev-parse HEAD`. ← RAN HERE (evidence already gathered in this session)

STEP 2  [needs 1] Hand-author front matter directly against `node.schema.json` (no scaffold script available in this checkout beyond following the schema by hand): id `capabilities-workflows-delay-action`, type `capabilities` (following the `architecture/flows/*` → `type: architecture` directory convention, stated in the body as an explicit, confidence-rated inference, not a schema fact), status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, one `relationships: references` entry targeting `architecture-flows-workflow-execution` (exists on `origin/launchpad`) since that node owns the run-level trigger/authority/failure story this node narrows into one action. Body sections mirror the issue's own DoD checklist and `architecture-flows-workflow-execution`'s own precedent for the same checklist (from issue #688): Trigger/preconditions/termination, Ordered interactions and data/state movement, Trust-boundary crossings, Failure/abort/rollback (with representative verification), Scope and omissions — every substantive claim cited to the exact lines gathered in STEP 1, no re-derivation of what the referenced flow node already states about the run as a whole.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until it reports zero new FAIL entries beyond the known 21 pre-existing ones tracked in issue #1951.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the commit-gate stamp; confirm `OK`. Only then, in a separate call, `git add` the new document and this plan file and `git commit -s`. Do not push, do not open a PR (batch mode — a later integration phase opens one PR per Feature).

PARALLEL: none — single file, single task, no dependency on any sibling `capabilities/workflows/*` task in this same batch (#822, #829-#844), none of which are merged yet and therefore none are valid `relationships` targets.

GATES: `validate.py` must exit with zero new FAIL entries. The corpus unittest suite must print `OK`. `review-code`/`review-adjudicate`/`review-final` are deferred to the batch owner's integration-phase review, not run here — noted explicitly in the commit's self-review step.

BUDGET: small — one document, no runtime code changes, evidence scoped to `crates/buzz-workflow/src/{schema,executor}.rs`, `crates/buzz-deletion/src/lib.rs`, and the one already-merged sibling flow node.

OPEN: `type: capabilities` for a single action-step node is a genuine judgment call — PRD #602 lists `capabilities` as a product-level "what the system can do" surface (e.g. "Workflow engine"), and a single `Delay` action step is arguably an implementation detail of that one capability rather than a capability in its own right. The task's own target path (`capabilities/workflows/delay-action.md`, assigned by `corpus-plan`, not by this task) is the strongest available signal, so `type: capabilities` is used and the reasoning is stated as an `INFERENCE` in the node body rather than presented as settled schema fact.

LEFT OUT: No re-narration of the three trigger paths, the shared step loop's condition/template/timeout machinery in general, or the owner-authority/webhook-SSRF/tenant-fence crossings already fully documented by `architecture-flows-workflow-execution` — this node `references` that node instead. No claim about `SendDm`/`SetChannelTopic`/`RequestApproval`'s own unimplemented/unfinished behavior — out of scope for a `Delay`-only node. No new relationship to any sibling `capabilities/workflows/*` node, since none are merged yet.
