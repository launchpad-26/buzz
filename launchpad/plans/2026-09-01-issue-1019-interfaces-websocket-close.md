# Issue #1019 — interfaces/websocket/close.md

Stated size: issue #1019 caps corpus document tasks at one hand-authored canonical file plus mechanical generated indexes -> cap: 5 steps.

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and `launchpad/docs/corpus/templates/interface.md` (id `corpus-template-interface`) are merged on `origin/launchpad`. `launchpad/docs/corpus/architecture/flows/websocket-connection.md` (id `architecture-flows-websocket-connection`, status `draft`) is also merged. `launchpad/docs/corpus/interfaces/` does not exist yet — this is the corpus's first `interfaces`-shaped instance node. `crates/buzz-relay/src/protocol.rs` (`ClientMessage::Close`, `ClientMessage::parse`'s `"CLOSE"` arm, `RelayMessage::closed`), `crates/buzz-relay/src/handlers/close.rs` (`handle_close`) and `crates/buzz-relay/src/connection.rs` (dispatch match arm, `enforce_ws_admission`, `handle_text_message`'s parse-error path) already implement and wire up CLOSE handling; `crates/buzz-test-client/tests/e2e_relay.rs::test_close_subscription_stops_delivery` and `crates/buzz-test-client/src/lib.rs::close_subscription` already exercise it end to end.

STEP 1 Gather evidence: re-read `crates/buzz-relay/src/protocol.rs` (CLOSE parse arm, `RelayMessage::closed`), `crates/buzz-relay/src/handlers/close.rs` (`handle_close` body), `crates/buzz-relay/src/connection.rs` (dispatch arm at the `ClientMessage::Close` match, `enforce_ws_admission`'s early-return for non-EVENT/REQ/COUNT messages, `handle_text_message`'s parse-error-to-NOTICE path), and `crates/buzz-test-client/tests/e2e_relay.rs::test_close_subscription_stops_delivery` plus `crates/buzz-test-client/src/lib.rs::close_subscription`. Confirm: CLOSE takes `["CLOSE", <subscription_id>]`; the relay always acknowledges with `["CLOSED", <sub_id>, ""]` regardless of whether the subscription existed (idempotent, no error path for "not found"); a malformed CLOSE (empty array, non-string sub_id) fails at `ClientMessage::parse` and produces a NOTICE, not a CLOSED; CLOSE is exempt from `enforce_ws_admission`'s per-principal rate limiting (only EVENT/REQ/COUNT are gated); authentication is enforced at the connection level (NIP-42 `AUTH_TIMEOUT`) rather than per-CLOSE-message. ← RUNS HERE [independent]
done when: each of the five claims above is confirmed by a specific line/symbol read directly from the named source, and any claim that does not hold is corrected before STEP 2 starts.

STEP 2 [needs 1] Write `launchpad/docs/corpus/interfaces/websocket/close.md` with schema-valid front matter (`id: interfaces-websocket-close`, `type: interfaces-events` — the enum's combined interfaces/events surface per `node.schema.json` and `corpus-template-interface`'s own "A note on `type`" section, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, a provenance `FACT` citing commit `c703ddf33fcda09e2a8399480061c4bda08bf162`, one `evidence` entry per substantive claim from STEP 1, and `relationships: [{type: implements, target: corpus-template-interface}, {type: part-of, target: architecture-flows-websocket-connection}]`) and a body following the interface template's required sections: Interface description, Operations (the CLOSE operation, pointing at `protocol.rs`/`close.rs`, and NIP-01's own spec text for the wire shape), Contract and stability (idempotent CLOSE, no rate-limit gate, connection-level auth, malformed-input NOTICE behavior), Boundary (not a single event kind's wire contract; not a parameter-by-parameter API reference; does not cover REQ/EVENT/COUNT/AUTH except as needed to place CLOSE among them), Relationships, and Scope and omissions (including anything from STEP 1 expected but not verified, e.g. whether CLOSE is documented in NIP-11's own advertised limits). Include one valid example (`["CLOSE","sub1"]` → `["CLOSED","sub1",""]`) and one failure example (malformed CLOSE → NOTICE).
done when: `launchpad/docs/corpus/interfaces/websocket/close.md` exists, parses as YAML front matter + Markdown, and contains every required-section heading the interface template lists (Interface description, Operations, Contract and stability, Boundary, Relationships, Scope and omissions).

STEP 3 [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL line it reports (UNVERIFIED notices, e.g. on the NIP-01 external URL citation and the commit-citation provenance entry, are expected and acceptable) and re-run until exit 0.
done when: the command's exit status is 0 and its output contains no line starting with `FAIL` that traces to the new node.

STEP 4 [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call and confirm `OK`; only then, in a separate call, `git add` the plan and the new document and `git commit -s`.
done when: the unittest command's output ends with `OK` and a subsequent `git log -1` shows a new commit containing both files with a `Signed-off-by` trailer.

STEP 5 [needs 4] Self-review the diff against issue #1019's Definition-of-done checklist line by line: exactly one hand-authored canonical document; schema-valid front matter with typed relationships; one independently maintainable idea; every substantive claim traceable and classed correctly; links to implementation/verification/spec without duplicating them; checked against the recorded revision; `validate.py` clean; inputs/outputs/errors, auth/versioning/ordering, spec link, valid + failure example all present.
done when: every bullet in issue #1019's Definition-of-done checklist has been checked against the actual committed file content and none is unmet.

PARALLEL: none — single file, single task, no independent sub-work to fan out.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here.

BUDGET: small — one new document (~1 file), no code changes; evidence gathering scoped to the handful of `buzz-relay` source files and one e2e test already located, plus the NIP-01 spec text for CLOSE/CLOSED.

OPEN: Whether CLOSE's advertised limits (subscription-id length, etc.) are asserted anywhere for CLOSE specifically the way NIP-11's `max_subid_length`/`max_filters` are asserted for REQ/COUNT — `protocol.rs`'s CLOSE parse arm does not re-check `MAX_SUB_ID_LENGTH`, unlike REQ/COUNT; this asymmetry is reported as an observed fact in the node's Contract and stability section, not silently normalized away, and no code change is made to address it (out of scope per the issue's own "changing runtime product behavior" exclusion).

LEFT OUT: No new node for REQ, EVENT, COUNT, or AUTH — each remains its own future task if not already covered. No attempt to fix or even flag the `MAX_SUB_ID_LENGTH` asymmetry as a code issue — documented as an observed contract fact only. No relationship to a NIP-01 corpus node, because none exists; the spec is cited directly as an external URL (`UNVERIFIED` per the checker, expected).
