# Issue #1082 — layers/data/postgres/indexes.md

Stated size: not labeled on the issue (labels: type:task, area:docs, by:agent; no Size line) -> cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/datastore.md`, `launchpad/docs/corpus/templates/data-entity.md`, `launchpad/docs/corpus/templates/reference.md` and `launchpad/docs/corpus/architecture/containers/postgres.md` (id `architecture-containers-postgres`) are merged on `origin/launchpad`. `launchpad/docs/corpus/layers/` does not exist yet on `origin/launchpad` — this is the first `layers/` node in the corpus. `launchpad/docs/corpus/layers/data/postgres/indexes.md` does not exist.

STEP 1  [independent] Gather evidence for Buzz's actual Postgres indexing strategy: every `CREATE INDEX`/`CREATE UNIQUE INDEX` across `migrations/*.sql` (76 statements across 20 files), the desired-state `schema/schema.sql` (58 `CREATE INDEX` statements — cross-checked against migrations, one real gap found: `idx_events_tags_gin` from migration 0004 is present in migrations but absent from `schema.sql`), the migration mechanism in `crates/buzz-db/src/migration.rs` (embedded `sqlx::migrate!` MIGRATOR, `SCHEMA_DESTRUCTION_LOCK_KEY` exclusive lock, the `embedded_migrator_contains_consolidated_initial_schema` regression test that pins specific index definitions), the partition-inheritance mechanism in `crates/buzz-db/src/partition.rs` and its call site `Db::ensure_future_partitions`, and access-pattern call sites in `crates/buzz-db/src/event.rs` that cite specific index names (`idx_events_parameterized`, `idx_events_tags_gin`) as serving specific query pushdowns. Also read `standards/taxonomy.md`'s "Choosing a value" section (the disclosure rule for an imperfect `type` fit) since this batch's precedent overrides the datastore template's own `type: architecture` guidance for a real instance to `type: layers`. ← RUNS HERE
  done when: a written evidence catalog exists (need not be committed) covering: the full index inventory grouped by table/concern, the migration/versioning mechanism, the partition-inheritance mechanism, at least two real access-pattern citations from application code, the schema.sql/migrations discrepancy, and the community-scoping (tenancy) pattern versus its one documented exception (`idx_channels_id_live`) — every entry naming the exact file (and line, where practical) it came from.

STEP 2  [needs 1] Write `launchpad/docs/corpus/layers/data/postgres/indexes.md`: front matter (`id: layers-data-postgres-indexes`, `type: layers` — with an evidence-ledger entry disclosing the override against the datastore template's own `type: architecture` reasoning, per `standards/taxonomy.md`'s disclosure rule; `status: draft`; `origin: launchpad`; `audiences: [agent, developer, operator, reviewer]`; a commit-citation provenance entry; `relationships: [{type: part-of, target: architecture-containers-postgres}]`) and a body shaped by the `datastore.md` template's required sections (purpose/scope naming this as a cross-cutting indexing-strategy view, not a single-instance datastore document; a store-role statement answering "authoritative, derived, cache, or transport" — indexes are wholly derived, Postgres-maintained, synchronous/transactional structures, never a second source of truth; an index inventory table, one row per index group, structural purpose only, no full DDL reproduction; the migration/versioning mechanism; an access-pattern summary citing real `event.rs` call sites; lifecycle/retention via partial-index `WHERE` clauses and TTL-pattern indexes; consistency semantics contrasting synchronous Postgres index maintenance with the removed, async external Typesense index; tenancy/security boundaries — the community-leading convention and its one documented, deliberate exception; failure behavior — non-`CONCURRENTLY` lock behavior on brownfield databases, `IF NOT EXISTS` idempotency, and sqlx's checksum-pinning of already-applied migrations; and a scope-and-omissions section naming the `type: layers` override, the schema.sql/migrations gap, and anything else expected but unverified).
  done when: the file exists at that path with front matter that parses as YAML and a body whose section list maps one-to-one onto every Definition of Done bullet in issue #1082 (store-role classification; owned data/access-patterns/lifecycle/consistency; tenancy/security/failure; links instead of copied DDL).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any reported error (schema violation, bad node id, broken relationship target, unresolvable source path) and re-run until it exits 0.
  done when: the command's exit code is 0 with no error lines, only (at most) non-fatal `UNVERIFIED` notices.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole, bare command in its own tool call (never piped) to earn the commit gate; confirm it reports `OK`. Then, in a separate tool call, `git add` the plan file and the new corpus document and commit with `git commit -s -m "docs(corpus): postgres indexing strategy concept (#1082)"`.
  done when: the unittest run reports `OK` in its own untouched output, and `git log -1` on the branch shows the new commit containing exactly the plan file and the target corpus document (no other files).

PARALLEL: none — one document, one linear research-then-write task; STEP 1 must land before drafting front matter/evidence, and validation/commit are strictly sequential after that.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 before committing. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK`, run bare and unpiped, immediately before the commit. `review-adjudicate` and any cross-model final review are deferred to the batch owner's later bundling/review pass — not run here per this task's isolate/plan/build/verify/commit scope.

BUDGET: small — one document, no runtime code changes, evidence gathering scoped to ~20 migration files, one desired-state schema file, and three `buzz-db` source files (`migration.rs`, `partition.rs`, `event.rs`).

OPEN: Whether "Indexes" should have been a `datastore`-shaped node (this plan's choice, since the issue's own DoD bullets — authoritative/derived/cache/transport, owned data, access patterns, lifecycle/retention, consistency semantics, tenancy/security boundaries, failure behavior, link-not-copy-DDL — map almost one-to-one onto `datastore.md`'s required sections) or a `reference`-shaped catalogue of index definitions is a judgment call the task brief itself flags as needing this document's own review; this plan resolves it toward `datastore`-shaped because the DoD bullets are a closer structural match than `reference.md`'s three-section (description/structured-entries/commands) shape, and says so in the node's own scope section rather than silently picking. Whether `type: layers` (the disclosed batch-precedent override) or `type: architecture` (the datastore template's own stated reasoning for a real instance) is the better long-term fit is left as the same kind of revisable, disclosed choice `standards/taxonomy.md` describes for `corpus-readme`'s own `type: governance` choice — not re-litigated here.

LEFT OUT: No relationships to any other `layers/data/postgres/*` sibling node — none exist on `origin/launchpad` (PR #1875, cited in the task brief, is unmerged). No attempt to fully re-catalogue every one of the 76 `CREATE INDEX` statements row-by-row in the document body — the index inventory groups by table/concern rather than reproducing every statement, per the DoD's own "link... rather than copying DDL" bullet. No attempt to resolve or fix the `idx_events_tags_gin`/`schema.sql` discrepancy found in STEP 1 — named as a gap in the document, not repaired (that would be a separate, out-of-scope code/schema change). No changes to `migrations/`, `schema/schema.sql`, or any `crates/buzz-db` source file. No push and no PR — this task's scope stops at a local commit on `task/1082-indexes`.
