# Issue #1070 — layers/data/object-storage/media-objects.md

Stated size: issue #1070 states no explicit Size line -> cap: 5 steps (one small hand-authored document, per this batch's own established pattern for sibling corpus-doc issues).

ALREADY TRUE: `launchpad/docs/corpus/layers/data/object-storage/media-objects.md` does not exist on `origin/launchpad` (no `layers/` tree exists there yet). `node.schema.json`'s `type` enum has no `data` member; `layers` is the closed-enum member for this corpus surface. Two siblings in the same group — `blossom-storage.md` (#1067) and `git-objects.md` (#1069) — are drafted on the still-open, unmerged PR #1873 (`task/610-batch-2-data-storage`), not on `origin/launchpad`; both chose `type: layers` over `templates/datastore.md`'s own `type: architecture` suggestion, and both disclose that tension in their own evidence ledgers per `standards/taxonomy.md`. `blossom-storage.md` (read via `git show origin/task/610-batch-2-data-storage:...`) already fully documents the Blossom/media half of the shared S3 bucket as a datastore (key-namespace inventory, migration, access, lifecycle, tenancy, failure) and its own Scope-and-omissions table explicitly leaves one gap unowned: "The domain meaning of an uploaded blob (what a message attachment or avatar is) — a future data-entity corpus node, not yet written." `crates/buzz-media/src/types.rs` defines `BlobDescriptor` (Blossom BUD-02 response) and `crates/buzz-media/src/storage.rs` defines `BlobMeta` (the sidecar's persisted shape); `upload.rs`'s `build_descriptor` maps one to the other. `crates/buzz-relay/src/handlers/imeta.rs` (`validate_imeta_tags`, `verify_imeta_blobs`) is the mechanism by which any Nostr event references a media object via an `imeta` tag (NIP-92/NIP-94/NIP-71), called from `ingest_event_inner` in `crates/buzz-relay/src/handlers/ingest.rs`, kind-independent. `architecture-containers-object-storage` exists and validates on `origin/launchpad` today and does not mention `imeta` or `BlobDescriptor` anywhere in its own text. Commit citation for this session: `338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5`.

STEP 1  [independent] Confirm scope: the issue's DoD checklist is the same boilerplate `templates/datastore.md`-shaped checklist #1069 carried, but `blossom-storage.md` already is that datastore document for the media half of the bucket — a second copy would duplicate canonical claims (against `AGENTS.md`'s one-idea rule and Feature #610's own anti-duplication acceptance criterion). Scope this document instead to the real, undocumented, adjacent surface the evidence above shows: the media object as a referenced/reference-checked domain unit (`BlobMeta`/`BlobDescriptor`, the `imeta` reference contract, and `verify_imeta_blobs`'s cross-check) — exactly the gap `blossom-storage.md` names as unowned. done when: this scoping decision and its evidence are recorded in the node's own evidence ledger (as TEAM_KNOWLEDGE/INFERENCE entries), not silently assumed. ← RUNS HERE

STEP 2  [needs 1] Write front matter (`id: layers-data-object-storage-media-objects`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`, one `relationships` entry: `part-of` -> `architecture-containers-object-storage`, confirmed present on `origin/launchpad`; no relationship to `blossom-storage.md`'s planned id, since it does not exist on `origin/launchpad` and would hard-error there per `AGENTS.md`'s step-9 warning) and the body: purpose/scope naming the reference-domain view and deferring bucket internals to `blossom-storage.md` (#1067, named not linked) and existence/technology to `architecture-containers-object-storage`; the two representations (`BlobMeta` vs `BlobDescriptor`) and their mapping; the `imeta` reference contract (NIP-92/94/71); store classification for this surface (a derived pointer, not a second store); referential-integrity/consistency semantics (`verify_imeta_blobs`'s checks, called kind-independently at ingest); tenancy boundary (`TenantContext`-scoped sidecar lookup); failure behavior (`IngestError::Rejected` per mismatch mode); scope and omissions with named owners. done when: the file exists with all schema-required fields and every DoD bullet from the issue is addressed against this scope.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the worktree root; fix and re-run until exit 0. done when: the command exits 0.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call and confirm OK; then, in a separate call, stage and commit only the plan file and the corpus doc, signed off (`git commit -s`). No push, no PR. done when: the unittest run reports OK and exactly one commit exists ahead of `origin/launchpad` touching only those two files.

STEP 5  [needs 4] Self-review: re-read the diff against the issue's DoD checklist line by line, confirm no bucket-internals content was duplicated from `blossom-storage.md`'s (unmerged) draft, and re-confirm `validate.py` exits 0. done when: self-review is complete and reported in the final task report.

PARALLEL: none — single file, single task, no independent workstreams to run alongside it.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 (STEP 3, re-checked in STEP 5). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report OK before the commit (STEP 4). `review-adjudicate` and any cross-model final review pass are deferred to the batch owner's later bundling/review step — not run here, per this task's own instructions (isolate/plan/build/verify/commit only, no PR).

BUDGET: small — one corpus document (~150-250 lines), one plan document, no code changes. Evidence-gathering scoped to `crates/buzz-media/src/{types,storage,upload}.rs`, `crates/buzz-relay/src/handlers/imeta.rs`, its call site in `ingest.rs`, a handful of e2e tests, and three Nostr NIPs (92, 94, 71) fetched pinned to a commit SHA.

OPEN: Whether `media-objects.md`'s reference-domain scope is what the original corpus-plan author intended, versus a second datastore-shaped document repeating `blossom-storage.md`'s content, cannot be settled from the issue body alone — the issue body carries only the generic batch DoD checklist, identical in wording to #1069's. This plan's chosen scope is the one that is both evidence-grounded and non-duplicative of the sibling `blossom-storage.md` draft; it is disclosed as a reasoned choice in the node's own evidence ledger, not hidden. Whether PR #1873 (siblings #1065-#1069) merges before or after this branch is bundled is unknown; this document does not depend on that PR merging, since it declares no relationship to any node from it.

LEFT OUT: Any edit to `blossom-storage.md`, `git-objects.md`, `content-addressing.md`, or `architecture-containers-object-storage` — out of scope per the issue's own "Out of scope" section (no second hand-authored canonical document). Full Blossom BUD-01/02/11 protocol description and the bucket's own key-namespace/migration/access/lifecycle/tenancy/failure internals — already `blossom-storage.md`'s job (once merged) and the container node's job, named not repeated.
