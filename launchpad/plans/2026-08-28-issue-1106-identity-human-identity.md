# Issue #1106 — layers/identity/human-identity.md

ALREADY TRUE: `launchpad/docs/corpus/templates/concept.md` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/layers/identity/human-identity.md` does not exist yet (confirmed: `launchpad/docs/corpus/layers/` directory does not exist in this worktree at all). `architecture/context/human-user.md` (id `architecture-context-human-user`) is merged on `origin/launchpad` and already documents the human user as a context-level actor/boundary — this node must not restate that; it covers a different angle: the mechanics of how a human's identity is represented (kind:0 profile metadata + client-side keypair custody). Sibling #1102 (identity/actor.md) exists only as an unmerged open PR (`task/1102-identity-actor`, PR #1803) — `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` confirms no `layers/` path exists there yet, so it is not linkable per AGENTS.md step 9.

STEP 1 Gather evidence: confirm `pub const KIND_PROFILE: u32 = 0;` in `crates/buzz-core/src/kind.rs` (NIP-01 profile metadata). Read `crates/buzz-relay/src/handlers/side_effects.rs`'s `handle_kind0_profile` (dispatched at `side_effects.rs:199`, `0 => handle_kind0_profile(...)`) — it parses kind:0 JSON content, treats it as absolute replaceable state (empty string clears a field), validates/canonicalizes a NIP-05 handle against the tenant host, and calls `buzz-db`'s `update_user_profile` to sync `display_name`/`avatar_url`/`about`/`nip05_handle` onto the community-scoped `users` row. Read `crates/buzz-db/src/user.rs`'s `UserProfile` struct and `update_user_profile`/`ensure_user` signatures. Read `desktop/src-tauri/src/secret_store.rs`'s module doc ("OS keyring access for desktop nsec private keys") for desktop keypair custody, and `desktop/src-tauri/src/commands/profile.rs` for the client-side kind:0 publish path (`nostr::EventBuilder::new(nostr::Kind::Metadata, ...)`). Read `mobile/lib/shared/community/community_storage.dart` for mobile custody (`flutter_secure_storage`, `_legacyNsec`/`nsec` field on `Community`). ← RUNS HERE

STEP 2 [needs 1] Write front matter (schema-valid: id `layers-identity-human-identity`, type `layers` per the issue title, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, one `relationships` entry: `references` → `architecture-context-human-user`, confirmed present on `origin/launchpad`) using the `concept.md` template's shape (Definition, mechanics/use-cases, Comparison/boundaries, Related resources via `relationships`, Scope and omissions) since no `layers`-specific template exists. Classify every claim honestly: the kind:0/side-effect/keychain/secure-storage citations are FACT (source opened directly); any generalization beyond what was read is INFERENCE with a stated confidence; issue #1106's own DoD requirements are TEAM_KNOWLEDGE.

STEP 3 [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4 [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here (self-review only, stated explicitly in the PR body).

BUDGET: small — one document, no code changes, evidence gathering scoped to ~6 source files already identified above.

OPEN: Whether NIP-05 canonicalization behavior (silently clearing a contested/off-domain handle rather than rejecting the event) belongs in this node or in a separate NIP-05/verification-layer node is a real editorial question; this draft covers it briefly as part of "how identity is represented" since it directly affects the `nip05_handle` field, without attempting to be the canonical NIP-05 node. Sibling identity nodes (#1102 actor, #1103 agent-identity, #1111 keypair, #1112/#1113 private/public-key) are not merged on `origin/launchpad`, so this node cannot link to them yet — named in Scope and omissions rather than worked around.

LEFT OUT: No relationship to the still-unmerged `layers-identity-actor` node. No restatement of `architecture-context-human-user`'s boundary/actor-relationship content — only linked via `references`. No code changes to `handle_kind0_profile`, `secret_store.rs`, or mobile storage — documentation only.
