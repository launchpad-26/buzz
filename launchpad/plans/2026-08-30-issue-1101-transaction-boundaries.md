# Issue #1101 — layers/data/transaction-boundaries.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/reference.md` and `launchpad/docs/corpus/templates/datastore.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/architecture/containers/postgres.md` (id `architecture-containers-postgres`, status `draft`) is merged; `launchpad/docs/corpus/layers/data/transaction-boundaries.md` does not exist yet, and no `layers/data/*.md` node exists on `origin/launchpad` at all (the earlier batch's `consistency-model.md`, #1061, is still only on unmerged PR #1872).

STEP 1  Gather evidence: `grep`/read every `.begin()`, `sqlx::Transaction`, `FOR UPDATE`, and `pg_advisory_xact_lock` call site under `crates/buzz-db/src/` (non-test), read the enclosing function for each, and read the writer-pool `READ COMMITTED` isolation assertion in `crates/buzz-db/src/lib.rs`'s `connect_pool`. Distinguish this doc's actual subject — explicit multi-statement `BEGIN...COMMIT` transaction boundaries, what tables/rows each covers atomically, and the row-lock vs. advisory-lock choice within them — from the replica read-only `REPEATABLE READ` snapshot path and LWW/replica-fence consistency, which belong to `consistency-model.md` (#1061) and are only referenced in prose, not restated. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `layers-data-transaction-boundaries`, type `layers` — override disclosed in the evidence ledger per `standards/taxonomy.md`'s silence on a `layers`-specific definition — status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, no `relationships` unless `architecture-containers-postgres` is judged a genuine `part-of` target after STEP 1) and the body, in the `reference.md` template's shape (reference description, a structured-entries table of the real transaction-boundary functions found in STEP 1 — function, tables/locks touched, atomicity guarantee — plus prose on the `pg_advisory_xact_lock` vs. `FOR UPDATE` choice and the writer's `READ COMMITTED` default, a boundary section against `consistency-model.md`'s replica-snapshot territory and against documenting the whole Postgres datastore (that is `architecture-containers-postgres`'s and the sibling #1100 datastore document's job), and scope-and-omissions naming what was expected but not verified.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`, bare and unpiped) as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call. Do not push and do not open a PR — a batch owner bundles this branch with sibling #1100 afterward.

PARALLEL: none — single file, single task, no dependency on sibling #1100's own output (that document, if it exists, is not read or cited here; both target different subjects under the same `layers/data/` directory).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The unittest suite in STEP 4 must report OK. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's morning review — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to grep hits and their enclosing functions across roughly a dozen files in `crates/buzz-db/src/`.

OPEN: `check-plan.sh` (`/home/serina/.claude/skills/plan-issue/check-plan.sh`) is written for multi-step code-change plans with a stated Size line, per-step done-when checks, and independence tags. This plan's own dry run against it fails mechanically on all three (no Size line on the issue to cap against, no done-when per step, no independence tag on the single step) — the same mismatch the merged precedent `launchpad/plans/2026-08-28-issue-698-corpus-doc.md` also fails against, confirming this is a checker/format mismatch for single-sitting corpus-doc plans rather than a defect in this one. Proceeding by hand per the batch brief. Whether `architecture-containers-postgres` earns a `part-of`/`references` relationship is left to STEP 2 to decide from what STEP 1 actually finds — `postgres.md` documents the Postgres container's existence and one-line responsibility, not its transaction machinery, so a relationship is plausible but not asserted in advance of reading it.

LEFT OUT: No claim about Redis or object-storage transaction semantics — this node is Postgres-only, per the issue's own objective sentence and `crates/buzz-db`'s scope. No restatement of the replica-fence `REPEATABLE READ, READ ONLY` snapshot-consistency mechanism beyond the one sentence needed to draw the boundary against it — that mechanism's own explanation belongs to `consistency-model.md` (#1061), not this node, even though that node is not yet merged and cannot be linked. No attempt to catalogue every `pool.begin()` call site exhaustively (test-only fixtures and lock-holder test helpers are excluded from the structured-entries table); the table covers production write paths only.
