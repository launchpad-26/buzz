# Issue #969 — ingestion/release-history.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/policy.md` (id `corpus-template-policy`), `launchpad/docs/corpus/standards/code-references.md` (id `corpus-standard-code-references`) and `launchpad/docs/corpus/standards/decision-references.md` (id `corpus-standard-decision-references`) are all merged on `origin/launchpad`. Only one Feature #620 sibling — `agents/invariants.md` (id `agents-invariants`, #649) — is merged; every other `agents/*.md`/`ingestion/*.md` sibling exists only as an unmerged branch and is not a valid relationship target. `launchpad/docs/corpus/ingestion/release-history.md` does not exist yet. `RELEASING.md` lives at the repo root.

STEP 1  Gather evidence, all directly verified rather than assumed: read `RELEASING.md` in full (three independent lanes — desktop/relay/mobile — each with its own version authority and tag shape); run `git tag --sort=-creatordate --format='%(creatordate:iso) %(creatordate:unix) %(refname)'` and confirm a real mixed-UTC-offset pair whose ISO-string order contradicts its unix order (found: `desktop-v0.5.12` at `13:54:39 -0600` string-sorts before `mobile-v0.11.0-rc.2` at `14:31:30 +0000`, but the unix timestamps — 1786737279 vs 1786717890 — put `mobile-v0.11.0-rc.2` first); read `_COMMIT_CITATION_RE` and `_classify_citation`'s fallthrough order in `launchpad/project-intelligence/corpus/validate.py` to confirm a bare tag name matches no recognised citation shape and falls through to the repo-path rule as a hard error; and confirm via `git fetch origin --tags` + `git ls-remote --tags upstream` that `desktop-v0.5.19`/`desktop-v0.5.20` exist on `upstream` (block/buzz) but not on this fork's `origin` remote, even though the corresponding `chore(release)` commits are ancestors of `HEAD`. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `ingestion-release-history`, type `ingestion` — matching every merged/drafted sibling under `ingestion/*.md`, not the separate `release` surface enum value, which documents Buzz's own release engineering rather than how the corpus cites it — status `draft`, origin `launchpad`, audiences `[agent, reviewer]`; relationships `depends-on: corpus-agents`, `implements: corpus-template-policy`, `references: corpus-standard-code-references`, `references: corpus-standard-decision-references` — all four resolvable on `origin/launchpad`) and the body using `templates/policy.md`'s six required sections: Scope and authority; MUST (named-release citation must be an immutable tag not a branch/"latest"/bare version; tag-date comparison must use `:unix` never `:iso` string order, with the verified pair as evidence; a "current release" claim is time-relative and must record its as-of check date; a bare tag name is not a valid citation shape — cite `commit <sha>` and name the tag in prose, or cite the changelog/RELEASING.md; a changelog/release-commit entry is not proof a tag was created, per the 0.5.19/0.5.20 fork-vs-upstream gap; one lane's version authority must not stand in for another's); SHOULD; Enforcement (nothing mechanical checks any of this — `validate.py` never opens a citation's content or compares dates); Exceptions and escalation; Scope and omissions (naming what this node does not decide, e.g. whether the fork ever intends to mirror upstream's full tag set — not verified, named as a gap).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call (`git commit -s`). No push, no PR — this build stops at commit per the batch runner's instructions.

PARALLEL: none — single file, single task, no dependency on any other in-flight sibling.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover ...` must print `OK`. An independent `serina:review-code` pass (or, if genuinely unreachable, a documented self-review) runs after the commit, before reporting done.

BUDGET: small — one document, no code changes; evidence gathering scoped to `RELEASING.md`, `git tag`/`git ls-remote` output, and ~40 lines of `validate.py`'s citation classifier.

OPEN: Whether the fork's `origin` remote is expected to eventually mirror every upstream release tag, or whether the gap found in STEP 1 (missing `desktop-v0.5.19`/`0.5.20`) is permanent fork behavior, is not something this task can settle — recorded as an unresolved, dated observation in the node's own Scope and omissions rather than asserted either way.

LEFT OUT: No relationship to any unmerged Feature #620 sibling (`agents/*.md`, other `ingestion/*.md`). No restatement of `RELEASING.md`'s process content, `code-references.md`'s citation-shape table, or `decision-references.md`'s intent/behaviour split — each is linked, not duplicated. No attempt to fix or explain *why* the fork's tag mirror lags upstream — that is separate infrastructure work, not this corpus-authoring task.
