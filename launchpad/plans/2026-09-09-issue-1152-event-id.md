Issue #1152 — task: create `launchpad/docs/corpus/layers/protocol/event-id.md` (concept node: the event id)
Stated size: no `Size` line on the issue; scope stated unambiguously as exactly one hand-authored
corpus document -> cap: 5 steps

ALREADY TRUE  (verified against the worktree at `29ca9b189bd3f639ba09c972b57c70538c0860c6`, not notes)
  `git rev-parse HEAD` in `__worktrees/task-1152-event-id` is
  `29ca9b189bd3f639ba09c972b57c70538c0860c6`; `git cat-file -e` on it exits 0. That is the
  provenance revision for this node.
  `launchpad/docs/corpus/layers/protocol/` does not exist on `origin/launchpad` — the merged-id
  inventory carries `layers/` subtrees `compute`, `configuration`, `data`, `lifecycle`,
  `observability` only. So no sibling in this Feature is a legal relationship target.
  `crates/buzz-core/src/verification.rs` — `verify_event()` calls `event.verify_id()` first and
  `event.verify_signature()` second, and on id failure recomputes via
  `EventId::new(&event.pubkey, &event.created_at, &event.kind, &event.tags, &event.content)` to
  build `VerificationError::InvalidId { computed, got }`. Both are `nostr` crate calls — read
  directly.
  `Cargo.toml` pins `nostr = { version = "0.44", features = ["nip44", "nip98"] }`; the crate
  source is not vendored in this repository.
  `crates/buzz-pair-relay/src/lib.rs` — `verify_event_sig()` is a *second, independent*
  implementation that builds the commitment array `[0, pubkey, created_at, kind, tags, content]`,
  serializes it with `serde_json::to_string`, `Sha256::digest`s it, compares against the claimed
  `id` (`"invalid: event id mismatch"`), and then Schnorr-verifies the signature **over that
  32-byte hash**. Read directly.
  `crates/buzz-core/src/event.rs` — `tampered_signature_fails_verify` asserts
  `tampered.verify_id()` is still **true** after only `sig` is replaced. Read directly.
  `crates/buzz-relay/src/handlers/ingest.rs` wraps `verify_event` in
  `tokio::task::spawn_blocking` and turns `Err` into `IngestError::Rejected(format!("invalid:
  {e}"))`. Read directly.
  `migrations/0001_initial_schema.sql` — `events.id BYTEA NOT NULL`, primary key
  `(community_id, created_at, id)`, and every listed secondary index carries `id` as its trailing
  column. Read directly.
  `crates/buzz-db/src/store/event.rs` — `insert_event` binds `event.id.as_bytes()` and uses
  `ON CONFLICT DO NOTHING`; `query_events` orders `created_at DESC, id ASC` as a deterministic
  pagination tiebreak. Read directly.
  `crates/buzz-db/src/store/replaceable.rs` — same-second replaceable conflicts are resolved by
  lowest lexicographic event id, commented "deterministic across relays". Read directly.
  `migrations/0010_nip_rs_exact_replay_guard.sql` — the watermark trigger advances on
  `created_at`, tiebreaks on `EXCLUDED.event_id < ...event_id`, and treats an exact
  `(coordinate, created_at, event_id)` match as an idempotent `RETURN NULL`. Read directly.
  `crates/buzz-pubsub/src/nip98_replay.rs` — the replay seen-set key is
  `buzz:{community}:nip98:{event_id_hex}`. Read directly.
  `crates/buzz-core/src/pairing/NIP-AB.md` documents the `id` field as `"<sha256 hash per
  NIP-01>"` — an openable in-repo file stating the SHA-256 claim.
  No upstream numbered NIP text is in this repository: `git ls-files 'docs/nips/*' | grep -E
  'NIP-[0-9]'` returns no output, exit status 1.
  Merged, grep-confirmed relationship targets: `architecture-principles-signed-events`,
  `implementation-crates-buzz-core`, `layers-data-postgres-events-table`,
  `verification-security-replay`, `capabilities-media-content-hash`.

STEP 1  Fix the front-matter contract for the node before writing prose.       [independent]
        `id: layers-protocol-event-id`, `type: layers`, `status: draft`, `origin: launchpad`,
        `audiences: [agent, developer]` (the subject is what clients build and what the relay
        checks, not relay operations — `operator` is not honest here). Exactly one commit-only
        FACT, the provenance entry naming `29ca9b189bd3f639ba09c972b57c70538c0860c6`.
        done when: the seven-field set matches `schema/node.schema.json`'s
        `additionalProperties: false` list and no eighth key is present.

STEP 2  Write one evidence entry per substantive claim, each against a source  [needs 1]
        actually opened in ALREADY TRUE. FACT for every in-repo file claim (bare paths, no line
        numbers). The one claim about what upstream NIP-01 *requires* is not a FACT on a repo
        path — carry it as a FACT on `crates/buzz-pair-relay/src/lib.rs` worded as "the
        pair-relay's own implementation builds the commitment as ...", plus a TEAM_KNOWLEDGE
        entry attributing the NIP-01 requirement itself. The absence claim (no upstream NIP text
        in this repository) gets a tool-result citation quoting the real
        `git ls-files ... | grep` result and its exit status.
        done when: every FACT names a path that exists in this worktree, and no FACT rests only
        on a commit or tool-result citation except the single provenance entry.

STEP 3  Write the body to `templates/concept.md`'s required sections —          [needs 2]
        title/intro, Definition (with the explicit name-collision disambiguation against
        `capabilities-media-content-hash`'s media content hash, which the concept template
        requires when a name collides), an optional Mermaid visual aid only if it earns its
        place, Background, Use cases, Comparison, Related resources via typed relationships, and
        Scope and omissions. Link neighbouring nodes rather than restating them: the
        accept/reject invariant belongs to `architecture-principles-signed-events`, the primary
        key and `ON CONFLICT DO NOTHING` to `layers-data-postgres-events-table`, the NIP-98
        seen-set to `verification-security-replay`.
        done when: no section restates a merged sibling's canonical claim, and the Definition
        states in one sentence what the id is before anything else.

STEP 4  Write Scope and omissions carrying two distinct things, per            [needs 3]
        `AGENTS.md` step 8: (a) what this node does not cover and who owns it — the signature
        (#1166), the event as a whole (#1156), the `e` tag (#1149), and the merged nodes above;
        and (b) separately, what was expected to be verified and could not — specifically that
        the `nostr` crate's own `EventId::new` body was never opened (not vendored here), so the
        byte-level serialization is evidenced from the pair-relay's independent implementation
        rather than from the code the relay actually runs, and that no test was found asserting
        the two implementations agree on the same event.
        done when: both halves are present as separate, labelled disclosures, not one merged
        paragraph.

STEP 5  Validate, self-review, stamp, commit.                                  [needs 4]
        `python3 launchpad/project-intelligence/corpus/validate.py` exits 0. Re-read every
        evidence entry against its cited source and every DoD bullet on #1152 BEFORE stamping,
        because editing after the commit clears the stamp. Then the verify-gate suite as a sole
        foreground command, then `git add` + `git commit -s`.
        done when: validate.py exits 0, the unittest run's final line is `OK`, and the commit
        exists. No push, no PR.
