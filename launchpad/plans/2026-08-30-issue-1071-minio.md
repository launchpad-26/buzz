# Issue #1071 — layers/data/object-storage/minio.md
Stated size: no `Size` line in the issue → cap: 5 steps (per the overnight batch dispatch brief: "this is one small document")

ALREADY TRUE  (verified against git, not notes)
  `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/datastore.md` and `launchpad/docs/corpus/architecture/containers/object-storage.md` (`architecture-containers-object-storage`) are merged on `origin/launchpad` (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`).
  `launchpad/docs/corpus/layers/data/object-storage/minio.md` does not exist yet, and neither does the `layers/data/object-storage/` directory (`find` errors "No such file or directory" in this worktree).
  Two sibling documents in that eventual directory — `blossom-storage.md` (#1067) and `git-objects.md` (#1069) — exist only on unmerged local branches (`task/1067-blossom-storage`, `task/610-batch-2-data-storage`), not on `origin/launchpad`; both chose `type: layers` over the datastore template's own `type: architecture` suggestion (confirmed by reading `blossom-storage.md`'s front matter directly from its branch), and this task follows that precedent for consistency rather than re-deciding it.

STEP 1  Gather evidence directly from merged code, not from the unmerged sibling files (which cannot be cited — they do not resolve on disk in this worktree): `docker-compose.yml`'s `minio`/`minio-init` services, `.env.example`'s S3 section, `crates/buzz-media/src/storage.rs` (`ping`, `bucket_versioning_detected`, the MinIO-specific already-absent-key quirk comment on `delete_objects`), `crates/buzz-media/src/bucket_index.rs` (coarse media key taxonomy only), `crates/buzz-relay/src/api/git/manifest.rs`'s `pointer_key` (community-scoped) and `crates/buzz-relay/src/api/git/store.rs` (`packs/`/`idx/`/`manifests/` — content-addressed, unscoped CAS), `crates/buzz-media/tests/static_creds_minio.rs`, `deploy/charts/buzz/README.md` plus `templates/quickstart-minio*.yaml`, `ARCHITECTURE.md`'s infra table. Confirm no S3 lifecycle/expiration config exists.                                    [independent]
        done when: every fact used in STEP 2's evidence ledger has been read directly from the file it cites, in this worktree, this session.

STEP 2  Write front matter (id `layers-data-object-storage-minio`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator]`, one `relationships` entry: `part-of` → `architecture-containers-object-storage`) and the body per `templates/datastore.md`'s seven required sections, scoped one level coarser than the unmerged sibling docs: purpose/scope naming the container node this zooms into; technology & attachment profile (linking to the container node rather than repeating its credential-chain detail); store classification; a two-row (media / git-CAS) namespace split, not the fine-grained per-key-class tables the siblings own; migration/versioning mechanism (none exists); access-pattern summary; operational characteristics actually wired into the codebase; tenancy/security boundaries; and a scope-and-omissions section disclosing the `type: layers` vs. datastore-template's `type: architecture` tension per `standards/taxonomy.md`'s "say so, don't silently pick" rule, plus the unmerged-sibling-citation limit from STEP 1.  [needs 1]
        done when: the file exists at `launchpad/docs/corpus/layers/data/object-storage/minio.md` and every evidence entry cites a path that exists in this worktree.

STEP 3  Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until it exits 0.                     [needs 2]  ← RUNS HERE
        done when: the command's exit code is 0.

STEP 4  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call.  [needs 3]
        done when: the unittest run reports OK, and `git log -1` on this branch shows a signed-off commit containing both files. No push, no PR — this branch is bundled by a later batch step.

PARALLEL  None. All four steps touch the same one document (or gate it), so none is independent of the others in the shared-file sense, and this is a single isolated worktree task with no sibling work to fan out to.
GATES     `python3 launchpad/project-intelligence/corpus/validate.py` after STEP 3, must exit 0. `python3 -m unittest discover ...` before the commit in STEP 4, must report OK. `review-adjudicate` and cross-model final review are deferred to the batch owner's later bundling/review pass — not run here. No `qa` explore mode — this is a docs-only change with no runtime interface to exercise.
BUDGET    STEP 1 (evidence gathering) is most likely to eat the budget — the shared object-storage bucket has two consumers (media, git-CAS) across ~10 files, and getting the tenancy/consistency claims right requires reading actual key-construction code, not just docstrings.
OPEN      `blossom-storage.md` and `git-objects.md` are real precedent (read directly from their branches) but are not citable as evidence in this node, since a bare-path citation must resolve on disk in *this* worktree and `AGENTS.md` step 9 forbids relationships to nodes absent from the branch being merged into. Whether the eventual bundled PR wants a `references` edge between this node and the two siblings once all three are merged together is left to whoever bundles the batch — not decided here.
LEFT OUT  No fine-grained per-key-class table for the media namespace (owned by the unmerged `blossom-storage.md`) or the git-CAS namespace (owned by the unmerged `git-objects.md`) — this node states only the coarse two-namespace split needed to describe the shared store honestly, independently verified against the same underlying code rather than copied from those drafts. No claim about production object-storage providers beyond what `deploy/charts/buzz/README.md` and `architecture-containers-object-storage.md` already establish. No attempt to resolve the `type: layers` vs. `type: architecture` tension — named and left for a later corpus-wide pass, per precedent.
