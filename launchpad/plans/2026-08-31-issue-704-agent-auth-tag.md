# Issue #704 — capabilities/agents/agent-auth-tag.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and the `capability.md` template (`launchpad/docs/corpus/templates/capability.md`) are merged on `origin/launchpad`; `launchpad/docs/corpus/capabilities/` does not exist yet, so this is the corpus's first `type: capabilities` node. `launchpad/docs/corpus/capabilities/agents/agent-auth-tag.md` does not exist.

STEP 1  Gather evidence on the `BUZZ_AUTH_TAG` mechanism (NIP-OA owner attestation): `crates/buzz-sdk/src/nip_oa.rs` (compute/verify/parse + preimage spec + test vectors), `crates/buzz-acp/src/lib.rs` (owner resolution priority, relay-membership delegation, MCP env forwarding, forwarding tests), `crates/buzz-cli/src/lib.rs` (flag/env wiring, shorthand normalization, verify-before-use), `crates/buzz-relay/src/handlers/auth.rs` + `crates/buzz-relay/src/api/relay_members.rs`-facing calls (WebSocket AUTH cascade: ban check → allowlist → membership, with NIP-OA owner fallback), `crates/buzz-relay/src/api/bridge.rs` (`x-auth-tag` HTTP header path), `crates/buzz-relay/src/handlers/identity_archive.rs` (archive-request owner proof), `crates/git-sign-nostr/src/lib.rs` and `crates/git-credential-nostr/src/lib.rs` (git push signing precedence), `crates/buzz-backend-kubernetes/src/env.rs` (Blox/k8s injection + forged-tag test), and `desktop/src-tauri/src/managed_agents/reserved_env_keys.rs` + `env_vars.rs`/`env_vars/tests.rs` (reserved-key, anti-forgery bypass fix). Cross-check against already-merged sibling corpus nodes that touch the same mechanism from their own angle (`architecture/context/ai-agent.md`, `architecture/flows/websocket-authentication.md`, `architecture/flows/git-push.md`, `architecture/containers/agent-runtime.md`) so this node cites and references rather than restates them. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `capabilities-agents-agent-auth-tag`, type `capabilities`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator]`, `relationships: references` toward the four sibling architecture nodes identified in STEP 1, all already merged on `origin/launchpad`) and the body per the `capability.md` template's required sections (capability statement, maturity, boundary) plus an explicit behavioral-rules/constraints/variants section to satisfy issue #704's own DoD bullet, plus relationships and scope-and-omissions.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Confirm zero new FAIL entries against the known 21-error baseline (issue #1951).

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the commit-gate stamp, confirm `OK`, then `git add` the new document + this plan and `git commit -s`. Do NOT push and do NOT open a PR — this is a corpus batch-authoring task; a later integration phase folds this commit into the Feature #613 PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no new FAIL entries. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before the commit. `review-adjudicate` and cross-model final review are deferred to the Feature-wide integration/review pass — not run here.

BUDGET: small — one document, no code changes; evidence gathering scoped to the ~10 source files above plus 4 sibling corpus nodes, all already located via `grep`/RepoQL in this session.

OPEN: This is the first `type: capabilities` node in the corpus, so there is no precedent capability sibling to check the template against end-to-end (the `capability.md` template itself flags this as unverified). `relationships` targets four already-merged architecture nodes that discuss this same mechanism from the container/flow/context angle — verified present on `origin/launchpad` via the worktree's own checkout, which was created from `origin/launchpad` at the start of this task.

LEFT OUT: No new interface or flow node for the NIP-OA protocol itself (owned by future `#1342`-style interface work and `#1338`-style flow work per the capability template's own boundary section). No change to `crates/buzz-sdk`, `crates/buzz-acp`, `crates/buzz-relay`, or any other runtime code — this task is documentation-only. No resolution of whether other `BUZZ_AUTH_TAG` call sites (e.g. `buzz-dev-mcp`'s `view_image.rs`) need their own node — out of scope for one capability document.
