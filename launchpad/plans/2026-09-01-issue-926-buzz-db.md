# Issue #926 — implementation/crates/buzz-db.md

Stated size: issue #926 carries no Size label; the batch-dispatch brief for this task explicitly capped it at 5 steps. -> cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/templates/implementation-reference.md`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/architecture/containers/postgres.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/implementation/crates/buzz-db.md` does not exist yet and no `implementation/` subtree exists in the corpus at all — this is the first node built from the `implementation-reference` template.

STEP 1  Gather evidence at `git rev-parse HEAD`: read `crates/buzz-db/Cargo.toml`, `crates/buzz-db/src/lib.rs`'s full module re-export surface, `crates/buzz-db/src/error.rs`, `crates/buzz-db/src/runtime/{mod,migration,replica_fence,observability}.rs`, every `crates/buzz-db/src/store/*.rs` module (headline + test-attribute census), `docs/multi-tenant-conformance.md`, `migrations/0001_initial_schema.sql`'s header, `Justfile`'s `test`/`test-unit` targets, `scripts/run-tests.sh`, and `.github/workflows/ci.yml`'s `backend-integration` job's buzz-db-specific `nextest -E` filters. Confirm the `crates/buzz-db/src/migration.rs` and `crates/buzz-db/src/replica_fence.rs` paths `architecture-containers-postgres.md` cites have moved to `runtime/migration.rs` and `runtime/replica_fence.rs` (PR #6987, `refactor(db): extract domain stores from database runtime`), and confirm the exact count and gating of `#[ignore = "requires Postgres"]` tests plus which of them CI actually selects by name. ← RUNS HERE [independent] done when: every claim destined for the node's `evidence` ledger has been opened and copied into working notes, including the migration-path divergence and the test-execution gap.

STEP 2  [needs 1] Write `launchpad/docs/corpus/implementation/crates/buzz-db.md`: front matter (`id: implementation-crates-buzz-db`, `type: implementation`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, one `relationships` entry `part-of` -> `architecture-containers-postgres`, evidence ledger with FACT/INFERENCE entries citing only sources opened in STEP 1) and the template's required body sections (Realization statement, Target, Implementation surface, Divergences, Verification, Relationships, Scope and omissions) sized to what STEP 1 actually found — Target names `docs/multi-tenant-conformance.md`'s DB/index/RLS-scope column plus buzz-db's own crate-doc invariants (neither carries a corpus node id yet, so no `implements` edge is declared toward either); Divergences names the moved runtime paths; Verification states the ignored-test/CI-filter gap plainly. done when: the file exists at the target path and every issue #926 Definition-of-Done bullet has a corresponding sentence or section in the body. [needs 1]

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL naming this node and re-run until either exit 0 or the only remaining FAILs match the pre-existing `origin/launchpad` baseline (confirmed by running the same command against a clean `origin/launchpad` checkout or `git stash` if the count looks off). [needs 2] done when: the validator exits 0, or exits 1 with zero FAIL lines naming `implementation-crates-buzz-db`.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the commit-gate stamp; confirm `OK`. Then, in a separate tool call, `git add` the node and this plan file and `git commit -s` with a `docs(corpus)` message referencing #926. Do not push and do not open a PR. [needs 3] done when: the unittest run reports `OK` and `git log -1 --stat` shows a new commit containing exactly the two intended files.

PARALLEL: none — one document, one plan file, strictly sequential (evidence before drafting, draft before validation, validation before the commit gate).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 for this node (pre-existing unrelated baseline failures on `origin/launchpad` are not this task's to fix). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before the commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration review, not run here, per the batch-dispatch brief.

BUDGET: small — one ~46k-line crate read at headline/structure granularity (not full-content), one existing corpus node re-read, a handful of CI/Justfile/script greps; no code changes, no test runs beyond the two gate commands named above.

OPEN: Whether `docs/multi-tenant-conformance.md` and buzz-db's own crate-doc invariants should eventually become their own corpus nodes (which would let this node declare a real `implements` edge instead of naming them only in prose) is not this task's to decide — `AGENTS.md`'s own unresolved gap ("whether/when a NIP document or an ADR itself gets promoted to a corpus node id") applies identically here, and this node states the target in prose per that same rule rather than inventing an edge. Whether CI's narrow, explicitly-enumerated selection of `#[ignore]`d buzz-db tests (versus the ~227 that are never selected by name anywhere in this repository's automation) is a deliberate coverage decision or an unnoticed gap is not something this corpus-authoring task can resolve — it is reported as an observed fact in the Verification section, not adjudicated.

LEFT OUT: No `implements` edge to `docs/multi-tenant-conformance.md` or to buzz-db's own crate-doc invariants — neither has a corpus node id yet, and inventing one would be a hard validation error. No restatement of `architecture-containers-postgres.md`'s own canonical claims (writer/reader pool sizing, the floor-guard trigger, `BUZZ_AUTO_MIGRATE` gating, partitioning) — this node goes one layer deeper (concrete `store/`/`runtime/` modules, functions, and tests) and points back at that node via `part-of` instead of duplicating it. No claim about `buzz-conformance`, `buzz-relay`'s own wiring, or Redis/media/git-hosting rows of the multi-tenant conformance table — those are owned by other crates and other future corpus nodes, not by buzz-db's own implementation-reference node. No attempt to fix the CI test-selection gap found in STEP 1 — that is separate implementation work, not this docs-only task.
