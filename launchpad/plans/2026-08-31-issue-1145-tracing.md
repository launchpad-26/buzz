Issue #1145 — task: document layers/observability/tracing.md
Parent PRD #611. No `Size` line on the issue; the batch-run brief caps this single-document
task at 5 steps, so that is used as the declared cap rather than guessed per-step.

Stated size: no Size line on issue -> cap: 5 steps (batch-run instruction)

ALREADY TRUE
  `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and
  `launchpad/docs/corpus/templates/concept.md` are merged on `origin/launchpad` (HEAD
  ed133f4c5dbd546a67d963f11ffa630a4513b228), and
  `launchpad/docs/corpus/layers/observability/tracing.md` does not exist yet. No `layers/`
  subtree exists under the corpus root on `origin/launchpad` at this revision at all (checked
  via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`) — every sibling
  observability node (#1135, #1136, #1137, #1138, #1139, #1140, #1141, #1142, #1143, #1144) is
  either drafted-but-uncommitted-to-launchpad or committed only in its own sibling worktree, not
  merged. `#1138` (observability-liveness) and `#1139` (logging) are open issues with committed
  drafts in `__worktrees/task-1138-observability-liveness` and `__worktrees/task-1139-logging`
  respectively, giving a concrete front-matter/body precedent to follow, but neither is a valid
  `relationships` target since neither resolves on `origin/launchpad`. #1136 (datastore-tracing)
  and #1141 (opentelemetry) worktrees exist but have no corpus doc committed yet (checked: no
  commits past the shared base in either worktree).

STEP 1  [independent]  Gather evidence for the general/cross-cutting `tracing` crate usage
        across the codebase, distinct from #1141's OTEL exporter-wiring mechanism and #1136's
        `buzz-datastore-tracing` policy-macro crate: workspace dependency declaration
        (`Cargo.toml:85-87`); the one `#[tracing::instrument]` doc-comment diagram in
        `crates/buzz-relay/src/telemetry.rs`; declare-then-record `#[instrument(skip_all,
        fields(...))]` usage on `handle_event` (`crates/buzz-relay/src/handlers/event.rs:607-616`)
        and `handle_auth` (`crates/buzz-relay/src/handlers/auth.rs:42,70-72`); manual
        `tracing::info_span!` + `.instrument()` span-per-operation usage in
        `crates/buzz-relay/src/connection.rs:560-636` (`ws.auth`/`ws.event`/`ws.req`/`ws.count`),
        with its explicit comment that a bare `tokio::spawn` drops tracing context and the span
        must be captured before the spawn; `tower_http::trace::TraceLayer` HTTP-request span
        wiring via `make_http_span`/`http_trace_layer` (`crates/buzz-relay/src/router.rs:209-220`);
        the `.instrument(tracing::info_span!("llm", ...))` pattern wrapping an LLM call in
        `crates/buzz-agent/src/agent.rs:409-410`; the `#[tracing::instrument(...)]` attribute
        the `datastore_span` proc macro injects into caller functions
        (`crates/buzz-datastore-tracing/src/lib.rs:100-112`, described only as the general
        `#[instrument]` mechanism it reuses — its policy specifics are #1136's territory);
        `CONTRIBUTING.md:293-302`'s "use `tracing`, prefer structured fields" convention;
        `.env.example:174-183`'s `RUST_LOG`/`BUZZ_OTEL_FILTER` block; and confirmation (by grep)
        that no W3C `traceparent` header extraction/injection or cross-process propagator exists
        anywhere in the workspace, so trace context propagation here means in-process span
        nesting across async/spawn boundaries, not cross-process header propagation.
        done when: each source above has been opened directly and its relevant lines/behavior
        recorded, not assumed from a filename.

STEP 2  [needs 1]  ← RUNS HERE  Write the node: front matter (`id: layers-observability-tracing`,
        `type: layers`, `status: draft`, `origin: launchpad`, `audiences`, an `evidence` ledger
        with one entry per claim from STEP 1 classified FACT/INFERENCE/TEAM_KNOWLEDGE honestly,
        no `relationships` since no sibling node is merged on `origin/launchpad`) using the
        `layers-observability-logging`/`layers-observability-liveness` sibling-worktree drafts as
        the structural precedent (definition, per-surface/per-pattern picture, boundaries and
        non-goals explicitly excluding #1141's OTEL exporter-wiring specifics and #1136's
        datastore-tracing-policy specifics, use cases, scope-and-omissions naming both what is
        out of scope and what was expected to verify and could not).
        done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0.

STEP 3  [needs 2]  Re-run validate.py after any fix and iterate until clean; this step exists
        separately from STEP 2 because a first draft is not expected to pass on the first try
        given the citation-shape and evidence-class rules in `AGENTS.md`.
        done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0 with no
        further edits pending.

STEP 4  [needs 3]  Earn the verify-gate stamp and commit. Run the corpus unittest suite as the
        sole command in its own tool call, confirm `OK`, then commit in a separate call.
        done when: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`
        reports `OK`, and `git commit -s -m "docs(corpus): document tracing (#1145)"` succeeds.

STEP 5  [needs 4]  Self-review: re-read the diff against issue #1145's DoD checklist line by
        line, confirm no second canonical document was created, confirm `validate.py` still
        exits 0.
        done when: every DoD bullet is checked off against the actual diff and `validate.py`
        exits 0 on a final run.

PARALLEL  None — one file, five sequential steps.

GATES     `validate.py` (STEP 2/3) and the corpus unittest suite (STEP 4), run locally in this
          worktree. No push, no PR — a separate integration step cherry-picks this commit into
          a shared batch PR, so `review-adjudicate` and cross-model final review are explicitly
          deferred to that later stage, not run here.

BUDGET    STEP 1 and STEP 2 together. The hard part is scoping a general/cross-cutting `tracing`
          umbrella node honestly against two in-flight siblings (#1141 opentelemetry, #1136
          datastore-tracing) that claim the mechanism-specific and crate-specific territory,
          without duplicating their content or leaving this node too thin to define the term —
          the same shape of risk the merged-but-uncommitted `logging` sibling names for its own
          adjacent siblings.

OPEN      Whether the `buzz-datastore-tracing` proc macro's generated `#[tracing::instrument]`
          attribute should be cited here at all, given #1136 owns its policy specifics — resolved
          by citing only that the macro reuses the general `#[instrument]` mechanism this node
          describes, not any of the macro's `postgresql`-only/safe-fields/`otel.status_code`
          policy details.

LEFT OUT  Any second authored document. #1141's OTEL SDK/exporter-wiring mechanism and #1136's
          `buzz-datastore-tracing` crate deep-dive — named as siblings and excluded by scope, not
          folded in. Cross-process W3C trace-context header propagation, since none exists in
          this codebase today (confirmed by grep) — recorded as an absence, not fabricated as a
          feature. Any change to runtime code, `node.schema.json`, or `AGENTS.md`.
