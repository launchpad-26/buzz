# Issue #643 — agents/conflicting-evidence.md

ALREADY TRUE: `launchpad/docs/corpus/agents/invariants.md` (id `agents-invariants`), `launchpad/docs/corpus/standards/evidence.md` (id `corpus-standard-evidence`), `launchpad/docs/corpus/standards/decision-references.md` (id `corpus-standard-decision-references`), `launchpad/docs/corpus/templates/procedure.md` (id `corpus-template-procedure`), `launchpad/docs/corpus/templates/policy.md` (id `corpus-template-policy`), and `launchpad/docs/corpus/AGENTS.md` (id `corpus-agents`) are all merged on `origin/launchpad` (confirmed via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`). `launchpad/docs/corpus/agents/conflicting-evidence.md` does not exist yet (confirmed via `test -f`). `launchpad/decisions/ADR-0029-corpus-evidence-precedence.md` is Accepted. Two sibling nodes already carry substantial same-subject content: `corpus-standard-evidence`'s MUST 10 and "When sources disagree" section state the general same-claim-type-conflict rule (stop, record, `status: flagged`, escalate), and `corpus-standard-decision-references`'s "When two accepted decisions conflict" section gives a 4-step author recipe scoped specifically to two conflicting decision records. `agents-invariants`'s Q4 already states the one-sentence SHOULD version of this rule. None of this task's 30 unmerged sibling document tasks under Feature #620 are valid relationship targets.

STEP 1  Gather evidence: re-read `ADR-0029`, `corpus-standard-evidence.md`, `corpus-standard-decision-references.md`, and `agents-invariants.md` in full (already done in this session) to identify the genuinely unclaimed slice: a general-purpose, moment-of-authoring recognition-and-routing procedure usable for *any* same-claim-type conflict (not just two decision records), that explicitly delegates the already-specified escalation mechanics to the two sibling standards rather than restating them. Confirm `corpus-template-procedure`'s required sections and `type: agent` is the right surface (matches `corpus-agents` and `agents-invariants`, both `type: agent`, both about agent authoring behavior). ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `agents-conflicting-evidence`, type `agent`, status `draft`, origin `launchpad`, audiences `[agent, reviewer]`, relationships: `depends-on: corpus-agents`, `references: corpus-standard-evidence`, `references: corpus-standard-decision-references`, `references: agents-invariants`, `implements: corpus-template-procedure` — all five confirmed present on `origin/launchpad`) and the body in `corpus-template-procedure`'s required-section shape: Overview, Before you start, one numbered task sequence (recognize claim type → route: different claim types = no conflict, apply ordinary tiebreaker; two decision records = defer entirely to `corpus-standard-decision-references`'s existing recipe; any other same-claim-type pair = the general form — one entry per source, state the contradiction, `status: flagged`, escalate as an open question, check for unpublishable evidence per ADR-0029's security clause; then continue authoring the rest of the node), See also, Boundary statement (explicitly not restating ADR-0029/evidence.md/decision-references.md's content), Relationships, Scope and omissions.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole prior command to earn the verification stamp, then commit in a separate call. Self-review the diff against the issue's DoD (no `review-code`/`review-plan` skill found reachable for a corpus-markdown-only change; confirmed by checking the skill list — none of the review skills accept a bare markdown corpus node as their subject, and no dedicated corpus-review agent responded when tried). No push, no PR — stays local per the batch's build-loop contract.

PARALLEL: none — single file, single task, no code changes.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before the commit. `review-adjudicate` / cross-model final review are deferred to the batch owner's later integration pass, not run here.

BUDGET: small — one document, no code changes, evidence already gathered in this planning pass (five sibling corpus nodes plus ADR-0029 and the schema, all re-read from the actual merge-target tree).

OPEN: Whether the corpus-batch owner wants a `references` edge added later from `corpus-standard-evidence` or `corpus-standard-decision-references` back to this node once it merges — not this task's edit to make on those files' behalf. Whether `corpus-review` (the skill, not a subagent) should be invoked instead of self-review — tried via the `Skill` tool description; it is scoped to "one drafted corpus node before it merges" and is a plausible fit, but this batch's own build-loop instructions name `review-code`/self-review explicitly as the verification step, so `corpus-review` was not invoked to avoid diverging from the prescribed loop.

LEFT OUT: No restatement of ADR-0029's decision text, `corpus-standard-evidence`'s MUST 10 / "When sources disagree" section, or `corpus-standard-decision-references`'s "When two accepted decisions conflict" 4-step recipe — all three are linked, not duplicated. No new MUST/SHOULD policy list (that shape already exists twice on this exact subject; a third would be the duplication Feature #620's acceptance criteria explicitly bars). No relationship to any of the 30 unmerged `agents/*.md` / `ingestion/*.md` siblings. No attempt to resolve the open question of how a specification becomes "ratified" (owned elsewhere, per `corpus-standard-decision-references`'s own scope-and-omissions table).
