# Plan: issue #1198 — corpus doc `operations-databases-migrations`

Stated size: issue #1198 carries no explicit "Size" line; the batch dispatch brief for Feature #618 caps every task in this run at 5 steps for one hand-authored document -> cap: 4 steps.

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/reference.md` are merged on `origin/launchpad` (checked at commit 473205a7457b208455f188847bfb27b01aa83cac); `launchpad/docs/corpus/operations/databases/migrations.md` does not exist yet (confirmed with `test -f`); no `operations/**` corpus node exists on `origin/launchpad` yet (confirmed against `<SCRATCH>/existing-node-ids.txt`, 204 ids, none under `operations-`); `architecture-containers-postgres` and `layers-lifecycle-startup` do exist there and already narrate the general Postgres container and startup-sequence facts about migrations, so this reference node must add operator-facing lookup detail without duplicating either.

STEP 1 [independent] — Gather evidence: read `crates/buzz-db/src/runtime/migration.rs` in full (the `sqlx::migrate!` embedding, the exclusive advisory-lock wrapper, the pre-0007 brownfield guard, the additive/checksum-immutability convention documented in its own test comments, the `admin_schema_parity_between_desired_state_and_migrations` test, and the `pre_0007_ambiguous_nip_rs_data_blocks_without_mutation_and_allows_retry` test); `crates/buzz-relay/src/main.rs` (the `BUZZ_AUTO_MIGRATE` gate, `buzz_auto_migrate_enabled`, and its own unit test `buzz_auto_migrate_is_opt_in`); `crates/buzz-admin/src/main.rs` (the `migrate` subcommand); `migrations/` directory listing (naming/ordering convention, no `.down.sql` files); `schema/schema.sql` header; `scripts/reconcile-schema-after-pgschema.sql` in full; `.github/workflows/ci.yml` (the two `pgschema apply` + reconcile call sites); `Justfile` (`_ensure-migrations`, `migrate`, `relay`); `deploy/compose/compose.yml` and `deploy/compose/.env.example`; `.env.example`; `docs/multi-tenant-conformance.md` header; `TESTING.md`'s `BUZZ_AUTO_MIGRATE` row; and issue #1198 itself. RUNS HERE — already substantially complete before this plan file was written.
done when: every source path listed above has been opened and read (not merely grepped) at least once, and its supporting facts are noted for step 2.

STEP 2 [needs 1] — Write front matter (id `operations-databases-migrations`, type `operations`, status `draft`, origin `launchpad`, audiences `[operator, developer, reviewer]`, `relationships: [references: architecture-containers-postgres, references: layers-lifecycle-startup]` — both ids confirmed present in `<SCRATCH>/existing-node-ids.txt`) and a reference-template body (per `templates/reference.md`'s *Required sections*): a reference-description paragraph, a structured-entries table cataloguing the migration mechanism's parts (source directory, runner, trigger gate, concurrency lock, naming/ordering convention, checksum-immutability convention, desired-state alternative, post-apply reconciliation, manual CLI invocation, failure behavior, rollback path), an optional Commands table for the CLI/script surface, an explicit Boundary paragraph naming the concept/procedure/API-reference exclusions plus the sibling failure-runbook (#1222) boundary, the Relationships section, and Scope and omissions carrying both the ownership table and the expected-but-unverified list (staging/production `BUZZ_AUTO_MIGRATE` setting in the private deploy repos; whether a genuine mid-migration SQL failure — as opposed to the explicit pre-0007 guard — leaves partial DDL applied, which this task did not independently verify against `sqlx`'s own transactional behavior).
done when: `launchpad/docs/corpus/operations/databases/migrations.md` exists with schema-legal front matter (only the seven permitted keys) and a body carrying all six of `templates/reference.md`'s required sections.

STEP 3 [needs 2] — Validate: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 from the repository root of this worktree; fix any reported error (broken node id, invalid path, schema violation) and re-run until clean.
done when: the command's exit status is 0 on a run taken after the last edit to the document.

STEP 4 [needs 3] — Earn the commit gate and commit: run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own Bash call and confirm `OK`; then, in a separate call, `git add -A && git commit -s -m "docs(corpus): document the schema migration mechanism (#1198)"`.
done when: the unittest run's final line reads `OK` and `git log -1 --format=%H` on this branch returns a commit whose message contains `(#1198)` and whose trailers include `Signed-off-by`.

PARALLEL: none — single file (plus this plan file), single task, no dependency on sibling `operations/**` tasks in this batch (none of their ids are targeted, and none target this one).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` (must exit 0). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` (must report `OK`, run alone to earn the commit stamp). review-adjudicate and the cross-model final-review pass are explicitly deferred to the batch owner's later review — not run in this task.

BUDGET: small — one reference document, no runtime code change, target under ~1-2 hours of agent time.

OPEN: whether a genuine in-flight SQL failure inside one of the 40 migration files (as distinct from the explicit `reject_legacy_nip_rs_cardinality_ambiguity` pre-check, which is demonstrated by test to fail closed without mutation) leaves the target database with partial DDL applied is not established from this repository alone — it depends on `sqlx::migrate!`'s own per-migration transaction handling, and this task did not open the `sqlx` crate's source to verify it. Recorded in the body's scope-and-omissions rather than asserted either way. Also open: whether the private `squareup/block-coder-tf-stacks` and `squareup/sprout-oss` pipelines set `BUZZ_AUTO_MIGRATE` for staging/production is unknown from this repository, matching the same gap `architecture-containers-postgres` already discloses.

LEFT OUT: the failure-recovery runbook (what an operator does when a migration fails at startup, how to diagnose a stuck advisory lock, when to intervene manually) is explicitly out of scope — it is sibling task #1222's subject, named as a boundary in this node's body rather than written here. No second canonical corpus document is created. No runtime/product behavior is changed. The desired-state `schema/schema.sql` file's own content and the multi-tenant conformance rules it encodes are not re-narrated beyond what operators need to know about its role in the migration mechanism.
