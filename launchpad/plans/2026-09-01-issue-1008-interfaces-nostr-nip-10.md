# Issue #1008 — interfaces/nostr/nip-10.md
Stated size: no Size line in issue body  →  cap: 5 steps (per task brief)

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and the `interface` template (`launchpad/docs/corpus/templates/interface.md`) are merged on `origin/launchpad`. `launchpad/docs/corpus/interfaces/nostr/nip-10.md` does not exist yet (confirmed: no `interfaces/` directory exists anywhere under `launchpad/docs/corpus/` — this is the first `interfaces-events`-typed instance node in the corpus). `node.schema.json`'s `type` enum has no `interfaces` value — the correct value is the single hyphenated `interfaces-events`, per the enum itself and per the interface template's own "A note on `type`" section. `crates/buzz-core/src/nip10.rs` is the single shared NIP-10 marker parser (`parse_thread_markers`, `ThreadMarkers::resolve`), consumed by `buzz-relay`'s ingest resolver, `buzz-workflow`'s `trigger_is_reply` predicate, and `buzz-cli`'s `messages` commands. `crates/buzz-test-client/tests/e2e_nostr_interop.rs` has three NIP-10-specific `#[ignore]`d live tests (`test_nip10_thread_reply_creates_metadata`, `test_nip10_unknown_parent_rejected`, `test_nip10_root_mismatch_rejected`) that already exercise a valid case and two rejection cases.

STEP 1  Write the front matter and body of `launchpad/docs/corpus/interfaces/nostr/nip-10.md` against the `interface` template's required sections (Interface description, Operations, Contract and stability, Boundary, Relationships, Scope and omissions), covering: the shared parser (`buzz-core/src/nip10.rs`), the relay ingest resolver and its `IngestError::Rejected`/`AuthFailed`/`Internal` taxonomy (`crates/buzz-relay/src/handlers/ingest.rs`), the `buzz-db` `thread_metadata` counters (`reply_count`/`descendant_count`, `crates/buzz-db/src/store/thread.rs`), the outbound tag builder (`crates/buzz-sdk/src/builders.rs`'s `thread_tags`), the `buzz-cli` `--reply-to` flag and `messages thread` command, and the `buzz-workflow` `trigger_is_reply` condition variable. Cite the upstream spec at a full-SHA-pinned GitHub link (`nostr-protocol/nips` commit `dabfcb2aaecf4fa374eda8b1232ab303a03f60ba`, path `10.md` — already used for NIP-01/NIP-29 in the merged templates, and independently confirmed by fetching that exact URL during evidence-gathering for this plan). Document, as a `FACT` comparing the two opened sources, the one concrete deviation found: upstream NIP-10 text recommends a direct reply to the thread root carry a single `e` tag marked `"root"`, while Buzz's own convention (`nip10.rs`, `builders.rs::thread_tags`) treats a lone `"reply"`-marked tag as the direct-reply-to-root case and never emits a lone `"root"` marker for that case; also note Buzz does not implement the deprecated positional (unmarked) `e`-tag convention (`nip10.rs` only reads 4+-part marked tags) and does not auto-populate `p` tags for ancestor authors (mentions are a separate, explicit, capped mechanism in `builders.rs::mention_tags`). Give one valid example (`test_nip10_thread_reply_creates_metadata`) and one failure example (`test_nip10_unknown_parent_rejected` or `test_nip10_root_mismatch_rejected`), citing the test file and function names. No `relationships` — enumerate the four nodes merged on `origin/launchpad` (`corpus-agents`, `corpus-readme`, `corpus-standard-confidence`, `corpus-standard-decision-references`) and confirm none is a legitimate target; mention (do not link) the unmerged sibling event-kind node for kind 9 by filename if relevant.  [independent]
        done when: the file exists at the target path with schema-required front matter fields present (`id: interfaces-nostr-nip-10`, `type: interfaces-events`, `status: draft`, `origin`, `audiences`, `evidence`) and every one of the issue's Definition-of-done bullets (inputs/messages, outputs/responses, error/rejection behavior, auth/authz, versioning/compatibility, ordering/idempotency, spec link, valid + failure example) is addressed in the body.

STEP 2  [needs 1] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root of this worktree; fix any FAIL line (UNVERIFIED notices are acceptable) and re-run until it exits 0.  ← RUNS HERE
        done when: the command's exit code is 0 and its output contains no line beginning `FAIL` that names the new node.

STEP 3  [needs 2] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call and confirm it prints `OK`.
        done when: the command's final output line is `OK` with no `FAILED`/`ERROR` summary.

STEP 4  [needs 3] Stage exactly the new corpus document and this plan file and commit with `git commit -s -m "docs(corpus): document NIP-10 interface (#1008)"`.
        done when: `git log -1 --format=%s` reports that message and `git show --stat HEAD` lists only `launchpad/docs/corpus/interfaces/nostr/nip-10.md` and `launchpad/plans/2026-09-01-issue-1008-interfaces-nostr-nip-10.md`.

STEP 5  [needs 4] Self-review: re-read the committed diff against the issue's Definition-of-done checklist line by line, confirm every evidence entry's citation was actually opened during STEP 1's research (this plan's own ALREADY TRUE / STEP 1 notes are the record of what was opened), and confirm no second hand-authored canonical corpus document was created.
        done when: each Definition-of-done bullet has a named section/sentence in the committed body satisfying it, and `git show --stat HEAD` (from STEP 4) still lists exactly the two files.

PARALLEL: none of the five steps can run as an independent subagent relative to another — STEP 1 must finish (and be re-readable) before validate.py in STEP 2 can pass, STEP 2 must pass before the test-suite gate in STEP 3 is meaningful, STEP 3's `OK` is the commit gate STEP 4 depends on, and STEP 5 reviews STEP 4's actual commit. This is a single-file authoring task with a strict evidence-then-write-then-validate-then-commit-then-review order; there is no independent second file to farm out.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` (STEP 2) is the mechanical structural gate and must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` (STEP 3) is the commit-eligibility gate this repository's commit hook checks for — it must print `OK` before `git commit -s` is attempted, and if the commit is still rejected for a missing stamp, that is reported as a finding rather than routed around (no `--no-verify`, no hand-editing a stamp file). `review-adjudicate`, `review-final`, and any cross-model review pass are out of scope for this single-document task per the batch-author convention already established for sibling corpus-doc issues in this plans directory (see `2026-08-28-issue-698-corpus-doc.md`) — deferred to the batch owner. `qa` explore mode does not apply: this is a documentation-only change with no runtime interface of its own to exercise.

BUDGET: STEP 1 (the writing itself) is the step most likely to eat the budget — the interface template's required sections are extensive (six sections plus a template skeleton) and the evidence-gathering already performed spans seven+ source files (`nip10.rs`, `ingest.rs`, `store/thread.rs`, `builders.rs`, `commands/messages.rs`, `buzz-workflow/lib.rs` + `executor.rs`, `e2e_nostr_interop.rs`) plus one external spec fetch. STEP 2/3 are mechanical re-runs; STEP 4/5 are fast.

OPEN: The issue's own Definition-of-done line 2 asks for "typed relationships appropriate to the node" but the corpus (per the interface template's own precedent, section "This node's own relationships") currently carries no interface- or event-shaped merged node this can legitimately point at — `relationships` is expected to be empty or absent, and this plan does not treat that as a defect to work around. Whether the concrete NIP-10-vs-Buzz marker-convention deviation found during research (lone `"reply"` marker standing in for upstream's lone `"root"` marker on a direct reply) is itself worth a separate corpus concept/decision node is not this issue's call — it is reported in the body as a documented fact, not escalated to a new task, unless STEP 5's self-review finds it rises to "a newly discovered second concept" per the issue's own out-of-scope bullet.

LEFT OUT: No relationship edge to any unmerged sibling `interfaces-events` or event-kind node (e.g. a future kind:9/NIP-29 event-kind node) — per `AGENTS.md`'s own rule, only targets merged on `origin/launchpad` at build time are legal, and none exists yet; any such node this document's prose wants to point at is prose-linked by filename/kind number, not a `relationships` edge. No change to `crates/buzz-core/src/nip10.rs`, `crates/buzz-relay/src/handlers/ingest.rs`, or any other runtime code — this is documentation only, and the issue's own "Out of scope" section bars runtime behavior changes. No attempt to un-`#[ignore]` or run the three live `e2e_nostr_interop.rs` NIP-10 tests against a running relay — they are cited as source evidence for the valid/failure examples, not executed, since this task needs no Postgres/Redis integration environment for a docs-only change.
