# Plan: issue #711 — document capabilities/agents/agent.md

## ALREADY TRUE

- `launchpad/docs/corpus/capabilities/` does not exist yet on `origin/launchpad`
  (confirmed via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`).
  No sibling capability node — including all of #705-#710, #712, #713, #715, #716 —
  is merged, so `relationships` may target no capability-typed node.
- Three `architecture`-typed nodes already documenting the AI-agent subject ARE
  merged: `architecture-context-ai-agent`, `architecture-containers-agent-runtime`,
  `architecture-flows-agent-turn`, plus `architecture-principles-humans-and-agents-are-peers`.
  These are valid `references` targets per the capability template's own guidance
  ("may declare `references` toward architecture nodes that realize this capability").
- `launchpad/docs/corpus/templates/capability.md` is the template to follow; it
  requires: Capability statement, Maturity, Boundary, Relationships, Scope and
  omissions.
- `node.schema.json`'s `type` enum member for this task is `capabilities` (plural),
  per the template's own "Note on `type`" section — not `agent` (a different enum
  value used by `AGENTS.md` itself for corpus-governance docs).
- VISION_PROJECTS.md's Status table already names this capability at product level:
  "MCP server + ACP agent harness | Ships today" (VISION_PROJECTS.md:247-259).
- Repository revision for provenance: `131b02f989684117d9ab1dd426f1673fa638e523`.

## STEP 1 — Draft front matter and evidence ledger

Write `id: capabilities-agents-agent`, `type: capabilities`, `status: draft`,
`origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`.
Evidence entries, each opened directly this session:
- Provenance commit.
- VISION_PROJECTS.md Status row (maturity: shipped).
- VISION.md's "humans and agents get the same thing" framing (peer identity).
- `KIND_AGENT_PROFILE` (10100) and `KIND_MANAGED_AGENT` (30177) from `kind.rs`.
- NIP-OA's owner `auth` tag mechanism (docs/nips/NIP-OA.md).
- `crates/buzz-acp/README.md`'s Inbound Author Gate, owner control commands
  (`!shutdown`/`!cancel`/`!rotate`), and kind:9 `#p`-tag mention trigger (with line
  citations gathered this session).
- `crates/buzz-cli/src/agent_management.rs`'s "Owner-reviewed agent draft requests"
  module doc comment — the one place "owner-reviewed" is a real, sourced term, scoped
  narrowly to self-service agent-management drafts, not to ordinary turn responses.
`relationships`: `references` toward the four merged architecture/principle nodes
above (all resolve against `origin/launchpad` today).

**Done when:** ledger entries are each FACT with a real citation opened this session,
or TEAM_KNOWLEDGE/INFERENCE classed honestly.

## STEP 2 — Draft the body from the template skeleton

Capability statement (noun phrase, "AI Agent participation" style), Maturity (cite
the Status row + the merged architecture nodes as evidence the mechanism is built),
Boundary (explicitly not architecture/interface/flow/operations, and explicitly not
the still-unmerged sibling capability facets: agent-turn, agent-memory, agent-mention,
agent-owner, agent-response, agent-shutdown, backend-provider, managed-agent, mcp,
persona, remote-agent, acp — named so a reader knows where each facet will live once
drafted, without duplicating content that does not exist yet). Precisely distinguish
"owner review" of self-service agent-management drafts from the unrelated,
unreviewed publishing of an ordinary turn response — do not conflate the two.

**Done when:** every Required Section from the template is present and the Boundary
section names all twelve sibling facets without asserting their content.

## STEP 3 — Validate

Run `python3 launchpad/project-intelligence/corpus/validate.py` from the worktree
root. Confirm the known 21 pre-existing FAIL baseline (#1951) is unchanged and this
node adds zero new FAIL entries.

**Done when:** exit code and FAIL count match the baseline plus zero.

## STEP 4 — Earn the commit gate and commit

Run, as the sole command in its own call:
`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`.
Confirm `OK`. Then `git add` the new node + this plan file and
`git commit -s -m "docs(corpus): document capabilities/agents/agent (#711)"`.

**Done when:** commit succeeds locally; no push, no PR.

## STEP 5 — Self-review

Re-read the diff against #711's DoD line by line; re-open every cited source;
confirm no second canonical document was created; confirm the validate.py FAIL
count is unchanged from baseline.

## PARALLEL

None — single-file task, steps are sequential.

## GATES

- `validate.py` exit 0 with zero new FAIL entries beyond the #1951 baseline.
- `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` → `OK`.

## BUDGET

Single node, ~1 sitting. No sub-agents.

## OPEN

- Whether a later pass revisits this node's `relationships` once sibling capability
  nodes (#705-#710, #712, #713, #715, #716) merge — deliberately left for that future
  pass, per `AGENTS.md`'s own precedent.

## LEFT OUT

- Any edit to the twelve sibling capability files — each is its own task/issue.
- Any relationship to a not-yet-merged node.
