# Issue #442 — bug(ci): paths-filter lists 'justfile' lowercase; the tracked file is 'Justfile', so the entry never fires

Stated size: none on the issue (assumed ≤30-minute tier — one-line config fix plus a small, already-patterned regression check)  ->  cap: 5 steps

ALREADY TRUE (verified against git and the repo, not notes):
- `.github/workflows/ci.yml` line 52 reads `- 'justfile'` (lowercase), inside the `rust:` filter block passed to `dorny/paths-filter`.
- The tracked file is `Justfile` (capital J): `git ls-files -- justfile` returns nothing; `git ls-files -- Justfile` matches.
- Every other literal (non-glob) path entry across all four filter blocks (`rust`, `desktop`, `desktop-rust`, `web`, `mobile`), and every glob directory root (`crates`, `migrations`, `schema`, `desktop`, `web`, `mobile`), was checked the same way and matches tracked casing exactly — no sibling case-mismatch bug exists in this filter block, so scope stays exactly this one line.
- `scripts/test-ci-changed-paths-filter.sh` already exists, runs in the `changes` job as the "Changed-paths filter contract" step, and parses the same `filters: |` block with `awk`. It currently only guards against launchpad-26/buzz#181 (a negated pattern mixed with positive patterns in one filter silently becoming always-true). It does not check literal entries for case-mismatch against tracked filenames — this bug class is currently uncaught.
- Working tree is clean on `fix/issue-442-paths-filter-justfile-case`, branched from `origin/launchpad` at `aef93f2c2a`.

STEP 1  Fix the entry: change line 52 from `- 'justfile'` to `- 'Justfile'` in `.github/workflows/ci.yml`. No other line in the file changes.                                                                                    [independent]  ← RUNS HERE
        done when: `grep -n "'Justfile'" .github/workflows/ci.yml` shows the corrected line 52, and `grep -x "'justfile'" .github/workflows/ci.yml` (lowercase, quoted as in the file) finds nothing.

STEP 2  Extend `scripts/test-ci-changed-paths-filter.sh` to also check this bug class: for every literal, non-glob path pattern parsed out of the `filters: |` block (i.e. entries with no `*`, `**`, or `?`), assert it matches a tracked file with exact case via `git ls-files -- <pattern>`, and fail with a clear `::error::` message naming the offending entry if it doesn't (mirroring the existing error-message style already in the script). Leave the existing #181 mixed-pattern check untouched.                                                                            [needs 1]
        done when: running the extended script against the fixed `ci.yml` from STEP 1 exits 0; temporarily reverting STEP 1's line back to lowercase `'justfile'` and re-running the script makes it fail with a message naming `justfile`/`rust`, proving the new check actually catches this exact bug (then re-apply the STEP 1 fix before continuing).

STEP 3  Run local verification and record output: `bash scripts/test-ci-changed-paths-filter.sh` (must pass on the fixed tree), plus the regression demonstration from STEP 2's done-when (temporary revert → fail → restore). No other test suite is relevant — this change touches only `.github/workflows/ci.yml` and one shell script, neither of which is exercised by `just test-unit`/`just test`.                                                                          [needs 2]
        done when: both runs produce the outputs described above, captured verbatim for the PR/report.

STEP 4  Commit with `git commit -s` (message referencing #442), push the branch, and open a PR against `launchpad` with "Closes #442" in the body. Check 2–3 recent agent-authored merged PRs' labels to confirm whether `by:agent` is applied automatically or must be added explicitly, and match that convention.                                                                                                                     [needs 3]
        done when: PR is open, based on `launchpad`, body contains "Closes #442", and the label convention question is resolved (either applied automatically and confirmed, or added explicitly with the same rationale recent PRs show).

PARALLEL: None. All four steps touch the same two files in sequence (STEP 1 → STEP 2 depends on STEP 1's exact line existing; STEP 3 depends on STEP 2's script; STEP 4 depends on a verified, working tree from STEP 3). Not worth fanning out for a 1-line YAML fix plus one script extension.

GATES: `review-code` — light pass over the `ci.yml` line and the script diff after STEP 2 (small surface, but a CI gating change is worth a second look before it merges). `review-tests` applies narrowly to the extended assertion added in STEP 2 (does it actually fail on the bug it targets — proven directly by the temporary-revert step, which doubles as the test-review evidence). `qa` explore mode does **not** apply — there is no runtime UI/API/CLI interface to exercise; this is a CI workflow config plus a bash script, verified by directly running the script (STEP 3), not by exploratory interaction. Per this task's process, a cross-model/root-cause final review is not a separate gate here — root cause was independently re-confirmed against current code before this plan was written (see the task's own O/H/T/R/C record), and `serina:build-change` hands off to its own review gate at the end.

BUDGET: STEP 2 is the step most likely to eat the budget — matching the existing script's `awk`-based parsing style for a new check (rather than introducing a second, inconsistent way of reading the filters block) takes more care than the one-line fix in STEP 1.

OPEN: The issue has no `Size` line, so the step cap here is an assumption (treated as the ≤30-minute tier) rather than something the issue stated — noted rather than blocking on it, since the fix itself is unambiguous and small. Whether `by:agent` is applied automatically by a bot/label-sync workflow or must be set explicitly on `gh pr create` is left for STEP 4 to determine from recent PR evidence, not decided here.

LEFT OUT: No changes to any other filter entry — the sibling-bug check in this plan's evidence section found nothing else to fix, and expanding scope beyond #442 was explicitly out of bounds for this task. No attempt to make `dorny/paths-filter` itself case-insensitive (e.g. via a config option) — the issue's own "Expected behaviour" section offers that as an alternative but the simpler, more auditable fix (correct the casing) is chosen, consistent with "Cargo.lock and ci.yml entries in the same filter are unaffected; they are spelled correctly" implying case-correctness is the intended norm. No changes to `scripts/test-ci-changed-paths-filter.sh`'s existing #181 check or its output format beyond adding the new independent check.
