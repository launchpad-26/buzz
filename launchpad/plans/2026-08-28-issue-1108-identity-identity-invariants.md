# Issue #1108 — layers/identity/identity-invariants.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/invariant.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/layers/identity/identity-invariants.md` does not exist yet (confirmed: `launchpad/docs/corpus/layers/` has no tree entry at all on `origin/launchpad`). Revision recorded: `git rev-parse HEAD` = `338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5`.

STEP 1  Gather evidence for the core identity invariant — a Buzz identity is exactly a secp256k1 keypair, proven per-event by Schnorr signature, never by any server-side account record. Read and cite: `crates/buzz-core/src/verification.rs` (`verify_event` — id-hash + signature check, called from `crates/buzz-relay/src/handlers/event.rs` at both the ephemeral and standard event ingestion paths, lines ~805 and ~960, each rejecting the event with `OK false "invalid: {e}"` on failure and never storing/broadcasting it); `crates/buzz-auth/src/nip42.rs` (`verify_nip42_event` — calls `buzz_core::verify_event` first, then challenge/relay/timestamp checks); `crates/buzz-auth/src/lib.rs` (`AuthContext.pubkey` — the identity bound to a connection); `crates/buzz-relay/src/handlers/auth.rs` (`handle_auth` — the `AuthState::Authenticated(_)` arm rejects a second AUTH outright rather than rebinding the connection to a different pubkey; confirmed by grep that no test in `crates/buzz-test-client/tests/` or `crates/buzz-relay/src/` exercises this specific rejection path, so this sub-invariant is structurally- not test-enforced); `desktop/src-tauri/src/secret_store.rs` module doc (private key custody stays client-side in the OS keyring; the store is explicitly kept off any relay-communication path). Confirm the unit tests backing the signature/id and NIP-42 checks (`verification.rs::tests::rejects_tampered_id`, `::rejects_tampered_signature`; `nip42.rs::tests::wrong_challenge_rejected`, `::wrong_kind_rejected`, `::expired_event_rejected`, `::wrong_relay_rejected`). ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: `id: layers-identity-identity-invariants`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships` — no node currently merged on `origin/launchpad` is a legitimate target; `architecture/principles/signed-events.md` and `architecture/flows/websocket-authentication.md` exist but this task's own DoD forbids inventing a mapping, and no existing node declares itself as this invariant's target, so the edge would have to originate here with no reciprocal need — recorded as a deliberate omission, not silence) and the body against `templates/invariant.md`'s required sections: Invariant statement (the keypair-is-identity + per-event-signature-proof property, stated as a condition, not a goal), Scope (every Nostr event the relay ingests, kind-agnostic at `verify_event`; NIP-42 AUTH events specifically bind a WebSocket connection's `AuthContext.pubkey` for the connection's lifetime, and that binding does not change on a second AUTH), Enforcement today (naming the honest, weakest tier per facet: test-enforced for the signature/id check itself and for NIP-42's challenge/relay/timestamp checks; structurally-enforced-but-untested for the no-rebind-on-reauth behavior; convention-only for private-key custody staying client-side, since the relay protocol has no code path that even accepts a private key — nothing forces a future client to keep it that way), Consequence of violation (event rejected with `OK false "invalid: {e}"`, never stored or broadcast; failed AUTH sets `AuthState::Failed` and blocks the connection from authenticated operations), Boundary (not community/channel authorization — that is `buzz-auth`'s scope system and `moderation_restriction_state`, a separate node's territory; not NIP-OA owner delegation — a related but distinct mechanism), Relationships (none, with the reasoning above), Scope and omissions (what's covered, what's not, and what was expected but not verified — e.g., whether the relay has zero private-key-accepting code paths was checked by reading `AuthService`'s public API surface, not by an exhaustive repo-wide search).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here.

BUDGET: small — one document, no code changes; evidence gathering scoped to ~6 source files already located via RepoQL explore/read, no open-ended search.

OPEN: Whether a future `interface`-shaped node for NIP-42/NIP-98 (once `templates/interface.md` merges) should declare a `references` edge back to this node is left for that later task, per `templates/invariant.md`'s own stated relationship direction (the interface node declares the edge, not this one). Whether the private-key-never-leaves-client claim should eventually get its own dedicated node (e.g. covering `secret_store.rs`'s full custody model, migration, and backup/recovery in `key_backup.rs`) rather than being one supporting fact inside this node is not decided here — it is named as a gap in Scope and omissions rather than expanded into a second document, per the DoD's "no second canonical document" rule.

LEFT OUT: No claim about NIP-05/NIP-98 HTTP Auth's full contract, mobile's identity handling, or the NIP-AB device-pairing flow (`buzz-pair-relay`, `buzz-pairing-cli`) — real identity-adjacent subjects, but each is either a separate mechanism or would require evidence gathering this 5-step budget does not cover; named as an omission, not silently dropped. No attempt to add `relationships` to `architecture/principles/signed-events.md` or `architecture/flows/websocket-authentication.md` — those nodes do not declare a `references` edge to this one, and per the template, this node does not originate that edge either.
