# Issue #1075 — layers/data/postgres/audit-tables.md

Stated size: issue #1075 states no explicit Size line -> cap: 5 steps (one small hand-authored document, per this batch's own established pattern for sibling corpus-doc issues).

ALREADY TRUE: `launchpad/docs/corpus/layers/data/postgres/audit-tables.md` does not exist on `origin/launchpad` or in this worktree (`launchpad/docs/corpus/layers/data/postgres/` has no files at all yet). `node.schema.json`'s `type` enum has 13 members and no `data` member; `type: layers` is the closed-enum member every merged/open sibling under `layers/data/**` uses — confirmed by reading the actual front matter of `layers-data-object-storage-media-objects` and `layers-data-object-storage-minio` on open PR #1874, both `type: layers`, both chosen explicitly over `templates/datastore.md`'s own `type: architecture` suggestion for a real instance, with the tension disclosed in each node's own evidence ledger per `standards/taxonomy.md`. Issue #1075's own Definition-of-Done bullets ("Defines identity/key and semantic ownership," "Summarizes fields by meaning without duplicating generated schema detail," "Defines relationships, lifecycle and invariants," "Links authoritative migration/schema and read/write code paths") map onto `templates/data-entity.md`'s five required sections (Identity, Attributes and shape, Invariants, Relationships, Provenance/Storage pointer) — not onto `templates/datastore.md`'s sections, which sibling issue #1070 (media-objects, worded "canonical **datastore** node") uses instead; the two issue bodies were read side by side to confirm this is a real difference in DoD wording, not an assumption. This document therefore follows `data-entity.md`'s section shape, with `type: layers` overriding that template's own `type: implementation` suggestion, the same way the batch already overrides `datastore.md`'s `type: architecture` suggestion. `architecture-containers-postgres` is merged and validated on `origin/launchpad` (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`) and its own body already names `audit_log` as one of Postgres's tables and the 5-connection `BUZZ_AUDIT_ENABLED`-gated audit pool `buzz-relay` opens directly — a valid `part-of` target, the same relationship shape `media-objects.md` used against `architecture-containers-object-storage`. `migrations/0001_initial_schema.sql` defines the `audit_log` table (`PRIMARY KEY (community_id, seq)`, unique `(community_id, hash)`); `crates/buzz-audit/` (`entry.rs`, `hash.rs`, `action.rs`, `service.rs`, `error.rs`) is the crate that owns all chain logic against it, already read in full this session; `migrations/0029_community_deletion.sql` attaches the community write-fence trigger to `audit_log` (line 548) and defines that trigger's behavior (lines 402-425). Sibling issue #1084 (`moderation-tables.md`) is the separate task documenting `moderation_actions` (`migrations/0006_moderation.sql`), confirmed distinct from `audit_log` by reading both DDLs. Commit citation for this session: `338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5`.

STEP 1 [independent] Confirm scope: `audit_log` alone is this node's subject; `moderation_actions` is issue #1084's, not re-described here. Record this boundary check as a TEAM_KNOWLEDGE/FACT evidence entry rather than leaving it implicit. done when: the boundary check and its source (issue #1084's title, `migrations/0006_moderation.sql`) are recorded in the drafted node's own evidence ledger. ← RUNS HERE

STEP 2 [needs 1] Write front matter (`id: layers-data-postgres-audit-tables`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`, one `relationships` entry: `part-of` -> `architecture-containers-postgres`) and the body per `data-entity.md`'s required sections: Identity (`(community_id, seq)` PK, per-community monotonic `seq`, unique `(community_id, hash)`, cite `migrations/0001_initial_schema.sql`); Attributes and shape (`hash`/`prev_hash` BYTEA SHA-256 digests, `action` VARCHAR(64) from `AuditAction::as_str()`, `actor_pubkey`/`object_id`/`detail` nullable per `entry.rs`'s doc comments, `created_at` TIMESTAMPTZ truncated to storage precision before hashing per `hash.rs::to_storage_precision`); Invariants (hash-chain integrity via `hash.rs::compute_hash`'s fixed field order with `community_id` folded in first, cite the `cross_community_row_does_not_verify` and `verify_detects_tampering_within_a_community` tests in `service.rs`; the community write-fence trigger rejecting writes to a non-active community's chain per `migrations/0029_community_deletion.sql`; per-community serialization via `pg_advisory_lock` in `service.rs::AuditService::log`); Relationships (`communities(id)` foreign key on `community_id`; no entity-to-entity corpus edges yet, no sibling data-entity node merged); Provenance (`audit_log` is a purely server-derived projection with no Nostr event of its own — `buzz-audit`'s own module doc: "this crate is pure chain logic and ships no DDL"); Storage pointer (Postgres, table `audit_log`, linked via the `part-of` relationship rather than restating `architecture-containers-postgres`'s pool/migration content); Scope and omissions (names `moderation-tables.md` #1084 as `moderation_actions`'s owner; names the gap that no production caller of `AuditService::verify_chain`/`get_entries` was found outside the audit crate's own `#[cfg(test)]` module in `event.rs`, so read access is exercised only by tests today). done when: the file exists with every schema-required field and every issue-#1075 DoD bullet addressed against this scope, and every substantive claim has a classified evidence entry (FACT for files opened this session, INFERENCE with confidence for the no-production-reader gap, TEAM_KNOWLEDGE with provided_by for issue-sourced claims).

STEP 3 [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the worktree root; fix and re-run until exit 0. done when: the command exits 0, with no relationship target besides `architecture-containers-postgres`, no non-`.md` file added, and no second hand-authored corpus document.

STEP 4 [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole, unpiped command in its own call and confirm `OK`; then, in a separate call, stage and commit only the plan file and the corpus doc, signed off (`git commit -s`). No push, no PR. done when: the unittest run reports `OK` and exactly one commit exists ahead of `origin/launchpad` touching only those two files (a second small commit is acceptable only if a post-commit fix is needed and `git-safety.sh` blocks amending).

STEP 5 [needs 4] Self-review: re-read `git diff origin/launchpad -- .` against issue #1075's DoD checklist line by line, check every evidence entry against the source it cites, confirm no second canonical document was created, and re-confirm `validate.py` exits 0. done when: self-review is complete and its result is reported in the final task report.

PARALLEL: none — single file, single task, no independent workstreams to run alongside it.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 (STEP 3, re-checked in STEP 5). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK`, run bare and unpiped, before the commit (STEP 4) — the commit gate this worktree's hooks require. `review-adjudicate` and any cross-model final review pass are deferred to the batch owner's later bundling/review step, not run here, per this task's own isolate/plan/build/verify/commit-only instructions (no PR).

BUDGET: small — one corpus document (~150-250 lines), one plan document, no code changes. Evidence-gathering scoped to `migrations/0001_initial_schema.sql`, `migrations/0029_community_deletion.sql`, `crates/buzz-audit/src/{entry,hash,action,service,error}.rs`, `crates/buzz-relay/src/main.rs`'s audit-pool wiring, and `architecture-containers-postgres` — all already read this session.

OPEN: Whether `AuditService::verify_chain`/`get_entries` having no production caller today (only exercised in a `#[cfg(test)]` tenant-isolation test in `event.rs`) is a deliberate "log now, wire up an operator-facing reader later" design or an unfinished feature — named as a gap in the document, not resolved here. Whether `moderation-tables.md` (#1084) will itself declare an edge back toward this node once both are merged — left for whichever of the two merges second, per `AGENTS.md`'s relationships-must-resolve-on-the-merge-target rule.

LEFT OUT: Any edit to `moderation_actions`, `_operator_global_tables`, or any other table — out of scope per issue #1075's own "Out of scope" section and the one-document rule. Any relationship edge to `layers-data-postgres-*` siblings (channels-table, events-table, moderation-tables, etc.) — none are merged on `origin/launchpad` yet, so none are valid targets today. Cross-model review — not part of this isolate/plan/build/verify/commit task; the batch owner's later bundling PR carries that.
