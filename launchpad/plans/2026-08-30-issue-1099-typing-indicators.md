# Issue #1099 — layers/data/redis/typing-indicators.md

Stated size: not specified on the issue -> cap: 5 steps (batch dispatch brief cap).

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/standards/taxonomy.md`, `launchpad/docs/corpus/templates/datastore.md` and `launchpad/docs/corpus/architecture/containers/redis.md` (id `architecture-containers-redis`) are merged on `origin/launchpad`; `launchpad/docs/corpus/layers/data/redis/typing-indicators.md` does not exist yet, on this branch or on `origin/launchpad`.

STEP 1  [independent] Gather evidence: read `crates/buzz-core/src/kind.rs` (`KIND_TYPING_INDICATOR = 20002`, the ephemeral range 20000-29999, `is_ephemeral`), `crates/buzz-relay/src/handlers/event.rs` (`handle_ephemeral_event`, the `MessagesWrite` scope gate, the community-write-fenced check, the channel-membership check, and — critically — that typing carries no special case the way `KIND_PRESENCE_UPDATE` does), `crates/buzz-pubsub/src/{publisher.rs,topic.rs,presence.rs}` (Redis `PUBLISH` only, channel-name shape `buzz:{community}:channel:{id}` / `buzz:{community}:global`, contrasted with presence's `SET ... EX 180` keyed state, to establish typing indicators own zero Redis keys), `crates/buzz-acp/src/relay.rs` (`build_typing_event`, `try_publish_event`) and `crates/buzz-acp/src/lib.rs` (3s `typing_refresh` interval), `desktop/src/features/messages/{useTypingBroadcast.ts,useChannelTyping.ts}` (3s client throttle, 8s client-side TTL, all liveness client-managed), and `crates/buzz-test-client/tests/conformance_multitenant.rs` (`pubsub_presence_typing` module, the tenant-fence test for typing). Record the stray orphaned doc-comment `/// Typing indicator tracking in Redis.` above `pub use error::PubSubError;` in `crates/buzz-pubsub/src/lib.rs` line 43 as a named, unresolved discrepancy (crate-level doc claims a dedicated typing-tracking module; none exists). done when: every file above has been opened and its relevant line(s)/symbols noted for citation. ← RUNS HERE

STEP 2  [needs 1] Write front matter for `launchpad/docs/corpus/layers/data/redis/typing-indicators.md`: id `layers-data-redis-typing-indicators`, `type: layers` (an explicit, disclosed override of `templates/datastore.md`'s own worked example, which directs a real instance node to `type: architecture` — precedent from this batch's earlier `layers/data/...` siblings uses `type: layers` instead, and the choice is disclosed per `standards/taxonomy.md`'s "when the fit is imperfect, say so" rule rather than silently deviating), `status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`, one `relationships` entry (`part-of` → `architecture-containers-redis`, confirmed merged) and no others (no `layers/data/redis/*` sibling — including the presence-tracking node from #1095 — exists on `origin/launchpad` yet, so no edge to it, per `AGENTS.md` step 9's merge-target rule). done when: front matter parses as valid YAML and every field above is present with the stated value.

STEP 3  [needs 2] Write the body, adapted from `templates/datastore.md`'s seven required sections to this narrower feature-scoped subject: purpose/scope (typing indicators specifically, not presence), technology & attachment profile (Redis `PUBLISH`/`SUBSCRIBE`, no dedicated key), "schema" as **no owned data at rest** (transport only — the central, DoD-required classification), the migration/versioning section stating none applies (no schema to version), the access-pattern summary (who publishes: `buzz-relay`'s `handle_ephemeral_event`, `buzz-acp`'s `RelayHandle::build_typing_event`; who subscribes: desktop's `useChannelTyping`), operational characteristics (no TTL/retention at the Redis layer — all liveness is client-managed: 3s throttle, 8s client TTL), tenancy/security boundary (community-scoped channel key, `MessagesWrite` scope, write-fencing check), failure behavior (fire-and-forget `PUBLISH`; a missed message is not retried or backfilled — ephemeral events are never stored), and scope-and-omissions naming the presence sibling and the orphaned doc-comment as out-of-scope/unresolved gaps. done when: every issue DoD bullet (authoritative/derived/cache/transport; owned data, key access patterns, lifecycle/retention, consistency semantics; tenancy/security boundaries and failure behavior; links instead of copied DDL) has a corresponding section, and every substantive claim has an evidence-ledger entry classified FACT/INFERENCE/TEAM_KNOWLEDGE.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the worktree root; fix and re-run until exit 0. done when: the command exits 0.

STEP 5  [needs 4] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, confirm OK, then commit the plan + document in a separate call. No push, no PR — this task ends at commit; a later batch-bundling step opens the PR. done when: the suite reports OK and `git log` on this branch shows exactly one new commit ahead of `origin/launchpad` containing both files.

PARALLEL: none — single file, single task, no dependency on sibling `layers/data/redis/*` tasks in this batch beyond the shared, already-merged `architecture-containers-redis` target.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The bare `unittest discover` command must report OK before commit — never piped through `tail`/`head`, since a piped exit code belongs to the pipe. `review-adjudicate` and any cross-model review pass are deferred to the batch owner's later bundling/review step, not run here.

BUDGET: small — one document, no runtime code changes, evidence gathering scoped to roughly a dozen already-identified files across `buzz-core`, `buzz-relay`, `buzz-pubsub`, `buzz-acp`, `desktop/src/features/messages`, and one E2E test file.

OPEN: Whether `type: layers` (this batch's established precedent for `layers/data/...` documents) or `type: architecture` (what `templates/datastore.md`'s own worked example directs a real instance to use) is the better long-term fit is not re-litigated here — the override is disclosed in the evidence ledger and the node's own scope-and-omissions section, per `standards/taxonomy.md`'s guidance for an imperfect or contested fit, and left for a later corpus-wide pass to resolve consistently across all `layers/data/...` siblings rather than decided unilaterally by one task. Whether the orphaned `crates/buzz-pubsub/src/lib.rs` doc-comment reflects a planned-but-never-built typing-tracking module, or a stale leftover from a refactor, was not established by reading git blame/history in this task — named as a gap in the document's own scope-and-omissions section rather than resolved.

LEFT OUT: No relationship edge to a presence-tracking sibling document (#1095) — unmerged on `origin/launchpad`, per the batch's own precedent note and `AGENTS.md`'s merge-target rule. No restatement of presence's own Redis key design (`SET ... EX 180`) beyond the one-sentence contrast needed to justify typing indicators' "transport, not cache" classification. No attempt to fix or clarify the orphaned doc-comment in `buzz-pubsub/src/lib.rs`, and no attempt to resolve the `type: layers` vs `type: architecture` question — both are named as gaps, not fixed, since fixing either is implementation/taxonomy work a separate task owns.
