# Plan: issue #1278 — corpus node for platforms/relay/nip-05

## ALREADY TRUE

- Issue #1278 (parent Feature #614) asks for exactly one hand-authored corpus
  node at `launchpad/docs/corpus/platforms/relay/nip-05.md`, documenting the
  relay's NIP-05 identifier-verification HTTP endpoint
  (`GET /.well-known/nostr.json`).
- `launchpad/docs/corpus/platforms/` does not exist yet on `origin/launchpad`
  (confirmed via `git ls-tree -r --name-only origin/launchpad --
  launchpad/docs/corpus`); no `platforms/**` node has merged, so no sibling
  `platforms` node is a valid `relationships` target yet.
- No `platforms`-specific template exists in `launchpad/docs/corpus/templates/`.
  Per the batch's settled convention (this Feature's earlier tasks), a
  `platforms/**` node uses front matter `type: platforms` and borrows
  `templates/component.md`'s section shape (Responsibility / Public interface
  / Dependencies / Boundary / Relationships / Scope and omissions) as an
  INFERENCE, since no platforms-specific template has landed.
- The actual handler lives at `crates/buzz-relay/src/api/nip05.rs`
  (`nostr_nip05`, `canonicalize_nip05`, `relay_url_for_tenant_host`,
  `extract_domain`), routed at `crates/buzz-relay/src/router.rs:66`. The read
  side queries `crates/buzz-db/src/store/user.rs::get_user_by_nip05`
  (community-scoped, backed by `migrations/0001_initial_schema.sql`'s
  `idx_users_nip05` unique index). The write side (registering a handle) is
  `crates/buzz-relay/src/handlers/side_effects.rs::handle_kind0_profile`,
  which validates via `canonicalize_nip05` against the row-zero-bound tenant
  host, not the process-global `relay_url`.
- Row-zero host binding (`crates/buzz-relay/src/tenant.rs::bind_community`) is
  already documented at the principle level by
  `architecture-principles-host-selects-community` (merged on
  `origin/launchpad`); this node references it rather than re-explaining it,
  per the corpus's own duplication rule.
- Multi-tenant conformance coverage exists at
  `crates/buzz-test-client/tests/conformance_multitenant.rs` (module
  `users_profiles_nip05`, test `same_nip05_local_part_on_two_hosts_is_independent`,
  `#[ignore]`-gated live-host test) plus inline unit tests in `nip05.rs` itself.

## STEP 1 — Confirm scope and gather evidence

Read `nip05.rs`, `router.rs`, `tenant.rs`, `user.rs` (buzz-db), the
`0001_initial_schema.sql` migration, `side_effects.rs::handle_kind0_profile`,
and `conformance_multitenant.rs`'s `users_profiles_nip05` module. Done when
every claim the node will make traces to an opened file/line. (Done — see
citations gathered above.)

## STEP 2 — Draft `launchpad/docs/corpus/platforms/relay/nip-05.md`

Front matter: `id: platforms-relay-nip-05`, `type: platforms`, `status: draft`,
`origin: launchpad`, `audiences: [agent, developer, reviewer]`, evidence
ledger with the recorded revision plus one entry per substantive claim
(FACT/INFERENCE/TEAM_KNOWLEDGE classified honestly). Body follows
`component.md`'s shape: Responsibility, Public interface (the HTTP endpoint +
canonicalize/lookup helpers), Dependencies (buzz-db, buzz-core tenant types,
migration), Boundary (not the whole relay HTTP surface, not row-zero binding
itself — that's the principle node's job, not profile-write validation logic
beyond what registers the handle), Relationships (`references` the
host-selects-community principle node), Scope and omissions. Done when every
DoD bullet in #1278 has a corresponding body section or evidence entry.

## STEP 3 — Run corpus unit tests, then commit

`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests
-p "test_*.py"` must print `OK` as the sole content of its own Bash call, then
`git add` + `git commit -s` as a separate call. Done when the commit exists.

## STEP 4 — Verify zero new validate.py FAILs

Temporarily move the new file aside, run `validate.py`, record the FAIL set,
restore the file, re-run, diff the two FAIL sets — must be identical (the new
node contributes zero new FAILs; whatever it may add is new, not a change to
the existing baseline count, or it's a genuine schema violation to fix).

## GATES

- `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` → `OK`.
- `python3 launchpad/project-intelligence/corpus/validate.py` contributes no
  new FAIL beyond the pre-existing baseline.
- Every evidence citation points to a real file/line actually opened during
  Step 1.

## OPEN

- Whether a future `platforms.md` template (once authored) will ask for a
  different section shape than `component.md`'s borrowed one — out of this
  task's control; flagged in the node's own Scope and omissions.
- Whether `relationships: references` toward
  `architecture-principles-host-selects-community` is the right relationship
  *type* (vs. `depends-on`) is a judgment call: `references` is chosen because
  this node's claims do not require that principle node to stay unchanged for
  correctness, it just avoids re-explaining shared ground.

## LEFT OUT

- Any change to `nip05.rs`, `router.rs`, or any other product code — this is
  a documentation-only task.
- Broad documentation of the relay's entire HTTP bridge surface (`/events`,
  `/query`, `/count`, NIP-11, media, git) — out of scope per the issue's own
  "Explains only component-level behavior, not the entire containing
  platform" DoD bullet.
- A `platforms.md` template — not this task's deliverable; #1278 is a
  document task, not a template-authoring task.
