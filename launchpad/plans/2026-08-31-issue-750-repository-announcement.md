# Issue #750 — capabilities/git/repository-announcement.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and `launchpad/docs/corpus/templates/capability.md` are merged on `origin/launchpad`; no `launchpad/docs/corpus/capabilities/` directory exists yet anywhere in the merged corpus tree, so `capabilities/git/repository-announcement.md` does not exist. `KIND_GIT_REPO_ANNOUNCEMENT = 30617` is defined in `crates/buzz-core/src/kind.rs`. `handle_git_repo_announcement_inner` (`crates/buzz-relay/src/handlers/side_effects.rs`) implements the handler; `crates/buzz-sdk/src/builders.rs::build_repo_announcement`/`build_repo_announcement_with_tags` build the event; `crates/buzz-db/src/store/git_repo.rs` owns the Postgres name registry; `crates/buzz-relay/src/api/git/binding.rs` resolves the `buzz-channel` ACL tag carried on the announcement.

STEP 1 [done, this session] Gather evidence: `kind.rs` (kind constant + parameterized-replaceable range assertions), `side_effects.rs` (`handle_git_repo_announcement`/`_inner`, `validate_repo_id`, the reserve/seed/ensure-pointer/emit-30618 sequence, the deletion-fence `serving_write` lease), `buzz-sdk/src/builders.rs` (`build_repo_announcement`, `check_repo_id`, `GitRepoCoord`), `buzz-db/src/store/git_repo.rs` (`ReserveOutcome`, `reserve_repo_name`, `repo_name_owner`, `count_repos_for_owner`), `buzz-relay/src/api/git/binding.rs` (`buzz-channel` tag as the git ACL, first-tag/fail-closed semantics), `buzz-relay/src/api/git/manifest_event.rs` (derived kind:30618 on first announce), `buzz-relay/src/config.rs` (`git_max_repos_per_pubkey`, default 100), and `crates/buzz-test-client/tests/e2e_git.rs` (e2e coverage announcing via a raw `Kind::from(30617)` event bound to a test channel). Checked `VISION_PROJECTS.md:256` ("Git hosting (smart HTTP + NIP-34) | Ships today") for maturity evidence and confirmed no `capabilities/` corpus node exists yet to duplicate, and confirmed `architecture-flows-git-push`, `architecture-containers-relay`, `architecture-containers-object-storage`, `architecture-containers-postgres` are merged ids available as `references` targets. ← RUNS HERE

STEP 2 [needs 1] Write front matter (schema-valid: id `capabilities-git-repository-announcement`, type `capabilities`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator]`, `references` toward `architecture-containers-relay`, `architecture-containers-postgres`, `architecture-containers-object-storage` and `architecture-flows-git-push` since all four are merged and each realizes a piece of this capability) and the body per `templates/capability.md`'s required sections: Capability statement (announcing/creating a git repo as a signed kind:30617 event), Maturity (cite `VISION_PROJECTS.md:256`, the handler, and the SDK builder), Boundary (not architecture/relay internals, not the interface/CLI surface, not the step-by-step push flow — that's `architecture-flows-git-push` — not operations), Relationships, and Scope and omissions (name the `buzz-channel` ACL binding, quota, and idempotent re-announce behavior as covered; leave repo browsing/smart-HTTP/patch/signing as siblings' territory per the batch's own issue split).

STEP 3 [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from repo root; fix and re-run until exit 0, confirming zero *new* FAIL entries beyond the 21 pre-existing ones tracked in #1951.

STEP 4 [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the commit-gate stamp; confirm `OK`. Then, in a separate call, `git add` the new document + this plan file and `git commit -s`. Stop there — no push, no PR (batch integration phase handles that).

PARALLEL: none — single file, single task, no code changes.

GATES: `validate.py` must exit 0 with no new FAIL entries. The unittest suite must print `OK` before committing. `review-code`/`review-adjudicate`/cross-model final review are deferred to the batch owner's integration-phase review, per this task's own process note — this plan's STEP 5 (self-review) substitutes for them here.

BUDGET: small — one document, no code changes; evidence gathering scoped to ~7 already-read source files plus one e2e test file and one VISION doc line.

OPEN: `templates/capability.md` allows `implements` toward `corpus-template-capability` itself as optional (only useful once the generated `implemented-by` edge matters) — omitted here since the node's own section structure already signals which template it followed, matching the template's own stated rationale. No `part-of` relationship: `capabilities/git/` has no broader parent capability node merged yet (its siblings — git-hosting #745, repository #752 — are unmerged sibling tasks in this same batch, not valid targets against `origin/launchpad`).

LEFT OUT: No `references` to an interface node (`#1342`'s template) for the CLI/HTTP surface that triggers a repo announcement — no interface-type corpus node is merged yet for this subject. No description of the pre-receive hook script, smart-HTTP transport, or object-storage manifest format — those are `architecture-flows-git-push`, `#753` (smart-http) and `#746` (git-object-storage) territory respectively, referenced rather than restated. No relationship to a `patch`/`repository-browser`/`repository` capability node — none merged yet.
