# Issue #1219 — operations/reliability/redis-failure.md

Stated size: issue #1219 carries no explicit Size line -> cap: 5 steps (the batch dispatch brief's own cap: "Max 5 steps; this is one document").

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/reference.md`, and `launchpad/docs/corpus/architecture/containers/redis.md` are merged on `origin/launchpad` (confirmed against `<SCRATCH>/existing-node-ids.txt`); `launchpad/docs/corpus/operations/reliability/redis-failure.md` does not exist yet (confirmed with `ls`, exit 2).

STEP 1  [independent] Gather evidence: open every Redis-dependent call site named in the task — `crates/buzz-pubsub/src/{lib,error,presence,publisher,subscriber,rate_limiter,cache_invalidation,conn_control}.rs`, the fail-open/closed handling in `crates/buzz-relay/src/admission.rs` and its three callers (`connection.rs`, `api/gifs.rs`, `api/bridge.rs`), the readiness probe in `crates/buzz-relay/src/router.rs`, Redis-pool construction in `crates/buzz-relay/src/main.rs`, the mesh readiness registry (`crates/buzz-relay-mesh/src/{registry,runtime,membership}.rs`, `crates/buzz-relay/src/mesh_boot.rs`), the community-namespace purge in `crates/buzz-deletion/src/lib.rs`, the Helm chart's Redis/multi-replica hard-fail (`deploy/charts/buzz/templates/_validate.tpl`, `values.yaml`, `README.md`), and `docker-compose.yml`'s single-node Redis. Confirm `buzz-search` has no Redis dependency (`crates/buzz-search/Cargo.toml`). Record `git rev-parse HEAD`. done when: every file above has been opened and read, and the revision is recorded. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `operations-reliability-redis-failure`, type `operations`, status `draft`, origin `launchpad`, audiences `[operator, developer, reviewer]`, one `references` relationship to `architecture-containers-redis` — merged on `origin/launchpad`, describes the same subsystem's normal-path shape) and the body per the reference template's required sections: reference description, structured entries (one row per Redis-dependent capability naming what breaks, fails open/closed, or degrades), an explicit boundary paragraph (not the operator runbook, which is sibling issue #1226; not the architecture description of normal-path Redis usage), relationships, and scope-and-omissions (what this node does not cover and who owns it, separately from what was expected but could not be verified). done when: the file exists at `launchpad/docs/corpus/operations/reliability/redis-failure.md` with schema-legal front matter and every required template section present.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. done when: the command exits 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own Bash call to earn the verification stamp, confirm `OK`, then commit the plan + document in a separate `git commit -s` call. Do not push; do not open a PR. done when: the test suite reports `OK` and `git log -1` shows a signed-off commit containing the plan and the document.

PARALLEL: none — single file, single task, no dependency on sibling batch agents.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK` before the commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration pass — not run here.

BUDGET: small-to-medium — one document, no code changes; evidence gathering spans roughly a dozen source files across four crates plus the Helm chart and docker-compose, all read-only.

OPEN: Whether the node should also name `crates/buzz-relay-mesh`'s "fenced Redis session directory" for huddle-audio session-generation ownership (mentioned only in `registry.rs`'s own module doc comment, distinct from the mesh readiness registry this task covers) is left to the body's scope-and-omissions section rather than investigated further — it is a separate capability (huddle audio session ownership) outside this task's named list (`buzz-pubsub` fan-out, presence, typing, rate limiting, mesh readiness registry, `buzz-deletion` purge).

LEFT OUT: The operator response runbook (procedures, escalation, on-call steps) for a live Redis outage — that is sibling issue #1226 (`redis-unavailable`), explicitly out of scope per the task brief. No relationship to that runbook is declared (it is not merged on `origin/launchpad`). No code changes to any fail-open/fail-closed behavior found during evidence-gathering, even if a gap is noticed (e.g. an unused `check_ip_connection` path) — reported as an observation in the node's body, not fixed here.
