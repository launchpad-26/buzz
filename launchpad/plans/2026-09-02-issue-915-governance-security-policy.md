# Issue #915 — governance/security-policy.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/policy.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/governance/` does not exist yet — this task creates both the directory and the file. `architecture-principles-community-is-security-boundary` (status `draft`) is a merged, valid relationship target on `origin/launchpad`.

STEP 1  Gather evidence: read `SECURITY.md` (root — vulnerability disclosure, supported versions, design principles, dependency management), `launchpad/SECURITY-POSTURE.md` (cohort posture, accepted risks, open ADRs), `launchpad/AGENTS.md` §8 (public-repo secret rule), `.gitleaks.toml` + `launchpad/decisions/ADR-0006` (secret-scanning engine/allowlist), `launchpad/decisions/ADR-0008` (audit privilege), `.github/workflows/launchpad-security-audit.yml` + `launchpad/scripts/security_audit_registry.py` (what's actually registered/run), `.github/workflows/codex-security-review.yml` (per-PR AI security review — `block/buzz` upstream trigger, not launchpad-scoped), and `crates/buzz-auth` (name only, to state the governance/architecture boundary without duplicating it). Independently verify, not restate: branch protection (`gh api repos/launchpad-26/buzz/branches/launchpad` → `protected: true`; `/protection` 404s; token is `admin:false, maintain:true`), CODEOWNERS invalidity (`gh api repos/launchpad-26/buzz/codeowners/errors` → Unknown owner), absence of a DCO Check (a recent merged PR's `statusCheckRollup` has no such check), and that the `Security` CI job (`ci.yml`) runs `cargo-deny check` against `deny.toml`'s `[advisories]` block — not `cargo audit` as `SECURITY.md` claims. ← RUNS HERE

STEP 2  [needs 1] Write front matter (id `governance-security-policy`, type `governance`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, one evidence entry per substantive claim honestly classified FACT/INFERENCE/TEAM_KNOWLEDGE, `relationships: [{type: references, target: architecture-principles-community-is-security-boundary}]`) and the body per `templates/policy.md`'s six required sections (Scope and authority, MUST, SHOULD, Enforcement, Exceptions and escalation, Scope and omissions), H1 `# Policy: <subject>`. State explicitly that this node covers governance/process (disclosure, scanning, review) and not `crates/buzz-auth`'s technical security model or the corpus's own architecture `*-security-boundary` nodes.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) bare and unpiped as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call. Stop at the commit — no push, no PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The unittest discover command above must exit 0 before committing. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later review — not run here.

BUDGET: small — one document, no code changes; evidence gathering scoped to ~10 already-identified files plus a handful of `gh api` calls to verify platform facts directly rather than trust prior claims.

OPEN: Whether `SECURITY.md`'s "we use cargo audit in CI" line is a stale/inaccurate description of the actual `cargo-deny` advisories check, or whether both once ran and one was removed, is not established here — reported as a documentation/reality mismatch in the node's evidence, not resolved. Whether `.github/workflows/codex-security-review.yml` (gated `github.repository == 'block/buzz'`) ever fires on this fork's own PRs at all is reported as a fact (it does not, by its own `if:` condition) rather than assumed from the workflow's presence in the tree.

LEFT OUT: No new relationship to a corpus node about `deny.toml`/dependency policy or to `ADR-0011`'s deployment smoke-test floor — both are operations/deployment-scoped, not governance/process-scoped, and no merged corpus node covers them yet. No attempt to fix `SECURITY.md`'s stale `cargo audit` claim, the invalid CODEOWNERS entry (#1428's), or file the missing-DCO-Check gap (#2044 already covers it) — those are separate tasks already tracked. No push, no PR, no branch beyond this task's own.
