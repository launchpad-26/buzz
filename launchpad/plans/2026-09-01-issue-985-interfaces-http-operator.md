# Issue #985 — interfaces/http/operator.md

Stated size: none given in issue #985 — capped per task instruction at a small single-document task  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/interfaces/http/operator.md` does not exist (confirmed with `test -f`). No `interfaces/` directory exists yet under `launchpad/docs/corpus/`; the only prior art is `launchpad/docs/corpus/templates/interface.md` (a template, not an instance) and `launchpad/docs/corpus/schema/node.schema.json`, whose `type` enum has no dedicated HTTP value — the only interface-shaped member is `interfaces-events`, which the template's own evidence ledger confirms an instance node carries. `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` shows no interface-shaped node merged, so `relationships` has nothing legitimate to target. `crates/buzz-relay/src/api/operator.rs` (1314 lines) implements the deployment-root `/operator/communities*` HTTP surface (list/provision/archive/unarchive/transfer/availability-check), registered at `crates/buzz-relay/src/router.rs:86-105`, authenticated via NIP-98 signing gated by the `RELAY_OPERATOR_PUBKEYS` allowlist (`crates/buzz-relay/src/config.rs:742-791`). That same allowlist is also one path into the separate `/api/admin/v1/*` dashboard's principal resolution — `crates/buzz-relay/src/config.rs:775-790`'s own comment states "RELAY_OPERATOR_PUBKEYS is the shared allowlist for BOTH the community-provisioning endpoints and the NIP-98 admin console" — so the two surfaces share one allowlist but are registered as distinct route trees with distinct required config (provisioning additionally requires `RELAY_OPERATOR_API_ORIGIN`; the admin console does not). `crates/buzz-admin/src/main.rs` is a separate CLI that connects directly to Postgres/Redis and never calls this HTTP surface.

STEP 1  Re-confirm, at this worktree's current `HEAD`, every source this plan already cites: `crates/buzz-relay/src/api/operator.rs` (routes, request/response shapes, status codes, idempotency doc comments, `authorize_operator_request`/`check_operator_replay`, the `happy_path_create_returns_created_and_bootstraps_owner` and `non_allowlisted_operator_key_gets_403` tests), `crates/buzz-relay/src/handlers/community_provisioning.rs` (request/response structs, validation, idempotency doc comment), `crates/buzz-relay/src/router.rs` (route registration), `crates/buzz-relay/src/config.rs` (`RELAY_OPERATOR_PUBKEYS`/`RELAY_OPERATOR_API_ORIGIN`/`AdminAuth`/`AdminConfig`), `crates/buzz-relay/src/api/mod.rs` (`api_error`/`internal_error` envelope), and `crates/buzz-relay/src/api/bridge.rs#verify_bridge_auth_with_options` (the NIP-98 verification path). Re-run the OpenAPI/AsyncAPI grep to confirm no such document exists for this surface. Record `git rev-parse HEAD`. done when: every cited path/symbol above has been opened in this session and the exact line ranges to cite are noted. [independent] ← RUNS HERE

STEP 2  [needs 1] Write `launchpad/docs/corpus/interfaces/http/operator.md`: schema-valid front matter (`id: interfaces-http-operator`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [operator, developer, agent, reviewer]`, no `relationships` — no merged corpus node is a legitimate target yet) and a body covering the interface description, the six operations (list/provision/archive/unarchive/transfer/availability-check), contract and stability (NIP-98 auth, replay protection, idempotency, status-code/error-envelope behavior), an explicit boundary paragraph distinguishing this node from the unmerged `interfaces-http-admin` (#977 — distinct route tree, distinct auth config, shared-allowlist caveat named explicitly) and from a single Nostr event kind's own wire contract, one valid example (a provision-community round trip) and one failure example (403 for a non-allowlisted signer), and a scope-and-omissions section. done when: the file exists and every Definition-of-done bullet from issue #985's body is addressed by a named section in it. [needs 1]

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repository root; fix any FAIL line it reports (UNVERIFIED notices are acceptable) and re-run until it exits 0. done when: the command's own exit status is 0. [needs 2]

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call. done when: the command prints `OK` and exits 0. [needs 3]

STEP 5  [needs 4] Stage and commit exactly this plan file plus the new corpus document with `git commit -s -m "docs(corpus): document HTTP operator interface (#985)"`. done when: `git log -1` shows the new commit carrying a `Signed-off-by` trailer for exactly these two paths, or, if the commit is rejected for a missing gate stamp, that rejection is recorded as a finding rather than bypassed with `--no-verify`. [needs 4]

PARALLEL: none — five sequential steps producing one document and one commit.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 (step 3). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit is attempted (step 4) — this is the commit gate a pre-commit/pre-push hook checks for; a rejection here is reported as a finding, not routed around.

BUDGET: small — one new Markdown file (roughly 150-250 lines), no code changes, evidence gathering scoped to the ~6 `buzz-relay` files already identified above.

OPEN: whether issue #977 (`interfaces-http-admin`, unmerged as of this run) documents the `/api/admin/v1/*` dashboard as a fully separate interface node or takes some other shape is #977's call, not this task's — this node prose-links it by issue number and filename rather than by `relationships`, since it does not yet resolve as a corpus node id. Whether the corpus later wants a `references` edge between the two once both merge is left for whichever node lands second to add.

LEFT OUT: no `relationships` entries (no interface-shaped node is merged on `origin/launchpad` yet to target). No documentation of `/api/admin/v1/*`'s own routes, `AdminAuth` principal-resolution detail, or the `relay_operators` DB table beyond the one sentence establishing the shared-allowlist boundary — that is #977's document to write. No documentation of `buzz-admin` (the CLI crate): confirmed via `crates/buzz-admin/src/main.rs` to be a separate, direct-to-Postgres/Redis tool that never calls this HTTP surface, so it is out of scope for an HTTP interface node. No change to `crates/buzz-relay` code, tests, or configuration.
