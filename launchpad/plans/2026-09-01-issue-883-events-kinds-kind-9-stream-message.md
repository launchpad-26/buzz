# Issue #883 — events/kinds/kind-9-stream-message.md

Stated size: single corpus document, per the task dispatch ("this is a small single-document task").  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/event-kind.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/events/` does not exist yet on `origin/launchpad` (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` shows no `events/` prefix), so `launchpad/docs/corpus/events/kinds/kind-9-stream-message.md` does not exist and no id starting `events-` is loaded. `crates/buzz-core/src/kind.rs` defines `KIND_STREAM_MESSAGE: u32 = 9` (line 479) and it is actively read/written throughout `crates/buzz-relay/src/handlers/ingest.rs`, `event.rs`, `moderation_notices.rs`, and `crates/buzz-relay/src/workflow_sink.rs` — it is not a retired/legacy kind, and coexists with kind 40002 (`KIND_STREAM_MESSAGE_V2`) as a distinct, still-live kind rather than a superseded one.

STEP 1  [independent] Gather evidence directly from the repo (already substantially done in this session): `kind.rs` doc comments and range (lines 473-493, 700-766, 768-786) for identity/range/classification; `ingest.rs` for required scope (`required_scope_for_kind`, lines 437-484), the `h`-tag requirement (`requires_h_channel_scope`, line 704; enforcement at line 2460-2464), NIP-10 threading (`resolve_nip10_thread_meta`, line 813; `thread_meta` gating at line 2987), and link-preview tag validation (`validate_link_preview_tags`, line 314, invoked at line 2968); `AUTHOR_ONLY_KINDS`/`P_GATED_KINDS`/`SHARED_GATED_KINDS`/`RESULT_GATED_KINDS` (lines 129-215) to confirm kind 9's absence from all four (world-readable, channel-scoped only); `schema/schema.sql` lines 203-227 for the `search_tsv` generated column's NULL-kind exclusion list (kind 9 not excluded → FTS-searchable); `crates/buzz-db/src/store/thread.rs` (`insert_thread_metadata`, reply_count/descendant_count increments) and `crates/buzz-relay/src/handlers/side_effects.rs` (`emit_live_thread_summary`, kind:39005 overlay) for the reply-count/fanout side effect; `crates/buzz-relay/src/handlers/moderation_notices.rs` lines 152-183 for the relay itself authoring kind:9 (moderation DM notices); `crates/buzz-relay/src/workflow_sink.rs` for the workflow engine authoring kind:9; `crates/buzz-relay/src/handlers/event.rs` lines 563-599 for the generic `AuditAction::EventCreated` audit entry (kind-agnostic, so kind 9 is captured); `AGENTS.md` (repo root) "Event kinds"/"Channel scoping"/"Thread counters" sections for the house description already cited by the template. Record the repository revision (`git rev-parse HEAD` = `650354eab8d41ab6ce1a71de079a6c6d95c69052`), and note kind 40002 (`events-kinds-kind-40002-stream-message-v2`, issue #877) is on an unmerged sibling branch — do not cite it as a corpus relationship target; mention it in prose only. ← RUNS HERE
done when: every symbol/line cited above has actually been opened and read in this session (it has), and the recorded HEAD sha is captured for the provenance citation.

STEP 2  [needs 1] Write the front matter (`id: events-kinds-kind-9-stream-message`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships` — no merged sibling event-kind node exists yet to point at, and `corpus-template-event-kind` itself is `type: governance`, not an instance this node would `implements`-link without re-checking that call) and the body, following `templates/event-kind.md`'s nine required sections: (1) title/kind identity (`KIND_STREAM_MESSAGE = 9`, `interfaces-events`, regular per NIP-01's 4≤n<45 range), (2) referenced NIP (NIP-29 group chat message kind, no `docs/nips/NIP-*.md` proposal exists for kind 9 itself), (3) kind range/classification cross-check (`is_replaceable`/`is_parameterized_replaceable`/`is_ephemeral` all false for 9 → plain regular, matching NIP-01's range), (4) tag shape (`h` required per `requires_h_channel_scope`; NIP-10 `e`/optional threading tags; optional `link-preview` snapshot tags per `validate_link_preview_tags`; optional `p`/`imeta`), (5) content semantics (plaintext, with the documented `"!shutdown"` agent-shutdown convention), (6) access control/storage (world-readable within channel membership, `MessagesWrite` scope, FTS-searchable, not in any of the four gated sets, channel-scoped storage with NIP-10 thread metadata and reply_count/descendant_count materialization, generic `AuditAction::EventCreated` audit entry), (7) worked example JSON event, (8) versioning/supersession (name kind 40002 by filename only, no relationship edge, explaining why), (9) relationships section explicitly stating none declared and why. Classify every claim FACT (cite the exact file/line/symbol opened), state the recorded-revision commit citation in the evidence ledger, and add a scope-and-omissions section naming what this node does not cover (kind 40002's own contract, the moderation-notice and workflow-sink producer flows as their own features, `buzz-cli` consumer commands) and anything expected-but-unverified (e.g., whether `buzz-audit` entries are ever queried per-kind by an operator tool).
done when: `launchpad/docs/corpus/events/kinds/kind-9-stream-message.md` exists, contains all nine required sections from `templates/event-kind.md`, and every FACT/INFERENCE entry names a real file/line/symbol opened in STEP 1.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root. Compare its error set against the 21 known pre-existing failures (architecture-containers-postgres, architecture-context-human-user, architecture-flows-event-ingestion, architecture-flows-workflow-execution, architecture-principles-community-is-security-boundary, and the `corpus-template-*` files — issue #1951). Fix anything new and re-run until the new node introduces zero additional FAIL lines.
done when: the validator run's FAIL-line set is exactly the 21 pre-existing ones, with no new FAIL attributable to the new node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the commit gate; confirm `OK`. Then, in a separate call, `git add` the two files and `git commit -s` with message `docs(corpus): document kind 9 stream message event (#883)`.
done when: the unittest run prints `OK` and `git log -1` shows a new signed-off commit containing exactly the two intended files.

PARALLEL: none — single file, single task, no dependency on any other in-flight corpus branch (kind 40002's node on `task/877-...` is deliberately not read as a relationship target).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must introduce zero new FAIL lines beyond the 21 pre-existing ones. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit. `review-adjudicate` and cross-model final review are deferred to the batch owner's review pass — not run here.

BUDGET: small — one document, no runtime code changes, evidence gathering scoped to `kind.rs`, `ingest.rs`, `event.rs`, `moderation_notices.rs`, `workflow_sink.rs`, `thread.rs`, `side_effects.rs`, and `schema.sql`, all already read in this session.

OPEN: No merged sibling event-kind node exists on `origin/launchpad` at this revision, so this node declares zero `relationships` — consistent with `templates/event-kind.md`'s own guidance and the corpus's general rule that only nodes present on the merge target are legitimate targets. Whether every Buzz kind is expected to get its own `docs/nips/NIP-XX.md` proposal is unsettled corpus-wide (per the template's own scope-and-omissions gap) — kind 9 is documented here as governed by the external NIP-29 spec directly, with no internal proposal document, which is stated as the actual situation rather than a gap to fix.

LEFT OUT: No second corpus document. No claim about kind 40002's own wire contract (that is issue #877's node, unmerged). No change to `kind.rs`, `ingest.rs`, or any other runtime code. No `buzz-cli` interface-node content (that boundary belongs to the interface template, per `templates/event-kind.md`'s own "Boundary against interface" section) — CLI commands that read/post kind 9 are named as consumers in section 6, not documented as their own operation-surface node here.
