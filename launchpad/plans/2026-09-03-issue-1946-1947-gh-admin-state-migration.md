Issues #1946 + #1947 — gh-admin `state.py`: connection leak + missing additive-column migration path
Stated size: none (no `Size` line on either issue)  ->  cap: 8 steps
(Both are found-in-review bugs against the same ~380-line file, each independently scoped to one method — `__init__`'s except clause, and `_migrate()`. Judgment call made rather than blocking on the missing `Size` line: treated as a single combined "30-60 minute" class job. Recorded here per plan-issue's "no Size line → ask" rule, since Auto Mode biases toward proceeding on a reasonable call rather than stopping.)

ALREADY TRUE (verified against git + source, not notes)
  - `launchpad/skills/gh-admin/scripts/state.py` exists, HEAD is aef93f2c2a (origin/launchpad), working tree clean.
  - `State.__init__` (lines 178-192) opens `self.db = sqlite3.connect(...)`, then calls `self._migrate()` and `self._assert_fingerprint()` inside one `try`; the `except (sqlite3.Error, OSError)` clause (189-192) re-raises as `StatePersistenceError` without calling `self.db.close()`. Confirmed by reading the file in full: `self.db.close()` appears exactly once in the file, at line 228 inside the public `close()` method — never in `__init__`'s except path.
  - `_migrate()` (lines 240-332) contains only `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS` statements. `grep -n "_add_column_if_missing\|ALTER TABLE"` against the whole `gh-admin` skill directory returns zero hits outside this plan.
  - `launchpad/skills/review-queue-automation/scripts/common.py`'s `State._migrate()` (lines 201-376) calls `self._add_column_if_missing("jobs", "snapshot_hash", "TEXT")` after its `executescript`, and `_add_column_if_missing` (lines 378-384) is: read `PRAGMA table_info(table)`, if `column` not in the existing name set, `ALTER TABLE {table} ADD COLUMN {column} {decl}`, return whether it added.
  - `launchpad/skills/gh-admin/tests/test_state.py` exists, fixture-free (`fresh_state()` = `State(tempfile.mkdtemp())`), run directly with `python3 test_state.py`, a manual `for name, fn in sorted(globals().items())` runner, no pytest. It already has `test_named_baseline_first_writer_wins_is_caught_not_silent` (lines 59-98), which builds a collider DB with review-queue-automation's `leases` shape at the exact `gh-admin-state.sqlite3` path and asserts `State(str(tmp_dir))` raises `StatePersistenceError` containing `"don't match this suite's expected shape"`. This test currently passes today (the shape-mismatch path already raises correctly) but leaks the connection opened before the raise — it does not itself assert anything about the leak.
  - No additive-column test exists in `test_state.py` today (`test_migrate_creates_every_expected_table` only checks table names via `EXPECTED_TABLES`, not an upgrade-from-old-schema scenario).
  - `EXPECTED_COLUMNS`/`EXPECTED_TABLES` (lines 120-157) is the per-table column-set fingerprint `_assert_fingerprint()` checks after migration — any new column added via an additive migration must also be added here, or `_assert_fingerprint()` will reject the freshly-migrated (correct) database as a shape mismatch.

STEP 1  Fix #1947: close the leaked connection before re-raising in `State.__init__`     [independent]
        Wrap `self._migrate()` / `self._assert_fingerprint()`'s failure path so `self.db.close()` runs before `StatePersistenceError` propagates. `sqlite3.connect()` itself succeeding is what makes `self.db` non-None going into the `except` — connect() failing is a different, already-correctly-handled case (self.db was never assigned, nothing to close). Use `except (sqlite3.Error, OSError) as exc: self.db.close(); raise StatePersistenceError(...) from exc` (close before constructing/raising the wrapped error), not a bare `finally` (a `finally` would also fire on the success path and close a connection callers still need).
        done when: `self.db.close()` appears in `__init__`'s except clause, and `git diff` shows no change to `_migrate`, `_assert_fingerprint`, or any other method.

STEP 2  Port `_add_column_if_missing` from `common.py` into `gh-admin/scripts/state.py`     [independent]
        Add a method identical in behavior to `common.py`'s `_add_column_if_missing(self, table: str, column: str, decl: str) -> bool` (read `PRAGMA table_info`, no-op if present, else `ALTER TABLE ... ADD COLUMN`, return whether added). Place it directly after `_migrate()` (mirroring `common.py`'s ordering: `_migrate` then `_add_column_if_missing` immediately below it) with a short comment matching `common.py`'s: "`CREATE TABLE IF NOT EXISTS` above cannot add a column to an existing table, so each new column is applied idempotently here." Do not add any actual `_add_column_if_missing(...)` *call* in `_migrate()` yet — there is no real new column needed today (issue #1946 is "no path exists," not "a specific column is missing"); adding a no-op call for a column that doesn't need to exist would be dead code with no product justification. This mirrors the issue's own framing: the fix is that the *path* exists for the next sibling task, not that a schema change ships now.
        done when: `_add_column_if_missing` exists in `state.py` with the same signature and behavior as `common.py`'s, and `_migrate()`'s body is otherwise unchanged (still zero `ALTER TABLE` calls fired at runtime, since no caller invokes the new method yet — that's correct: nothing needs a new column today).

STEP 3  Add a regression test for the additive-column migration path     [needs 2]  ← RUNS HERE
        In `test_state.py`, add a test that: opens a `State` against a fresh tmpdir (creating the on-disk file with the current schema), closes it, then directly `ALTER TABLE`-drops-equivalent — actually: since SQLite can't drop columns pre-3.35 easily, instead simulate "old schema" by opening a *raw* `sqlite3.connect` against a fresh tmp path, creating one real table (e.g. `sessions`) via the *current* `CREATE TABLE IF NOT EXISTS` DDL but deliberately omitting one column that a hypothetical next column would add, then call `State(tmp_dir)` and assert the missing column is absent before migration and present after — using `_add_column_if_missing` directly (not via `_migrate()`, since no call site exists yet per Step 2's scope) to prove the helper works idempotently: call it once (returns `True`, column now present), call it again (returns `False`, no error, still present). This exercises the exact "on-disk file already has the old schema" scenario the issue names, without inventing a fake product column inside `_migrate()`.
        done when: `python3 launchpad/skills/gh-admin/tests/test_state.py` (run from the `gh-admin` skill's own interpreter/venv per its existing convention) shows the new test passing, and the test fails if `_add_column_if_missing` is reverted to a stub that always returns `False` without altering the table (prove it's not vacuous by temporarily breaking the implementation and re-running once, then restoring it).

STEP 4  Confirm the existing leak-path test still passes, and add an explicit no-leak assertion     [needs 1]
        Run `test_named_baseline_first_writer_wins_is_caught_not_silent` unchanged first to confirm Step 1 didn't alter its `StatePersistenceError` message or trigger condition. Then decide, per the issue's own instruction ("make sure your fix keeps it passing, or update it if the fix legitimately changes its assertions, but don't weaken what it verifies"): since the connection is now closed before the exception propagates, strengthen the test to additionally assert the leak is gone — e.g. after catching `StatePersistenceError`, attempt to open the same `db_path` again with a fresh `sqlite3.connect` and a short `busy_timeout`, or (simpler, no new flakiness risk) inspect that no `-wal`/`-shm` file is left locked by checking the connection object is unusable / attempting `PRAGMA quick_check` via a new connection succeeds without a `database is locked` error. Pick whichever assertion is deterministic and does not depend on OS-specific lock timing; if none is reliably deterministic, leave the existing test's assertions untouched (still valid, still passing) and note in the plan's LEFT OUT that a direct "no leaked file descriptor" assertion was not added because it cannot be made deterministic cross-platform.
        done when: the existing test's original assertion (`"don't match this suite's expected shape" in str(exc)`) still holds unmodified or is a strict superset of the original, and the full test file run shows this test passing.

STEP 5  Run the full gh-admin test suite     [needs 1, 3, 4]
        done when: `python3 launchpad/skills/gh-admin/tests/test_state.py` reports `N passed, 0 failed` for the full file (all pre-existing tests plus the new one from Step 3), pasted verbatim as command + raw output.

STEP 6  Commit, push, and open the PR     [needs 5]
        `git commit -s` with a message describing both fixes. Push the branch. `gh pr create` as a standalone command (no `cd` prefix — this repo's pr-gate hook rejects that shape), base `launchpad`, body filled from `launchpad/AGENT_PR_TEMPLATE.md`, "Closes #1946" and "Closes #1947" each on their own line under Related issue, `by:agent` label if recent agent-authored merged PRs use it (check first).
        done when: PR is open against `launchpad`, body contains both closing keywords verified by `gh pr view <n> --json closingIssuesReferences` showing both #1946 and #1947, and CI/hooks have started (pre-push hooks passed locally, since push succeeded).

PARALLEL  Steps 1 and 2 are independent (different methods, no overlapping lines) and could run as parallel subagents. Steps 3 and 4 both depend on their respective upstream step and both edit `test_state.py` — sequential, not parallel, since they touch the same file (Step 4 first since it's smaller and lower-risk, or accept a merge step). Step 5 needs all of 1/3/4. Step 6 needs 5. Given the small size of this job, sequential execution by one agent is simpler than coordinating parallel dispatch and file-merge for two steps in the same test file.

GATES     `review-code` after Step 5 (both fixes touch runtime state-persistence logic). `review-tests` after Step 5 (new/modified test assertions). `qa` explore mode does not apply — no CLI/API/UI runtime surface changed, this is an internal library fix exercised entirely by the existing unittest-style script; explicitly out of scope for this issue.

BUDGET    Step 4 is most likely to eat the budget — deciding whether a deterministic no-leak assertion exists (WAL file lock probing across platforms) without introducing flakiness is a judgment call, not a mechanical port like Steps 1-3.

OPEN      Neither issue states a `Size` line; capped at 8 steps by treating this as one combined 30-60-minute job (both are single-method, well-evidenced fixes in the same file). Whether Step 4's leak-detection assertion is worth adding at all, versus leaving the existing test's assertion as sufficient regression coverage (the issue's own "why now" section calls the leak "low likelihood of causing real damage in a short-lived CLI process") — the plan defers this to the builder's judgment at Step 4 rather than deciding here.

LEFT OUT  No `_add_column_if_missing(...)` call is added inside `_migrate()` for a real column — issue #1946 asks for the *path* to exist, not for a schema change; inventing a placeholder column purely to exercise the call site would be scope creep the issue itself doesn't ask for and would need its own justification in `EXPECTED_COLUMNS`. No changes to `common.py` itself (issue's root file is `state.py` only). No change to the Windows `msvcrt` locking path — unrelated to either issue.
