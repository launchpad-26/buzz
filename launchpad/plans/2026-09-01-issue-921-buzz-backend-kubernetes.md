# Issue #921 — implementation/crates/buzz-backend-kubernetes.md

Stated size: none given -- the corpus document task template has no Size field  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/implementation-reference.md` and `launchpad/docs/corpus/architecture/deployment/kubernetes.md` and `launchpad/docs/corpus/architecture/containers/agent-runtime.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/implementation/crates/buzz-backend-kubernetes.md` does not exist yet; `crates/buzz-backend-kubernetes` is a 14-module, ~6,400-line Rust binary crate with 155 `#[test]` functions across its `src/` and one wire-fixture integration test file, already read in full (all 14 `src/*.rs` files plus `Cargo.toml`, `tests/wire_fixtures.rs`, `tests/fixtures/provider-wire/README.md`).

STEP 1  [independent] Gather remaining evidence: cross-check `docs/remote-agents.md`'s "The Kubernetes Binding" (§991-1410), "[L3] Kubernetes binding conformance" (§1493), "Known Defects" (§1581) and "Implementation Correspondence" (§1687) sections against current code and CI (`Justfile:352` unit-test job comment, `Dockerfile.sprig` presence, `.github/workflows/sprig-image.yml`/`sprig.yml`, `release.yml`/`*-canary.yml` `-p buzz-backend-kubernetes` build lines, `ci.yml:1209` sidecar stub) to confirm or refute whether the spec's Implementation Correspondence table's two "*to be added*" rows for this crate and `Dockerfile.sprig` are stale.
        done when: each of those six citation targets has been opened directly (not assumed) and the staleness claim is confirmed or refuted with a specific line/row cited either way. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `implementation-crates-buzz-backend-kubernetes`, type `implementation`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, `relationships: references` toward `architecture-deployment-kubernetes` and `architecture-containers-agent-runtime` -- both merged nodes that already discuss this crate from their own side; no `implements` edge, since `docs/remote-agents.md` carries no corpus node id yet, named in prose instead per `AGENTS.md` step 9 and the `buzz-admin.md` precedent) and the body per the template's required sections: realization statement, target, implementation surface table, divergences (the stale Implementation Correspondence rows; any other drift found in STEP 1), verification (155 fake-substrate/pure-function unit tests + golden wire fixtures against the built binary; no envtest/kind conformance suite exists in this repo), relationships, scope and omissions.
        done when: the file exists at the target path with all seven required template sections present and every substantive claim carries an `evidence` entry classified FACT, INFERENCE or TEAM_KNOWLEDGE.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Confirm the pre-existing baseline of unrelated failures on `origin/launchpad` is unrelated to this node (diff or stash-compare if the exit code is nonzero).
        done when: the validator exits 0 and, if any pre-existing failures remain, each is confirmed by name to predate this change and not implicate the new file.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the verification stamp; confirm `OK`. Then, in a separate tool call, `git add` the two new files and `git commit -s`. Do not push or open a PR -- this is one document in a 37-document batch integrated later.
        done when: the unittest run reports `OK` with no other command in the same call, and a signed-off commit exists on this branch containing exactly the plan file and the corpus document.

PARALLEL: none -- single file, single task, no other batch sibling touches this path.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 for this node (pre-existing unrelated failures on `origin/launchpad` are not this task's to fix). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before committing. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration review -- not run here.

BUDGET: small-to-medium -- one document, no code changes; evidence-gathering already completed against 14 source files, 1 test file, 1 fixtures README, and the ~1,780-line spec document; STEP 1 only needs to re-confirm CI/Dockerfile staleness claims already spotted, not re-read the crate.

OPEN: `docs/remote-agents.md`'s own "Implementation Correspondence" table (written when the spec merged at commit `28ae6cd21`, before this crate existed) still marks the Kubernetes binding and `Dockerfile.sprig` rows "*to be added*" -- both now exist in the tree at significant size, so that table is stale relative to current code. This node reports that as a divergence; it does not fix the spec document, which is out of this task's scope. Whether `docs/remote-agents.md` itself should become a corpus node (making a future `implements` edge from this node possible) is unresolved and not this task's to decide.

LEFT OUT: No `implements` relationship (target has no corpus node id). No attempt to update `docs/remote-agents.md`'s stale correspondence table or fix any of its eight "Known Defects" (desktop/harness-side, outside this crate's own files). No new automated test or CI change. No claim about live cluster behavior -- this is a static-source reading, consistent with the crate's own conformance section naming an envtest/kind suite as future work that does not exist in this repository today.
