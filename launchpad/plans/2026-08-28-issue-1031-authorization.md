# Issue #1031 — layers/authorization/authorization.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/layers/authorization/authorization.md` does not exist yet, and no `launchpad/docs/corpus/layers/` directory exists at all. Sibling document tasks #1032 (channel-membership), #1033 (channel-roles), #1034 (community-membership) and #1035 (event-authorization) are open with no corresponding files. `architecture-principles-community-is-security-boundary` is already merged.

STEP 1  Gather evidence: read the issue and its DoD; confirm the target file and sibling files do not exist (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`); read `launchpad/docs/corpus/AGENTS.md`, `node.schema.json`, and one existing content node (`architecture/principles/community-is-security-boundary.md`) as a structural precedent, since no `layers`-type template exists. Trace the real authorization code: `buzz-auth` (`nip42.rs`, `nip98.rs`, `scope.rs`, `access.rs`, `error.rs`), `buzz-db` (`relay_members.rs`, `channel.rs`, `migrations/0001_initial_schema.sql`'s `relay_members`/`channel_members` tables), `buzz-core::channel::MemberRole`, `buzz-core::kind` (`is_moderation_command_kind`, `is_shared_gated_kind`), and `buzz-relay::handlers::{ingest, moderation_authz}`. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `layers-authorization-authorization`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, one `references` relationship to `architecture-principles-community-is-security-boundary` — confirmed present on `origin/launchpad`) and the body: a one-sentence definition, a boundary against authentication and against the already-documented community boundary, the five-layer composed shape (scope → community binding → community membership/role → channel membership/role → action-specific authorization), an enforcement-points table, and an honest verification section naming only the unit-level tests actually found (`buzz-auth::access` tests, `moderation_authz.rs` tests) with no end-to-end coverage claimed. Defer all four sibling layers' full content to their expected ids/issue numbers in prose only — no `relationships` edge to any of them, since none exists as a loaded node yet.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task; four sibling tasks (#1032–#1035) are being authored in isolated worktrees by other agents and are explicitly not to be duplicated here.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK`. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here; this PR is opened as a draft for that reason.

BUDGET: small-to-medium — one document, no code changes, evidence gathering scoped to buzz-auth, buzz-db, buzz-core::channel/kind, and two buzz-relay handler modules (roughly a dozen files read).

OPEN: Whether the five-layer decomposition stated here (scope → community binding → community role → channel membership/role → action-specific authorization) is the vocabulary the four sibling documents will themselves converge on is not settled by this task — each sibling author decides their own node's shape independently, and this node may need a light edit once all four land to keep the shared vocabulary consistent. This node's own `relationships` array is empty of edges to any sibling by design (none exists yet); the batch owner's review is the place to add them once all four merge.

LEFT OUT: No relationships to the four sibling layer nodes (none merged yet — each would be a hard validation error today). No attempt to write any of the four siblings' own content — channel-membership, channel-roles, community-membership and event-authorization specifics are explicitly out of scope per the issue body and per this corpus's one-idea-per-node rule. No claim about authentication internals (NIP-42/NIP-98 mechanics) beyond naming that they exist and run first — that is a different subsystem's subject. No end-to-end/integration verification was run or claimed; only the unit-test suites that were actually found and read are cited.
