# Issue #944 — implementation/crates/git-sign-nostr.md

Stated size: issue carries no explicit Size label (type:task, area:docs, by:agent); dispatch brief caps this batch task at 5 steps -> cap: 5 steps.

ALREADY TRUE: `launchpad/docs/corpus/templates/implementation-reference.md`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/schema/node.schema.json` are merged on `origin/launchpad`. `crates/git-sign-nostr/{Cargo.toml,README.md,src/lib.rs,src/main.rs}` and `docs/nips/NIP-GS.md` exist and were read in full. `git ls-tree -r --name-only HEAD -- launchpad/docs/corpus` (HEAD = `76a0a4ebbe4bc4d852b0d04362ed768620da34b3`) shows no node for `docs/nips/NIP-GS.md` and no `implementation-crates-git-sign-nostr` node yet — no `implements` edge is legal today. `launchpad/docs/corpus/architecture/flows/git-push.md` (merged) already carries a FACT evidence entry naming `git-sign-nostr`/NIP-GS as orthogonal to push-transport auth, citing `crates/git-sign-nostr/README.md` and `docs/nips/NIP-GS.md`. `launchpad/docs/corpus/implementation/crates/git-sign-nostr.md` does not exist yet.

STEP 1  [independent] Write the node's front matter and evidence ledger: `id: implementation-crates-git-sign-nostr`, `type: implementation`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`. One evidence entry per substantive body claim (FACT, each citing the actual file/line already read in this session: `Cargo.toml`, `README.md`, `src/lib.rs` symbols `parse_args`/`do_sign`/`do_verify`/`compute_signing_hash`/`build_envelope`/`load_key`/`load_auth_tag`/`run`, `src/main.rs`, `docs/nips/NIP-GS.md`'s Specification/Owner Attestation/Security Considerations sections, and the `#[cfg(test)] mod tests` block's representative tests e.g. `test_signing_hash_matches_spec`, `test_sign_verify_round_trip`, `test_verify_rejects_non_canonical_json`), plus one commit-citation FACT recording HEAD `76a0a4ebbe4bc4d852b0d04362ed768620da34b3`. No `relationships` array — no `implements` edge is legal per ALREADY TRUE. ← RUNS HERE
    done when: front matter parses as valid YAML with all evidence entries classified and cited to a real path already opened this session.

STEP 2  [needs 1] Write the body against the template's required sections (Realization statement / Target / Implementation surface / Divergences / Verification / Relationships / Scope and omissions): state the crate's responsibility (a standalone Unix `gpg.x509.program` signing/verification backend implementing NIP-GS, invoked per commit/tag via git's pluggable signing interface) and what it deliberately does not own (NIP-98 HTTP push-transport auth, credential-helper duties — both `git-credential-nostr`'s job, cited in `architecture-flows-git-push` and `crates/git-credential-nostr/README.md`; and the object-store/CAS layer). Name the Target as `docs/nips/NIP-GS.md` by path (no corpus node id exists for it). Build the Implementation surface table from `parse_args`/`do_sign`/`do_verify`/`compute_signing_hash`/`build_envelope`/`load_key`/`load_auth_tag`/status-line emission mapped to NIP-GS's Signature Format / Signing Hash / Key Loading / Owner Attestation / CLI Interface sections. State Divergences honestly (or that none were found and what was checked). State Verification as the `#[cfg(test)] mod tests` unit-test suite (spec test vectors + round-trip + rejection cases), noting no live/e2e test exists for this crate. Satisfy every DoD bullet from the issue body, not just schema fields.
    done when: the file exists at `launchpad/docs/corpus/implementation/crates/git-sign-nostr.md` with every template-required section present and every DoD checklist bullet from issue #944 addressed in the body or evidence ledger.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL entries for this node and re-run until it exits 0 (the pre-existing ~21 unrelated baseline failures on `origin/launchpad` are not this task's to fix — confirm via `git stash`/diff if the exit code is nonzero and failures are unclear).
    done when: `validate.py` exits 0, or exits 1 with only the pre-existing baseline failures and none naming this node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the commit gate; confirm `OK`.
    done when: the suite reports `OK` with no failures or errors.

STEP 5  [needs 4] In a separate tool call, `git add launchpad/docs/corpus/implementation/crates/git-sign-nostr.md launchpad/plans/2026-09-01-issue-944-git-sign-nostr.md` and `git commit -s -m "docs(corpus): add git-sign-nostr implementation reference (#944)"`. Do not push, do not open a PR — this batch integrates into one Feature-level PR later.
    done when: `git log -1` shows the new commit with both files staged, signed off, on branch `task/944-git-sign-nostr`.

PARALLEL: none — single file, single task, steps are strictly sequential (evidence ledger before body, body before validation, validation before the test-gate command, test-gate command isolated in its own call before commit per the harness's stamp requirement).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 for this node. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` as the sole command in its tool call, immediately before the commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's integration-phase review — not run here, per this task's own instructions.

BUDGET: small — one document (~150-250 lines), no code changes, evidence gathering already complete (4 crate source files + NIP-GS.md + git-credential-nostr/README.md + the merged git-push.md flow node, all read this session).

OPEN: The issue's DoD checklist item "Links relevant implementation, verification, specification/decision and neighboring corpus nodes" cannot be fully satisfied with a real `implements` edge today — `docs/nips/NIP-GS.md` has no corpus node id yet, confirmed by `git ls-tree` against `origin/launchpad`'s current corpus tree. Per the template's own instruction ("If the target is not itself a corpus node yet, say so; do not invent an `implements` edge to a node id that does not exist"), the Target section names the NIP by path instead. Whether `references: architecture-flows-git-push` is a legitimate `references` edge (that node discusses this crate tangentially in one evidence entry, not as its primary subject) is left to STEP 2 to decide against `relationships.schema.json`'s actual directionality for `references`, not decided here.

LEFT OUT: No `implements` edge to NIP-GS or to `architecture-flows-git-push` as a target-of-implementation — neither is legal or accurate. No attempt to promote `docs/nips/NIP-GS.md` to its own corpus node — that is a separate, unscoped task per the template's own "Scope and omissions" table. No claim about `git-credential-nostr`'s own implementation beyond the boundary statement (it is a sibling crate documented by a different issue in this batch). No coverage of the object-store/CAS publish path, which `architecture-flows-git-push` already owns. No attempt to execute the crate's test suite live in this session beyond what STEP 4's unittest command requires for the commit gate — the crate's own `cargo test -p git-sign-nostr` is cited as Verification from reading the test source, consistent with how `architecture-flows-git-push` treats its own `#[ignore]`-gated e2e tests.
