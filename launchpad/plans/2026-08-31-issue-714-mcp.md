# Plan: issue #714 — document capabilities/agents/mcp.md

## ALREADY TRUE

- `launchpad/docs/corpus/templates/capability.md` is merged on `origin/launchpad`
  (`type: capabilities`, required sections: Capability statement, Maturity,
  Boundary, Relationships, Scope and omissions).
- `launchpad/docs/corpus/capabilities/` does not exist on `origin/launchpad` or in
  this worktree — no sibling capability nodes (agent, agent-turn, agent-owner,
  managed-agent, backend-provider, acp) are merged yet, so this node declares no
  inbound/outbound `relationships` to them.
- Three architecture nodes relevant to MCP already exist on `origin/launchpad`:
  `architecture-containers-agent-runtime`, `architecture-flows-agent-turn`,
  `architecture-context-ai-agent` — valid `references` targets.
- Code confirmed by direct read: `crates/buzz-dev-mcp/src/lib.rs` (tool_router:
  `shell`, `read_file`, `view_image`, `str_replace`, `todo`, `_Stop`,
  `_PostCompact`), `crates/buzz-dev-mcp/src/shim.rs` (multicall shim: rg, tree,
  buzz, git-credential-nostr, git-sign-nostr), `crates/buzz-acp/src/acp.rs`
  (`McpServer` struct passed to ACP `session/new`), `crates/buzz-acp/src/lib.rs`
  (`build_mcp_servers`, env forwarding of `BUZZ_RELAY_URL`/`BUZZ_PRIVATE_KEY`/
  `BUZZ_AUTH_TAG`/`BUZZ_ACP_DISPLAY_NAME`), `crates/buzz-acp/README.md`
  (`BUZZ_ACP_MCP_COMMAND` config table, default empty).
- Maturity evidence: `VISION_PROJECTS.md:251` — "MCP server + ACP agent harness |
  Ships today".
- Deployment/operational framing: `launchpad/deploy/runbooks/dev-deployment-SOP.md`
  states `buzz-agent` has *no built-in tools whatsoever* without
  `BUZZ_ACP_MCP_COMMAND=buzz-dev-mcp` set.
- Repository revision for provenance: `131b02f989684117d9ab1dd426f1673fa638e523`.

## STEP 1 — Draft the node

Write `launchpad/docs/corpus/capabilities/agents/mcp.md` by hand (no
`scaffold.py` manifest row available; front matter authored directly against
`node.schema.json`, matching what `scaffold_node` would produce). Front matter:
`id: capabilities-agents-mcp`, `type: capabilities`, `status: draft`,
`origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`,
one evidence entry per substantive claim (provenance, tool inventory, transport,
credential/env forwarding, config surface, maturity, no-builtin-tools framing),
each classified FACT (opened source) with real `path:line`/`path:start-end`
citations — no `#symbol=`/`#line=` fragment syntax.

Body follows `capability.md`'s required sections: Capability statement,
Maturity (cited to VISION_PROJECTS.md:251 and the crate/config evidence),
Boundary (not architecture/component internals of buzz-acp's pooling, not the
interface a capability is exposed through beyond the MCP stdio transport
itself, not the step-by-step agent-turn flow — already owned by
`architecture-flows-agent-turn`), Relationships (`references` to the three
architecture nodes above), Scope and omissions (what's not covered + what
was expected but not verified, e.g. no live agent run exercised in this task).

Done when: file exists, front matter is schema-shaped by inspection, every
claim has a citation opened directly.

## STEP 2 — Validate

Run `python3 launchpad/project-intelligence/corpus/validate.py`. Fix any error
this new node introduces. Confirm the known 21 pre-existing FAIL baseline
(#1951) is unchanged — i.e. this node adds zero new FAIL entries.

Done when: exit code 0, or exit 1 with only the pre-existing 21 baseline
failures (none attributable to `capabilities/agents/mcp.md`).

## STEP 3 — Earn the commit gate

Run, as the sole command in its own tool call:
`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`

Done when: output ends in `OK`.

## STEP 4 — Commit

`git add` the new node file and this plan file; `git commit -s` with message
`docs(corpus): document capabilities/agents/mcp (#714)`. No push, no PR.

Done when: commit exists on `task/714-mcp`, `git status` clean.

## STEP 5 — Self-review

Re-read the diff against issue #714's DoD checklist line by line. Re-open
every cited source to confirm it says what the evidence entry claims. Confirm
no second canonical document was created and no new corpus files besides the
one node + the plan.

Done when: every DoD bullet is satisfied and directly traceable to a diff
line or evidence entry.

## GATES

- `validate.py` exit 0 (or only pre-existing #1951 baseline failures).
- `unittest discover` on corpus tests: `OK`.
- No second hand-authored canonical document.
- `git commit -s` succeeds (DCO trailer present).

## BUDGET

Single session, no parallel agents needed — one node, already evidenced.

## OPEN

- Whether a future `capabilities/agents/agent.md` (or similar) sibling node
  will want a `references` edge back to this one — left for that task, per
  AGENTS.md's rule to declare relationships only to nodes that exist on the
  merge-target branch today.

## LEFT OUT

- No live `buzz-agent` + `buzz-dev-mcp` run was exercised for this task — the
  claims rest on source code and existing docs/runbooks, not a fresh
  execution trace. Flagged explicitly in the node's own scope section.
- Push and PR creation — explicitly out of scope per the batch process note;
  a later integration phase handles that.
