# Issue #931 — implementation/crates/buzz-pairing-cli.md

Stated size: not specified by issue #931 -> cap: 5 steps.

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/implementation-reference.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/implementation/crates/buzz-pairing-cli.md` does not exist yet, and no `implementation/`-typed node exists in the corpus at all yet. `launchpad/docs/corpus/architecture/context/nostr-network.md` (id `architecture-context-nostr-network`) is merged and already cites `crates/buzz-pairing-cli/src/main.rs` as evidence for how this crate talks to `buzz-pair-relay`.

STEP 1 [independent]  Gather evidence: read `crates/buzz-pairing-cli/Cargo.toml`, `crates/buzz-pairing-cli/README.md`, and the crate's single source file `crates/buzz-pairing-cli/src/main.rs` in full. Read `crates/buzz-core/src/pairing/{mod.rs,session.rs,crypto.rs,qr.rs,types.rs}`'s public API to establish the ownership boundary (protocol state machine, crypto derivation, and QR encode/decode all live in `buzz-core`; `buzz-pairing-cli` owns only CLI args, raw `tokio-tungstenite` WebSocket transport, stdio prompts, and output formatting). Read `crates/buzz-core/src/pairing/NIP-AB.md`'s section headers to confirm it is the spec this crate exercises. Confirm zero test files exist for this crate (`find crates/buzz-pairing-cli -type f`) and cross-check against `launchpad/docs/audits/audit-2026-08-18-full-ecosystem.md`'s BL2 finding (nsec-in-argv) and its "buzz-admin and buzz-pairing-cli ... have zero tests at all" finding, plus `launchpad/docs/Observability/current-state/coverage.md`'s T06 row for the `buzz-pair` process. Record `git rev-parse HEAD`. ← RUNS HERE
  done when: every path above has been opened and read, `git rev-parse HEAD` output is recorded, and the ownership boundary (buzz-core protocol/crypto/QR vs. buzz-pairing-cli transport/CLI/stdio) is written down in working notes.

STEP 2 [needs 1]  Write front matter (schema-valid: id `implementation-crates-buzz-pairing-cli`, type `implementation`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, one `references` relationship targeting `architecture-context-nostr-network` since it already cites this crate's own source as supporting context for the same topology — no other merged node is a legitimate target) and the body using the implementation-reference template's required sections verbatim: Realization statement, Target (naming `crates/buzz-core/src/pairing/NIP-AB.md` by path since it has no corpus node id yet — no invented `implements` edge), Implementation surface (table: `main.rs`'s `source`/`target`/`test-vectors` subcommands and helper functions against what part of the protocol/spec each realizes), Divergences (the BL2 nsec-argv gap between the crate's own `Cargo.toml` clap `env` feature and the unwired `--nsec` flag; the README's `.scratch/e2e-pair-local.sh` reference to a script not present in this checkout), Verification (none automated — zero test files, confirmed by both direct inspection and the audit), Relationships, and Scope and omissions.
  done when: `launchpad/docs/corpus/implementation/crates/buzz-pairing-cli.md` exists with all seven required-section headings from the template and every substantive claim has a matching `evidence` entry classified FACT/INFERENCE/TEAM_KNOWLEDGE.

STEP 3 [needs 2]  Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. If nonzero, confirm via `git stash`/diff whether any failures pre-exist on `origin/launchpad` before treating them as caused by this change.
  done when: the validator exits 0, or every remaining FAIL is confirmed (via `git stash`/diff against `origin/launchpad`) to pre-exist and not name this node.

STEP 4 [needs 3]  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the commit-gate stamp, confirm `OK`, then in a separate call `git add` the two files and commit with `-s`. Do not push, do not open a PR — this batch integrates all 37 documents into one later PR.
  done when: the unittest run prints `OK` and a new commit exists on `task/931-buzz-pairing-cli` containing exactly the plan and the corpus document, with no push and no PR opened.

PARALLEL: none — single file, single task, single crate under investigation.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with zero FAIL entries attributable to this node. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration review — not run here.

BUDGET: small — one document, no code changes; evidence gathering scoped to one crate's ~620-line single source file, `buzz-core`'s pairing module's public surface, one spec doc's headers, and one existing audit finding.

OPEN: The issue's DoD asks for "typed relationships appropriate to the node." Only one merged corpus node — `architecture-context-nostr-network` — substantively discusses this crate, and it does so as supporting context rather than as a spec this crate implements, so `references` (not `implements` or `part-of`) is the correct type; this is confirmed by reading the node, not assumed from its title. `implements` toward the NIP-AB spec itself is deliberately not declared because the spec document has no corpus node id yet, per `AGENTS.md`'s explicit rule against inventing one.

LEFT OUT: No relationship to `architecture-containers-cli.md` (id `architecture-containers-cli`) — that node documents the unrelated `buzz-cli`/`buzz` binary, not `buzz-pairing-cli`/`buzz-pair`; the name similarity is a false lead, checked and rejected. No `implements` edge to a NIP-AB corpus node — none exists yet. No relationship toward `buzz-pair-relay`'s implementation-reference node — a sibling agent in this same batch run is authoring it in parallel on an unmerged branch (`task/930-buzz-pair-relay`), not visible here; the relationship is noted in this node's prose only, per the dispatch brief. No attempt to fix the BL2 argv-secret finding or add tests to the crate — that is separate implementation work owned by other issues, not this documentation task.
