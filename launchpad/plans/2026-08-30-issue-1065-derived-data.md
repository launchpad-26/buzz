# Issue #1065 — layers/data/derived-data.md

Stated size: issue #1065 carries no explicit Size line; the batch dispatch brief (corpus-batch-author, Feature #610) caps every single-document corpus task at 5 steps  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad` (HEAD `338b4d0cf` at worktree creation). `launchpad/docs/corpus/layers/` does not exist yet — this is the first node under the `layers` type. `launchpad/docs/corpus/templates/concept.md` is the matching template: the issue's own Objective line calls the target "the single canonical concept node for derived data," and `concept.md` is the template for exactly that documentation form (Definition, Use cases, Comparison, Scope and omissions). A batch of architecture-surface nodes (containers, context, deployment, flows, principles) is already merged, including `architecture-principles-relay-is-source-of-truth`, `architecture-flows-event-ingestion`, and `architecture-containers-postgres` — all three are legitimate `relationships` targets for a derived-data concept node.

STEP 1  [independent] Gather evidence: read `crates/buzz-db/src/thread.rs` in full (module doc, `ThreadSummary`/`ThreadMetadataRecord` structs, `insert_thread_metadata`/`increment_reply_count`/`decrement_reply_count`) for the application-maintained-counter form of derived data (`thread_metadata.reply_count`/`descendant_count`, transactionally incremented on reply insert). Read `migrations/0001_initial_schema.sql`'s `thread_metadata` and `events.search_tsv` DDL, and `crates/buzz-search/src/lib.rs`'s module doc, for the database-computed-generated-column form (`events.search_tsv TSVECTOR GENERATED ALWAYS AS (...) STORED`). Confirm the buzz/CLAUDE.md "Thread counters" gotcha (§ Thread counters) matches what the code shows. done when: both source forms are quoted with exact file:line citations captured for the evidence ledger. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `layers-data-derived-data`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, `relationships`: `references` → `architecture-principles-relay-is-source-of-truth` (derived data is downstream of, and must never contend with, the source-of-truth event log), `references` → `architecture-flows-event-ingestion` (where `thread_metadata` is populated), `references` → `architecture-containers-postgres` (where both derivation forms physically live)) and the body per `concept.md`'s required sections: Definition (one sentence, then scope/boundary — derived data is never itself the source of truth), Use cases, a Comparison table contrasting the two derivation mechanisms found in STEP 1 (app-maintained incremental counters vs. Postgres `GENERATED ALWAYS ... STORED` column), and Scope and omissions. done when: `launchpad/docs/corpus/layers/data/derived-data.md` exists with every DoD bullet from the issue body addressed.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. done when: the command's own exit code is 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, then in a separate call stage and commit the plan + document with `git commit -s`. Do not push and do not open a PR — this branch is bundled into a batch PR by a later orchestration step. done when: the unittest run reports OK and `git log -1` shows the new commit on `task/1065-derived-data`.

PARALLEL: none — single file, single task, one isolated worktree.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 before commit. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report OK before commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's morning review when this branch is bundled into a batch PR — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to `crates/buzz-db/src/thread.rs`, two migration files, and `crates/buzz-search/src/lib.rs`.

OPEN: Whether other derived-data instances exist elsewhere in the codebase (e.g. presence/typing state in `buzz-pubsub`, unread counts, audit hash-chain state) beyond the two forms evidenced in STEP 1 was not exhaustively searched — the two forms found (transactional incremental counters, and a Postgres generated column) are sufficient to define and scope the concept per AGENTS.md's "one independently maintainable idea," and are the two the issue's own parent PRD context (data/storage layer) most directly points at. A third example would only strengthen the Use cases section, not change the Definition.

LEFT OUT: No new relationships to any of the 41 sibling `layers/data/*` documents this issue's parent Feature #610 also requires — none of those exist in this isolated worktree yet (each is built in its own worktree per the batch's own design), so none are legitimate `relationships` targets today. No attempt to enumerate every column/table Buzz derives from primary events (a reference-shaped catalogue) — that belongs to a reference-typed node (template `reference.md`), not this concept node, per `concept.md`'s own boundary-against-reference section. No change to `thread.rs`, the migrations, or `buzz-search` — this is documentation only.
