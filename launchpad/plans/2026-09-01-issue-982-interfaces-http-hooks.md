# Issue #982 — interfaces/http/hooks.md

Stated size: none given in issue #982 — the dispatching task instructions cap this at 5 steps as a small single-document task  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/interfaces/http/hooks.md` does not exist (`test -f` confirms). `launchpad/docs/corpus/schema/node.schema.json`'s `type` enum has no `interfaces` or `http` value; the correct value for an interface-shaped node is the combined `interfaces-events` member, per `launchpad/docs/corpus/templates/interface.md`'s own "A note on `type`" section. No sibling `interfaces/**` node exists anywhere in the corpus tree yet (confirmed by `find launchpad/docs/corpus -name '*.md'`), so no `relationships` target exists to point at — none will be declared. `crates/buzz-relay/src/router.rs` registers `POST /hooks/{id}` to `api::bridge::workflow_webhook`, which is the sole handler for this route (confirmed by reading `router.rs` and `bridge.rs`). Only one corpus node namespace exists today (`architecture/`) plus governance/agent files at the root — this is the first node of a wholly new `interfaces/` subtree, which is fine per `AGENTS.md`'s "no relationships" precedent for a first-of-kind node.

STEP 1  [independent] Gather evidence: read `crates/buzz-relay/src/api/bridge.rs`'s `workflow_webhook` handler in full (request extraction, community/tenant binding via `crate::tenant::bind_community`, workflow lookup via `state.db.get_workflow`, trigger-type check against `buzz_workflow::TriggerDef::Webhook`, secret verification via `crate::webhook_secret::verify_secret`/`extract_secret`, body parsing into `buzz_workflow::executor::TriggerContext`, the SEC-006 owner-authority recheck via `check_owner_authority`, `create_workflow_run`, the `tokio::spawn` async execution via `buzz_workflow::executor::execute_from_step` + `finalize_run`, and the `202 Accepted` response shape). Read `crates/buzz-relay/src/webhook_secret.rs` in full (secret generation/injection/verification, the hash-ordering contract in its module doc comment, constant-time comparison). Read `crates/buzz-relay/src/router.rs`'s `build_router` for the exact route registration and its ordering relative to other HTTP routes (no NIP-98 auth layer on this route). Read `crates/buzz-workflow/src/executor.rs`'s `TriggerContext` struct (fields, `webhook_fields` map) and `crates/buzz-db/src/store/workflow.rs`'s `RunStatus` enum and `WorkflowRunFailure` struct for the run/failure shape. Read `crates/buzz-relay/src/handlers/command_executor.rs`'s `handle_workflow_def` (lines ~638-810) only far enough to state, as boundary/context, that the workflow itself (including its webhook secret's first appearance) is created via a `kind:30620` Nostr command event, not via this HTTP door — this node does not document that creation flow. Read `crates/buzz-test-client/tests/conformance_multitenant.rs`'s `workflows` module (around line 1645-1780+) for the cross-tenant fail-closed behavior it asserts (same generic 404 for unmapped host, wrong-community workflow id, and revoked owner). Record `git rev-parse HEAD` for the provenance citation.
        done when: all six named source spans have been opened and read in this session, and `git rev-parse HEAD` output has been recorded for use in the provenance evidence entry. ← RUNS HERE

STEP 2  [needs 1] Write the front matter (schema-valid against `node.schema.json`: `id: interfaces-http-hooks`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships` per ALREADY TRUE) and the body, following `launchpad/docs/corpus/templates/interface.md`'s required sections (Interface description, Operations, Contract and stability, Boundary, Relationships, Scope and omissions). Cover every Definition-of-done bullet from the issue: inputs (path param `id` = workflow UUID, `X-Webhook-Secret` header or `?secret=` query param, optional JSON body → `webhook_fields`), outputs (`202 Accepted` with `{run_id, workflow_id, status: "pending"}`; async execution, not synchronous result), error/rejection behavior (400 invalid UUID/bad JSON/non-webhook-trigger workflow, 401 missing-or-wrong secret, 404 for unmapped host / wrong-tenant workflow / disabled-or-inactive workflow / no-channel-scope / failed owner-authority recheck — all collapsed to the same generic 404 to prevent cross-tenant probing), authentication (webhook-secret bearer token, explicitly not NIP-98 — the one HTTP route in the router table with no user-identity auth), versioning/compatibility (the `_webhook_secret` field lives inside the versioned `definition_hash`-covered JSON; no separate interface version), ordering/idempotency (no idempotency key — each accepted POST unconditionally creates a new `workflow_run` row via `create_workflow_run`; concurrent/duplicate deliveries produce duplicate runs), and one valid example (a webhook POST with a matching secret, 202 response) plus one failure example (wrong/missing secret → 401, or unknown id → 404). Every FACT evidence entry cites the specific file/symbol/line opened in STEP 1; the workflow-creation boundary claim cites `command_executor.rs`'s `handle_workflow_def` narrowly, framed as a boundary exclusion, not restated as this node's own contract.
        done when: `launchpad/docs/corpus/interfaces/http/hooks.md` exists on disk, its front matter parses as valid YAML, and its body contains every required template section plus every Definition-of-done bullet listed above.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL it reports and re-run until it exits 0 (UNVERIFIED notices are acceptable).
        done when: the command's exit status is 0 and its output contains no line starting with `FAIL` attributable to the new node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the commit gate, confirm it prints `OK`, then in a separate call `git add` the new document plus this plan file and commit with `git commit -s -m "docs(corpus): document HTTP workflow hooks interface (#982)"`.
        done when: the unittest command's output ends with `OK`, and `git log -1 --format=%H` on the branch shows a new commit containing exactly the two intended files (`git show --stat HEAD` lists only `launchpad/docs/corpus/interfaces/http/hooks.md` and `launchpad/plans/2026-09-01-issue-982-interfaces-http-hooks.md`).

STEP 5  [needs 4] Self-review the diff against the issue's Definition-of-done checklist line by line; re-open every cited source to confirm it actually supports its claim; confirm no second hand-authored canonical corpus document was created; confirm `validate.py` still exits 0.
        done when: each Definition-of-done bullet has been checked off against the committed body with a specific section/sentence identified as satisfying it, and a re-run of `validate.py` after STEP 4's commit still exits 0.

PARALLEL: none — one document, one commit, strictly sequential (evidence → draft → validate → commit → self-review).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no FAIL lines attributable to this new node. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit is attempted. No PR is opened, pushed, or merged in this task — that is explicitly out of scope per the dispatching instructions.

BUDGET: small — one corpus document (~150-250 lines), evidence gathering scoped to roughly 6 source files already located (`bridge.rs`, `webhook_secret.rs`, `router.rs`, `executor.rs`, `store/workflow.rs`, `command_executor.rs`) plus one test file, no code changes, no new dependencies.

OPEN: Whether the corpus-wide convention for `interfaces/**` subtree layout (e.g. `interfaces/http/*.md` vs. a flatter scheme) is settled anywhere is not resolved by this task — the issue names the exact target path (`launchpad/docs/corpus/interfaces/http/hooks.md`), so this plan follows that path literally rather than deciding a subtree convention. Whether `handle_workflow_def`'s `d`-tag-vs-server-generated workflow-id story (the conformance test's comment claims the server generates it; `command_executor.rs`'s code reads it via `extract_d_tag`) is internally consistent is a real open question surfaced during evidence-gathering — this node does not resolve it because workflow *creation* is out of this node's scope by the Boundary section; it is named here so a future kind:30620 interface node inherits the question rather than it silently disappearing.

LEFT OUT: No documentation of workflow creation/definition (`kind:30620`, `handle_workflow_def`) as its own contract — that is a separate future node's territory, referenced only as a boundary exclusion. No documentation of `GET /workflows/{workflow_id}/runs` or `/runs/{run_id}/approvals` (separate NIP-98-authenticated read endpoints in `api/workflows.rs`) beyond a passing mention that they exist as the way to observe an async run's outcome — they are a different interface surface with their own auth model. No `relationships` edges — no interface-shaped or event-kind-shaped sibling node exists yet on `origin/launchpad` to target. No attempt to resolve the d-tag/server-generated-id inconsistency noted in OPEN — that is implementation or a future corpus node's problem, not this task's.
