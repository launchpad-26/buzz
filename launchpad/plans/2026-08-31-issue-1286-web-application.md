# Issue #1286 — platforms/web/application.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`, and its `type` enum already includes `platforms`. `launchpad/docs/corpus/architecture/containers/web.md` (id `architecture-containers-web`) is merged and already documents the `web/` container's responsibility, inbound/outbound interfaces, deployment, data and security implications. `launchpad/docs/corpus/platforms/web/application.md` does not exist yet, and no `platforms/` directory exists on `origin/launchpad` at all. Sibling tasks #1287 (authentication), #1288 (invite-ui), #1289 (relay-serving) and #1290 (repository-browser) are open but unmerged, so none is a valid `relationships` target yet.

STEP 1  Gather evidence: read every file under `web/` outside `node_modules`/`dist` (`web/src/main.tsx`, `web/src/app/{App,router,routes}.{ts,tsx}`, `web/src/app/routes/root.tsx`, `web/src/app/routes/index.tsx`, `web/index.html`, `web/package.json`, `web/vite.config.ts`, `web/biome.json`, `web/components.json`, `web/tsconfig.json`, `web/scripts/check-file-sizes.mjs`, `web/scripts/check-pubkey-truncation.mjs`, `web/playwright.config.ts`, `web/tests/e2e/smoke.spec.ts`) plus the repo-root `Justfile`'s `web`/`web-check`/`web-fix`/`web-typecheck`/`web-build`/`web-e2e-smoke` targets, to establish the app shell's structure, provider stack, routing mechanism, build/lint/test tooling and file-size gate — distinct from the container-level (deployment/serving/security) ground `architecture-containers-web` already covers and from the four sibling feature-level tasks. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `platforms-web-application`, type `platforms` — the sibling-settled convention for `platforms/**` nodes, borrowing `component.md`'s section shape since no platforms-specific template exists yet — status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, one `references` relationship toward `architecture-containers-web`, which is confirmed present on `origin/launchpad`) and the body: purpose/scope, responsibility (entry point, provider stack, root layout, route registration), public interface/boundary (`index.html` + `#root` mount contract, the `routes.ts` virtual route table other features register against), dependencies both directions (build/lint/test tooling manifests, feature modules that plug into routing), an explicit boundary statement naming auth/invite-ui/relay-serving/repository-browser and the already-covered container-level ground as out of scope, and scope-and-omissions.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Confirm zero new FAIL lines by temporarily stashing the new file and re-running.

STEP 4  [needs 3] Run the corpus unittest suite as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call. Stop there — no push, no PR (this batch task rolls up into one later integrated PR for Feature #614).

PARALLEL: none — single file, single task, worktree-isolated from the other #1287/#1288/#1289/#1290 sibling tasks.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no new FAIL entries beyond the ~21-23 pre-existing ones. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK`. `review-adjudicate` and cross-model final review are deferred to the batch owner once all sibling tasks are integrated into the single Feature #614 PR.

BUDGET: small — one document, no code changes, evidence scoped to `web/`'s non-feature app-shell files plus the repo-root `Justfile`'s web targets.

OPEN: Whether a `part-of` or `depends-on` edge should later point from this node toward the sibling `platforms/web/*` nodes (#1287-#1290) once they merge — deferred, since none exists yet and adding one now would be a hard validation error against `origin/launchpad`. Whether `ThemeToggle.tsx` (present in `web/src/shared/theme/` but not imported anywhere in `web/src`) is dead code or a pending feature is left unresolved and flagged as a gap rather than asserted either way.

LEFT OUT: No re-documentation of the `web` container's deployment, inbound/outbound interfaces, or security implications — `architecture-containers-web` already owns that ground and is referenced instead of duplicated. No auth flow detail (NIP-07/NIP-98 signing mechanics — #1287), no invite-page UI detail (#1288), no relay-side serving/gating detail (#1289), no repository-browser feature detail (git-client/IndexedDB/tree-blob rendering — #1290). No change to any `web/` source file.
