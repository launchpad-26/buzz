# Issue #1049 — layers/compute/sprig-runtime.md

Stated size: no explicit Size line in the issue body -> cap: 5 steps (per the corpus batch dispatch brief's default).

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and `launchpad/docs/corpus/templates/concept.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/architecture/containers/agent-runtime.md` (id `architecture-containers-agent-runtime`) is merged and already carries a summary-level FACT about sprig's packaging role, plus a table row describing it as "not a separate architectural actor, just a distribution unit." `launchpad/docs/corpus/layers/compute/sprig-runtime.md` does not exist yet on `origin/launchpad` or in this worktree. No `layers/` directory exists on `origin/launchpad` yet (sibling compute docs #1041-#1045 are open, unmerged drafts in this same batch run — not linkable).

STEP 1  [independent] Gather evidence: read `crates/sprig/Cargo.toml` and `crates/sprig/src/main.rs` in full (the multicall dispatch logic and independent-versioning rationale); `crates/buzz-dev-mcp/src/lib.rs` dispatch block for the five sub-personalities (`rg`, `tree`, `buzz`, `git-credential-nostr`, `git-sign-nostr`); `Dockerfile.sprig` and `scripts/sprig-entrypoint.sh` (image build, symlink installation, entrypoint `exec` requirement); `.github/workflows/sprig.yml` and `.github/workflows/sprig-image.yml` (release triggers, tagging); `Cargo.toml`'s `[profile.sprig]` (size-optimized release profile); `crates/buzz-backend-kubernetes/src/config.rs`, `src/image.rs`, `src/pod.rs` (how the Kubernetes provider consumes the published `ghcr.io/block/buzz-sprig` digest-pinned image as its compute payload); and `docs/remote-agents.md`'s `§Image` and `§Entrypoint and launch ABI` sections (already-written narrative on image contents, digest-pinning policy, and the ABI a conforming custom image must preserve). Record `git rev-parse HEAD`. Confirm via `git log --oneline -- crates/sprig` that no dispatch-logic change has landed beyond the three commits already found (origin commit #605, the `sprout`->`buzz` rename #958, and an unrelated release-lane fix #1173). ← RUNS HERE
done when: all listed files have been opened and read, `git rev-parse HEAD` and `git log --oneline -- crates/sprig` output are recorded for citation in STEP 2.

STEP 2  [needs 1] Write front matter: id `layers-compute-sprig-runtime`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, one `relationships` entry of type `part-of` targeting `architecture-containers-agent-runtime` (confirmed merged on `origin/launchpad`; sprig is the packaging/runtime detail underneath that container's existing summary-level sprig claim, so `part-of` is directionally correct — sprig-runtime is a constituent detail of that container, not a peer or a replacement). Classify every claim from STEP 1's sources as FACT (opened source), INFERENCE (reasoned, with `confidence`), or TEAM_KNOWLEDGE (attributed, with `provided_by`) — do not promote `docs/remote-agents.md`'s prose to FACT without checking it against the actual K8s provider code cited alongside it.
done when: front matter is written with one evidence entry per substantive claim planned for the body, correctly classified, and the single `part-of` relationship targets a confirmed-merged id.

STEP 3  [needs 2] Write the body using `templates/concept.md`'s required-section shape: definition (what sprig is — a multicall binary bundling `buzz-acp`/`buzz-agent`/`buzz-dev-mcp`, one sentence first), boundary/non-goals (sprig is packaging, not a fourth architectural actor — distinguish it from the container node's broader scope and from `buzz-backend-kubernetes`, which is not this node's subject), use cases (how it's deployed as compute — image build, personality symlinks, entrypoint ABI, Kubernetes consumption), an optional comparison table of its personalities, a `relationships`-based related-resources section, and the required scope-and-omissions section naming what is expected-but-not-verified (no `crates/sprig/tests/` exist; the unmerged `tomb/auth-tag-tool` branch's fourth personality is a signal the personality set isn't final, noted but not cited as current fact).
done when: the body contains a one-sentence definition, a boundary/non-goals statement, at least one use case, and a scope-and-omissions section naming the untested gaps found in STEP 1.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.
done when: the command exits 0.

STEP 5  [needs 4] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own tool call to earn the verification stamp, confirm `OK`, then commit the plan + document with `git commit -s` in a separate tool call. Do not push and do not open a PR.
done when: the unittest run reports `OK` and a signed-off commit exists on `task/1049-sprig-runtime` containing the plan and the document, with no push and no PR opened.

PARALLEL: none — single file, single task, no dependency on the five sibling compute-layer tasks (#1041-#1045) beyond deliberately not linking to their unmerged nodes.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK` before committing. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later review — not run here.

BUDGET: small — one document, no runtime code changes, evidence gathering scoped to ~10 files already located (sprig crate, buzz-dev-mcp dispatch, Dockerfile.sprig, two workflow files, one Cargo profile, three buzz-backend-kubernetes files, one narrative doc section).

OPEN: Whether `part-of` or `references` is the more precise relationship type to `architecture-containers-agent-runtime` is a judgment call this plan makes (favoring `part-of` because sprig is described in that node's own body as "a distribution unit," i.e. a constituent packaging detail, not merely related supporting context) — a reviewer may prefer `references` instead; either is schema-valid and the choice does not block validation.

LEFT OUT: No relationships to the five sibling compute-layer nodes (#1041-#1045, kubernetes-provider etc.) — confirmed not present on `origin/launchpad`'s corpus tree at STEP 1's recorded revision, so linking to them would be a hard validation error in CI even though they resolve in this local worktree. No new corpus node for the `buzz-backend-kubernetes` provider itself, or for the Docker image build pipeline in isolation — both are candidate follow-up subjects noted in the final report, not folded into this node. No fix to the missing `crates/sprig/tests/` directory — that is implementation work, not documentation work, and is named as a gap in the node's scope-and-omissions section instead.
