# Plan: issue #1279 — document platforms/relay/nip-11.md

## ALREADY TRUE

- `launchpad/docs/corpus/platforms/` does not exist yet on `origin/launchpad` —
  this is the first document under `platforms/**`. No `platforms.md` /
  `platforms-*` template exists in `launchpad/docs/corpus/templates/`; per
  `AGENTS.md`'s documented no-template path and the batch's settled convention
  (finding #4), this node is hand-authored against `node.schema.json` with
  `type: platforms`, borrowing `templates/component.md`'s section shape
  (Responsibility / Public interface / Dependencies / Boundary /
  Relationships / Scope and omissions), and the body says explicitly that no
  platforms-specific template existed at the recorded revision.
- `launchpad/docs/corpus/architecture/containers/relay.md` (`architecture-containers-relay`)
  already exists and already names `GET /` as "WebSocket upgrade, or NIP-11
  relay info for a plain HTTP request" in its routes table, and explicitly
  scopes out per-endpoint depth ("this node only names the endpoints that
  carry that protocol"). Per finding #5, this new node `references` that
  container node rather than re-describing the container, and narrows scope
  to what that node does not cover: the NIP-11 document's actual shape,
  content-negotiation behavior, per-community fields, and the
  advertised-vs-implemented NIP audit.
- The real handler lives at `crates/buzz-relay/src/nip11.rs` (`RelayInfo`,
  `SUPPORTED_NIPS`, `relay_info_handler`, `nip11_document`, `nip11_facts`,
  `relay_limitation`, `push_descriptor`, `workspace_icon_for_host`,
  `admin_api_advertisement`), wired into the router at
  `crates/buzz-relay/src/router.rs:64-65` (`GET /` content-negotiated,
  `GET /info` unconditional) and `router.rs:304-389`
  (`nip11_or_ws_handler`'s admin-host/accept-header branching).
- `docs/multi-tenant-conformance.md:43` carries an existing conformance table
  row titled "NIP-11 relay info and relay `self`" describing the same
  tenancy/fail-open contract the handler's own doc comments describe; this is
  real corroborating evidence, not a claim invented for this node.
- Verified independently (not assumed): `SUPPORTED_NIPS` in `nip11.rs:15` is
  `[1, 2, 10, 11, 16, 17, 23, 25, 29, 33, 38, 42, 50, 56]` and does **not**
  include 45, yet NIP-45 (COUNT) is genuinely implemented — a dedicated
  handler (`crates/buzz-relay/src/handlers/count.rs`), a `COUNT` protocol
  message (`protocol.rs:28,213`), a `POST /count` HTTP route, and NIP-45
  count support in `buzz-db` (`store/event.rs:658,1459`). This is the same
  gap sibling issue #1268's count-handler node already found. Every other NIP
  in the list (1, 2, 10, 11, 16, 17, 23, 25, 29, 33, 38, 42, 50, 56, plus the
  conditional 43) has a real, independently-checked implementation footprint.
- `test_nip11_relay_info` (`crates/buzz-test-client/tests/e2e_relay.rs:955-1000+`,
  `#[ignore]`-gated E2E) and 15 unit tests inside `nip11.rs`'s own `#[cfg(test)]
  mod tests` block already exercise this endpoint's behavior.

## STEP 1 — Investigate and record evidence

Read `nip11.rs` in full, `router.rs`'s wiring (lines 26, 64-65, 304-389),
`docs/multi-tenant-conformance.md`'s NIP-11 row, `ARCHITECTURE.md`'s two
route-table rows and e2e-test-coverage row, and the `req.rs` cross-reference
test for `max_limit`. Done — see evidence bundle above.

## STEP 2 — Verify the SUPPORTED_NIPS claim honestly

Grep every advertised NIP number (both `NIP-N` and zero-padded `NIP-0N` forms)
across `crates/` to confirm each has independent implementation evidence
outside `nip11.rs` itself, and specifically check NIP-45 (mentioned by
sibling #1268) since it is the one number wired into the HTTP/protocol layer
but absent from the constant. Done — result recorded above as a `FACT`, not
assumed from the sibling's report.

## STEP 3 — Draft `launchpad/docs/corpus/platforms/relay/nip-11.md`

Front matter: `id: platforms-relay-nip-11`, `type: platforms`,
`status: draft`, `origin: launchpad`, `audiences: [agent, developer,
operator, reviewer]`, one evidence entry per substantive claim (FACT for
everything read directly in source/tests/docs, INFERENCE for the one
reasoned claim about the discrepancy's likely cause), a `references`
relationship to `architecture-containers-relay` (confirmed present on
`origin/launchpad` — see `ALREADY TRUE` above). Body follows
`component.md`'s shape: purpose/scope paragraph, Responsibility, Public
interface (the `RelayInfo`/`RelayLimitation` field table plus the two route
handlers), Dependencies (config, `buzz-db`'s `DEFAULT_MAX_PAGE_LIMIT`,
`buzz-relay-mesh`/`tenant.rs`'s `bind_community`, admin API, GIF/push
descriptors), the SUPPORTED_NIPS honesty section (the NIP-45 gap), Boundary,
Relationships, Scope and omissions (including the no-template note).

## STEP 4 — Validate

Run the corpus unit tests, then `validate.py`, confirming the new file
introduces zero new FAIL lines versus a run with the file removed.

## STEP 5 — Commit

Two separate Bash calls per the harness's stamp-attribution rule: the bare
`python3 -m unittest discover ...` command alone, then `git add` + `git
commit -s`.

## GATES

- `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests
  -p "test_*.py"` must report `OK`.
- `python3 launchpad/project-intelligence/corpus/validate.py` must produce
  the same FAIL set as a clean `origin/launchpad` checkout with this file
  removed (pre-existing ~21-23 FAILs, unrelated).
- Every evidence citation opened and read, not guessed.
- No `relationships` target unless confirmed present on `origin/launchpad`.

## OPEN

- Whether a `platforms.md` (or `platforms-relay.md`) template lands later and
  reshapes this node's required sections — flagged in the node's own scope
  section per `AGENTS.md`'s documented no-template path, not resolved here.
- Whether other advertised-vs-implemented NIP mismatches exist beyond NIP-45
  — not exhaustively re-derivable from this node's narrow scope; named as a
  gap rather than silently assumed absent.

## LEFT OUT

- Auditing NIP-43's conditional-advertisement logic beyond what `nip11.rs`
  and its own tests already state — that is NIP-43/membership's own subject,
  not this endpoint-document node's.
- Re-describing the relay container's full inbound/outbound surface —
  already covered by `architecture-containers-relay`, referenced not
  duplicated.
- Auditing every one of buzz's ~60+ NIPs for advertisement accuracy — out of
  this task's scope; only the one gap actually found (NIP-45) is recorded.
