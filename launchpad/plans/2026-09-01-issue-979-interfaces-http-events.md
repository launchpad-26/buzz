# Issue #979 — interfaces/http/events.md

Stated size: not given as an explicit Size line in the issue body -> cap: 5 steps (per dispatch instructions: "this is a small single-document task").

ALREADY TRUE: `launchpad/docs/corpus/interfaces/` does not exist anywhere in the worktree (`find launchpad/docs/corpus -name "*.md"` lists `architecture/`, `schema/`, `standards/`, `templates/` subtrees only) — the target file `launchpad/docs/corpus/interfaces/http/events.md` is genuinely new, not an update. `launchpad/docs/corpus/schema/node.schema.json`'s `type` enum has no separate `interface` value; the only interface-shaped member is `interfaces-events`. `launchpad/docs/corpus/templates/interface.md` (id `corpus-template-interface`) already exists on `origin/launchpad` and states a node built from it "carries `type: interfaces-events`". Two sibling flow nodes already merged on `origin/launchpad` cover adjacent ground: `architecture-flows-http-event-submission` (id, `type: architecture`) documents the HTTP-transport specifics of `submit_event`, and `architecture-flows-event-ingestion` (id, `type: architecture`) documents the shared `ingest_event` pipeline both WS and HTTP feed into — both are legitimate `references` targets since they resolve on `origin/launchpad` today. `POST /events` is registered in `crates/buzz-relay/src/router.rs:72` as `api::bridge::submit_event`; the handler and its NIP-98 auth/admission/replay/parse/ingest helpers live in `crates/buzz-relay/src/api/bridge.rs`; the shared `ingest_event` function both this HTTP handler (via `submit_event_authed`) and the WebSocket `handle_event` (in `crates/buzz-relay/src/handlers/event.rs:761`) call lives in `crates/buzz-relay/src/handlers/ingest.rs:2100`. Current worktree HEAD is `650354eab8d41ab6ce1a71de079a6c6d95c69052`.

STEP 1  [independent] Re-confirm relationship-target candidates against the actual merge base before drafting: run `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` and confirm `architecture/flows/http-event-submission.md` and `architecture/flows/event-ingestion.md` are present with those exact ids, and confirm no `interfaces/` path exists there either. done when: both files appear in the `origin/launchpad` tree listing and no `interfaces/` path does. ← RUNS HERE

STEP 2  [needs 1] Write `launchpad/docs/corpus/interfaces/http/events.md` with front matter (`id: interfaces-http-events`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, a commit-citation evidence entry for the recorded revision, one evidence entry per substantive claim actually verified in this session — router registration, NIP-98/dev X-Pubkey auth, admission/replay checks, shared `ingest_event` pipeline, response shape `{event_id, accepted, message}`, error status codes 400/401/403/404/429/500/503, the 1 MiB body limit, and the replaceable-event dedup/LWW `was_inserted=false -> {accepted:true, message:"duplicate:"}` behavior — plus `relationships: [{type: references, target: architecture-flows-http-event-submission}, {type: references, target: architecture-flows-event-ingestion}]`) and a body following `templates/interface.md`'s required sections (Interface description, Operations, Contract and stability, Boundary, Relationships, Scope and omissions) with at least one valid-request example and one failure example drawn from actual test code (`crates/buzz-test-client/tests/e2e_nostr_interop.rs` for a valid dev-mode submission, `crates/buzz-relay/src/api/bridge.rs`'s `verify_bridge_auth_rejects_nip98_event_signed_for_wrong_communitys_host` test for a 401 failure), and a link to NIP-01 as the authoritative wire-format spec. done when: the file exists, is schema-shaped per the six evidence/relationship rules above, and every DoD bullet from the issue body has a corresponding section in the draft.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repository root; fix any FAIL line (UNVERIFIED notices are acceptable) and re-run until it exits 0. done when: the command exits 0 and prints no FAIL line attributable to the new node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call and confirm it prints `OK`; only then, in a separate tool call, `git add` the two new files and `git commit -s -m "docs(corpus): document HTTP events interface (#979)"`. done when: the unittest run prints `OK` and `git log -1` on the branch shows the new commit with both files in its diff.

PARALLEL: none — one document, one plan file, no independent workstreams.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no FAIL line. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit gate accepts it. No code review agents are run — this is a documentation-only corpus node, not a code diff.

BUDGET: small — one Markdown file plus its plan document, evidence scoped to files already read in this session (`router.rs`, `bridge.rs`, `ingest.rs`, `replaceable.rs`, two e2e/regression test files, `templates/interface.md`, the two sibling flow nodes), no code changes, no new tests to write.

OPEN: Whether `relationships: references` toward `architecture-flows-http-event-submission` and `architecture-flows-event-ingestion` is the right call versus declaring none — both nodes are `status: draft` themselves (not yet `active`), and `AGENTS.md` says a relationship is valid only if the target id "is already merged," which both are (present on `origin/launchpad`) regardless of their own `status`. This plan takes merged-not-active as sufficient, matching `templates/interface.md`'s own stated preference for `references` toward loosely-coupled neighboring nodes, but a reviewer may disagree.

LEFT OUT: No changes to `architecture-flows-http-event-submission.md` or `architecture-flows-event-ingestion.md` even though they overlap in subject matter — the issue's own DoD requires filing overlap as a separate task rather than editing an existing node inside this one, and reconciling three nodes' overlapping evidence ledgers is out of scope for a single-document task. No changes to `POST /query`, `POST /count`, or any other HTTP bridge route — issue #979 scopes this node to `/events` only. No attempt to resolve `#1321`'s open question about whether a recorded revision may stay put across future edits — not triggered by a brand-new node. No second hand-authored corpus document of any kind.
