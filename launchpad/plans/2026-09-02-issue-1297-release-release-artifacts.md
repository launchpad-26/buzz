# Issue #1297 — releases/release-artifacts.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/templates/reference.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/releases/` does not exist yet — this is the first node in that directory.

STEP 1  Resolve node shape. Issue #1297's Objective and DoD tail say "single canonical procedure node" and list a procedure-shaped checklist (goal/prerequisites/steps/verification/rollback), but all ten sibling releases/ tasks (#1291-#1296, #1298-#1301) carry byte-identical wording regardless of subject — the same boilerplate-copy pattern `templates/reference.md`'s own evidence ledger documents for issue #1346. Because the subject ("what artifacts does each release surface produce and where do they land") is a lookup catalogue, not an operator procedure, and because #1292/#1293 already separately own the actual cut-a-release steps, build this node against `templates/reference.md` instead of the copied DoD text. ← RUNS HERE

STEP 2  [needs 1] Gather evidence directly from workflow YAML, not from AGENTS.md's ecosystem-table prose: `.github/workflows/release.yml` (desktop, 5 jobs incl. the ungated `release-windows`), `docker.yml` (relay image + push-gateway, both variants, qualify/attest), `helm-chart.yml` + `push-gateway-helm-chart.yml` (chart publish, gating asymmetry), `sprig.yml` + `sprig-image.yml` (agent binary + image, no repository gate on either), `mobile-release-candidate.yml` + `promote-oss-desktop-release.yml` (tag-only / promote-only, both block/buzz-gated), `auto-tag-on-release-pr-merge.yml` (the actual tag-creation convergence point), and `desktop-release-candidate.yml` / `desktop-release-cache-proof.yml` (no artifact, excluded). Cross-check version sources (`crates/buzz-relay/Cargo.toml`, both `Chart.yaml`s) and root `AGENTS.md`'s ecosystem table for the three private downstream repos this checkout cannot inspect.

STEP 3  [needs 2] Scaffold via `scaffold.scaffold_node` (template `reference`, type `release`, origin `launchpad`, id `releases-release-artifacts` via a `releases-`-prefixed `filename` per the settled `<directory>-<stem>` convention, #2029) and write the body: Reference description, one structured-entries table (trigger → workflow/job → artifact → destination → runs-in-launchpad-26-fork?), the tag-creation convergence note, an explicit exclusions note, Boundary (naming #1291/#1292/#1293/#1298/#1299/#1300/#1301 and the three uninspectable private repos), Relationships (none — no sibling releases/ node exists yet), and Scope and omissions (including the fork-specific repo-variable and job-skip items this checkout could not execute to confirm).

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 5  [needs 4] Run the corpus unittest suite as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call. Stop at the commit — no push, no PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here.

BUDGET: medium — one document, no code changes, evidence gathering across ~11 workflow files plus 3 version-source files.

OPEN: `relationships` is empty because `launchpad/docs/corpus/releases/` has no other merged node yet, and #1292/#1293 (the procedure nodes this reference node's Boundary section defers to) are not yet merged either — both are named as gaps in *Scope and omissions* rather than guessed at. Whether `launchpad-26/buzz` sets the `GHCR_SPRIG_IMAGE` / `GHCR_CHART_REPO` repository variables (which would redirect `sprig-image.yml` / `helm-chart.yml` away from the `ghcr.io/block/...` defaults) could not be checked from this worktree and is recorded as unverified rather than assumed either way. Whether `release.yml`'s `release-windows` job actually no-ops in this fork when `setup` is skipped is stated as an INFERENCE against GitHub Actions' documented default `needs`-skip behavior, not something this task executed the workflow to confirm.

LEFT OUT: No relationships to `auto-update.md`, `release-tags.md`, `versioning.md`, `release-provenance.md`, or `rollback.md` — none of those sibling nodes exist on `origin/launchpad` yet either. No attempt to verify what the three private repositories (`squareup/buzz-releases`, `squareup/sprout-oss`, `squareup/block-coder-tf-stacks`) actually do once handed a candidate tag or image digest — out of scope and not inspectable from this checkout. No attestation-verification walkthrough (owned by `release-provenance.md`). No fix proposed for the gating asymmetries this node surfaces (e.g. `push-gateway-helm-chart.yml` publishing without the same `block/buzz` gate its own image build carries) — flagging them is this node's job; changing the workflows is not.
