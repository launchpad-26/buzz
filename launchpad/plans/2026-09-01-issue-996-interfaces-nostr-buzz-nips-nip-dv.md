# Issue #996 — interfaces/nostr/buzz-nips/nip-dv.md

Stated size: small (single document, task dispatch caps this at 5 steps) -> cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/interfaces/nostr/buzz-nips/nip-dv.md` does not exist yet (directory `launchpad/docs/corpus/interfaces/` is absent entirely on this branch). `docs/nips/NIP-DV.md` is the authoritative spec text (draft, optional, relay-scoped; kind:30622 DM Visibility Snapshot). `launchpad/docs/corpus/templates/interface.md` (type `interfaces-events`, id `corpus-template-interface`) is the merged corpus convention for interface-shaped nodes and gives the required body sections (Interface description, Operations, Contract and stability, Boundary, Relationships, Scope and omissions) plus the rule that a node built from it carries `type: interfaces-events` — the only interface-shaped value in `node.schema.json`'s enum. Only four nodes are merged on `origin/launchpad`'s corpus tree today (`corpus-agents`, `corpus-readme`, `corpus-standard-confidence`, `corpus-standard-decision-references`), none of them interface-shaped, so no `relationships` target exists yet; sibling unmerged buzz-nip nodes from the same dispatch batch are not valid targets either.

STEP 1  Gather and confirm evidence against current code: `crates/buzz-core/src/kind.rs` (`KIND_DM_VISIBILITY = 30622`, `is_relay_only_kind`, parameterized-replaceable range assertion), `crates/buzz-relay/src/handlers/side_effects.rs` (`publish_dm_visibility_snapshot`: d/p/h tag construction, relay-signing, `replace_parameterized_event`), `crates/buzz-relay/src/handlers/command_executor.rs` (hide and re-open call sites), `crates/buzz-relay/src/handlers/ingest.rs` (relay-only-kind write rejection), `crates/buzz-relay/src/handlers/req.rs` (`p_gated_filters_authorized`'s ids-exemption removal for `KIND_DM_VISIBILITY`), `crates/buzz-relay/src/handlers/event.rs` (owner-only fan-out gate), `crates/buzz-search/tests/fts_integration.rs` (kind:30622 excluded from FTS index), and `crates/buzz-test-client/tests/e2e_nostr_interop.rs` (`test_nipdv_hide_then_reopen_updates_snapshot` as the valid example, `test_nipdv_explicit_kind_query_forbidden_for_third_party` as the 403 failure example). done when: every citation above is opened and confirmed to say what this plan claims, with exact file paths (and line numbers where stable) recorded for step 2's evidence ledger. [independent] <- RUNS HERE

STEP 2  [needs 1] Write `launchpad/docs/corpus/interfaces/nostr/buzz-nips/nip-dv.md`: front matter (`id: interfaces-nostr-buzz-nips-nip-dv`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships`, one commit citation recording `git rev-parse HEAD` as the checked revision) and a body covering the interface description, the `kind:30622` operation (relay-signed publish triggered by `kind:41012`/`kind:41010`, cited to `side_effects.rs` and `command_executor.rs`), inputs/outputs, error/rejection behavior (client-submitted `kind:30622` rejected per `is_relay_only_kind` + `ingest.rs`), authentication/authorization (the `#p` read-gate and result-level owner check, cited to `req.rs`/`event.rs`), versioning/compatibility (NIP-01 parameterized-replaceable semantics), ordering/idempotency (recompute-and-replace, no delta merge), a link to `docs/nips/NIP-DV.md` as the authoritative spec, one valid example (the hide-then-reopen e2e test) and one failure example (the 403 cross-viewer query test), a Boundary section, and Scope and omissions. done when: the file exists with all of the above present and every substantive claim has a matching `evidence` entry classified FACT/INFERENCE/TEAM_KNOWLEDGE per the rules in `launchpad/docs/corpus/AGENTS.md`.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL line (schema violations, unresolved citations, broken relationship targets) and re-run until it exits 0. UNVERIFIED notices are acceptable; any FAIL not caused by this new node is reported as a finding, not silently worked around. done when: the command prints exit status 0 with no FAIL lines attributable to the new node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call, to earn the commit gate stamp. done when: the command's output includes `OK`.

STEP 5  [needs 4] In a separate call from STEP 4, stage exactly the new corpus document and this plan file and commit with `git commit -s -m "docs(corpus): document Buzz NIP-DV interface (#996)"`. If the commit is rejected for a missing gate stamp, do not touch any stamp file and do not use `--no-verify` — report it as a finding instead. done when: `git log -1` shows the new commit on `task/996-interfaces-nostr-buzz-nips-nip-dv` with both files present in its diff, or a `BLOCKED` finding is reported in its place.

PARALLEL: none — single file, single task, steps are strictly sequential (each step's output is the next step's input).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 before commit. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` immediately before commit, as its own tool call, to earn the pre-commit gate stamp. `review-adjudicate` and the cross-model final review pass are deferred to the batch/PR review stage, not run in this worktree.

BUDGET: small — one hand-authored Markdown document (~150-250 lines), no code changes, evidence gathering scoped to roughly 8 already-identified source files plus the NIP-DV spec itself.

OPEN: whether `audiences` should include `operator` in addition to `[agent, developer, reviewer]` — NIP-DV's Security Considerations section describes a current implementation posture (client does not yet verify the relay-identity signature) that is arguably operator-relevant; this plan follows `templates/interface.md`'s own audience list and leaves the addition to reviewer judgment rather than deciding it here.

LEFT OUT: no `relationships` entries (no merged interface- or event-kind-shaped corpus node exists yet as a legitimate target; sibling buzz-nip nodes from this same dispatch batch are unmerged and excluded per `AGENTS.md` step 9). No edits to `docs/nips/NIP-DV.md` itself. No second hand-authored corpus document. No implementation of the client-side relay-identity signature verification NIP-DV's own Security Considerations section names as an open cross-cutting hardening gap — that is separate implementation work, not this documentation task.
