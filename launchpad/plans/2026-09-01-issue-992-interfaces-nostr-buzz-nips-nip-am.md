# Issue #992 — interfaces/nostr/buzz-nips/nip-am.md

Stated size: task, no size label on the issue; the issue and dispatch prompt both call this a small single-document task -> cap: 5 steps

ALREADY TRUE: `docs/nips/NIP-AM.md` exists at repo root and is the authoritative spec for kind:44200 (Agent Turn Metric). `launchpad/docs/corpus/interfaces/` does not exist anywhere under `origin/launchpad`'s corpus tree (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` lists no `interfaces/` path), so `launchpad/docs/corpus/interfaces/nostr/buzz-nips/nip-am.md` does not exist and there is no sibling buzz-nip node merged to collide or link against. `launchpad/docs/corpus/schema/node.schema.json`'s `type` enum has no `interface` value; the correct value is the combined `interfaces-events` member, confirmed by `launchpad/docs/corpus/templates/interface.md`'s own front matter and "A note on `type`" section. The kind is implemented in code: `crates/buzz-core/src/kind.rs:545` (`KIND_AGENT_TURN_METRIC = 44200`), `crates/buzz-core/src/agent_turn_metric.rs` (payload struct + encrypt/decrypt), `crates/buzz-relay/src/handlers/ingest.rs:1884` (envelope validation) and `:2733-2767` (ownership check + rejection), `crates/buzz-relay/src/handlers/req.rs:1310-1376` and its test at `:1918-1972` (read-gating), `crates/buzz-acp/src/pool.rs:4602` (publisher), and the FTS-exclusion migration asserted at `crates/buzz-db/src/runtime/migration.rs:901-902` and exercised by `crates/buzz-search/tests/fts_integration.rs:1162,1257`.

STEP 1  Confirm the target path is still absent on `origin/launchpad` and re-read `docs/nips/NIP-AM.md` in full plus `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/schema/node.schema.json`, and `launchpad/docs/corpus/templates/interface.md` (the closest template, since no `interface`-specific template is mandatory but its required-sections list is the best available guide). done when: all four files have been read this session and the absence of `launchpad/docs/corpus/interfaces/**` on `origin/launchpad` is confirmed by `git ls-tree`. [independent] ← RUNS HERE

STEP 2  [needs 1] Draft `launchpad/docs/corpus/interfaces/nostr/buzz-nips/nip-am.md` with schema-valid front matter (`id: interfaces-nostr-buzz-nips-nip-am`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships` — no merged node is a legitimate target, and sibling buzz-nip nodes are unmerged so they are prose-linked by filename, not by `relationships`), an `evidence` ledger with one entry per claim (provenance commit citation, spec citations, and the code citations gathered in STEP 1), and a body covering: interface description, the event/message shape (kind, tags, encryption, decrypted payload fields), inputs/outputs, error/rejection behavior, authentication/authorization, versioning/compatibility, ordering/idempotency, a link to `docs/nips/NIP-AM.md` as the authoritative spec, one valid example and one failure example, a boundary/scope-and-omissions section. done when: the file exists with front matter that has all six required keys and no invented enum values. [needs 1]

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL line (not pre-existing UNVERIFIED notices) and re-run until it exits 0. done when: the command's exit code is 0 and its output contains no `FAIL` line attributable to the new node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call and confirm it prints `OK` — this is the commit gate, not a formality to skip. done when: the command's output contains `OK` and no test failure.

STEP 5  [needs 4] In a separate tool call, `git add` only the new corpus document and this plan file, then `git commit -s` with a message referencing #992. Do not push, do not open a PR, do not touch any stamp/gate file directly if the commit is rejected. done when: `git log -1` shows a new commit containing exactly those two files, or the task is reported `BLOCKED` with the rejection reason.

PARALLEL: none — one document, one plan, strictly sequential (each step needs the previous file/state).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no FAIL line from the new node. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit in STEP 5. Cross-model/final review and adjudication are deferred to the batch owner, per existing corpus-doc precedent (e.g. `launchpad/plans/2026-08-28-issue-698-corpus-doc.md`) — not run here.

BUDGET: small — one Markdown document (~150-250 lines), one spec file already read in full, roughly a dozen code citations gathered via targeted greps/reads, no code changes.

OPEN: Whether `docs/nips/NIP-AM.md` should be linked via a `references` relationship once a future event-kind-shaped sibling node for kind:44200 exists, or whether this single interfaces-events node stays the sole home for both the interface and the kind — the template boundary section flags event-kind depth as a different template (`#1337`), but no such sibling node exists yet to decide against. Left as an explicit gap in the node's own Scope and omissions section rather than resolved here.

LEFT OUT: No second hand-authored corpus document (e.g. a separate event-kind node for kind:44200) — issue #992's own DoD and scope forbid materially editing or creating a second canonical document in this task. No `relationships` edges to the four existing merged corpus nodes (`corpus-agents`, `corpus-readme`, `corpus-standard-confidence`, `corpus-standard-decision-references`) — all are governance/agent nodes about the corpus itself, not interface-shaped subject matter this node would reference, implement, or sit part-of. No edits to `docs/nips/NIP-AM.md` itself or to any Rust source — this task is documentation-only, per the issue's "Out of scope" section.
