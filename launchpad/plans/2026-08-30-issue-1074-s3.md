Issue #1074 — task: document layers/data/object-storage/s3.md

Stated size: issue #1074 has no explicit Size line; the task brief driving this plan explicitly caps it for "one small document" -> cap: 5 steps

ALREADY TRUE

- Repository revision for this plan: `338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5` (origin/launchpad tip at fetch time, confirmed via `git rev-parse HEAD` in this worktree).
- Target path `launchpad/docs/corpus/layers/data/object-storage/s3.md` does not exist; `launchpad/docs/corpus/layers/` does not exist anywhere in this worktree's corpus tree (`find launchpad/docs/corpus -type d` lists no `layers/*` directory).
- Issue #1074's body confirms the target path via its own `corpus-plan:v2 alias:DOC:layers/data/object-storage/s3.md` header comment and its Objective sentence, matching the task brief's guess exactly. Its DoD checklist is the generic datastore-shaped one: store classification (authoritative/derived/cache/transport), owned data/access patterns/lifecycle/consistency, tenancy/security/failure behavior, link schema/migrations/code/tests rather than copying DDL.
- `launchpad/docs/corpus/schema/node.schema.json`'s `type` enum is `architecture, layers, capabilities, platforms, implementation, interfaces-events, verification, operations, development, release, governance, agent, ingestion` — `layers` is a real member.
- `launchpad/docs/corpus/templates/datastore.md` exists; its own evidence ledger states a real datastore instance "most plausibly takes `type: architecture`" (INFERENCE, confidence 0.6) — but this is not followed literally, per precedent below.
- **Precedent, read directly from the open (unmerged) sibling PR #1873** (`docs(corpus): data layer batch 2 ... blossom-storage, ... git-objects (#1065-#1069)`), which authored the two other `layers/data/object-storage/*.md` documents in this same batch: both `blossom-storage.md` (#1067) and `git-objects.md` (#1069) chose `type: layers`, not the template's suggested `architecture`, and both disclosed the tension in their evidence ledger citing `standards/taxonomy.md`'s step-4 disclosure rule ("when the fit is still imperfect after [checking precedent], say so in the node's own scope-and-omissions section, rather than silently picking"). The task brief for this issue directs following that same precedent; this plan does so rather than re-deciding it.
- `launchpad/docs/corpus/standards/taxonomy.md` (id `corpus-standard-taxonomy`, merged, `type: governance`) exists on `origin/launchpad` and its "Choosing a value" section states step 4 verbatim as summarized above; this is the standard both sibling nodes cite and this node will cite identically.
- A merged sibling corpus node already exists at container level: `launchpad/docs/corpus/architecture/containers/object-storage.md` (id `architecture-containers-object-storage`, type `architecture`, status `draft`). It documents the shared S3-compatible bucket's existence, its `buzz-media`/git-CAS ownership split, and states MinIO backs local dev while production points `BUZZ_S3_*` at "an externally managed S3-compatible service (AWS S3, Railway Storage Buckets, or similar)" — confirming AWS S3 is named there only as one example among several, never itself detailed. That gap (what is specific to the AWS S3 backend option, as opposed to MinIO or the shared interface generally) is what this node exists to fill.
- Issue #1071 (`minio.md`, the MinIO backend sibling) and issue #1073 (`role.md`, the generic storage-role sibling) are both open with **no PR yet** (`gh pr list --search "1071 OR 1073 in:body"` returns empty), so this node cannot read or link either — same situation `templates/datastore.md` itself documents for its own unread sibling (#1333) and resolves the same way: state the boundary from first principles, do not read unwritten text, do not declare a relationship to either.
- This document is scoped narrowly to what is *specific to real AWS S3 (or an AWS-API-compatible managed provider)* as the physical backend satisfying the shared object-storage container's S3-compatible interface — not a second copy of the container node's existence/technology summary, and not a second copy of the key-namespace/access-pattern detail `blossom-storage.md`/`git-objects.md` already own for their respective consumers (per `AGENTS.md`'s one-node-one-idea rule and the datastore template's own boundary-against-container-document test).
- AWS-specific facts confirmed in code/config this session: `crates/buzz-media/src/storage.rs`'s `MediaStorage::new` doc comment states credential resolution falls back to "the AWS default credential chain ... environment, shared profile, web-identity token (IRSA on EKS — `AssumeRoleWithWebIdentity`), container, and [instance-metadata] providers" when static keys are unset; `crates/buzz-media/src/config.rs`'s `s3_region` doc comment states the region "must match the region of `s3_endpoint` for real AWS S3, otherwise requests are signed with the wrong credential scope and AWS rejects them"; `deploy/charts/buzz/README.md`'s addressing-style table names `virtual` addressing for "AWS-style providers and new Railway Storage Buckets" versus `path` for "Bundled MinIO and endpoints whose DNS does not resolve bucket subdomains."
- `crates/buzz-relay/src/api/git/store.rs`'s `GitStore::new` resolves credentials the identical way (static keys, else the AWS default chain), confirmed by reading the file this session — the AWS-specific credential/region/addressing behavior is shared by both S3 client constructions in the codebase, not unique to media.
- AWS's own primary-source documentation (fetched this session, `https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html#ConsistencyModel`) states: "Amazon S3 provides strong read-after-write consistency for PUT and DELETE requests of objects in your Amazon S3 bucket in all AWS Regions," including for new-object writes, overwrites, and deletes, and that "updates to a single key are atomic." This is a technology-level guarantee genuinely specific to naming AWS S3 as the backend (as opposed to a generic "S3-compatible" claim, since other implementations are not bound by AWS's own published consistency contract) and is exactly the kind of fact `templates/datastore.md`'s "Operational characteristics the technology itself provides" section calls for.
- `crates/buzz-deletion/src/lib.rs`'s `bucket_versioning_detected` preflight probe (already cited by the merged/PR'd sibling `blossom-storage.md`) treats a bucket that has ever had S3 Versioning enabled as a hard, permanent failure for whole-community deletion — AWS S3's own optional bucket-versioning feature (confirmed via the AWS docs page fetched above, which documents S3 Versioning and version IDs) is the real-world trigger this code path guards against; worth naming here as an AWS-S3-specific operational hazard, with a link to `blossom-storage.md` rather than re-deriving the deletion-path mechanics.
- `crates/buzz-relay/src/api/git/store.rs` (per the already-open `git-objects.md` node's own evidence, re-confirmed by direct read this session) runs a startup `run_conformance_probe` gate before serving git traffic — an empirical admission check of whatever S3-compatible backend is actually configured (AWS S3 included), not an AWS-specific mechanism, but relevant context for why an operator would point this container at real AWS S3 rather than assume compatibility.

STEP 1 [independent] <- RUNS HERE — front matter and Purpose/Scope + Technology sections

Create `launchpad/docs/corpus/layers/data/object-storage/s3.md` with:
- `id: layers-data-object-storage-s3`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`.
- Evidence entries for: recorded revision (commit citation); the `node.schema.json` enum fact; the `type: layers` choice as an INFERENCE disclosing the datastore-template's contrary `architecture` suggestion and citing the two sibling nodes' identical precedent plus `standards/taxonomy.md`, mirroring `git-objects.md`'s own ledger entry structure; the AWS credential-chain/region/addressing facts (FACT, citing `storage.rs`/`config.rs`/`store.rs`/the Helm README); the AWS consistency-model claim (FACT, citing the AWS docs URL fetched this session).
- Body: a Purpose & scope statement that names `architecture-containers-object-storage` as the container-level node this zooms into, states this document covers the **AWS S3 backend option specifically** (not the shared interface generally — that is #1073's job, unwritten — and not MinIO — that is #1071's job, unwritten — and not the per-consumer key namespace/access patterns — that is `blossom-storage.md`/`git-objects.md`'s job, already written), and a Technology & attachment profile section covering credential resolution, region/signing requirements, and addressing style as they specifically apply to real AWS S3.

done when: front-matter parses as YAML; `id` matches path-derived kebab-case; `type` is a legal enum member; the scope statement names all four sibling documents/issues by id or number.

STEP 2 [needs 1] — store classification, operational characteristics (consistency, versioning hazard), tenancy/security

- Store classification: authoritative, restated briefly with a link to `blossom-storage.md`/`git-objects.md`'s own fuller classification (this fact is backend-agnostic — true regardless of which S3-compatible technology serves the bucket — so it is not re-derived here, only confirmed and linked).
- Operational characteristics: AWS S3's own published strong read-after-write consistency guarantee (cited to the AWS docs URL); the bucket-versioning hazard `buzz-deletion` guards against, framed as "this is what triggers on real AWS S3 if an operator ever enables S3 Versioning," linking to `blossom-storage.md`'s own fuller failure-mode writeup rather than repeating it.
- Tenancy/security: credential handling specifically — static keys (any S3-compatible provider) vs. the AWS default credential chain including IRSA-on-EKS role assumption (AWS-specific, avoids long-lived static keys), applying identically to both `buzz-media`'s and `GitStore`'s clients.

done when: every claim is backend-specific to AWS S3 (not a restatement of container- or consumer-level facts already owned elsewhere) or explicitly marked as a brief, linked restatement of a fact those sibling documents establish in full.

STEP 3 [needs 2] — access-pattern and schema/namespace deferral, failure behavior

- Access-pattern summary: state explicitly, with citations, that the access pattern is backend-agnostic (same `rust-s3` client calls against MinIO or AWS S3) and link to `blossom-storage.md`/`git-objects.md` for the actual per-consumer tables rather than reproducing them.
- Schema/namespace inventory: same deferral — the key namespace is defined by client code, not by which S3-compatible technology serves the bucket, so this section states that explicitly and links out rather than duplicating either sibling's table.
- Failure behavior: SigV4 signing rejection on a region/endpoint mismatch (`config.rs`'s own doc comment, "AWS rejects them"), framed as an AWS-S3-specific failure mode distinct from MinIO's looser regional handling.

done when: no row restates a key shape or access-pattern detail already tabulated in `blossom-storage.md` or `git-objects.md`; the AWS-specific failure modes are each cited to a real path opened this session.

STEP 4 [needs 3] — relationships, evidence-class audit, scope-and-omissions

- Add `relationships: [{type: part-of, target: architecture-containers-object-storage}]`. No edge to `blossom-storage.md`, `git-objects.md`, `minio.md`, or `role.md`: the first two are not yet merged to `origin/launchpad` (both are only on the open, unmerged PR #1873) and the latter two have no PR at all — `AGENTS.md` step 9 requires a relationship target to resolve on the branch being merged into, and none of the four does today.
- Re-read every evidence entry: FACT only where the cited source was actually opened this session (including the AWS docs URL, fetched directly rather than assumed); INFERENCE for the `type: layers` choice; TEAM_KNOWLEDGE for anything sourced only from issue text.
- Scope-and-omissions: link out to `architecture-containers-object-storage` (container existence/technology/shared-bucket summary), `blossom-storage.md`/`git-objects.md` (per-consumer key namespace and access patterns, both unmerged — named as a real forward reference, same as `git-objects.md` itself named `architecture-flows-git-push`-style siblings), and the not-yet-drafted `minio.md`/`role.md` (explicitly named as unread, with the same "re-check once it lands" framing `templates/datastore.md` uses for its own unread #1333 boundary). Name the private `squareup/block-coder-tf-stacks` staging-provisioning gap identically to the container node's own disclosure.

done when: no relationship target is unresolved against `origin/launchpad`; no evidence entry rests only on an `UNVERIFIED` citation shape while classed `FACT`; the type-choice tension and the unread-sibling gaps are both named in scope-and-omissions, not just in the evidence ledger.

STEP 5 [needs 4] — validate, test-gate, commit

- Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.
- Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call; confirm OK.
- `git add` the plan and the target file; `git commit -s`.

done when: validator exits 0; unittest suite reports OK; exactly one new commit ahead of `origin/launchpad` (or a second small follow-up commit only if forced by a post-commit fix, never an amend).

PARALLEL

None. All five steps edit the same single file sequentially (front matter, then
each body section, then relationships/audit, then validate+commit) and each
depends on the text the previous step wrote, so `[needs N]` chains 1->2->3->4->5
with no independent branch to parallelize.

GATES

- `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 before commit.
- `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report OK before commit, run alone in its own tool call.
- No second hand-authored canonical corpus document created in this change.
- No relationship declared to `blossom-storage.md`, `git-objects.md`, `minio.md`, or `role.md` — none resolves against `origin/launchpad` at this revision.

BUDGET

One document (~150-250 lines including front matter — smaller than the sibling
per-consumer datastore nodes because this one deliberately defers key-namespace and
access-pattern content to them rather than re-deriving it), five steps, single commit.
No code changes — corpus content only.

OPEN

- Whether `type: layers` or `type: architecture` is the better long-run fit is not settled anywhere in the corpus today — resolved by following the two already-drafted `layers/data/object-storage/*` siblings' precedent (`layers`) and disclosing the tension, per the task brief's explicit direction and `standards/taxonomy.md`'s step-4 rule. Revisable later without an id change.
- Whether `minio.md` (#1071) or `role.md` (#1073) will describe this backend's relationship to the generic role differently than assumed here is unknown — neither has a PR yet, so this node's stated boundary against them is first-principles reasoning, not a read of their actual text, matching the same honest gap `templates/datastore.md` names for its own unread #1333 boundary.
- Whether staging deploys use managed AWS S3 or something else is not established (owned by the private `squareup/block-coder-tf-stacks` repo) — named as a gap, matching the sibling container doc's own identical disclosure.

LEFT OUT

- Any edit to `architecture-containers-object-storage.md`, `blossom-storage.md`, or `git-objects.md` — this task only adds the new AWS-S3-backend node and links out.
- MinIO's own backend specifics (#1071) and the generic storage-role abstraction (#1073) — both out of scope per issue #1074's own Objective (AWS S3 only) and explicitly named in the task brief as a sibling to keep distinct, not duplicate.
- Any runtime/product code change — this is documentation-only, per the issue's own Out-of-scope section.
