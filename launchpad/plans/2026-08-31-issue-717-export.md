# Plan: issue #717 — document capabilities/archive/export.md

## ALREADY TRUE

- Target file `launchpad/docs/corpus/capabilities/archive/export.md` does not exist
  (confirmed via `test -f`).
- `launchpad/docs/corpus/templates/capability.md` is the only template for `type:
  capabilities` nodes; no per-instance capability node exists in the corpus yet.
- The corpus tree merged to `origin/launchpad` at `cad6c375f` contains only
  `AGENTS.md`, `README.md`, `templates/**`, `standards/**`, and the `architecture/**`
  family — no `capabilities/` subtree and no sibling node (`local-archive`,
  `identity-archive`, `restore`) is merged, so this node declares zero
  `relationships`.
- The capability this issue names ("export", grouped under an `archive/` family
  alongside `local-archive`, `identity-archive`, `restore`) is the desktop app's
  **agent/team snapshot export** feature: `export_agent_snapshot` /
  `encode_agent_snapshot_for_send` (`desktop/src-tauri/src/commands/personas/snapshot.rs`)
  and `export_team_snapshot` (`desktop/src-tauri/src/commands/team_snapshot.rs`),
  both registered in `desktop/src-tauri/src/lib.rs`'s `generate_handler!` list.
  Shipped by PR #1753 (agent snapshot v1) and extended by #1784/#1846 (team
  snapshot v1). NIP-IA (identity archival) is a separate protocol with no
  "export" concept of its own — confirmed by reading `docs/nips/NIP-IA.md` in full.

## STEP 1 — Confirm scope and gather evidence (done during planning)

Read `docs/nips/NIP-IA.md` in full (no export concept there), the export/import
Tauri command modules (`personas/snapshot.rs`, `personas/snapshot/import.rs`,
`team_snapshot.rs`, `managed_agents/agent_snapshot.rs`), the export dialog
(`AgentSnapshotExportDialog.tsx`), the mutation hooks (`snapshotHooks.ts`), and
`lib.rs`'s command registration. Confirmed both `.agent.{json,png}` and
`.team.{json,png}` export exist, are wired into the running app, and are
desktop-only (no `buzz-cli` equivalent — `buzz-cli/src/commands/agents.rs` has no
snapshot-export subcommand).

**Done when**: evidence ledger entries below are each backed by an opened source.

## STEP 2 — Draft the node

Create `launchpad/docs/corpus/capabilities/archive/export.md` against
`node.schema.json` and `templates/capability.md`'s required sections (Capability
statement, Maturity, Boundary, Relationships, Scope and omissions).
`id: capabilities-archive-export`, `type: capabilities`, `status: draft`,
`origin: launchpad`, `audiences: [agent, developer, reviewer]`.

**Done when**: the file exists with schema-shaped front matter and all five
required body sections.

## STEP 3 — Validate

Run `python3 launchpad/project-intelligence/corpus/validate.py` from repo root.
Confirm it introduces zero new FAIL lines versus a baseline run without the new
file staged (21 pre-existing FAILs are tracked in #1951, unrelated to this node).

**Done when**: FAIL-line diff is empty for the new file.

## STEP 4 — Earn the commit gate and commit

Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests
-p "test_*.py"` as the sole command in its own call; confirm `OK`. Then `git add`
the new node + this plan file and `git commit -s`.

**Done when**: commit exists on `task/717-export`.

## STEP 5 — Self-review

Re-read the diff against issue #717's DoD checklist line by line; re-open cited
sources; confirm no second canonical document was created; confirm no new
validate.py FAIL entries.

**Done when**: every DoD bullet is checked off against actual diff content.

## PARALLEL

None — single-file task, no parallelizable sub-work.

## GATES

- `python3 launchpad/project-intelligence/corpus/validate.py` — zero new FAIL
  lines attributable to the new file.
- `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p
  "test_*.py"` — must print `OK` before commit.

## BUDGET

Single node, capped at the 5 steps above. No sub-agents, no worktree fan-out
beyond the one already created for this issue.

## OPEN

- Whether "export" as scoped by the batch dispatcher is meant to cover team
  snapshots too, or agent-only. Resolved by treating both as the same capability
  (they share the same manifest format family, dialog pattern, and file-format
  choice) and stating team-snapshot as an in-scope variant, not a separate
  capability, since no sibling issue in this batch names a "team snapshot" node
  distinctly.

## LEFT OUT

- The interface contract (Tauri command signatures in full) — belongs to a
  future interface-type node, not this capability node.
- The step-by-step export→send/save→import flow — belongs to a future flow-type
  node (`#1338`'s template family, not yet drafted).
- How snapshot memory decryption/encryption works internally — architecture/
  component territory, not capability territory.
