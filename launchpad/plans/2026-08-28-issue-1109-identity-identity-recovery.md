# Issue #1109 — layers/identity/identity-recovery.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; no `launchpad/docs/corpus/layers/` directory exists yet on `origin/launchpad` (confirmed by `find launchpad/docs/corpus/layers` at HEAD `338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5`), so none of the 13 sibling `layers/identity/*.md` tasks in the #607 batch (#1102–#1114) have merged either — no `relationships` target exists yet.

STEP 1  Gather evidence: confirm there is no server-side or cryptographic recovery of a truly lost Nostr private key (self-custody is the base case), then inventory Buzz's actual local mitigations against key loss by reading `desktop/src-tauri/src/key_backup.rs` (NIP-49 encrypted `ncryptsec` backup, `generate_passphrase`, `recover_keys_from_input`), `desktop/src-tauri/src/identity_storage.rs` (`RecoveryState::{None,Lost,KeyringLocked}`), `desktop/src-tauri/src/app_state.rs` (`signing_keys()` blocking publish while lost/locked, keyring→file fallback resolution, ephemeral in-memory `Keys::generate()` while lost), `desktop/src-tauri/src/commands/identity.rs` (`generate_backup_passphrase`, `create_ncryptsec_backup`, `verify_ncryptsec_backup`, `save_ncryptsec_copy`, `import_identity`, `persist_current_identity`), `desktop/src-tauri/src/commands/pairing.rs` (`start_identity_recovery_pairing` / `PairingMode::RecoverIdentity`, the app-level `&mode=recover` query-param extension on top of the NIP-AB QR URI), `crates/buzz-core/src/pairing/NIP-AB.md` (confirms `mode` is an unspecified param NIPs allow clients to add, and that `nsec`/`ncryptsec1` is a defined payload type — recovery reuses that transport, it does not change the spec), the mobile side (`mobile/lib/features/pairing/pairing_page.dart`, `pairing_provider.dart`, `settings_page.dart`), and the onboarding/settings UI copy (`desktop/src/features/onboarding/ui/BackupStep.tsx`, `DownloadKeyStep.tsx`, `EncryptedBackupCreator.tsx`, `IdentityRecoveryPairing.tsx`) and `desktop/tests/e2e/identity-lost.spec.ts` for the exact lost/locked-boot behavior. Distinguish this from #1105 device-pairing (adding a *new* device while the key is still held) — recovery pairing is initiated by a device that has *lost* its local copy, from a device that still holds it. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `layers-identity-identity-recovery`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, no `relationships` unless `architecture-containers-desktop` and/or `architecture-principles-signed-events` are confirmed still present on disk at write time) and the body: define identity recovery in one sentence, state plainly that Buzz has no mechanism to recover a lost key with zero surviving copies (self-custody, matching Nostr generally) as the primary FACT, then document the two real local mitigations Buzz ships — (a) password-encrypted local NIP-49 `ncryptsec` backup file, decryptable back into the same keys; (b) phone-to-desktop recovery pairing that re-obtains the key from another device that still holds it — plus the `RecoveryState::Lost` vs `RecoveryState::KeyringLocked` distinction and the "start a new identity" abandonment path. Boundaries section explicitly separates this from device-pairing (#1105/`layers-identity-device-pairing`, not yet merged) and from key-backup-as-a-concept if that turns out to warrant its own node.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Re-open every cited source and confirm each evidence statement is actually supported (not just structurally valid); confirm exactly one hand-authored file was created.

STEP 5  [needs 4] Run the corpus unittest suite as the sole command in its own tool call to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here.

BUDGET: small — one document, no code changes; evidence gathering scoped to ~10 desktop/mobile files plus the NIP-AB spec doc, all already located.

OPEN: Whether the two local mitigations (encrypted backup file, recovery pairing) and the "no true recovery" fact belong in one node or should split further is a judgment call this task makes once, honestly documenting both the absence of true recovery and the mitigations Buzz actually built, rather than inventing a mechanism that doesn't exist or omitting the real ones that do.

LEFT OUT: No claim about mobile-only or CLI-only key loss flows beyond what the recovery-pairing code shows (mobile is the trusted source of recovery, not itself documented here as losing/recovering its own key by a different path unless evidence says otherwise). No edits to `layers/identity/device-pairing.md`, `keypair.md`, `identity-storage.md`, or any other sibling — those are separate #607 tasks. No attempt to add `relationships` to sibling identity nodes that have not merged.
