# Issue #702 — capabilities/activity/pulse.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and `launchpad/docs/corpus/templates/capability.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/capabilities/activity/pulse.md` does not exist yet (`launchpad/docs/corpus/capabilities/` does not exist as a directory at all). `node.schema.json`'s `type` enum member for this subject is `capabilities`; the capability template requires a Capability statement, Maturity (cited), Boundary, Relationships and Scope-and-omissions section.

STEP 1  Gather evidence: confirm Pulse's product framing in `preview-features.json` ("Activity feed with notes, social posts, and agent activity", no `defaultEnabled` → off by default) and `desktop/src/shared/features/resolveEnabled.ts`. Trace the implementation: `desktop/src/app/routes/pulse.tsx` (route, `usePreviewFeatureWarning("pulse")`), `desktop/src/features/pulse/ui/{PulseScreen,PulseView,PulseTabBar,NoteCard,AgentActivityCard}.tsx` (tabs: search/everyone/people/liked/agents/mine), `desktop/src/features/pulse/hooks.ts` and `desktop/src/shared/api/social.ts` (client API), and `desktop/src-tauri/src/commands/social.rs` (backend: `publish_note`/`get_global_notes`/`get_user_notes`/`get_note` on NIP-01 kind:1, `get_contact_list`/`set_contact_list` on NIP-02 kind:3, `get_note_reactions` on NIP-25 kind:7 filtered against kind:5 deletions). Confirm agent-activity grouping logic in `desktop/src/features/pulse/lib/groupAgentNotes.ts` (same kind:1 notes, grouped by pubkey + 300s time window). Note existing tests as verification: `noteActions.test.mjs`, `replies.test.mjs`, `focusRefetchPolicy.test.mjs`. Confirm `architecture-containers-desktop` is the only merged corpus node plausibly `references`-able (desktop is the container Pulse ships in); confirm no capability/interface/flow sibling nodes exist yet on `origin/launchpad`. Record HEAD commit (`cad6c375fdcc590158c1456c9fc7875f0f84a844` at worktree creation — re-check before writing the provenance entry). ← RUNS HERE

STEP 2  [needs 1] Create `launchpad/docs/corpus/capabilities/activity/` and write the front matter (id `capabilities-activity-pulse`, type `capabilities`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, evidence ledger with one FACT per substantive claim citing the exact files/lines from STEP 1, plus a commit-citation FACT for provenance) and the body per the capability template skeleton: Capability statement (activity feed of short social notes plus grouped agent activity, product-stakeholder language), Maturity (preview/gated — cite `preview-features.json` and the route's `usePreviewFeatureWarning` call, not "shipped"), Boundary (not architecture/interface/flow/operations, per the template's four exclusions — name the desktop container and the underlying NIP-01/02/25 kinds as what it rests on, not what it *is*), Relationships (`references: architecture-containers-desktop`; no interface or flow node exists yet to reference), Scope and omissions (what's not covered — no interface node exists for the Tauri command surface, no flow node exists for the step-by-step note-publish path; expected-but-not-verified gaps named explicitly, e.g. whether Forum's kind:45001/45003 range and Pulse's kind:1 range are ever cross-referenced in the same UI).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` alone to earn the commit gate, confirm `OK`, then in a separate call `git add` the plan + new document and `git commit -s`. Per this batch's process (unmerged skill update, PR #1945), do **not** push and do **not** open a PR — the commit stays local on `task/702-pulse` for a later integration pass.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before committing. `review-code`/`review-adjudicate`/cross-model final review are deferred to the batch integration pass — not run here; a self-review against the issue's DoD checklist substitutes.

BUDGET: small — one document, no code changes, evidence gathering scoped to ~10 desktop/Tauri files already read plus 3 test files for verification citations.

OPEN: Pulse's own product surface is not listed in `VISION_PROJECTS.md`'s "Capability | Status" table (that table is scoped to the git/forge story) — Maturity is therefore cited from `preview-features.json` and the feature-gate code path, not from a VISION status marker, which is an equally legitimate citation per the capability template ("code, test, or merged PR") but the choice is called out explicitly rather than silently substituted. Whether Pulse (kind:1 NIP-01 notes) and the separate Forum feature (kind:45001/45003) are intended to converge, stay parallel, or be disambiguated for users is not established by any source read here — left as an open question in the node's own scope-and-omissions section rather than resolved. Mobile has a `mobile/lib/features/pulse/` module (page, provider, note card, agent-activity card, compose page) but a grep of `mobile/lib/features/home/home_page.dart` (the file holding the bottom-navigation destinations) shows zero references to it — the mobile Pulse code exists but is not currently wired into mobile navigation. This is reported as a fact about current mobile maturity, not smoothed over as "mobile parity, out of scope."

LEFT OUT: No interface node reference (no `#1342`-style interface node exists yet for the Tauri command surface `publish_note`/`get_global_notes`/etc.). No flow node reference (no `#1338`-style flow node exists yet for the step-by-step note-publish/reply/react path). No relationship to any other capability node (none merged yet, and none of this batch's siblings are valid targets per `AGENTS.md`'s own merge-base rule). No investigation of *why* mobile Pulse is unwired (no PR/issue search performed) — the code-presence-vs-navigation-absence fact is reported, its history is not.
