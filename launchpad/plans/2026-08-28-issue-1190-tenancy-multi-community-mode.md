# Issue #1190 — layers/tenancy/multi-community-mode.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/concept.md`, and the merged `launchpad/docs/corpus/architecture/deployment/multi-community.md` (deployment/infra framing) are all present on `origin/launchpad`. `launchpad/docs/corpus/layers/tenancy/multi-community-mode.md` does not exist yet on `origin/launchpad` or in this worktree.

STEP 1  Gather evidence on the relay's runtime tenancy behavior (not infra/topology, which `architecture-deployment-multi-community` already owns): `crates/buzz-core/src/tenant.rs` (`TenantContext`, `CommunityId`, `normalize_host`, `relay_url_authority`) and `crates/buzz-relay/src/tenant.rs` (`HostResolver`, `bind_community`, `bind_deployment_community`, fail-closed row-zero binding) for how every request resolves its community identically regardless of how many communities exist; `crates/buzz-relay/src/main.rs` (~L254-340) for startup seeding of exactly one deployment community from `relay_url` via `ensure_configured_community`, run unconditionally; `crates/buzz-relay/src/config.rs` (`relay_operator_pubkeys`, `relay_operator_api_origin`, ~L650-689) and `crates/buzz-relay/src/api/operator.rs` (`authorize_operator_request`) for the `RELAY_OPERATOR_PUBKEYS` allowlist that is empty by default (provisioning fail-closed — a deployment stays effectively single-community) versus populated (an authorized operator may call `POST /operator/communities`, handled by `crates/buzz-relay/src/handlers/community_provisioning.rs`, to provision additional communities); `crates/buzz-relay/src/router.rs` (~L84-100) confirming the operator routes are always mounted regardless of config, so the mode difference is authorization-time, not route registration; and `crates/buzz-db/src/relay_members.rs` (`MAX_COMMUNITIES_PER_OWNER = 5`, `BUZZ_MAX_COMMUNITIES_PER_OWNER` override, `max_communities_per_owner`) plus `crates/buzz-db/src/lib.rs::create_community_with_owner` for the per-owner community cap, which is enforced only on the end-user `create_only` provisioning path and explicitly NOT on `bootstrap_owner`/`ensure_configured_community` (the deployment-root/startup path). Record `git rev-parse HEAD`. ← RUNS HERE

STEP 2  [needs 1] Write front matter against `node.schema.json`, following `templates/concept.md`'s shape (this is a runtime-behavior concept, not an architecture/deployment or interface node): `id: layers-tenancy-multi-community-mode`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`, one evidence entry per STEP 1 finding (FACT, citing the opened source files), plus a `relationships` block using only ids confirmed present on `origin/launchpad`: `references` → `architecture-deployment-multi-community` (the infra/topology sibling this node explicitly does not duplicate), `architecture-principles-host-selects-community`, `architecture-principles-community-is-security-boundary`, and `architecture-principles-fail-closed-boundaries` (the invariants this node's behavior instantiates).

STEP 3  [needs 2] Write the body per `templates/concept.md`'s required sections: a one-sentence definition of "multi-community mode" up front, the mechanism (row-zero host resolution is identical whether one or many communities are configured — there is no separate code path), the actual differentiator (`RELAY_OPERATOR_PUBKEYS` empty vs. non-empty, plus the always-present startup-seeded deployment community), a boundary/non-goals section distinguishing this from the deployment/infra node and from client-side community *switching* (desktop's key-remount pattern, out of scope per the task brief), and a scope-and-omissions section naming what was expected but not verified (e.g. whether any deployment today actually sets `RELAY_OPERATOR_PUBKEYS`).

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 5  [needs 4] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole prior command to earn the verification stamp, confirm `OK`, then commit the plan + document in a separate call, re-verify the diff against every DoD bullet, re-run `validate.py`, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The unittest suite must report `OK`. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here (stated explicitly in the PR body).

BUDGET: small — one document, no code changes, evidence gathering scoped to ~6 files already located (`buzz-core/src/tenant.rs`, `buzz-relay/src/tenant.rs`, `buzz-relay/src/main.rs`, `buzz-relay/src/config.rs`, `buzz-relay/src/api/operator.rs`, `buzz-relay/src/handlers/community_provisioning.rs`, `buzz-relay/src/router.rs`, `buzz-db/src/usage.rs`).

OPEN: Whether any real deployment (staging or otherwise) actually sets `RELAY_OPERATOR_PUBKEYS` non-empty today could not be checked from this checkout — `squareup/block-coder-tf-stacks` and `squareup/sprout-backend-blox` are private and not present here; named as a gap rather than assumed either way, matching how `architecture-deployment-multi-community` already handles the same limit.

LEFT OUT: No relationships to any push-gateway, git, or media node — none is a legitimate target for this subject. No re-derivation of the deployment/infra content (execution nodes, Helm chart, backup list) already owned by `architecture-deployment-multi-community` — linked, not duplicated. No claim about desktop's community-switching UI (`resetCommunityState`) — that is client-side state management, not relay runtime behavior, and out of scope per the task brief.
