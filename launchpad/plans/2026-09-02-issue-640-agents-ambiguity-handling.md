# Issue #640 — agents/ambiguity-handling.md

ALREADY TRUE: `launchpad/docs/corpus/agents/invariants.md` (id `agents-invariants`), `launchpad/docs/corpus/standards/taxonomy.md` (id `corpus-standard-taxonomy`), `launchpad/docs/corpus/standards/atomicity.md` (id `corpus-standard-atomicity`), `launchpad/docs/corpus/standards/decision-references.md` (id `corpus-standard-decision-references`), `launchpad/docs/corpus/standards/review-requirements.md`, `launchpad/docs/corpus/templates/procedure.md` (id `corpus-template-procedure`) and `launchpad/docs/corpus/templates/runbook.md` (id `corpus-template-runbook`) are all merged on `origin/launchpad` (confirmed via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`). `launchpad/docs/corpus/agents/ambiguity-handling.md` does not exist yet (confirmed via `test -f`). None of the other 31 sibling `agents/*.md`/`ingestion/*.md` tasks under parent Feature #620 is merged.

Stated size: issue #640 carries no `Size` line (checked via `gh issue view 640 --json labels,body`); this task's dispatch brief states the cap explicitly for this corpus-batch-author run -> cap: 5 steps.

STEP 1  [independent] Gather evidence: read issue #640 live (`gh issue view 640`), `agents/invariants.md` in full (gold-standard sibling), `AGENTS.md`'s three worked-failure passages ("nothing to point at" line 154, the miscounted-table failure line 207, the missing-evidence-class gap line 269), `standards/taxonomy.md`'s choosing-a-value steps 1/3/4, `standards/atomicity.md`, `ADR-0029`, `standards/decision-references.md`'s "When two accepted decisions conflict" section, `standards/review-requirements.md`'s MUST 8, `launchpad/AGENTS.md` section 5 rules 1 and 2, and `.claude/skills/corpus-plan/SKILL.md` / `.claude/skills/corpus-batch-author/SKILL.md` / `.claude/skills/corpus-review/SKILL.md` for their in-repo statements about ambiguity — plus the user-level `~/.claude/skills/plan-issue/SKILL.md`'s "ambiguity is surfaced, not resolved" line (not repo-tracked, so attributed as TEAM_KNOWLEDGE rather than cited as an in-repo FACT). Determine template/type by reasoning, not the issue's boilerplate DoD tail. done when: every source above has actually been opened (not assumed) and its relevant passage/line quoted or paraphrased in working notes. ← RUNS HERE

STEP 2  [needs 1] Decide `type: agent` (matches sibling `agents-invariants`, per `standards/taxonomy.md`'s own precedent test) and template `templates/procedure.md` (a how-to for a decision point reached mid-chosen-task, not `templates/runbook.md`'s alert/severity/escalation shape for an already-firing operational condition). Write front matter (id `agents-ambiguity-handling`, status `draft`, audiences `[agent, reviewer]`, evidence ledger, relationships: `depends-on: corpus-agents`, `references: corpus-standard-taxonomy`/`corpus-standard-atomicity`/`corpus-standard-decision-references`/`agents-invariants`, `implements: corpus-template-procedure`) and the body per `templates/procedure.md`'s required sections: Overview, Before you start, a recognition step plus three lettered branches (unclear requirement/DoD; plausible dual `type`; source readable two ways / contradicting authorities) each handing off to its owning document rather than restating it, an escalate-rather-than-invent closing section, See also, Boundary, Relationships, Scope and omissions. done when: the file exists with front matter parsing as valid YAML and every required template section present.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the worktree root; fix and re-run until exit 0. done when: the command's own exit status is 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, confirm `OK`, then commit with `git commit -s` in a separate call. Do not push and do not open a PR — this commit is integrated into one Feature-wide PR later by the batch owner. done when: the unittest run reports `OK` and `git log` shows one new signed-off commit on this branch.

STEP 5  [needs 4] Self-review (or run `review-code` if reachable): re-read the diff against issue #640's DoD line by line, confirm every evidence entry actually supports its claim, confirm no second canonical document was created, and re-run `validate.py` after any fix. done when: each DoD checklist item has been checked against the actual diff and `validate.py` still exits 0 after any resulting fix.

PARALLEL: none — single file, single task, no cross-step independence beyond STEP 1 itself being the entry point.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before the commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review before merge, per `corpus-batch-author`'s own stated scope — not run here. `qa` explore mode does not apply — this is a documentation-only change with no runtime interface to exercise.

BUDGET: small — one document, no code changes, evidence gathering scoped to roughly a dozen already-merged corpus/decision/skill files, all read directly rather than sampled.

OPEN: Whether the three named branches (unclear requirement, plausible dual `type`, source readable two ways) exhaust every kind of authoring-time ambiguity a corpus author can hit is not settled — this plan builds only the three the task brief named explicitly, and the node's own Scope and omissions says so rather than claiming completeness. Whether any later-merged sibling `agents/*.md`/`ingestion/*.md` node will declare a relationship toward this one is that sibling's own edit, not decided here.

LEFT OUT: No relationship edges to any of the 31 unmerged sibling tasks under Feature #620. No restatement of `standards/taxonomy.md`'s, `standards/atomicity.md`'s, or `ADR-0029`/`standards/decision-references.md`'s full substantive procedures — this node hands off to each rather than duplicating its canonical content, per Feature #620's own acceptance criteria against duplication. No attempt to resolve issue #640's own how-to-shaped DoD-tail boilerplate literally where it conflicts with the real acceptance criteria from Feature #620 — the mismatch is named in the node's evidence ledger instead, following the same pattern `templates/procedure.md`'s own "Note on Definition of Done" documents for a sibling task.
