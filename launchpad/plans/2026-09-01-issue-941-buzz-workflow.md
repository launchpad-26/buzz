Issue #941 — task: document implementation/crates/buzz-workflow.md
Stated size: no `Size` line on the issue (task instructions cap this batch document at 5 steps)  →  cap: 5 steps

ALREADY TRUE  (verified against git, not notes)
  `launchpad/docs/corpus/templates/implementation-reference.md`, `launchpad/docs/corpus/AGENTS.md` and `launchpad/docs/corpus/schema/node.schema.json` are merged on `origin/launchpad` (worktree HEAD `76a0a4ebbe4bc4d852b0d04362ed768620da34b3` == `origin/launchpad`).
  `launchpad/docs/corpus/implementation/crates/buzz-workflow.md` does not exist (`ls` fails, no such file).
  No `launchpad/docs/corpus/implementation/**` directory exists at all yet on `origin/launchpad` (`git ls-tree -r --name-only HEAD -- launchpad/docs/corpus` lists no `implementation/` entries) — this is the first node under that surface.
  `launchpad/docs/corpus/architecture/flows/workflow-execution.md` (id `architecture-flows-workflow-execution`, type `architecture`, status `draft`) is merged and already documents `crates/buzz-workflow`'s three trigger paths and shared executor in detail — it is the strongest candidate `implements`/`references` target and evidence source is not duplicated from it here.
  `crates/buzz-workflow/src/` has exactly five files (`lib.rs` 2090 lines, `executor.rs` 1969, `schema.rs` 1001, `error.rs` 114, `action_sink.rs` 73), no `README.md`, no separate `tests/` directory (tests are inline `#[cfg(test)] mod tests` blocks in `error.rs`, `executor.rs`).
  `crates/buzz-relay`, `crates/buzz-admin` declare `buzz-workflow` as a workspace dependency in `Cargo.toml`; `crates/buzz-relay/src/workflow_sink.rs` implements the crate's `ActionSink` trait (`impl ActionSink for RelayActionSink`), and `crates/buzz-relay/src/main.rs:436,644` is the real (non-test) construction/wiring site.

STEP 1  Finish evidence gathering: read `crates/buzz-workflow/src/{lib,schema,error,action_sink}.rs` in full (executor.rs's public surface and safety bounds — `EVAL_TIMEOUT` 100ms, `MAX_EXPR_LEN` 4096 bytes — and representative tests already located); confirm `buzz-db/src/store/workflow.rs` owns all DB persistence and `buzz-relay/src/{router.rs,handlers/command_executor.rs,workflow_sink.rs}` own HTTP routing, kind:30620 definition-save, and side-effect execution, so the implementation-reference node states these as explicit non-ownership boundaries rather than restating `architecture-flows-workflow-execution`'s behavioral claims.  [independent]
        done when: every claim planned for the node's evidence ledger has been read directly at HEAD `76a0a4ebbe4bc4d852b0d04362ed768620da34b3` (not inferred from the flow doc), covering: crate responsibility/non-ownership, public interfaces (`WorkflowEngine`, `ActionSink`, `WorkflowDef`/`TriggerDef`/`ActionDef`/`Step`, `WorkflowError`), the evalexpr safety bounds and their test, and the relay wiring points named above.

STEP 2  [needs 1] Write `launchpad/docs/corpus/implementation/crates/buzz-workflow.md` following `templates/implementation-reference.md`'s required sections (Realization statement, Target, Implementation surface, Divergences, Verification, Relationships, Scope and omissions) with schema-valid front matter: `id: implementation-crates-buzz-workflow`, `type: implementation`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, one `evidence` entry per substantive claim (FACT for anything opened directly, TEAM_KNOWLEDGE with `provided_by` for issue-sourced requirements only), and a `references` (not `implements` — there is no spec/decision/contract this crate realizes; `architecture-flows-workflow-execution` documents behavior, not a normative target) relationship to `architecture-flows-workflow-execution`, the one existing merged node whose subject overlaps.  ← RUNS HERE  [needs 1]
        done when: the file exists, `python3 launchpad/project-intelligence/corpus/validate.py` produces zero FAIL entries naming this node (a pre-existing ~21-failure baseline on other nodes is expected and not this task's to fix — confirmed via `git stash`/diff against `origin/launchpad` before assuming a new failure is this node's), and every Definition-of-Done bullet from issue #941 is satisfied by a specific section in the body.

STEP 3  [needs 2] Re-read the diff against issue #941's Definition of Done line by line and confirm every citation in the new node's `evidence` ledger actually supports its statement (open the cited file again, do not trust the earlier read from memory).  [needs 2]
        done when: each DoD bullet is matched to the section/citation that satisfies it, and no evidence entry cites a source that was not reopened during this review pass.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the commit-gate stamp; confirm `OK`.  [needs 3]
        done when: the suite reports `OK` with no failures or errors.

STEP 5  [needs 4] `git add` the plan and the new corpus node (only those two files) and `git commit -s` with a DCO-signed message referencing #941. Do not push, do not open a PR — this batch integrates all 37 documents into one Feature-level draft PR later.  [needs 4]
        done when: `git log -1` shows the new commit with a `Signed-off-by` trailer and both files present in `git show --stat HEAD`.

PARALLEL: none as subagents — this is one file, and steps 2–5 each depend on the file state left by the previous step (the commit gate specifically requires the unittest run as the sole prior command, so step 4 cannot be folded into step 5). Step 1 is independent of nothing preceding it (it is the first step) but is marked `[independent]` because no other pending step touches the same files.
GATES: `python3 launchpad/project-intelligence/corpus/validate.py` after step 2, re-checked in step 3. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` is the commit-gate stamp, run alone per the task's instructions — no `--no-verify`, no touching the stamp file if it refuses. `review-adjudicate` and the cross-model final review pass are deferred to the batch integration PR, not run per-document here. `qa` explore mode does not apply — this is a docs-only corpus node with no runtime interface to exercise.
BUDGET: step 2 (writing the node body and getting `validate.py` clean) is the step most likely to eat the budget — the template's required sections plus honest FACT/TEAM_KNOWLEDGE classification for a crate with five source files and 5000+ lines take real reading, and getting the `references`-not-`implements` relationship choice defensible against the template's own boundary section is the one judgment call in this plan.
OPEN: Whether `references` is the right relationship type versus declaring none at all is not fully settled by the template — the template's own worked example (`implements` toward a spec/decision/contract) does not fit buzz-workflow, since no ADR/NIP/spec document is the crate's target; `architecture-flows-workflow-execution` is a sibling documentation node, not a normative target the crate "realizes." This plan resolves it as `references` (the type's own directionality — "source cites target as supporting context" — fits a corpus node pointing at a closely related node without claiming realization) but names this as a judgment call for review, not a settled fact.
LEFT OUT: No claim about whether `buzz-admin`'s `Cargo.toml` dependency on `buzz-workflow` reflects actual runtime usage — grepping `crates/buzz-admin/src` for `buzz_workflow` found no call sites, so the node states the Cargo.toml dependency as a fact without speculating about why it exists. No attempt to document the `kind:30620` workflow-definition-save command's own validation/authority path in depth (owned by `buzz-relay/src/handlers/command_executor.rs`, out of this crate's implementation surface) or the approval-resume flow (WF-08, not implemented — already covered as a gap in `architecture-flows-workflow-execution`, not restated here). No second corpus document for any newly discovered second concept — none was found; the crate is one independently maintainable idea.
