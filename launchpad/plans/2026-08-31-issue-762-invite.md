# Plan: issue #762 — document capabilities/invites/invite.md

## ALREADY TRUE

- Worktree `__worktrees/task-762-invite` / branch `task/762-invite` exists off
  `origin/launchpad` at `cad6c375fdcc590158c1456c9fc7875f0f84a844` (a prior attempt
  crashed before any commit landed; working tree was clean, reused as-is).
- `launchpad/docs/corpus/templates/capability.md` exists and is the template to use
  (`type: capabilities`).
- No `launchpad/docs/corpus/capabilities/**` node exists yet on `origin/launchpad` —
  this is the first capability instance node. Per `AGENTS.md`, no `relationships` may
  be declared (no target id resolves on the merge base).
- Codebase has two distinct "invite" mechanisms, confirmed by direct file reads:
  1. NIP-29 `kind:9009` (`KIND_NIP29_CREATE_INVITE`) — defined in
     `crates/buzz-core/src/kind.rs:346-347`, routed but a no-op stub in
     `crates/buzz-relay/src/handlers/side_effects.rs:207-213` ("deferred to future
     phase").
  2. The actual shipped capability — a bespoke HTTP mint/claim API
     (`crates/buzz-relay/src/api/invites.rs`, routes in
     `crates/buzz-relay/src/router.rs:106-123`), backed by v1 stateless HMAC codes
     (`crates/buzz-relay/src/invite_token.rs`) and v2 durable, use-limited codes
     (`crates/buzz-core/src/invite.rs`, `crates/buzz-db/src/store/relay_invite.rs`,
     table `relay_invites` in `migrations/0025_relay_invites.sql`).
- Desktop (`desktop/src/features/community-members/ui/InviteLinkSection.tsx`,
  `CommunityInviteDialog.tsx`, `desktop/src/features/onboarding/ui/InviteRedeemForm.tsx`)
  and mobile (`mobile/lib/features/invites/invite_create_page.dart`,
  `invite_join_sheet.dart`) both expose create + redeem UI for this same HTTP capability.
- A separate, unrelated mechanism — private-channel member-add
  (`KIND_NIP29_PUT_USER`/9000, `RELAY_ADMIN_ADD_MEMBER`/9030) — is referred to as
  "invite" in `VISION.md:38-44` and in some test names
  (`test_private_channel_any_member_can_invite`) but is channel-scoped membership,
  not the community-level invite-link capability this task documents. This is a
  boundary to state explicitly, not to fold in.
- Siblings under the same `capabilities/invites/` family, not yet merged, so not
  duplicated here: #759 (invite-expiry, flow), #760 (invite-redemption, flow), #761
  (invite-token, the data-entity/opaque-code contract).

## STEP 1 — Draft `launchpad/docs/corpus/capabilities/invites/invite.md`

Using the capability template skeleton: capability statement, maturity (shipped, cited
to PR #1668 / CHANGELOG and the live route table), boundary (excludes: how it's built
— architecture; the interface contract — HTTP route table already cited as evidence,
not a separate interface node since none exists yet; step-by-step flows — #759/#760;
the invite-token data entity — #761; the unrelated private-channel member-add
mechanism), relationships (none — no target resolves on `origin/launchpad`), scope and
omissions.

Evidence: every substantive claim cited to a file already opened and confirmed above
(kind.rs, side_effects.rs, invites.rs, router.rs, invite_token.rs, invite.rs,
relay_invite.rs, migration 0025, VISION.md, CHANGELOG.md, desktop/mobile UI files,
e2e test names). No relationship edges.

**Done when:** file exists, front matter matches `node.schema.json`, body carries all
five required template sections plus every DoD bullet from issue #762.

## STEP 2 — Validate

Run `python3 launchpad/project-intelligence/corpus/validate.py` from repo root.
**Done when:** exit 0, and the pre-existing 21 FAIL baseline (issue #1951) is
unchanged — this node adds zero new FAIL entries.

## STEP 3 — Earn the commit gate and commit

Run, alone: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`.
**Done when:** `OK`. Then, in a separate call, `git add` the new doc + this plan file
and `git commit -s`. No push, no PR.

## GATES

- `validate.py` exits 0, zero new FAIL vs. the #1951 baseline.
- `unittest discover` on corpus tests prints `OK`, run as the sole command in its own
  tool call, before staging/committing.
- Every FACT/INFERENCE evidence entry cites a source actually opened in this session.

## BUDGET

3 steps, single file, one commit. No code changes, no push, no PR.

## OPEN

- Whether a dedicated interface node for the `/api/invites` HTTP surface gets authored
  later (per the template's "may reference an interface node" guidance) — left for
  whichever future task owns `#1342`'s interface template family; this node's boundary
  section says so and cites the route table itself as evidence in the meantime.

## LEFT OUT

- Any relationship edges (no sibling/target node exists on `origin/launchpad` yet).
- Rewriting or duplicating #759/#760/#761's canonical content — cross-referenced by
  id-shape only where they don't yet resolve, named in prose in the boundary/scope
  sections instead.
- Any runtime/code change to the invite system, including the deferred kind:9009 stub.
