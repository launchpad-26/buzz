# Issue #1115 — layers/lifecycle/background-workers.md

Stated size: none given — the task template has no Size field  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/templates/concept.md` (merged) is the template this node's DoD checklist (one-sentence definition, boundaries/non-goals, links to related concepts, examples-only-to-clarify) matches, not the flow.md sequence-diagram shape sibling nodes `layers/lifecycle/graceful-shutdown.md` and `layers/lifecycle/startup.md` used. `launchpad/docs/corpus/layers/lifecycle/background-workers.md` does not exist yet, and no `layers/lifecycle/*` node is merged on `origin/launchpad` (the sibling docs are unmerged sibling-task branches).

STEP 1  [independent] Gather evidence: identify Buzz's real background-worker code — `crates/buzz-relay/src/main.rs`'s spawned periodic tasks (NIP-43 membership reconciler, ephemeral-channel reaper, NIP-ER reminder scheduler, pool-metrics poller, leader-elected usage-metrics tick), `crates/buzz-relay/src/storage_sweep.rs` (hourly single-flight S3 sweep), `crates/buzz-db/src/replica_fence.rs::run_probe` (500ms freshness probe), and `crates/buzz-workflow/src/lib.rs::WorkflowEngine::run` (60s cron/interval tick) — already read directly in this session. Also confirm two things that turned out to be **non**-examples: `crates/buzz-backend-kubernetes/src/{gc,reconcile}.rs` run synchronously inside one `deploy()` call bounded by a 600s operation deadline, not as a standing timer loop, so despite the "GC"/"reconcile" naming they are out of scope; and `BUZZ_RECONCILE_CHANNELS`'s dev/CI task (`main.rs:583-624`) is a bounded 24-attempt retry-until-give-up, not a persistent worker. Note which workers are cancellation-aware (`community_revalidator` via `run_periodic_until_cancelled` + `CancellationToken`) versus fire-and-forget (every other one listed, confirmed by their `tokio::spawn` call sites retaining no `JoinHandle`). ← RUNS HERE
  done when: every cited symbol/line range has actually been opened and read (not assumed from a grep hit), and the fire-and-forget-vs-cancellable split is confirmed by reading each spawn site rather than inferred from one example.

STEP 2  [needs 1] Write front matter (schema-valid: id `layers-lifecycle-background-workers`, type `layers` — matching the sibling `layers/lifecycle/*` precedent's departure from `concept.md`'s own `type: architecture` skeleton default, for the same Feature-#611-taxonomy reasoning those siblings recorded — status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, `relationships: [references: architecture-containers-relay, references: architecture-flows-workflow-execution]` — both ids confirmed present on `origin/launchpad` via `git ls-tree`) and the body per `templates/concept.md`'s required sections: Definition (what makes a spawned task a "background worker" here, plus the GC/reconcile naming-trap disambiguation from STEP 1), a Mermaid visual aid, Use cases (why a developer adding a new periodic task, an operator tuning its interval env var, and a reviewer checking its shutdown behavior each need this), a Comparison table across the cited workers (interval, cross-pod coordination strategy, cancellation-awareness), Related resources (the two `relationships` edges plus explicit pointers to sibling docs #1116/#1117/#1118/#1119/#1120 for what they own instead), and the mandatory Scope and omissions section.
  done when: the file exists at `launchpad/docs/corpus/layers/lifecycle/background-workers.md`, every required `concept.md` section is present, and every substantive claim has a matching `evidence` entry classified FACT/INFERENCE/TEAM_KNOWLEDGE with real citations.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Then run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole prior command to earn the verification stamp.
  done when: `validate.py` prints a line ending `PASS` and exits 0, and the unittest run prints `OK`.

STEP 4  [needs 3] Commit the plan + document in a separate `git commit -s` call. No push, no PR — this task's output is cherry-picked onto one shared batch branch for Feature #611 by the batch owner.
  done when: `git show --stat HEAD` shows exactly the corpus document and this plan file, and the commit carries a `Signed-off-by` trailer.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 as the sole prior command before commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review of the combined PR — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to ~6 already-read source files (`buzz-relay/src/main.rs`, `buzz-relay/src/storage_sweep.rs`, `buzz-db/src/replica_fence.rs`, `buzz-workflow/src/lib.rs`, plus the two non-example files `buzz-backend-kubernetes/src/{gc,reconcile}.rs`).

OPEN: Whether `architecture-flows-workflow-execution`'s existing, detailed cron-loop narration (its own evidence entry already states "The cron/interval path is a `WorkflowEngine::run()` background loop ticking every 60 seconds...") means this node should describe the workflow cron loop only briefly and defer depth there via the `references` edge, versus narrating it independently — this plan takes the former (avoid duplicating that node's canonical content per `AGENTS.md`'s "links... without duplicating canonical content").

LEFT OUT: No relationships to sibling `layers/lifecycle/*` nodes (cancellation #1116, concurrency #1117, graceful-shutdown #1118, resource-cleanup #1119, startup #1120) — none are merged on `origin/launchpad` yet, so none are valid `relationships.target`s; they are named in prose in the Scope-and-omissions table instead, as `graceful-shutdown.md` already does for its own siblings. No attempt to document `buzz-acp`'s presence-heartbeat task or pool-wake/respawn machinery — those are `buzz-acp` process-lifecycle concerns already narrated by `graceful-shutdown.md`, not this node's subject. No fix for the fire-and-forget-vs-cancellable inconsistency found in STEP 1 — reported as a documented fact, not remediated (that would be a separate, linked implementation issue per #1115's own "Out of scope").
