# Issue #1176 — layers/security/security-invariants.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/invariant.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/architecture/principles/fail-closed-boundaries.md` (id `architecture-principles-fail-closed-boundaries`) and `launchpad/docs/corpus/architecture/principles/community-is-security-boundary.md` (id `architecture-principles-community-is-security-boundary`) are also merged there; `launchpad/docs/corpus/layers/security/security-invariants.md` does not exist yet, on `origin/launchpad` or in this worktree.

STEP 1  Gather evidence: read `docs/spec/MultiTenantRelay.tla`'s `Safety` property (the 12-conjunct invariant list plus `TypeOK`), the `buzz-conformance` crate (`lib.rs`, `checker.rs`, `LIMITS.md`) that independently replay-checks real traces against that same spec, and the `EmitGuard`/`state.rs` wiring in `buzz-relay` that arms it on the ingest/auth/read boundary (and defaults to a zero-cost `NoopTracer` in production). Confirm via `grep -rl check_trace crates/buzz-relay crates/buzz-test-client` that no call site yet replays a live relay's real traces through the checker — `LIMITS.md` names this gap itself as "the next ratchet." Enumerate the other `layers/security/*` sibling tasks (#1166 signature, #1135 audit-log, #1179 tenancy-boundary, #1169/#1170/#1172/#1173/#1175/#1178 boundary/protection topics, #1177/#1180/#1181/#1182/#1174 model/risk docs) so this node's *Boundary* section names real neighbors rather than an invented list. ← RUNS HERE

STEP 2  [needs 1] Write front matter (id `layers-security-security-invariants`, type `layers` per the issue's own impacted-component path, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, `relationships: references` toward both merged principle nodes above — each formalizes a subset of the Safety conjuncts this node names) against `templates/invariant.md`'s required sections: Invariant statement, Scope, Enforcement today (naming the weakest true tier honestly — test-enforced checker mechanics, but convention-and-review-only for arming new call sites, and not-yet-integration-tested against live traffic), Consequence of violation (what actually happens today: silent, since production runs `NoopTracer`), Boundary (the dense sibling list from STEP 1), Relationships, Scope and omissions.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the commit-gate stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here; the PR body says so explicitly.

BUDGET: small — one document, no code changes; evidence gathering scoped to the TLA+ `Safety` block, the `buzz-conformance` crate, and the wiring call sites in `buzz-relay`'s `ingest.rs`/`req.rs`/`state.rs`.

OPEN: Whether the umbrella claim ("Buzz's tenant/security invariants are the named `Safety` conjunction, and an independent runtime checker exists to verify real traces against it") is itself the right scope for a node titled *plural* "security-invariants," given `node.schema.json`'s enum has no dedicated "invariant-family index" shape and `AGENTS.md`'s own rule is one node is one idea — resolved here by treating the single idea as "these invariants are formally named and mechanically checkable, not merely asserted," with the *list* of named conjuncts as supporting detail rather than N separate claims, and the topic-specific siblings (#1166, #1135, #1179, etc.) owning each conjunct's own deep-dive.

LEFT OUT: No relationships to #1108 (identity-invariants), #1181/#1182 (trust-boundaries/trust-model), or any other `layers/security/*` sibling — none are merged on `origin/launchpad` yet, so none is a legitimate target; each is named in *Boundary* by issue number instead. No re-running of TLC against the spec or of the `#[ignore]`-gated live-relay integration replay (it does not exist yet) — this node reports what the crate's own `LIMITS.md` already says about that gap, not a fresh empirical run. No attempt to close the "next ratchet" gap itself — that is implementation work, not this documentation task.
