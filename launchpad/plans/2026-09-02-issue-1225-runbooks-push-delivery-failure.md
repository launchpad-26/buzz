# Issue #1225 — operations/runbooks/push-delivery-failure.md

Stated size: one hand-authored corpus document (issue body: "Create ... as the single canonical runbook node for push delivery failure")  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/runbook.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/operations/` does not exist anywhere in this worktree (`ls` on it fails), so `launchpad/docs/corpus/operations/runbooks/push-delivery-failure.md` does not exist yet. `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` confirms 204 nodes are merged there, including `capabilities/notifications/{push-notification,apns,endpoint-installation,push-lease,notification-preferences}.md`, `architecture/containers/push-gateway.md`, `architecture/flows/push-notification.md`, and `layers/observability/{metrics,prometheus,health-checks}.md` — all legitimate `relationships` targets for this node.

STEP 1 [independent] Gather evidence on the real push-delivery-failure surface: read `crates/buzz-push-gateway/src/{apns.rs,http.rs,metrics.rs,config.rs,main.rs}`, `crates/buzz-relay/src/push_runtime.rs` (matcher/delivery-worker retry+backoff, `MAX_ATTEMPTS`, `retry_or_fail`), `crates/buzz-relay/src/config.rs` (`BUZZ_PUSH_ENABLED`, `BUZZ_PUSH_GATEWAY_DELIVERY_URL`), `deploy/charts/buzz-push-gateway/templates/prometheusrule.yaml` (the five alerts, disabled by default), `docs/push-gateway-deployment.md` (the human-facing deployment reference this runbook should not duplicate but may cite), and `.github/workflows/docker.yml` (confirm the `github.repository == 'block/buzz'` publish gate). Record `git rev-parse HEAD`. ← RUNS HERE
done when: the revision is recorded and every file above has been opened (not skimmed via search) at least once.

STEP 2 [needs 1] Write front matter (schema-valid: id `operations-runbooks-push-delivery-failure`, type `operations`, status `draft`, origin `launchpad`, audiences `[operator, developer, reviewer]`, one `relationships` entry `{type: implements, target: corpus-template-runbook}` plus `references` entries to `capabilities-notifications-push-notification`, `architecture-containers-push-gateway`, `architecture-flows-push-notification`, and `layers-observability-prometheus` — all confirmed merged on `origin/launchpad` in ALREADY TRUE) and the body per `templates/runbook.md`'s five required sections (trigger, severity/impact, diagnosis, mitigation/resolution, escalation) plus scope-and-omissions carrying both what this node does not cover and what was expected but could not be verified — explicitly naming that this fork does not operate `push.buzz.xyz` (the gateway's publish workflow is gated to `block/buzz`) and what that means for who can action this runbook here.
done when: the file exists at `launchpad/docs/corpus/operations/runbooks/push-delivery-failure.md` with all five required sections plus scope-and-omissions present, and every evidence entry cites a file opened in STEP 1.

STEP 3 [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Confirm the file stays under the repo-wide 1000-line gate.
done when: the validator prints exit status 0 and `wc -l` on the new file reports fewer than 1000 lines.

STEP 4 [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own Bash call to earn the verify-gate stamp, confirm `OK`, then commit the plan + document with `git commit -s` in a separate call. Do not push and do not open a PR — this batch's orchestrator integrates all sibling documents into one Feature PR later.
done when: the test run prints `OK` and `git log -1` on the worktree branch shows a signed-off commit containing exactly the plan file and the new document.

PARALLEL: none — single file, single task, no dependency on sibling batch agents' output.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must print `OK` before commit, run un-piped and un-chained so the verify-gate hook can stamp it. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later review — not run here.

BUDGET: small — one document, no code changes; evidence gathering scoped to ~7 named source files plus one Helm template and one workflow file, all already enumerated above.

OPEN: Whether a runbook may itself trigger `buzz-workflow` automation rather than only describing manual operator steps is unresolved by `templates/runbook.md` itself and is not decided here. Whether this fork should carry its own operational contact/escalation path for a component (`buzz-push-gateway`) whose production image only `block/buzz` publishes is a process question for the batch owner, not something this single document can settle — it is reported honestly in the body instead.

LEFT OUT: No relay-side `buzz_push_*` metric alert rules exist in this repository (only the gateway's Helm chart ships a `PrometheusRule`) — this runbook does not invent alerting for the relay side that the repository does not implement. No claim about physical-device APNs behavior beyond what `crates/buzz-push-gateway/src/apns.rs`'s `classify` function and its tests establish. No edits to `docs/push-gateway-deployment.md` or any code — this is documentation-only, and any second concept discovered (e.g. a missing relay-side alerting gap) is reported rather than folded in.
