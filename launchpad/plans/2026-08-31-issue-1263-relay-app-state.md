# Issue #1263 — platforms/relay/app-state.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`. No `platforms/` directory exists anywhere under `launchpad/docs/corpus/` on `origin/launchpad` yet — this is the first node under that surface. `launchpad/docs/corpus/platforms/relay/app-state.md` does not exist. `launchpad/docs/corpus/templates/component.md` (unmerged as a template but present in this worktree) is the closest-fitting shape for a single-component node, but its own `INFERENCE` recommends `type: implementation`; the batch has instead settled on `type: platforms` for everything under `platforms/**` since `component.md` is itself unmerged and `platforms` is node.schema.json's own named surface for this content.

STEP 1  Gather evidence: read `crates/buzz-relay/src/state.rs` in full — the `AppState` struct (fields, doc comments), `AppState::new` (construction order, defaults), and its inherent methods (`mark_local_event`, `is_member_cached`, `invalidate_*`, `disconnect_pubkey_clusterwide`, `disconnect_community_clusterwide`, `revalidate_live_communities`, `channel_visibility_cached`, `mesh`). Confirm the `#[cfg(test)] mod tests` block's `test_state()` helper as evidence AppState is exercised by in-crate unit tests. ← RUNS HERE

STEP 2  [needs 1] Trace wiring: `AppState::new(...)` call site in `crates/buzz-relay/src/main.rs`, the `Arc::new(app_state)` wrap, `build_router(state: Arc<AppState>)` in `crates/buzz-relay/src/router.rs` and its `.with_state(...)` call sites, and confirm the `axum::extract::State<Arc<AppState>>` extractor pattern via `grep -rl "State<Arc<AppState>>" crates/buzz-relay/src/` (12 files) plus one concrete handler (`api/invites.rs`). Confirm the WebSocket path takes `Arc<AppState>` as a plain function argument (`connection::handle_connection`) rather than the axum extractor, since the upgrade handler resolves it once via `State` and threads the clone through.

STEP 3  [needs 2] Write front matter (schema-valid: id `platforms-relay-app-state`, type `platforms`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, no `relationships` — no existing merged node under `origin/launchpad`'s corpus tree is a legitimate target, see OPEN) and the body: responsibility, the full field inventory grouped by concern (config/db, pubsub/auth/search, connection registries, semaphores, caches, media/git storage, rate limiters, mesh), construction/wiring path, public interface (inherent methods), and an explicit boundary/scope-and-omissions section.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until it reports the same pre-existing FAIL count as a clean checkout with this file removed (diff-by-stash check).

STEP 5  [needs 4] Run the corpus unittest suite as the sole content of one Bash call to earn the verification stamp, then commit the plan + document in a separate call. Stop — no push, no PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` introduces zero new FAIL lines versus the file-removed baseline. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` as the sole content of its own Bash call, immediately before the commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration review — not run here.

OPEN: The issue's DoD asks for "typed relationships appropriate to the node," but at the recorded revision `origin/launchpad`'s corpus tree carries no node about Buzz platform/component subject matter to target — only meta/governance nodes about the corpus itself. `relationships` is therefore omitted, per `node.schema.json`'s own rule that a target naming an id no loaded node carries is a hard validation error, and per `AGENTS.md`'s instruction to check the merge-base tree rather than the author's own worktree before declaring an edge. `type: platforms` is an inferred convention (no platforms-specific template is merged yet); `component.md`'s own unmerged template recommends `type: implementation` instead, so this choice may need reconciling once a platforms template lands.

LEFT OUT: No relationships to a hypothetical `platforms-relay` container-level node or to `Config`/`Db`/`PubSubManager` component nodes — none exist in the merged corpus yet. No attempt to document `Config`'s own fields, the mesh subsystem's internals, or the git-object-storage protocol in depth — each is named only as a dependency/collaborator with a pointer to its own source, not restated here. No new template creation — `component.md` stays unmerged and is read only as a shape reference, not adopted as this node's declared template.
