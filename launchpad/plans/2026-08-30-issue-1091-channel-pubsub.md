# Issue #1091 — layers/data/redis/channel-pubsub.md

Stated size: not stated in the issue (a `corpus-plan:v2` single-document task) -> cap: 5 steps.

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/standards/{taxonomy,naming,evidence,linking}.md`, `launchpad/docs/corpus/templates/{data-entity,datastore,reference}.md` and `launchpad/docs/corpus/architecture/containers/redis.md` (id `architecture-containers-redis`) are all merged on `origin/launchpad`. `launchpad/docs/corpus/layers/` does not exist yet — this is the first `layers/data/redis/*` node in the corpus. No `layers/data/redis/{connection-pool,dedicated-pubsub-connection,key-namespacing}.md` sibling exists on `origin/launchpad` (batch precedent: they land later in this same overnight run), so no relationship may target them.

STEP 1  [independent] Gather evidence: read `crates/buzz-pubsub/src/{lib.rs,topic.rs,publisher.rs,subscriber.rs}` in full for the channel-naming scheme (`buzz:{community}:channel:{uuid}` / `buzz:{community}:global`), the `PubSubManager::publish_event`/`retain_topic`/`release_topic` API, the dynamic-subscription refcount+debounce mechanism, and the reconnect-backoff loop. Cross-check call sites in `crates/buzz-relay/src/{main.rs,state.rs,handlers/{req.rs,event.rs,close.rs,side_effects.rs},connection.rs}` for: subscriber-loop wiring, the local dedup mechanism (`AppState.local_event_ids`, a 60s-TTL moka cache keyed `(community_id, event_id)`, claimed before `PUBLISH` and invalidated on publish failure or on receipt), `filter_fanout_by_access` as the real author/tenant boundary (the Redis topic is a routing label only), and `retain_topic`/`release_topic` call sites on REQ/CLOSE/disconnect. Already-merged `architecture/containers/redis.md` corroborates several of these claims independently — reuse its citations where they overlap rather than re-deriving from scratch. ← RUNS HERE
done when: every file above has been read in full and the channel-naming scheme, refcount+debounce mechanism, dedup mechanism and reconnect backoff are each traced to a specific line range or symbol.

STEP 2  [needs 1] Choose the template. The issue's DoD bullets (authoritative/derived/cache/transport; owned data, access patterns, lifecycle/retention, consistency; tenancy/security boundary and failure behavior; link don't copy DDL) describe one narrow mechanism inside Redis, not the whole instance (already owned by `architecture-containers-redis`, `type: architecture`) and not a domain entity with identity/attributes (`templates/data-entity.md`'s shape). `templates/reference.md` — Diátaxis Reference form, "technical descriptions of the machinery and how to operate it" — fits: a structured catalogue of topic names, the PUBLISH/SUBSCRIBE mechanism, and the lifecycle/tenancy/failure facts the DoD asks for, cited to code rather than restated as prose theory. Use `templates/reference.md`'s required sections (reference description, structured entries, optional commands, boundary statement, relationships, scope and omissions).
done when: the template choice and its rejection of `data-entity`/`datastore` is written down as a one-paragraph decision, ready to paste into the node's own evidence.

STEP 3  [needs 2] Write `launchpad/docs/corpus/layers/data/redis/channel-pubsub.md`. Front matter: `id: layers-data-redis-channel-pubsub` (path-mirrored, matching `architecture-containers-redis`'s own precedent — `templates/reference.md`'s own guidance on `type` tracking corpus *surface*, not documentation form); `type: layers` (Feature-level override of what a real `reference`-template instance would otherwise pick, per this batch's own precedent for every `layers/data/...` node — disclosed in the ledger per `standards/taxonomy.md`'s "say so in the node's own scope-and-omissions section" rule); `status: draft`; `origin: launchpad`; `audiences: [agent, developer, operator]` (matches `architecture-containers-redis`, which shares an operator-relevant failure-behavior/reconnect surface). One `relationships` entry — `part-of` → `architecture-containers-redis` (merged, resolves; `channel-pubsub` is one of that node's own five catalogued jobs) — required by `standards/linking.md` MUST 6 once the body names that node in prose. Body: one H1 (`standards/naming.md` MUST 4), Reference description, a structured-entries table of the two topic shapes, a Commands table (PUBLISH/SUBSCRIBE/UNSUBSCRIBE), lifecycle/consistency/tenancy/failure-behavior prose answering the DoD bullets directly, a Boundary section naming what is deferred to `architecture-containers-redis` and to the not-yet-existing `connection-pool`/`dedicated-pubsub-connection`/`key-namespacing` siblings, and Scope and omissions. "Front matter" and "provenance ledger" spelled per `standards/naming.md` MUST 5/6.
done when: the file exists at the target path, every DoD bullet in the issue body is answered by a specific section, and every substantive claim in the body has a matching `evidence` entry.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.
done when: the command's exit status is 0.

STEP 5  [needs 4] Run the corpus unittest suite as the sole command in its own call to earn the verify-gate stamp, then commit the plan + document in a separate call. No push, no PR — a later orchestration step bundles this branch with its batch siblings.
done when: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` reports `OK`, and `git log -1` on this branch shows the new commit with both files staged.

PARALLEL: none — single file, single task, no dependency on the three later `layers/data/redis/*` siblings in this batch.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here, per the corpus-batch-author skill's own deliberate deferral.

BUDGET: small — one document, no code changes, evidence gathering scoped to 4 `buzz-pubsub` source files plus ~6 call-site files in `buzz-relay`, all already read once during planning.

OPEN: `type: layers` is a Feature-level override, not something this node's own reasoning would independently pick from `node.schema.json`'s enum (a real `reference`-template instance would otherwise track its subject's corpus *surface*; `templates/reference.md` itself gives no `layers/data` example) — disclosed as an override in the body's evidence, per this task's own dispatch brief and `standards/taxonomy.md`'s disclosure rule, not silently applied. Whether `part-of` (this node is one job of the Redis container) or `references` (looser, no ownership implied) is the better-fitting edge to `architecture-containers-redis` is a judgement call; `part-of` is chosen to match `templates/datastore.md`'s own precedent for the identical "zoom into one job of a container" shape, even though this node's `type` is `layers`, not `architecture`.

LEFT OUT: No relationship to the three later `layers/data/redis/*` siblings in this batch (`connection-pool` #1092, `dedicated-pubsub-connection` #1093, `key-namespacing` #1094) or to the further siblings in a later batch (presence, reconnect-behavior, role, ttl-policy, typing-indicators) — none merged yet. No restatement of `architecture-containers-redis`'s full five-job inventory, its deployment/security-implications sections, or its Scope-and-omissions gaps (Terraform provisioning, typing-indicator status) — linked, not duplicated. No description of `deadpool_redis::Pool` connection-pool sizing/limits (owned by the future `connection-pool` sibling) or of why the subscriber loop uses a dedicated non-pooled connection (owned by the future `dedicated-pubsub-connection` sibling) beyond naming that boundary. No push, no PR — bundling is a later orchestration step's job.
