# Issue #877 — events/kinds/kind-40002-stream-message-v2.md

Stated size: issue #877 names no explicit Size line (single hand-authored document, one event kind) -> cap: 4 steps.

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/event-kind.md` (id `corpus-template-event-kind`) are merged on `origin/launchpad` at HEAD `a8b5021efb92264e724366d08b47b2a3839eb90a`. `launchpad/docs/corpus/events/kinds/kind-40002-stream-message-v2.md` does not exist on `origin/launchpad` (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` lists no `events/` subtree at all). Five sibling event-kind docs (#872-#876) exist only on their own unmerged `task/87{2..6}-*` branches, not on `origin/launchpad` — they are read here for house style only, never as `relationships` targets, since none of them resolves in the corpus this node will actually be validated against. `crates/buzz-core/src/kind.rs` defines `KIND_STREAM_MESSAGE_V2: u32 = 40002`; it is outside NIP-01's addressable range (30000-39999) and outside every one of `kind.rs`'s own classification helpers (`is_replaceable`, `is_parameterized_replaceable`, `is_ephemeral` all return false for 40002), so ingest routes it through the generic "regular, persistent, thread-aware" storage branch. No corpus node currently references kind 40002.

STEP 1  [independent] Gather evidence: read `crates/buzz-core/src/kind.rs` (constant, doc comment, classification helpers, gating sets `AUTHOR_ONLY_KINDS`/`P_GATED_KINDS`/`SHARED_GATED_KINDS`/`RESULT_GATED_KINDS`/`is_relay_only_kind` — confirm 40002 is absent from all five); `crates/buzz-relay/src/handlers/ingest.rs` (`required_scope_for_kind`, `requires_h_channel_scope`, `is_global_only_kind`, the storage-branch dispatch, `resolve_nip10_thread_meta`, the 256KB `MAX_EVENT_CONTENT_BYTES` cap); `crates/buzz-core/src/nip10.rs` (e-tag root/reply marker shape); `crates/buzz-db/src/store/feed.rs`, `crates/buzz-db/src/store/push.rs`, and `migrations/0008_fresh_install_search_allowlist.sql` (mentions/push/search treatment); `crates/buzz-sdk/src/builders.rs` and `crates/buzz-cli/src/commands/messages.rs` (confirm no producer builds a kind-40002 event — `build_message` emits kind 9); `desktop/src/shared/constants/kinds.ts`, `desktop/src-tauri/src/commands/messages.rs`, and `mobile/lib/shared/relay/nostr_models.dart` (confirm 40002 is read/displayed everywhere kind 9 is, but never produced); `git log -p -S KIND_STREAM_MESSAGE_V2 -- crates/buzz-core/src/kind.rs` for the numbering history (commit `124047ef8`, kind 9's own renumbering from 40001, left 40002 untouched). Record the repository revision (`git rev-parse HEAD`).
    done when: every claim planned for STEP 2's evidence ledger has a specific file/line or command output recorded, and it is confirmed no in-repo producer (CLI, desktop, mobile) ever constructs a kind-40002 event outside tests.

STEP 2  [needs 1] Write `launchpad/docs/corpus/events/kinds/kind-40002-stream-message-v2.md`: front matter with `id: events-kinds-kind-40002-stream-message-v2`, `type: interfaces-events` (per `corpus-template-event-kind`'s own stated guidance for a realized instance), `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, a commit-citation FACT for the recorded revision, one evidence entry per substantive claim (FACT for anything read directly from `kind.rs`/`ingest.rs`/`nip10.rs`/`feed.rs`/`push.rs`/the migration SQL/the NIP text; INFERENCE with `confidence` for reasoned claims such as "kind 40002 shares kind 9's tag vocabulary by omission of any kind-specific validator"; no TEAM_KNOWLEDGE expected), and `relationships: [{type: implements, target: corpus-template-event-kind}]` only. Body follows the template's nine required sections (title/identity, referenced NIP, range/classification, tag shape, content semantics, access control/storage, worked example, versioning, relationships) plus a *Scope and omissions* section naming the no-producer gap and the NIP-01 range mismatch (40002 falls above NIP-01's own 30000-39999 addressable ceiling) as gaps, not resolved product decisions. ← RUNS HERE
    done when: the file exists, front matter parses as valid YAML, every one of issue #877's Definition-of-done bullets is addressed by a specific section, and no second hand-authored corpus file is created or materially edited.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root. Compare its error set against the 21 known pre-existing failures on `origin/launchpad` (issue #1951: the five `architecture-*` nodes and the `corpus-template-*` files) — fix anything attributable to the new node and re-run until the new node introduces zero additional FAIL lines.
    done when: the validator's output contains the same 21 pre-existing FAIL lines (or fewer, never more) and zero FAIL lines reference `kind-40002-stream-message-v2.md`.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call and confirm `OK`, then in a separate call `git add` the doc and this plan and commit with `git commit -s`.
    done when: the unittest run prints `OK` with no failures/errors, and `git log -1` on the worktree branch shows the new commit containing exactly the two intended files.

PARALLEL: none — single document, single task, strictly sequential (evidence before drafting, drafting before validation, validation before commit).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must introduce zero new FAIL lines beyond the 21 pre-existing ones tracked by #1951. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit gate will accept the change (per the task's own instruction not to bypass a refused commit with `--no-verify` or a hand-edited stamp). `review-adjudicate` and cross-model final review are deferred to the batch owner, per corpus-batch-author precedent — not run in this task.

BUDGET: small — one document, no code changes, evidence gathering scoped to roughly a dozen already-identified files (`kind.rs`, `ingest.rs`, `nip10.rs`, `feed.rs`, `push.rs`, one migration file, `builders.rs`, `messages.rs` in both buzz-cli and desktop-tauri, `kinds.ts`, `nostr_models.dart`) plus one git-log query and one NIP-01 text fetch already completed during issue research.

OPEN: Whether kind 40002 (`KIND_STREAM_MESSAGE_V2`) is a live, intentionally-reserved "richer message format" still awaiting its own producer, or a historical artifact from the pre-NIP-29-native-compat numbering scheme that every read path now humors defensively — no commit message, PR, or code comment in this repository states which, and this task does not decide it. The node reports the observable fact (defined, universally read/classified/search-indexed/push-eligible alongside kind 9, zero producers found) and leaves the "why" as a named gap. Whether `docs/nips/NIP-XX.md` is expected for this kind is also unresolved corpus-wide (per `corpus-template-event-kind`'s own scope table) and is not decided here.

LEFT OUT: No relationships to the five sibling event-kind docs (#872-#876) — none are merged on `origin/launchpad`, so none is a legitimate target; the natural future edges (a `references` to kind 9's own node, and possibly a `depends-on`-style link from kind 40003's edit-kind node back to this one) are left for whichever of those nodes merges second. No attempt to build or fix a kind-40002 producer, resolve the NIP-01 range question, or update `kind.rs`'s comment — those are implementation/spec changes, not this corpus-authoring task. No changes to any generated corpus index.
