Issue #1140 — task: document layers/observability/metrics.md
Parent PRD #611. No `Size` line on the issue; the batch-run brief caps this single-document
task at 5 steps, so that is used as the declared cap rather than guessed per-step.

Stated size: no Size line on issue -> cap: 5 steps (batch-run instruction)

ALREADY TRUE
  `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are
  merged on `origin/launchpad` (HEAD ed133f4c5dbd546a67d963f11ffa630a4513b228), and
  `launchpad/docs/corpus/layers/observability/metrics.md` does not exist yet — confirmed with
  `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` (no `layers/` path at
  all exists on `origin/launchpad` yet). Sibling nodes `layers-observability-logging` (#1139)
  and `layers-observability-liveness` (#1138) exist only as unmerged commits on sibling
  worktree branches (`task/1139-logging`, `task/1138-observability-liveness`), not on
  `origin/launchpad`, so no `relationships` target from this task resolves against the actual
  merge base; #1142 (prometheus) is likewise an open, unmerged sibling task.

STEP 1  [independent]  Gather evidence for what Buzz measures as "metrics" (counters, gauges,
        histograms as a category) and why, at the conceptual/cross-cutting level:
        `crates/buzz-relay/src/metrics.rs` (the `metrics`-crate facade, `PrometheusBuilder`
        install, per-metric bucket configuration, the `track_metrics` HTTP middleware and its
        cardinality-safety choices: matched-route-not-raw-URI, validated `caller` header),
        `crates/buzz-relay/src/main.rs` lines ~50-92 (`EmissionScope`/
        `BUZZ_USAGE_METRICS_PER_COMMUNITY`, the Datadog per-series cost rationale) and its
        `metrics::install(...)` call site (~line 170) plus the surrounding periodic gauge
        emission for pool/usage/community stats, `crates/buzz-relay/src/config.rs` (~line 823,
        `BUZZ_METRICS_PORT` default 9102), `crates/buzz-push-gateway/src/metrics.rs` (a second,
        differently-shaped installation: no public HTTP listener, served from the private
        health router instead), `crates/buzz-db/src/runtime/observability.rs` and
        `crates/buzz-db/src/store/usage.rs` (bounded-cardinality label design via closed Rust
        enums, no request-scoped identifiers as labels), a representative cross-section of
        `metrics::counter!`/`gauge!`/`histogram!` call sites already found via repo-wide grep
        (auth, git hydration/pack-cache, push delivery, storage sweep, websocket connections),
        and `ARCHITECTURE.md:780` (docker-compose Prometheus server on :9090 scraping the
        relay's :9102 exposition port). Explicitly do NOT go deep on Prometheus wire/exposition
        format mechanics — that is #1142's territory; note the boundary, don't document it.
        done when: each source above has been opened directly and its relevant lines/behavior
        recorded, not assumed from a filename or docstring alone.

STEP 2  [needs 1]  ← RUNS HERE  Write the node: front matter (`id: layers-observability-metrics`,
        `type: layers`, `status: draft`, `origin: launchpad`, `audiences`, an `evidence` ledger
        with one entry per claim from STEP 1 classified FACT/INFERENCE/TEAM_KNOWLEDGE honestly,
        no `relationships` since no sibling `layers/` node is merged on `origin/launchpad` at
        this revision) mirroring the structure the merged-precedent `logging.md` shape uses:
        definition (what "a metric" means in Buzz: the `metrics`-crate facade backing
        Prometheus, the three kinds — counter/gauge/histogram — and what each is for), the
        per-surface picture (relay's public :9102 exporter plus periodic gauge emission,
        push-gateway's private-router-only exporter, buzz-db's bounded-cardinality label
        convention), the `EmissionScope` cost-control concept and why it exists, boundaries and
        non-goals (explicitly excluding #1142's exposition-format/scrape-endpoint mechanics and
        any per-metric naming catalogue), and a scope-and-omissions section naming both what is
        out of scope and what was expected to verify and could not.
        done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0.

STEP 3  [needs 2]  Re-run validate.py after any fix and iterate until clean; this step exists
        separately from STEP 2 because a first draft is not expected to pass on the first try
        given the citation-shape and evidence-class rules in `AGENTS.md`.
        done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0 with no
        further edits pending.

STEP 4  [needs 3]  Earn the verify-gate stamp and commit. Run the corpus unittest suite as the
        sole command in its own tool call, confirm `OK`, then commit in a separate call.
        done when: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`
        reports `OK`, and `git commit -s -m "docs(corpus): document metrics (#1140)"` succeeds.

STEP 5  [needs 4]  Self-review: re-read the diff against issue #1140's DoD checklist line by
        line, confirm no second canonical document was created, confirm `validate.py` still
        exits 0.
        done when: every DoD bullet is checked off against the actual diff and `validate.py`
        exits 0 on a final run.

PARALLEL  None — one file, five sequential steps.

GATES     `validate.py` (STEP 2/3) and the corpus unittest suite (STEP 4), run locally in this
          worktree. No push, no PR — a separate integration step cherry-picks this commit into
          a shared batch PR, so `review-adjudicate` and cross-model final review are explicitly
          deferred to that later stage, not run here.

BUDGET    STEP 1 and STEP 2 together. The hard part is scoping an umbrella "metrics" concept
          node honestly against a sibling task (#1142 prometheus) that already claims the
          wire-format/scrape-endpoint territory, without either duplicating that content or
          leaving the umbrella node too thin to explain what Buzz measures and why.

OPEN      Whether the sibling `layers-observability-logging`/`layers-observability-liveness`
          nodes land on `origin/launchpad` before or after this one is outside this task's
          control; if they merge first, a future edit could add `relationships`, but that is
          not decided here and is not this task's job to pre-empt.

LEFT OUT  Any second authored document. #1142's Prometheus exposition-format/scrape-endpoint
          mechanics — named as a sibling and excluded by scope, not folded in. Any per-metric
          naming/label catalogue (there is no single canonical list; the body points at
          `metrics.rs` and representative call sites rather than enumerating every metric name
          in the workspace, since that list is a generated-view candidate, not hand-authored
          content, and would drift immediately). Any change to runtime code, `node.schema.json`,
          or `AGENTS.md`.
