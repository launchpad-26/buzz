# Issue #715 — capabilities/agents/persona.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and the `capability.md` template (`corpus-template-capability`) are merged on `origin/launchpad`; `launchpad/docs/corpus/capabilities/agents/persona.md` does not exist yet, and no `capabilities/` directory exists at all yet on `origin/launchpad` — this is the first `type: capabilities` instance node. `architecture-containers-agent-runtime` (merged) already states that persona-pack resolution is a harness-side responsibility of `buzz-acp` and links `PERSONA_PACK_SPEC.md` — this node stays at the "what the product can do" level and references that node rather than re-describing its content.

STEP 1  Gather evidence: read `crates/buzz-persona/PERSONA_PACK_SPEC.md` in full (Overview, OPS compatibility, pack layout, persona file format, two-layer prompt architecture, skills, MCP config, hooks, behavioral config/precedence) and `crates/buzz-persona/src/lib.rs`'s module list. Confirm via `grep` that `buzz-acp` depends on `buzz-persona` (Cargo.toml) but has no `buzz_persona::` call sites in its own `src/` — persona-pack resolution reaches the harness through `buzz-cli pack inspect --format json` plus the launchpad-specific projector (`launchpad/agents/project-pack.py`, STEP 2 of issue #239), not an in-process crate call. Confirm maturity against `VISION.md:167-169,228` ("Agent personas and teams — desktop-managed, built-in defaults, operator-defined", shipped). Collect the four "planned, not yet implemented" caveats stated directly in `PERSONA_PACK_SPEC.md` (system-prompt-per-turn vs. per-session, skill runtime copying, MCP env var interpolation, hook execution) so maturity isn't overstated. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `capabilities-agents-persona`, type `capabilities`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, relationships: `references` → `architecture-containers-agent-runtime` (merged, already documents the realizing architecture), `implements` → `corpus-template-capability` (merged template this node follows)) and the body per the capability template skeleton: Capability statement, Maturity (cited, with the shipped/planned split honest), Boundary (not architecture, not interface — none merged yet, not flow, not operations), Relationships, Scope and omissions.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; confirm zero new FAIL entries beyond the known 21 pre-existing baseline (issue #1951).

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command to earn the commit-gate stamp, confirm `OK`, then in a separate call `git add` the new document + this plan and `git commit -s`. Do not push, do not open a PR — local commit only, per this batch's process note.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no new FAIL entries. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit. `review-adjudicate` and the cross-model final review pass are deferred to the Feature-wide integration PR, not run here.

BUDGET: small — one document, no code changes, evidence scoped to one crate's spec file, one architecture corpus node, and `VISION.md`'s status table.

OPEN: No `interfaces-events` node exists yet for `buzz-cli`'s `pack validate`/`pack inspect` subcommands, so the Boundary section names that gap explicitly (owned by a future interface node) rather than fabricating a relationship target that doesn't exist. Whether `implements: corpus-template-capability` is worth declaring given the template calls it optional — included here since it costs nothing and documents genealogy for the first capability instance node.

LEFT OUT: No second canonical document. No claim that `buzz-acp` reads `.persona.md` files directly (verified false — it depends on `buzz-persona` but has no call sites; the wiring is external, via `buzz-cli pack inspect` output projected into env vars). No relationship to `architecture-flows-agent-turn` — that merged node never mentions persona and the connection would be inferred, not cited. No fix to any drift between `PERSONA_PACK_SPEC.md` Section 5's `[System]` layer naming and the `[Agent Instructions]` header found in `buzz-acp`'s own test fixtures — that is architecture-level nuance, out of scope for a capability-level node, and not this task's document to fix.
