# Plan: issue #1175 — corpus node layers-security-secret-management

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and
`launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad` at commit
338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5; `launchpad/docs/corpus/templates/concept.md`
is merged and fits this subject (a single defined idea needing discursive
explanation, boundaries, use cases and links — the same template the sibling
`layers/identity/private-key.md` task used, per its own open PR #1814); the
`standards/` set (evidence, front-matter, naming, status, taxonomy, linking,
atomicity, code-references, identifiers, normative-language, documentation-
standard, and others) is merged and binds this draft; no `layers/` directory
exists yet anywhere in the merged corpus, so no sibling `layers`-typed node
exists to link to; the target file
`launchpad/docs/corpus/layers/security/secret-management.md` does not exist;
two merged `architecture`-typed nodes —
`launchpad/docs/corpus/architecture/deployment/kubernetes.md` (id
`architecture-deployment-kubernetes`) and
`.../architecture/deployment/docker-compose.md` (id
`architecture-deployment-docker-compose`) — already document deployment-time
secrets handling for their own surfaces (Compose `${VAR:?}` gates, the Helm
chart's `secrets.existingSecret` vs. autogenerated `secret-chart.yaml`) and
are legitimate `references` targets for a node about the general concept.
Issue #1175 explicitly excludes identity private keys (#1110/#1112's
territory, `layers/identity/private-key.md`, unmerged at this revision) and
scopes this node to relay operator env vars, database credentials, API keys
for third-party services, and Kubernetes Secrets in the deploy path.

STEP 1 (RUNS HERE): Gather evidence — already read: `.env.example` (local-dev
secret-shaped vars: `DATABASE_URL`, `PGPASSWORD`, `REDIS_URL`,
`TYPESENSE_API_KEY`, `BUZZ_S3_ACCESS_KEY`/`BUZZ_S3_SECRET_KEY`),
`crates/buzz-relay/src/config.rs` (`Config::from_env` loads secrets via
`std::env::var` with dev-only defaults embedded in source; `Config` derives
`Debug`/`Clone` with `database_url: String` unredacted), `crates/buzz-relay/
src/main.rs` (startup log names only non-secret fields — `bind_addr`,
`relay_url`, `health_port`, `metrics_port`, `max_frame_bytes`,
`audit_enabled` — never a `{:?}` dump of the whole `Config`), `crates/
buzz-media/src/config.rs` (`MediaConfig` derives `Debug` with
`s3_secret_key: String` likewise unredacted), `crates/buzz-push-gateway/src/
config.rs` (a third and fourth loading shape: `BUZZ_PUSH_APNS_KEY_PATH` is a
filesystem path Buzz reads at runtime rather than an inline value, and
`BUZZ_PUSH_GRANT_KEYS`/`BUZZ_PUSH_TOKEN_KEYS` are comma-separated
`id:base64key` keyrings parsed by `parse_keyring`, with a doc comment stating
the two keyrings `MUST NOT` be reused for each other's purpose),
`deploy/compose/.env.example` and `deploy/compose/compose.yml` (every
secret-shaped var ships as a `CHANGE_ME_*` placeholder and Compose's
`${VAR:?message}` syntax refuses to start `postgres`/`redis`/`minio` at all
if unset), `deploy/charts/buzz/templates/secret-chart.yaml` (the chart's own
autogenerated-secret fallback, explicitly commented "Not GitOps-safe"),
`SECURITY.md` (confirmed its only secret-handling section, "Desktop Secret
Storage — OS Keyring," covers identity keys only — no server/operator
secrets section exists there, which is the gap this node fills), a
repository-wide grep confirming `TYPESENSE_API_KEY` has zero consumers in any
`.rs` file despite being declared in `.env.example` (Typesense is not wired
up; `buzz-search` is Postgres FTS per `AGENTS.md`'s own crate table).

STEP 2: Write front matter (id `layers-security-secret-management`, type
`layers`, status `draft`, origin `launchpad`, audiences `agent`/`developer`/
`operator`/`reviewer`) and body against `templates/concept.md`'s required
sections (Definition, Use cases, Comparison, Boundary, Related resources,
Scope and omissions), covering: what counts as a Buzz operator secret versus
a non-secret operator identifier (`RELAY_OWNER_PUBKEY`, APNs key id/team
id/topic); the four distinct loading shapes found in code (raw scalar env
var, split access/secret pair, filesystem-path reference, base64-encoded
keyring); the absence of any redaction wrapper on the two server-side
`Debug`-derived config structs holding secret fields, contrasted with the
selective, non-`Debug` startup log; and the two structural enforcement
mechanisms at the deployment layer (Compose's `${VAR:?}` + `CHANGE_ME` guard,
Helm's `existingSecret`/autogenerated-secret split), declared via
`references` edges to the two already-merged deployment nodes rather than
restated. Boundary section explicitly excludes identity private keys
(`BUZZ_PRIVATE_KEY`, `BUZZ_RELAY_PRIVATE_KEY`'s cryptographic handling),
naming `layers/identity/private-key.md` (#1112, unmerged) in prose only — no
relationship, since it does not exist on `origin/launchpad`. No other
`relationships`: no `layers`-typed or security-subject node exists in the
merged corpus at this task's merge base (checked via `git ls-tree -r
--name-only origin/launchpad -- launchpad/docs/corpus`).

STEP 3: Run `python3 launchpad/project-intelligence/corpus/validate.py`
from repo root; fix and re-run until exit 0.

STEP 4: Re-read the diff against every Definition-of-Done bullet in issue
#1175; re-open every source cited as FACT to confirm it still says what the
statement claims; confirm no second hand-authored canonical document was
created. Earn the verification stamp with
`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`
as the sole prior command, then commit the plan + target file in a separate
tool call.

PARALLEL: none — single hand-authored file, no fan-out.

GATES: `validate.py` must exit 0 locally before commit. The corpus unittest
suite must report `OK` as its own, standalone command before the commit
gate is considered earned. No second corpus document may be created or
materially edited in this change.

OPEN: whether `TYPESENSE_API_KEY`'s zero-consumer status should itself be
filed as a separate cleanup issue (dead config) is left to the batch owner —
this node records the observation as a gap, not an action item.

LEFT OUT: identity private keys (`BUZZ_PRIVATE_KEY`/`BUZZ_RELAY_PRIVATE_KEY`
cryptographic handling and desktop keyring storage) — #1110/#1112's
territory; full per-variable reference documentation of `.env.example` —
that file and `deploy/compose/README.md` are already authoritative and are
linked, not restated; Kubernetes/Compose deployment topology in general —
already covered by the two referenced `architecture` nodes; TLS/transport
security — `SECURITY.md`'s own "Transport Security" section, a different
concept.
