# Issue #1104 — layers/identity/community-identity.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/architecture/principles/host-selects-community.md` (id `architecture-principles-host-selects-community`) and `launchpad/docs/corpus/architecture/principles/community-is-security-boundary.md` (id `architecture-principles-community-is-security-boundary`) both exist on disk. No `launchpad/docs/corpus/layers/` directory exists yet anywhere in the tree — this is the first `layers`-typed node, and the sibling `layers/tenancy/community-id.md` (#1183) is not authored yet either, so it cannot be a `relationships` target.

STEP 1  Gather evidence: confirm `communities` is a Postgres table `(id UUID PK, host VARCHAR UNIQUE-by-lower, signing_key BYTEA, icon TEXT, created_at, archived_at, deletion_state)` (`migrations/0001_initial_schema.sql`, `migrations/0003_community_icon.sql`). Read `crates/buzz-relay/src/nip11.rs` in full — `RelayInfo::build`'s static-input fence, `nip11_document`, `workspace_icon_for_host`, `nip11_facts` — to establish that a community's externally observable identity is the NIP-11 relay-information document served per-host at `GET /` (`Accept: application/nostr+json`): a static `name`/`description`/`software`/`version` shared by every community on the relay process, one host-scoped mutable field (`icon`, sourced from `communities.icon` via `get_community_icon`/`set_community_icon` and the `RELAY_ADMIN_SET_WORKSPACE_PROFILE` (kind 9033) command in `crates/buzz-relay/src/handlers/relay_admin.rs`), and the relay's own signing pubkey (`self`, shared across communities on one relay process, not per-community). Cross-check `crates/buzz-core/src/kind.rs` (`KIND_NIP29_GROUP_METADATA` = 39000, addressable range 39000–39003 comment) to confirm kind:39000 is channel/NIP-29-group identity, not community identity, so the boundary between the two is drawn from code, not assumed. Note the `signing_key` column exists but no non-deletion code path reads it — do not build a claim on it. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `layers-identity-community-identity`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, relationships: `references` → `architecture-principles-host-selects-community` and → `architecture-principles-community-is-security-boundary`, both merged nodes this document directly builds on) and the body against `node.schema.json` directly (no matching template — `concept.md` is closest and is used as a structural model, not copied verbatim): one-sentence definition, boundary against `community-id` (internal DB primary key, tenancy angle, owned by unmerged #1183 — named but not linked, since its target id doesn't exist yet) and against channel/NIP-29-group identity (kind:39000, a nested but distinct concept), use cases, and an honest scope/omissions section.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to `nip11.rs`, `kind.rs`, `relay_admin.rs`, and a handful of migration files.

OPEN: NIP-11's `name`/`description`/`self` pubkey are relay-process-wide, not per-community — only `icon` is genuinely host-scoped state. Whether that is enough for "community identity" to earn its own node distinct from `community-id` (#1183) is a judgment call this task makes explicitly in the document's own boundary section, grounded in the host-derived-addressing evidence already recorded in `host-selects-community.md`, rather than asserted without support.

LEFT OUT: No relationships to `layers-tenancy-community-id` (#1183, unmerged — named in prose, not linked) or to any other `layers/identity/*` sibling (#1102–1114), none of which are merged yet. No attempt to change NIP-11 behavior, add a per-community name/pubkey field, or resolve whether `communities.signing_key` should be wired up — that is implementation work, not this documentation task.
