Issue #1157 (Feature #609, protocol and networking layer corpus) — document the filter
Stated size: the issue carries no `Size` line (corpus-plan task template has no size field) -> cap: 5 steps,
set by the batch dispatch brief rather than by the issue.

Scope: create exactly one corpus node, `launchpad/docs/corpus/layers/protocol/filter.md`, node id
`layers-protocol-filter`, from `launchpad/docs/corpus/templates/concept.md`. Subject: the filter — the
JSON object inside a `REQ` or `COUNT` that selects which events a client wants.

ALREADY TRUE  (verified in this worktree against the real code and real command output)
  Worktree `/home/serina/Launchpad/buzz/__worktrees/task-1157-filter`, branch `task/1157-filter`,
  branched from `origin/launchpad`. `git rev-parse HEAD` reports
  `29ca9b189bd3f639ba09c972b57c70538c0860c6`; `git cat-file -e` on that sha exits 0. That is the
  provenance revision, not a value copied from the dispatch brief.

  `launchpad/docs/corpus/layers/protocol/filter.md` does not exist yet, and neither does its parent
  directory — `ls launchpad/docs/corpus/layers/protocol/` fails with "No such file or directory". No
  protocol-layer node is merged on `origin/launchpad` at this revision; this node creates the
  directory. Checked directly rather than assumed.

  `crates/buzz-core/src/filter.rs` (322 lines) read in full. Its module doc states "Multiple filters
  are OR-ed; fields within one filter are AND-ed." `filters_match` is `filters.iter().any(...)`.
  `filter_match_one` evaluates, in order: `kinds`, `authors`, `since`, `until`, `ids` (with prefix
  matching, per its own NIP-01 comment), then every generic single-letter tag clause. `#h` carries a
  Buzz-specific fallback to `StoredEvent.channel_id`, used ONLY when the event has no `h` tags at
  all; an event carrying explicit `h` tags is matched strictly on those.

  `crates/buzz-relay/src/handlers/req.rs` read at the sections that matter:
  - `filter_to_query_params` pushes to SQL: `kinds`, `authors` (single via `pubkey`, multi via
    `authors`), `ids`, `since`, `until`, `#e` (any count), `#p` (single value only), `#d` (only when
    the filter targets NIP-33 kinds exclusively), plus the channel scope injected by
    `apply_channel_scope_to_query`. `limit` is `min(buzz_db::DEFAULT_MAX_PAGE_LIMIT)`; an absent
    `limit` defaults to that same ceiling.
  - The historical-delivery loop (phase 3) re-applies `filters_match` to every row the SQL returned,
    unconditionally, then applies channel accessibility, `event_visible_to_reader`, and dedupe.
  - `filter_fully_pushable` documents the non-pushable set: multi-`#p`, `#t`, `#a`, `search`, and
    `#d` on non-NIP-33 kinds.
  - `p_gated_filters_authorized` treats `kinds` absent as "can match a p-gated kind"
    (`filter.kinds.as_ref().is_none_or(...)`), so a kindless filter is authorized only via a
    non-empty `ids` exemption or `#p` values all equal to the reader. The `ids` exemption is
    withdrawn for filters explicitly naming `KIND_DM_VISIBILITY` or `KIND_AGENT_TURN_METRIC`.
    On WS the gate runs only when `channel_id.is_none()`.

  `grep_r('filter_fully_pushable', path='crates/', include='*.rs')` returns 5 matches: the definition
  in `handlers/req.rs`, two call sites in `handlers/count.rs`, two in `api/bridge.rs`. The REQ
  historical path never calls it.

  `crates/buzz-db/src/store/event.rs`: `DEFAULT_MAX_PAGE_LIMIT: i64 = 1_000`; `let limit_val =
  q.limit.unwrap_or(100).min(clamp)`; empty `kinds`/`authors`/`ids`/`e_tags` short-circuit to zero rows.

  `crates/buzz-relay/src/protocol.rs`: `MAX_FILTERS_PER_REQ: usize = 10`, enforced at parse time for
  both `REQ` and `COUNT`, with the error naming the count. `crates/buzz-relay/src/nip11.rs` advertises
  `max_filters: Some(10)` and `max_limit: Some(buzz_db::DEFAULT_MAX_PAGE_LIMIT as u32)`.

  `crates/buzz-relay/src/api/bridge.rs` returns HTTP 403 `restricted: p-gated kinds require #p tag
  matching your pubkey` from the same shared gate, on both `POST /query` and `POST /count`.

  `ls docs/nips/` shows 24 entries, every numbered-spec name absent: `NIP-01.md` and `NIP-42.md` do
  not exist. Upstream NIP text is not in this repository.

  Merged relationship targets confirmed present in the merged-id list: `architecture-flows-historical-query`,
  `capabilities-search-search-query`, `layers-data-postgres-indexes`, `implementation-crates-buzz-core`,
  `implementation-crates-buzz-relay`, `verification-contracts-nostr`. None of Feature #609's siblings
  (#1165 REQ, #1167 subscriptions, #1155 tags, #1150 EOSE) is merged, so none may be targeted.

  `architecture-flows-historical-query` already owns the request/response flow and already states the
  limit clamp and that post-filtering can return fewer events than the ceiling. This node must link to
  it, not restate it.

STEP 1  Draft the front matter against `node.schema.json`: `id: layers-protocol-filter`,     [independent]
        `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer]`.
        Write the evidence ledger first, one entry per claim the body will make, with exactly
        one commit-only FACT (the provenance entry for
        `29ca9b189bd3f639ba09c972b57c70538c0860c6`). Classify honestly: repo paths I opened
        are FACT; the absence of upstream NIP text is a FACT on a tool-result citation in
        `identifier(args) -> result` shape; issue #1157's definition of done is
        TEAM_KNOWLEDGE with `provided_by`; the reading of `CLAUDE.md`'s p-gate gotcha against
        the actual code is INFERENCE with a stated confidence.
        done when: every `evidence` entry names a path I have actually opened in this
        worktree, or a tool result I actually ran; the ledger holds exactly one commit-only
        FACT; no `confidence` on a FACT and no `provided_by` on anything but TEAM_KNOWLEDGE.

STEP 2  Write the body's concept half, following `templates/concept.md`'s required           [needs 1]
        sections: definition (what a filter is, in one sentence, before anything else), the
        clause vocabulary, and the AND-within/OR-across rule from `filter.rs`'s own module
        doc. Include the `#h` channel-id fallback and its strictness rule, since that is
        Buzz-specific and not derivable from any NIP. State boundaries explicitly: this is
        not the `REQ` message, not a subscription, not a tag, not EOSE — those are sibling
        nodes not yet merged, so name them in prose without a `relationships` edge.
        done when: the definition is one sentence and comes before any deeper explanation;
        no section restates `architecture-flows-historical-query`'s flow steps; every
        Buzz-specific claim in the section maps to a ledger entry written in step 1.

STEP 3  Write the body's consequential half — the two-tier evaluation. State the pushed set  [needs 2]
        and the non-pushed set from `filter_fully_pushable`'s own documentation, state that
        the SQL `LIMIT` is applied over the pushed predicate only, and state plainly that a
        filter whose clauses are not all pushable can therefore return fewer events than
        match it. Record honestly that `filter_fully_pushable` is consulted by the COUNT
        paths only and never by the REQ historical path, which post-filters unconditionally,
        and that COUNT's answer to the same problem is a bounded 5000-candidate fallback that
        reports over-budget rather than returning a truncated count. Add the `MAX_FILTERS_PER_REQ`
        cap and the silent `limit` clamp as clause-level facts, deferring the flow-level
        consequence to `architecture-flows-historical-query`.
        done when: the pushed/non-pushed lists match `filter_fully_pushable`'s doc comment
        exactly; the REQ-versus-COUNT asymmetry is stated, not implied; the clamp is stated
        once and the flow node is linked rather than paraphrased.

STEP 4  Write the p-gate section and the `Scope and omissions` section. The p-gate section    [needs 3]
        states the exact rule (`kinds` absent means the filter can match a p-gated kind, so a
        kindless filter needs `ids` or a self-matching `#p`), both response shapes (WS `CLOSED`
        for global subscriptions only; HTTP 403 on the bridge), and the withdrawal of the `ids`
        exemption for two kinds. `Scope and omissions` carries two distinct things: what this
        node does not cover and who owns it, and — separately — what I expected to verify and
        could not. Add the six confirmed `references` relationships.
        done when: `Scope and omissions` names both categories under separate headings; every
        `relationships[].target` appears in the merged-id list; no target belongs to Feature
        #609.

STEP 5  Validate, self-review, stamp, commit.                                                 [needs 4]
        done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0; every
        DoD bullet in issue #1157 is checked against the finished file by re-reading it; every
        ledger entry is re-opened against its cited source; then, as the sole foreground
        command with a 600000ms timeout, `python3 -m unittest discover -s
        launchpad/project-intelligence/corpus/tests -p "test_*.py"` ends `OK`; then a separate
        `git add` + `git commit -s -m "docs(corpus): document the filter (#1157)"`. No push,
        no PR.
