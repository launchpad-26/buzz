# Issue #942 — implementation/crates/buzz-ws-client.md

Stated size: dispatch brief caps this task at 5 steps (corpus-batch-author, one document per task).  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/implementation-reference.md`, `launchpad/docs/corpus/architecture/flows/websocket-connection.md`, and `launchpad/docs/corpus/architecture/flows/websocket-authentication.md` are all merged on `origin/launchpad`; `launchpad/docs/corpus/implementation/crates/buzz-ws-client.md` does not exist yet.

STEP 1 [independent]  Gather evidence: read `crates/buzz-ws-client/Cargo.toml`, its full `src/` tree (`lib.rs`, `connection.rs`, `message.rs`, `error.rs` — no `README.md`, no `tests/` directory), and its inline `#[cfg(test)]` module. Confirm real consumers with `grep -rn "buzz-ws-client" --include=Cargo.toml .` (workspace root, `buzz-cli`, `buzz-test-client`, `desktop/src-tauri` — NOT `buzz-pairing-cli` or `buzz-acp`, despite `architecture-context-nostr-network.md`'s claim otherwise) and confirm each consumer's actual call sites (`buzz-cli/src/client.rs`'s `publish_event` use, `desktop/src-tauri/src/native_relay_client.rs`'s `NostrWsConnection::connect_authenticated` use, `buzz-test-client/src/lib.rs`'s `TestRelayConnection` wrapper). Cross-check against `architecture/flows/websocket-authentication.md`'s existing FACT citations of `connection.rs`/`message.rs` to confirm no drift since that node was written. ← RUNS HERE
  done when: every claim destined for the evidence ledger has a source that was actually opened, and the real consumer set (buzz-cli, buzz-test-client, desktop/src-tauri) is confirmed distinct from `nostr-network.md`'s claimed set (buzz-acp, buzz-pairing-cli).

STEP 2 [needs 1]  Write front matter (schema-valid: id `implementation-crates-buzz-ws-client`, type `implementation`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, one commit-citation FACT for provenance, one FACT per substantive claim, `relationships: implements -> architecture-flows-websocket-authentication` (that node's own text already asserts this crate "implements the other half of the round trip", so the edge is bidirectionally evidenced) and `references -> architecture-flows-websocket-connection` (supporting context: the server-side connection lifecycle this crate's socket participates in; no direct citation of this crate found in that node, so `implements` would overclaim)) and the template's required body sections in order: Realization statement, Target, Implementation surface (table), Divergences, Verification, Relationships, Scope and omissions.
  done when: the file exists at `launchpad/docs/corpus/implementation/crates/buzz-ws-client.md` with every template-required section present and every evidence entry classified FACT/INFERENCE/TEAM_KNOWLEDGE honestly.

STEP 3 [needs 2]  Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Diff against a `git stash` of the pre-existing baseline failure count on `origin/launchpad` if the exit code is nonzero, to distinguish a pre-existing failure from one this node introduced.
  done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0 with zero FAIL entries naming this node.

STEP 4 [needs 3]  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` alone to earn the verification stamp, confirm `OK`, then in a separate call `git add` the plan + document and `git commit -s`. Re-read the diff against issue #942's Definition of Done checklist line by line before reporting done. Do not push, do not open a PR — this batch integrates into one Feature-level PR later.
  done when: the unittest run reports `OK`, the commit exists locally with both files staged, and every DoD bullet in issue #942 has been checked against the actual diff.

PARALLEL: none — single file, single task, no dependency on sibling batch documents.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 for this node (pre-existing unrelated baseline failures on `origin/launchpad` are not this task's to fix). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration review — not run here.

BUDGET: small — one document, no code changes, evidence scoped to one crate's four source files, three consumer crates' call sites, and two already-merged flow nodes.

OPEN: `buzz-ws-client` ships no crate-level `README.md` and no `tests/` integration-test directory of its own — its only in-crate tests are three `#[cfg(test)]` unit tests in `connection.rs` asserting timeout-constant floors (`auth_challenge_timeout_meets_floor`, `auth_ok_timeout_meets_floor`, `publish_ok_timeout_meets_floor`). Real behavioral coverage of this crate lives in its consumers: `desktop/src-tauri/src/native_relay_client_tests.rs` (a real-socket stub-relay integration test exercising `NostrWsConnection` through `run_session`/`run_connection`) and `crates/buzz-test-client/tests/e2e_relay.rs` (live-relay E2E tests through `TestRelayConnection`, already cited by `architecture-flows-websocket-authentication.md`). The Verification section cites all of these and says plainly that the crate has no dedicated test suite of its own — this is an honest gap report, not something this task fixes.

LEFT OUT: No `implements`/`references` edge toward a NIP-42 spec node — no corpus node for NIP-42 exists (it is an external, non-Buzz-authored Nostr NIP; `docs/nips/` in this repo holds only Buzz's own custom NIPs, none numbered 42), so the *Target* section names it by prose/URL per `AGENTS.md`'s rule against inventing an edge to a nonexistent id. No correction to `architecture-context-nostr-network.md`'s incorrect claim that `buzz-pairing-cli` and `buzz-acp` consume `buzz-ws-client` — verified false by fresh `grep` of both crates' `Cargo.toml` (neither depends on it; `buzz-pairing-cli` uses `tokio-tungstenite` directly), but editing another hand-authored corpus node is out of this issue's scope ("Out of scope: Broad 'while here' documentation cleanup" and "editing a second hand-authored canonical corpus document"). This node's own claims about consumers cite fresh evidence rather than that node's claim.
