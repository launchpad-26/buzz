# Issue #700 — capabilities/activity/mentions-feed.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and `launchpad/docs/corpus/templates/capability.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/capabilities/` does not exist as a directory yet, and `launchpad/docs/corpus/capabilities/activity/mentions-feed.md` does not exist. `capability.md`'s own required-sections skeleton (Capability statement / Maturity / Boundary / Relationships / Scope and omissions) is the template to follow — confirmed present and read in full.

STEP 1  Gather evidence: read `VISION.md`'s Home Feed paragraph (line 133: "@mentions, items needing action, channel activity, agent updates ... Agents read the same feed via MCP"); trace the mentions feed through three independent surfaces — (a) `crates/buzz-cli/src/commands/feed.rs` (`VALID_FEED_TYPES`, `feed get --types mentions`) and `crates/buzz-relay/src/api/bridge.rs` (`extract_feed_types`, dispatch to `query_feed_mentions_routed`); (b) `crates/buzz-db/src/store/feed.rs` (`build_mentions_query`/`query_mentions`/`query_feed_mentions_routed`, joined against `event_mentions`, populated by `crate::insert_mentions` per `runtime/mod.rs`); (c) desktop's own independent path — `desktop/src-tauri/src/commands/messages.rs::get_feed` (builds its own `#p` filter directly against the relay, not via the bridge `feed_types` extension) surfaced through `desktop/src/shared/api/tauri.ts` (`HomeFeedResponse.feed.mentions`) into the Home inbox's "Mentions" filter (`desktop/src/features/home/ui/InboxFilterMenu.tsx`, `desktop/src/features/home/lib/inboxViewHelpers.ts::matchesInboxFilter`). Confirm test coverage: `crates/buzz-db/src/store/feed.rs`'s own `mod tests` (26 tests, including `query_mentions_is_scoped_across_communities`), the replica-routing separation test in `crates/buzz-db/src/runtime/tests.rs` (`query_feed_mentions_routed` call), `crates/buzz-relay/src/api/bridge.rs`'s `extract_feed_types_*` unit tests, and `desktop/src/features/home/lib/inboxViewHelpers.test.mjs`'s `matchesInboxFilter` mentions-filter cases. Record `git rev-parse HEAD`. ← RUNS HERE

STEP 2  [needs 1] Create `launchpad/docs/corpus/capabilities/` and `capabilities/activity/` directories and write the node against `capability.md`'s skeleton: front matter (id `capabilities-activity-mentions-feed`, type `capabilities`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, one `relationships` entry — `implements` targeting `corpus-template-capability`, which is confirmed merged on `origin/launchpad` at `launchpad/docs/corpus/templates/capability.md`, per that template's own "may declare `implements` toward this template node itself ... optional" guidance; no architecture/interface/flow node scoped to feed/mentions exists to `references`) and a body with Capability statement, Maturity (shipped, cited to the three surfaces in STEP 1), Boundary (not architecture/interface/flow/operations, per the template's own four-item checklist), Relationships (the `implements` edge above, plus the same "no `references` target exists yet" reasoning), and Scope and omissions (including, as a named gap, that the desktop `get_feed` mentions path and the CLI/bridge `feed_types=mentions` path are two independently-coded routes to the same product capability that were not reconciled or deduplicated by this task).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own tool call to earn the commit gate; confirm `OK`. Then, in a separate call, commit the plan + document only — per this batch's process, do NOT push and do NOT open a PR.

PARALLEL: none — one document, one task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The unittest discover command above must print `OK`. `review-code`/`review-adjudicate`/cross-model final review are deferred to the batch integration phase — not run here; this task's own step 6 (self-review) substitutes.

BUDGET: small — one document, no runtime code changes, evidence gathering scoped to ~6 source files (CLI, relay bridge, db feed store, desktop tauri command, desktop shared API type, desktop inbox filter/helpers) plus their existing tests and one VISION.md paragraph.

OPEN: No corpus node currently on `origin/launchpad` documents any Buzz architecture container, interface, or flow scoped to feed/mentions specifically (the architecture/* nodes under `containers/`, `context/`, `deployment/`, `flows/`, `principles/` cover other subsystems), so no `references` edge is declared. The one relationship declared — `implements` → `corpus-template-capability` — is checked against `origin/launchpad`'s actual tree (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`), not assumed from this worktree, per `AGENTS.md` step 9's warning about the two branches diverging.

LEFT OUT: No attempt to reconcile the desktop `get_feed` mentions path (own `#p` filter, queried directly against the relay) with the CLI/bridge `feed_types=["mentions"]` path (routed through `query_feed_mentions_routed`'s `event_mentions` join) — both are cited as maturity evidence for the same shipped capability, and their divergence is named as a gap in Scope and omissions, not fixed. No claim about `needs_action` or `activity` feed types beyond what is needed to state the boundary of "mentions" among its sibling feed categories. No new corpus node for @mention extraction/p-tag construction (`crates/buzz-sdk/src/mentions.rs`) — that is a separate, already-independently-maintainable idea (message composition, not the feed capability) and is out of scope for this task.
