# Issue #952 — implementation/web/web-app.md

Stated size: issue #952 carries no explicit Size line or label; the dispatching `corpus-batch-author` skill caps every single-document corpus task in its batch -> cap: 5 steps.

ALREADY TRUE: `launchpad/docs/corpus/implementation/web/web-app.md` does not
exist (`ls launchpad/docs/corpus/implementation/web/` fails: no such
directory). `launchpad/docs/corpus/templates/implementation-reference.md`
exists and is the required template (Realization statement, Target,
Implementation surface, Divergences, Verification, Relationships, Scope and
omissions). `node.schema.json`'s `type` enum includes `implementation`.
`launchpad/docs/corpus/architecture/containers/web.md` (`id:
architecture-containers-web`, `status: draft`) is merged on `origin/launchpad`
at HEAD `76a0a4ebbe4bc4d852b0d04362ed768620da34b3` and is a valid `implements`
relationship target — it documents this container's architecture; this task
documents the code-level realization detail underneath it (build tooling,
entry point, relay serving) without restating its interfaces/deployment/
security sections. A sibling batch task owns `implementation/web/
feature-map.md` (per-feature breakdown of `web/src/features/`) — this task
stays out of that territory.

STEP 1  [independent] Re-verify the container node's relay-serving claims
against current HEAD and gather app-level evidence: read `web/package.json`,
`web/vite.config.ts`, `web/src/main.tsx`, `web/src/app/App.tsx`,
`web/src/app/router.tsx`, `web/tests/e2e/smoke.spec.ts`, and
`crates/buzz-relay/src/router.rs` / `config.rs` (`web_dir`,
`should_serve_spa`, `is_git_web_gui_path`, `nip11_or_ws_handler`). Confirm
each claim by opening the actual file at current HEAD rather than trusting
the container node's own (older-revision) citations. done when: every claim
planned for STEP 2 has been opened and confirmed at current HEAD. ← RUNS HERE

STEP 2  [needs 1] Write `launchpad/docs/corpus/implementation/web/web-app.md`
against the template skeleton: front matter (`id:
implementation-web-web-app`, `type: implementation`, `status: draft`,
`origin: launchpad`, `audiences: [agent, developer, reviewer]`, one `evidence`
entry per claim from STEP 1 plus a commit citation, `relationships: [{type:
implements, target: architecture-containers-web}]`); body sections Realization
statement, Target, Implementation surface (table), Divergences, Verification,
Relationships, Scope and omissions — satisfying every DoD bullet in #952
(responsibility and non-ownership, public interfaces/entry points and
dependencies, owned source paths and representative tests, no restated domain
semantics already canonical in `architecture-containers-web`). done when: the
file exists with all seven required sections present and every evidence entry
cites a source opened in STEP 1.

STEP 3  [needs 2] Run `python3
launchpad/project-intelligence/corpus/validate.py` from the worktree root.
done when: exit 0, or any reported failure is confirmed pre-existing on
`origin/launchpad` by diff/stash comparison.

STEP 4  [needs 3] Run, as the sole command in its own tool call: `python3 -m
unittest discover -s launchpad/project-intelligence/corpus/tests -p
"test_*.py"`. done when: output contains `OK`.

STEP 5  [needs 4] `git add
launchpad/docs/corpus/implementation/web/web-app.md
launchpad/plans/2026-09-01-issue-952-web-app.md`, then in a separate tool call
`git commit -s -m "docs(corpus): add web-app implementation reference
(#952)"`. done when: the commit exists locally on `task/952-web-app`; no push,
no PR opened (integration is a later, separate batch phase).

PARALLEL: none — single file, single task, no independent sub-tasks.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit
0 (or failure confirmed pre-existing). The corpus unittest suite must print
`OK` before the commit gate accepts. `review-adjudicate` and the cross-model
final review pass are deferred to the batch owner's later integration review
— not run per-document in this batch mode.

BUDGET: small — one document, no code changes, evidence gathering scoped to
~7 already-identified files plus the existing container node.

OPEN: Whether `architecture-containers-web` (itself still `status: draft`)
will be re-verified or promoted before this node's `implements` edge is reviewed
— out of scope for #952, left for the batch owner's integration pass.

LEFT OUT: No edit to `architecture-containers-web.md` itself — this task adds
a new node only. No documentation of `web/src/features/**` per-feature
behavior — reserved for the sibling `implementation/web/feature-map.md` task
in this same batch. No documentation of `admin-web/` — a separate,
undocumented sibling bundle, out of scope per the container node's own scope
table. No push, no PR — integration into the one Feature PR happens in a
later, separate phase per `corpus-batch-author`.
