# Issue #948 — implementation/desktop/tauri-backend.md

Stated size: issue #948 carries no Size label (`type:task`, `area:docs`, `by:agent` only); the batch dispatch instructions for this task explicitly cap it at 5 steps  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/templates/implementation-reference.md`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/architecture/containers/desktop.md` (id `architecture-containers-desktop`) are merged on `origin/launchpad`; `launchpad/docs/corpus/implementation/desktop/tauri-backend.md` does not exist yet, and no `implementation`-typed node exists anywhere in the merged corpus tree for this node to sit `part-of` a sibling of.

STEP 1  [independent] Gather evidence: read `desktop/src-tauri/Cargo.toml` (workspace exclusion, feature set, linked path crates), the full `desktop/src-tauri/src/` module tree (`find ... -name "*.rs"`), the `#[tauri::command]` surface (`grep -rc` across `src/`, plus `lib.rs`'s `invoke_handler!`/`.plugin(...)` registrations), `commands/project_git_exec.rs`, `managed_agents/runtime.rs`, `managed_agents/discovery.rs`, `managed_agents/backend.rs` (the compute-provider subprocess protocol), and confirm/refute the task brief's own claim that `buzz-relay-mesh` is a native integration — check `crates/buzz-relay-mesh/Cargo.toml`'s description and `grep -rn buzz_relay_mesh desktop/src-tauri/` directly rather than trusting the brief. Record `git rev-parse HEAD`. ← RUNS HERE
  done when: every claim planned for the node's evidence ledger has a citation to a file this step actually opened, and the `buzz-relay-mesh` question above has a yes/no answer backed by a grep result, not an assumption.

STEP 2  [needs 1] Write front matter (schema-valid: id `implementation-desktop-tauri-backend`, type `implementation`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, relationships: `part-of` → `architecture-containers-desktop` only — the sole merged node this subject sits under) and the body per the template skeleton: Realization statement, Target, Implementation surface (command-family table + native-integration table, each row FACT-cited to code actually opened), Divergences (including the corrected buzz-relay-mesh finding from STEP 1 if the brief's claim doesn't hold), Verification (`just desktop-tauri-check`/`desktop-tauri-clippy`/`desktop-tauri-test`, the `_tests.rs` sibling-file convention, the confirmed root-workspace exclusion), Relationships, Scope and omissions (explicitly excluding `desktop/src/` frontend — sibling issue's territory).
  done when: `launchpad/docs/corpus/implementation/desktop/tauri-backend.md` exists with all seven template-required sections present and every DoD bullet from issue #948 addressed.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Compare exit status/output against a `git stash`-based baseline check if any FAIL appears, per the pre-existing ~21-failure baseline noted in the task brief.
  done when: the validator exits 0 with zero FAIL entries naming this node's file path.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, confirm `OK`, then in a separate call `git add` the plan + document and `git commit -s`.
  done when: the unittest run prints `OK` and `git log -1` shows a new commit containing exactly the plan file and the corpus document.

PARALLEL: none — single file, single task, no dependency on the sibling `implementation/desktop/frontend.md` node (different territory: `desktop/src/` vs `desktop/src-tauri/`).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before commit. `review-adjudicate` and cross-model final review are deferred to the batch owner's later integration pass — not run here per this task's own instructions (no push, no PR).

BUDGET: small-to-medium — one document, no runtime code changes, evidence gathering scoped to ~10 targeted reads/greps across a large (348-command, 92-file) but already-enumerated module tree; no `cargo build`/`cargo test` execution required (structural evidence — `Cargo.toml`, `Justfile`, `grep` — is sufficient for the claims this node makes).

OPEN: the task brief describes `buzz-relay-mesh` as "the Kubernetes backend" — STEP 1's own read of `crates/buzz-relay-mesh/Cargo.toml` (description: "Inter-relay QUIC mesh: transport, membership, and the fenced wire contract") and `crates/buzz-backend-kubernetes/Cargo.toml` (description: "Kubernetes backend provider for Buzz remote agents") shows this is backwards — `buzz-backend-kubernetes` is the Kubernetes-backend externalBin, and `buzz-relay-mesh` is unreferenced anywhere under `desktop/src-tauri/` (zero grep hits). The node reports the verified pairing, not the brief's premise, and does not silently correct the brief without saying so.

LEFT OUT: no claim about `desktop/src/` (the React frontend) — sibling issue's territory per this task's own dispatch note. No claim about the `mesh-llm` Cargo feature's network protocol/trust model beyond what `architecture-containers-desktop` already states (not compiled into this checkout's default feature set). No new `implements` edge to any spec/ADR/NIP — none was found to be a corpus node yet, and inventing one is a hard validation error.
