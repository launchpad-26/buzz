---
id: plan-issue-749-patch
issue: 749
feature: 613
---

# Plan: document capabilities/git/patch.md (issue #749)

## ALREADY TRUE

- `launchpad/docs/corpus/templates/capability.md` exists and is merged on
  `origin/launchpad` — a `capability` template already landed, so this node is
  written against it directly rather than raw `node.schema.json`.
- `launchpad/docs/corpus/capabilities/` does not exist yet on `origin/launchpad`
  — this is the first node under that surface; no sibling capability nodes
  exist to relate to.
- The capability is real and shipped: `KIND_GIT_PATCH = 1617` (NIP-34) is
  defined in `crates/buzz-core/src/kind.rs`, built by
  `buzz_sdk::build_git_patch` (`crates/buzz-sdk/src/builders.rs`), exposed via
  `buzz patches send|get|list|status` (`crates/buzz-cli/src/commands/patches.rs`,
  `crates/buzz-cli/src/lib.rs`), and enforced server-side as an alternative to
  direct push via the `require-patch` branch-protection rule
  (`crates/buzz-core/src/git_perms.rs`).
- No relay side-effect handler or automated "apply" step exists for kind:1617
  — merging is manual (maintainer applies the patch and pushes; a status event
  records the outcome). This shapes the Maturity/Boundary sections honestly.
- `launchpad/docs/corpus/architecture/flows/git-push.md` exists and documents
  the *push* flow, not the patch flow — evaluated as a relationship target and
  rejected: it doesn't realize this capability, it's a sibling/alternative one.

## STEP 1 — Draft the node

Write `launchpad/docs/corpus/capabilities/git/patch.md` following the
`capability` template's required sections (Capability statement, Maturity,
Boundary, Relationships, Scope and omissions), citing:
- `crates/buzz-core/src/kind.rs` (kind 1617 + related status/PR/issue kinds)
- `crates/buzz-sdk/src/builders.rs` (`build_git_patch`, `GitPatchMeta`,
  `build_git_status`, `GitAppliedPatchRef`, size/validation rules, unit tests)
- `crates/buzz-cli/src/commands/patches.rs` + `lib.rs` (`buzz patches` subcommands)
- `crates/buzz-core/src/git_perms.rs` (`require-patch` protection rule,
  `evaluate_ref_update`)
- `crates/buzz-relay/src/handlers/ingest.rs` (scope = `MessagesWrite`, global-only
  kind — `a`-tag scoped not `h`-tag scoped)
- `VISION_PROJECTS.md` (product framing: patches as first-class review objects)

No `relationships` declared — no capability/interface/architecture node in
`origin/launchpad`'s corpus realizes or exposes this specific capability yet
(git-push.md is a sibling flow, not this capability's implementation).

**Done when:** file exists, front matter validates against
`node.schema.json`'s enums, every FACT/INFERENCE cites an opened source.

## STEP 2 — Validate

Run `python3 launchpad/project-intelligence/corpus/validate.py` from repo
root. **Done when:** zero new FAIL entries beyond the known 21 pre-existing
ones (issue #1951).

## STEP 3 — Commit gate + commit

Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`
as the sole command in its own call; confirm `OK`. Then `git add` + signed
commit. **Done when:** commit created, nothing pushed.

## PARALLEL

None — single-file task, no fan-out.

## GATES

- `validate.py` exits 0 with no new FAIL entries.
- `unittest discover` on corpus tests prints `OK`.

## BUDGET

Single node, ~1 file. No code changes, no PR.

## OPEN

- Whether a future `capabilities/git/git-hosting.md` (or similar umbrella
  node) should later declare `part-of` from this node — deferred; no such
  node exists yet.

## LEFT OUT

- Any relay-side "apply" automation — does not exist in the codebase; stated
  as absent in Maturity/Boundary rather than invented.
- Interface-boundary documentation (`buzz patches` CLI surface as its own
  interface node) — `#1342`'s interface template territory, not this task.
