# Issue #1159 — corpus node: Nostr as Buzz's protocol foundation

Parent Feature #609 (protocol and networking layer corpus). Stated size: no `Size` line
on the issue (corpus-plan `type:task` template carries none) -> dispatch brief caps this
at 5 steps. One hand-authored Markdown file plus this plan; no code changes.

## ALREADY TRUE (verified, not assumed)

Worktree `/home/serina/Launchpad/buzz/__worktrees/task-1159-nostr`, branch
`task/1159-nostr`, branched from `origin/launchpad` at
`29ca9b189bd3f639ba09c972b57c70538c0860c6` (`git rev-parse HEAD` in the worktree;
`git cat-file -e` exits 0). That is the provenance revision.

`launchpad/docs/corpus/layers/` exists with `compute/`, `configuration/`, `data/`,
`lifecycle/`, `observability/` — **no `protocol/` directory yet**, so this node creates
it and is the first node on that surface. `grep 'layers/protocol' corpus-merged-ids.txt`
returns nothing: no #609 sibling is merged, so none may be a relationship target.

The three boundary nodes named in the dispatch all exist on `origin/launchpad` and were
read in full:

- `architecture-principles-nostr-first` — owns the design invariant (model new backend
  work as a signed event; keep the HTTP surface narrow) and its (absent) enforcement.
- `architecture-context-nostr-network` — owns Nostr as an external system: actors,
  the community/host boundary, and the explicit statement that Buzz does not federate.
- `verification-contracts-nostr` — owns the interop *test* obligation, scoped to the
  twelve tests in `crates/buzz-test-client/tests/e2e_nostr_interop.rs` (NIP-50, NIP-10,
  NIP-17).

None of the three states which NIPs the relay advertises, which lettered NIPs Buzz
authors, or which NIPs it declines. That gap is this node's subject.

Evidence sources located and read:

- `crates/buzz-relay/src/nip11.rs` — `SUPPORTED_NIPS: &[u32] = &[1, 2, 10, 11, 16, 17,
  23, 25, 29, 33, 38, 42, 50, 56]`, plus `NIP_RELAY_MEMBERSHIP: u32 = 43` advertised
  conditionally by `RelayInfo::build`, and `supported_extensions` carrying `"nip-er"`
  always with `"buzz-gif"` and `"nip-pl"` conditionally.
- `docs/nips/` — 22 `.md` files (plus two `NIP-MP.*fixtures.json`), all two-letter or
  hyphenated-suffix codes. No upstream numbered spec anywhere in the tree.
- `crates/buzz-core/src/kind.rs:518` — `// Not using NIP-90 kinds (5000–6999) — Buzz
  requires auth chains (depth ≤ 3, breadth ≤ 10).`
- `NOSTR.md` (repo root) — "The old NIP-28 compatibility proxy has been removed" and a
  *What Doesn't Work* row reading "NIP-17 gift wraps supported; NIP-04/NIP-44 not
  implemented" for DMs.

Two asymmetries found and confirmed by grep: `NIP-AB` (device pairing) has 50
references in `crates/` and **no** `docs/nips/NIP-AB.md`; `NIP-AA` has a spec file and
**zero** references in `crates/`.

## Steps

### Step 1 — Fix the node's boundary against the three neighbours

Write the scope paragraph first, naming what each neighbour owns and what is left.
**Done when:** the draft's opening states the node's subject as the *relationship*
(adopt / extend / decline), and cites no claim that `architecture-principles-nostr-first`,
`architecture-context-nostr-network` or `verification-contracts-nostr` already makes.

### Step 2 — Build the adoption table from `nip11.rs`, corroborating each entry

For each of the 14 advertised NIPs plus conditional NIP-43, find implementing code in
`crates/` and record where. Record separately the NIPs implemented but **not**
advertised. **Done when:** every advertised NIP has a named corroborating file or an
explicit "could not corroborate" note, and the unadvertised-but-implemented set is
listed rather than glossed.

### Step 3 — Build the extension table from `docs/nips/`

One row per lettered NIP file with a one-line subject taken from that file's own
abstract. Note which are advertised (`supported_extensions`) and which are not.
**Done when:** all 22 files are represented, and the NIP-AB / NIP-AA asymmetries are
stated as findings with tool-result citations in `identifier(args) -> result` shape.

### Step 4 — Build the refusal section and the front matter

Record NIP-90, NIP-28 and NIP-04 with their sources; absences cite tool results, not
paths. Front matter: `id: layers-protocol-nostr`, `type: layers`, `status: draft`,
`origin: launchpad`, one commit-only provenance FACT, `relationships` targeting only
ids confirmed present in `corpus-merged-ids.txt`.
**Done when:** every evidence entry's class matches what was actually opened, and no
second commit-only FACT exists.

### Step 5 — Validate, self-review, stamp, commit

Run `python3 launchpad/project-intelligence/corpus/validate.py` to exit 0. Re-read every
evidence entry against its source. Then run the corpus unittest suite as a sole
foreground command with a 600000 ms timeout, confirm the final line is `OK`, and
`git commit -s`. **Done when:** validator exits 0, suite ends `OK`, one commit exists,
nothing is pushed.

## Out of scope

Explaining any single Nostr primitive (events, filters, tags, subscriptions, kinds) —
every one is a #609 sibling's node. Explaining upstream NIP text, which is not in this
repository. Changing runtime behaviour. Editing `docs/` (upstream's tree).
