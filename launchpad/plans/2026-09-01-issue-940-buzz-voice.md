# Issue #940 — implementation/crates/buzz-voice.md
Stated size: not stated in the issue body → cap: 5 steps (batch-dispatch instruction for this task)

ALREADY TRUE: `launchpad/docs/corpus/implementation/crates/buzz-voice.md` does not exist yet (confirmed via `ls`). `crates/buzz-voice` exists in the workspace (`Cargo.toml` line 30) with `src/{lib,pocket,pocket_april,pocket_models,imported}.rs` and `tests/pocket_import_audio.rs`, no `README.md`. Its Cargo.toml describes it as "Reusable local voice primitives for Buzz" and depends on `ort`/`sherpa-onnx` (ONNX Runtime), `symphonia` (audio decode), `tokenizers`/`sentencepiece-model` — a local, on-device TTS engine ("April Pocket"), not the huddle-audio WebSocket relay (`crates/buzz-relay/src/audio/`, already documented at `architecture-flows-huddle-audio`, which is a distinct, unrelated system). `desktop/src-tauri/Cargo.toml:109` depends on it as `buzz_voice_pkg`, consumed by `desktop/src-tauri/src/huddle/pocket.rs` and `tts_voice_import.rs`. The merged node `architecture-context-external-services` already states, in its own Scope and omissions section, that `buzz-voice`'s `PocketTts` primitives load a bundled on-device model rather than calling a network service. `git ls-tree -r --name-only HEAD -- launchpad/docs/corpus` lists no node whose subject is TTS/voice/Pocket, and no ADR/NIP under `launchpad/decisions/` or `docs/nips/` mentions Pocket/TTS/voice — so there is no existing corpus node or spec this crate would declare `implements` toward.

STEP 1  Confirm the dependency/integration surface precisely: re-check `grep -rn "buzz_voice_pkg" desktop/src-tauri/src` for every call site, and read enough of `desktop/src-tauri/src/huddle/pocket.rs` and `tts_voice_import.rs` (already read: they re-export `pocket::*` and use `imported::{ImportedVoice, PocketVoiceLibrary}`) to state accurately what the desktop huddle TTS pipeline uses from this crate vs. what it owns itself (chunking/streaming orchestration, playback, activity detection all live in `desktop/src-tauri/src/huddle/tts_*.rs`, outside this crate). Record `git rev-parse HEAD` as the provenance commit.                                    [independent]
        done when: a short list exists (in working notes, not committed) of which `buzz_voice_pkg` symbols each desktop file imports, and which `desktop/src-tauri/src/huddle/tts_*.rs` responsibilities are confirmed as NOT part of `buzz-voice`.

STEP 2  [needs 1] Write `launchpad/docs/corpus/implementation/crates/buzz-voice.md` following `launchpad/docs/corpus/templates/implementation-reference.md`'s required sections exactly (Realization statement, Target, Implementation surface, Divergences, Verification, Relationships, Scope and omissions). Front matter: `id: implementation-crates-buzz-voice`, `type: implementation`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, evidence entries classed FACT (crate source, Cargo.toml, desktop call sites, test file) plus a commit-citation provenance FACT for STEP 1's revision, no `relationships` declared (no merged corpus node or ADR/NIP is a legitimate `implements` target — record this as a checked absence, per `AGENTS.md`'s warning against a stale "nothing to point at" excuse, since a target does exist in prose form: `architecture-context-external-services`, which is architecture-surface, not a realization target). State plainly that the crate implements no named spec/ADR/NIP, only its own module contract; name what it does NOT own (huddle orchestration, playback, streaming pipeline glue — all in `desktop/src-tauri/src/huddle/tts_*.rs`).                                    [needs 1]
        done when: the file exists, every DoD bullet from issue #940's body is addressed in the body text, and every evidence entry cites a source actually opened in STEP 1 or during drafting. ← RUNS HERE

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL entries naming this node and re-run until either exit 0 or only the pre-existing ~21 unrelated baseline failures remain (confirmed by diffing against `origin/launchpad` if the exit code is nonzero).                                    [needs 2]
        done when: `validate.py` output contains zero FAIL lines whose node is `implementation-crates-buzz-voice`.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the commit-gate stamp; confirm `OK`.                                    [needs 3]
        done when: the command's output ends in `OK` with no errors or failures.

STEP 5  [needs 4] `git add launchpad/docs/corpus/implementation/crates/buzz-voice.md launchpad/plans/2026-09-01-issue-940-buzz-voice.md` and `git commit -s -m "docs(corpus): add buzz-voice implementation reference (#940)"` in a separate tool call from STEP 4. Do not push, do not open a PR (batch integration happens later, separately).                                    [needs 4]
        done when: `git log -1` shows the new commit on `task/940-buzz-voice` containing exactly those two files.

PARALLEL: none — one document, one dependent chain (evidence → draft → validate → gate → commit). STEP 1 is the only step with no predecessor.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 (or show only the pre-existing baseline failures, verified by diff). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before the commit. `qa` explore mode does not apply — this is a docs-only corpus node with no runtime interface to exercise. `review-adjudicate` and cross-model final review are deferred to the batch owner's later integration review, not run here. `corpus-review` skill used for self-review if reachable in-session; otherwise a careful manual re-read against the DoD checklist, disclosed in the final report.

BUDGET: small — one crate (5 source files, 1 test file, no README), no code changes, evidence gathering already mostly done (Cargo.toml, lib.rs, pocket.rs, pocket_models.rs, imported.rs, pocket_april.rs, pocket_import_audio.rs, desktop call sites, and the two corpus nodes that already mention this crate/subject area all read).

OPEN: Whether `type: implementation` (rather than an alternative like `interfaces-events`) is correct — resolved yes: the crate is a local library surface (Rust API + on-disk model bundle), not a wire-level protocol/contract, so `implementation` is the template's own default reading and no alternative fits better. Whether to declare a `references` or `part-of` relationship toward `architecture-context-external-services` (which already discusses this crate in prose) — resolved no: that node's own relationship type doesn't fit either `references` (reserved for a verification/test-strategy node) or `part-of` (reserved for a broader implementation node this is a sub-component of); the connection is instead made in prose citing that node directly, which is honest without inventing a relationship type the schema doesn't intend for this case.

LEFT OUT: No `implements` relationship — no ADR, NIP, or existing corpus node describes a "Pocket TTS contract" for this crate to realize against; this is stated as a checked absence in the node body, not silently omitted. No coverage of `desktop/src-tauri/src/huddle/tts_*.rs`'s own orchestration logic — that is desktop-app implementation surface, a separate (and much larger) potential corpus node, out of scope for this one crate's reference. No second corpus document, generated index, or edit to `architecture-context-external-services` — this task adds exactly one hand-authored node.
