# Issue #878 — events/kinds/kind-40003-stream-message-edit.md

Stated size: issue explicitly frames this as "a small single-document task" (per the corpus-plan dispatch convention that caps event-kind docs at 5 steps) -> cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/events/kinds/kind-40003-stream-message-edit.md` does not exist (confirmed: `test -f` on the target path and `git ls-tree -r origin/launchpad -- launchpad/docs/corpus/events/` both empty). `launchpad/docs/corpus/templates/event-kind.md` (id `corpus-template-event-kind`) is merged on `origin/launchpad` and states the required section list and evidence expectations for an event-kind node, though it is explicitly a template rather than a per-type standard. `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/standards/evidence.md` and `launchpad/docs/corpus/standards/atomicity.md` are merged and govern front matter, evidence classification and node granularity. No node currently on `origin/launchpad` is a plausible `relationships` target for this subject (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` shows only governance/template nodes, no event or architecture-flow nodes yet).

STEP 1  [independent] Gather evidence in `crates/buzz-core/src/kind.rs` (KIND_STREAM_MESSAGE_EDIT = 40003, its doc comment, its range vs. `is_replaceable`/`is_parameterized_replaceable`/`is_ephemeral`, and its absence from `AUTHOR_ONLY_KINDS`/`P_GATED_KINDS`/`SHARED_GATED_KINDS`/`RESULT_GATED_KINDS`), the tag/content shape in `crates/buzz-sdk/src/builders.rs::build_edit` and `desktop/src-tauri/src/events.rs::build_message_edit`, the ingest-time validation in `crates/buzz-relay/src/handlers/ingest.rs` (`required_scope_for_kind`, `requires_h_channel_scope`, the `skip_membership` branch, and `validate_edit_ownership`), the aux-closure/fanout treatment in `crates/buzz-relay/src/api/bridge.rs` (`WINDOW_AUX_KINDS`) and `docs/nips/NIP-CW.md`, the generic FTS/audit treatment (`crates/buzz-search/src/lib.rs`'s `search_tsv` doc comment, `crates/buzz-audit/src/action.rs`'s `EventCreated`, and its unconditional call site in `crates/buzz-relay/src/handlers/event.rs::enqueue_event_created_audit`), and client-side application in `desktop/src/features/messages/lib/formatTimelineMessages.ts` (`isAuthorizedMessageEdit`, `hasLinkPreviewSuppression`). Record the exact repository revision (`git rev-parse HEAD`). done when: a note exists recording, per source file, the specific symbol/constant read and what it showed, plus the recorded HEAD sha. ← RUNS HERE

STEP 2  [needs 1] Write the front matter: `id: events-kinds-kind-40003-stream-message-edit`, `type: interfaces-events` (per the event-kind template's own instruction that a real instance node takes this enum value), `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships` (per ALREADY TRUE — no merged node is a legitimate target yet), and one evidence entry per substantive claim classified FACT (opened source), INFERENCE (with confidence, for any reasoned step beyond what a source states outright), or TEAM_KNOWLEDGE (issue #878 itself, with `provided_by`), plus a commit citation recording the revision from STEP 1. done when: the front matter block parses as valid YAML with exactly those six keys and every evidence entry's `entry_class` matches the fields it carries (FACT/INFERENCE need `evidence`, INFERENCE also needs `confidence`, TEAM_KNOWLEDGE needs `provided_by`, none of FACT/TEAM_KNOWLEDGE carries `confidence`).

STEP 3  [needs 2] Write the body against the event-kind template's required sections (title/kind identity, referenced NIP/spec — note no `docs/nips/NIP-XX.md` file exists for this kind, so cite `kind.rs` and NIP-01/NIP-29 conventions directly — kind range and delivery classification, tag shape with cardinality, content-field semantics, access control and storage model, a worked example JSON event, and a scope-and-omissions section naming any gap found in STEP 1 that could not be verified). Satisfy every Definition-of-done bullet from the issue body directly, including producers/consumers/authorization/persistence/fanout/search/audit treatment and links to handler/registry and conformance/tests (`crates/buzz-sdk/src/builders.rs`'s `edit_happy_path`/`edit_content_too_large` tests, `crates/buzz-test-client/tests/e2e_human_edit_agent_content.rs`). done when: every bullet in the issue's Definition of done checklist has a corresponding sentence or section in the drafted body, checked line by line against the issue text.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; compare its error set against the 21 known pre-existing failures on `origin/launchpad` (issue #1951) and fix anything beyond that set until the new node introduces zero additional errors. done when: the validator's error output, diffed against the 21 pre-existing failures, contains zero lines naming the new node's file path.

STEP 5  [needs 4] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the commit gate stamp; confirm it prints `OK`. Then, in a separate call, `git add` the node and this plan file and commit with `git commit -s`. done when: the unittest command's output ends with `OK` and `git log -1` on the resulting commit shows both files staged and a `Signed-off-by` trailer.

PARALLEL: none — single file, single task, no independent sub-work.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must introduce zero new FAIL lines beyond the 21 pre-existing ones tracked by #1951. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit gate will accept the commit. No PR is opened and no push happens in this task — review and merge are out of scope per the dispatch instructions.

BUDGET: small — one Markdown document (~150-250 lines), evidence gathering scoped to `kind.rs`, `builders.rs`, `events.rs`, `ingest.rs`, `bridge.rs`, `formatTimelineMessages.ts`, and the search/audit crates' generic (non-kind-specific) treatment; no code changes.

OPEN: Whether every Buzz-proposed kind needs a `docs/nips/NIP-XX.md` file before a corpus event-kind node can be written is an unsettled gap the template itself names — kind 40003 has no such file, so this node cites `kind.rs` and the general NIP-01/NIP-29 conventions as its specification source instead, and states that explicitly rather than inventing a spec document. Whether `messages search`'s own kind allowlist (mentioned in root `AGENTS.md`'s gotcha #3) includes kind 40003 was not traced to CLI source in this task — the node describes only the crate-level (`buzz-search`) FTS treatment, which is generic and not kind-gated, and leaves any CLI-level restriction as an unverified gap rather than asserting one that was not checked.

LEFT OUT: No `relationships` entries (no legitimate merged target exists yet — see ALREADY TRUE). No second corpus document — sibling kinds (40001 unused/reserved, 40002 `KIND_STREAM_MESSAGE_V2`, 40004 pinned, 40005 bookmarked, etc.) are out of scope and belong to their own issues if not already filed. No change to `crates/buzz-core/src/kind.rs`, the relay, the SDK, or the desktop app — this is documentation only. No attempt to resolve the interfaces-events vs. a future "interface" node boundary the template itself flags as unresolved (#1342 in the template's own text) — this node states the boundary as the template does and does not adjudicate it.
