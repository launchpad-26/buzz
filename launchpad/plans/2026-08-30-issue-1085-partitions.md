# Issue #1085 — layers/data/postgres/partitions.md

Stated size: issue #1085 carries no explicit Size line; this is a single-document corpus-authoring task under the corpus-batch-author skill, which caps every task's plan at 5 steps  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/architecture/containers/postgres.md` (id `architecture-containers-postgres`, type `architecture`) is also merged there; `launchpad/docs/corpus/layers/data/postgres/partitions.md` does not exist yet, and no `layers/data/postgres/*` sibling exists on `origin/launchpad` (earlier batch's siblings are still unmerged PRs).

STEP 1 [independent] Gather evidence: read `crates/buzz-db/src/partition.rs` (`ensure_future_partitions`, the table allowlist, DDL-injection guards), `migrations/0001_initial_schema.sql` (the `events`/`delivery_log` `PARTITION BY RANGE` declarations and their initial monthly children), `migrations/0021_created_at_fence_floor.sql` (the replica-fence floor guard and its partition-cloning comment), `crates/buzz-db/src/replica_fence.rs::verify_floor_guard_catalog` (catalog check across `events` + every `pg_inherits` child), `crates/buzz-db/src/migration.rs::run_migrations_locked` (fails closed if the guard is missing on any partition), `migrations/0029_community_deletion.sql` (`attach_community_write_fence`, applied only to non-partition parents — Postgres trigger inheritance clones it to children), `crates/buzz-relay/src/main.rs` (startup call to `ensure_future_partitions(3)`), and `scripts/attach-schema-partitions.sql` (the pgschema-vs-raw-SQL partition-attach repair path). Confirm there is no partition-drop/retention code path and record that as a gap. ← RUNS HERE
done when: every listed source has been opened and its relevant lines noted (function name, migration id, or line range) for use as an evidence citation in STEP 2; the retention-code grep result (found/not found) is recorded.

STEP 2 [needs 1] Write front matter (schema-valid: id `layers-data-postgres-partitions`, type `layers` — overriding the `datastore.md` template's own worked-example type per prior batch precedent, disclosed in the ledger — status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, one `relationships` entry: `part-of` → `architecture-containers-postgres`, confirmed merged on `origin/launchpad`) and the body, shaped from `templates/datastore.md`'s required-sections list (purpose/scope, technology/attachment deferred to the container node, owned tables and their partition ranges, the monthly creation mechanism, consistency semantics tying partition topology to the replica-fence floor guard, tenancy/security boundary via trigger inheritance from the parent, failure behavior on a missing/mis-shaped guard, and the no-retention gap) rather than `data-entity.md` or `reference.md` — this subject is a cross-cutting physical storage-layout strategy spanning two tables, not one domain entity or a standalone procedure. Every claim gets an evidence entry (FACT for anything opened directly in STEP 1, INFERENCE with `confidence` for reasoned claims, TEAM_KNOWLEDGE only if attributing to an issue/PR/person). Link `migrations/`, `crates/buzz-db/src/partition.rs`, `crates/buzz-db/src/replica_fence.rs` and the `buzz-db` unit tests in `partition.rs` rather than reproducing DDL.
done when: `launchpad/docs/corpus/layers/data/postgres/partitions.md` exists, every DoD bullet in issue #1085's body is addressed by a section or an explicit named gap, and every evidence entry cites a source actually opened in STEP 1.

STEP 3 [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.
done when: the command's exit status is 0.

STEP 4 [needs 3] Run the corpus unittest suite as the sole, unpiped prior command to earn the verification stamp, then commit the plan + document in a separate call. No push, no PR — this worktree is bundled with its Feature #610 batch siblings by a later orchestration step.
done when: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` reports OK, and `git log -1` on this branch shows a commit containing exactly the plan file and the target document.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report OK before committing. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later review — not run here.

BUDGET: small — one document, no code changes, evidence scoped to the partition-manager module, two migrations, one runtime probe module, and their existing unit tests.

OPEN: `templates/datastore.md`'s own worked example scopes a datastore document to "one running technology instance" (all of Postgres); this task's actual subject, per the issue body and #610, is one operational mechanism (monthly range partitioning) spanning two tables within that instance, not the whole instance. The document adapts the same required-section shape (schema/namespace inventory, migration mechanism, access patterns, operational characteristics, tenancy/failure behavior) narrowed to that mechanism, and states this narrowing explicitly rather than either padding out a full Postgres profile the issue did not ask for or silently reusing the template's broader framing. Whether a full `layers/data/postgres/postgres.md` (or similarly-scoped) sibling should eventually own the `technology & attachment profile` section this document defers is out of scope for this task.

LEFT OUT: No relationships to `layers/data/postgres/*` siblings from earlier, unmerged batches (per the run's own precedent, they do not exist on `origin/launchpad` yet). No attempt to document Redis or the S3-compatible object store's own operational characteristics — this node is scoped to Postgres partitioning only. No attempt to add partition retention/dropping to the codebase — its absence is reported as a gap in the finished node, not fixed here.
