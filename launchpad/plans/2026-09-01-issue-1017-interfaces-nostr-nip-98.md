# Issue #1017 — interfaces/nostr/nip-98.md

Stated size: no Size line on the corpus-plan:v2 task template; cap set by the dispatch instructions for this task -> cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/interface.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/interfaces/` does not exist on `origin/launchpad` yet — no `interfaces/nostr/nip-98.md`, no `interfaces-http-media` node, no other interface-shaped node anywhere in the corpus. `crates/buzz-auth/src/nip98.rs` and `crates/buzz-auth/src/nip98_replay.rs` implement NIP-98 (kind:27235) verification and replay protection; `crates/buzz-relay/src/api/bridge.rs`, `admin/auth.rs`, `git/transport.rs`, `invites.rs`, `gifs.rs`, and `workflows.rs` all call into that shared verifier. `crates/buzz-relay/src/api/media.rs` authenticates Blossom media with a *different* scheme (kind:24242, BUD-01) that does not call `buzz_auth::verify_nip98_event` at all — the dispatch note's guess that NIP-98 is "likely used for Blossom media" does not hold and must not be carried into the node.

STEP 1  [independent] Confirm scope against the issue and read every NIP-98 call site's real behavior: `crates/buzz-auth/src/nip98.rs` (verification steps, `AuthError::Nip98Invalid`), `crates/buzz-auth/src/nip98_replay.rs` (`Nip98ReplayGuard`, `DEFAULT_REPLAY_TTL_SECS`/`MAX_REPLAY_TTL_SECS`, `AuthError::Nip98Replay`), `crates/buzz-core/src/kind.rs` (`KIND_HTTP_AUTH = 27235`), `crates/buzz-relay/src/api/bridge.rs` (`verify_bridge_auth_with_options`, `nip98_expected_url`, `check_nip98_replay`, used by `/events` `/query` `/count`), `crates/buzz-relay/src/api/admin/auth.rs` (admin API NIP-98 mode), `crates/buzz-relay/src/api/git/transport.rs` (git smart HTTP — deliberately skips method-binding and replay dedup, see its inline comments), and `crates/buzz-relay/src/api/invites.rs`/`gifs.rs`/`workflows.rs` (reuse `bridge::nip98_expected_url`/`check_nip98_replay`). Confirm `KIND_BLOSSOM_AUTH = 24242` is a distinct scheme owned by `buzz-media`, not this interface. ← RUNS HERE
    done when: notes list every call site above with the file:line(s) actually opened, and explicitly record whether Blossom media auth is in or out of scope (it is out).

STEP 2  [needs 1] Draft `launchpad/docs/corpus/interfaces/nostr/nip-98.md` following `templates/interface.md`'s required sections (Interface description, Operations, Contract and stability, Boundary, Relationships, Scope and omissions). Front matter: `id: interfaces-nostr-nip-98`, `type: interfaces-events` (the schema's single combined interfaces/events value, per the template's own "A note on `type`" section), `status: draft`, `origin: launchpad` (matching every other merged corpus node), `audiences: [agent, developer, reviewer]`, no `relationships` (no merged corpus node is a legitimate target — `interfaces-http-media` does not exist on `origin/launchpad`, checked in STEP 1's ALREADY TRUE). Evidence entries cite only source paths/symbols opened in STEP 1, classed FACT (code opened), INFERENCE (reasoned, with `confidence`), or TEAM_KNOWLEDGE (attributed, with `provided_by`) per `AGENTS.md`'s rules. Include the upstream NIP-98 spec link pinned to the same commit SHA this corpus already uses for other NIPs (`dabfcb2aaecf4fa374eda8b1232ab303a03f60ba`, used in `templates/event-kind.md` and `templates/data-entity.md`), one valid-auth example (bridge `/events` flow) and one failure example (timestamp/URL/method mismatch, or git transport's deliberate replay-skip as a documented exception).
    done when: the file exists at that path with schema-shaped front matter and all six required template sections present.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any FAIL line (UNVERIFIED notices are acceptable) and re-run until exit 0.
    done when: the command's exit code is 0 and its output contains no `FAIL` line attributable to the new node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the commit gate stamp; confirm it prints `OK`.
    done when: the command's final output line is `OK` with no errors or failures.

STEP 5  [needs 4] Stage `launchpad/docs/corpus/interfaces/nostr/nip-98.md` and this plan file, commit with `git commit -s -m "docs(corpus): document NIP-98 interface (#1017)"`, then self-review the diff against the issue's Definition-of-done checklist line by line.
    done when: `git log -1` shows the signed commit and a re-read of the diff confirms every DoD bullet is satisfied and `validate.py` still exits 0.

PARALLEL: none — single file, single task, steps are strictly sequential (each depends on the evidence or artifact the previous step produced).

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before commit. No PR is opened by this task — review/adjudication is deferred to whoever assembles the Feature #616 batch.

BUDGET: small — one corpus document, no runtime code changes, evidence gathering scoped to the ~10 call sites already enumerated in ALREADY TRUE plus `buzz-core/src/kind.rs` and `buzz-auth/src/error.rs`.

OPEN: Whether a future `interfaces-http-media` node (issue #984, not yet merged) should later gain a `references` edge back to this node, or vice versa, once both exist — left for whoever authors #984 or a subsequent edit, since `relationships.schema.json` requires the target to already resolve and it does not today. Whether git smart HTTP's deliberate skip of method-binding and replay dedup (documented in `git/transport.rs`'s own comments) belongs in this node's Contract-and-stability section as a named exception or should instead wait for a dedicated git-transport interface node — this plan resolves it by documenting the exception inline, since it is a real, current behavior of the same `verify_nip98_event` call this node otherwise describes uniformly.

LEFT OUT: No node for Blossom media auth (kind:24242, BUD-01) — confirmed in STEP 1 to be a distinct scheme `buzz-media`/`media.rs` owns, not NIP-98; that is #984's or a future task's subject, not this one. No re-derivation of scope from parent Feature #616 or PRD #602 beyond what issue #1017 itself states. No attempt to add the `interfaces/` directory's own index or a second hand-authored document — this task produces exactly one canonical node. No change to `crates/buzz-auth` or `crates/buzz-relay` runtime code.
