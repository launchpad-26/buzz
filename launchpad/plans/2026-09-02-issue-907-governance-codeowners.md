# Issue #907 — governance/codeowners.md

ALREADY TRUE: `launchpad/docs/corpus/AGENTS.md`, `schema/node.schema.json`, `templates/policy.md` and `standards/review-requirements.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/governance/` does **not** exist (`git ls-tree origin/launchpad launchpad/docs/corpus/governance/` is empty) — this node creates that directory. `.github/CODEOWNERS` **does** exist and is one line.

STEP 1  Establish ground truth on what actually routes review, rather than assuming. Confirmed already: `.github/CODEOWNERS` is `* @block/buzz-oss-team`, an upstream file authored by upstream contributors (`git log -- .github/CODEOWNERS` shows only Will Pfleger commits) and byte-identical to `block/buzz`'s; `gh api repos/launchpad-26/buzz/codeowners/errors` reports the sole rule as `Unknown owner`, while the identical file on `block/buzz` reports `errors: []`; the `launchpad-26` org has teams `maintainers` and `students`, not `block/buzz-oss-team`; `gh api repos/launchpad-26/buzz/rulesets?includes_parents=true` returns `[]`; `gh api repos/launchpad-26/buzz/branches/launchpad/protection` returns 404 and every one of 100 branches reports `protected: false`. ← RUNS HERE

STEP 2  [needs 1] Establish what the fork's own documents *claim* about this, so the node can report the delta rather than restating a claim. `launchpad/AGENTS.md` §6 asserts `launchpad` is protected with one required approval, "measured 2026-08-28"; §3's closed exception list does not include `.github/CODEOWNERS`; ADR-0033 (Accepted) decided a six-line CODEOWNERS boundary ruleset with Task #1428 owning implementation; ADR-0056's Provenance records that ADR-0033's CODEOWNERS half survives because `maintainers`/`students` already exist.

STEP 3  [needs 2] Write front matter against `node.schema.json`: id `governance-codeowners`, type `governance` (confirmed present in the schema's `type` enum), status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, evidence ledger opening with the revision commit citation (`git cat-file -e aef93f2c2…` exits 0), relationships `depends-on: corpus-agents` and `references: corpus-standard-review-requirements` — both ids confirmed with `git show origin/launchpad:<path>`.

STEP 4  [needs 3] Write the body on `templates/policy.md`'s six required sections in order (Scope and authority / MUST / SHOULD / Enforcement / Exceptions and escalation / Scope and omissions), H1 `# Policy: code ownership and review routing`, RFC 2119 framing, authority stated as derived. Record the ownership gap as a named gap, not a proposal. Link `standards/review-requirements.md` for corpus-node review duty rather than duplicating it. Then run `python3 launchpad/project-intelligence/corpus/validate.py` until PASS.

STEP 5  [needs 4] Run the corpus unittest suite bare and unpiped as the sole command in its own call, confirm OK, then `git add` document + plan and `git commit -s` in a separate call. Stop at the commit.

PARALLEL: none — one document, one task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must report PASS. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report OK before the commit. No push, no PR.

OPEN: Whether an **organization-level** ruleset targets this repository cannot be settled from this session — `gh api orgs/launchpad-26/rulesets` requires the `admin:org` scope this token lacks. The repository-scoped query with `includes_parents=true` returning `[]` is the strongest available evidence and is what the node cites; the scope limitation is recorded in the node's own not-verified list rather than smoothed over. Similarly, `branches/launchpad/protection` 404s under a `maintain`-role token, so that endpoint alone would be ambiguous — the node rests the claim on the `protected: false` flag from the branches listing, which is readable at this permission level, and says so.

LEFT OUT: Not filing the ADR-0033-vs-§3 tension, the falsified §6 protection claim, the invalid CODEOWNERS owner, or the stale rule-1 quote in `launchpad-review-agent-publish.yml` as issues — this task authors one document and reports findings to its caller. Not editing `.github/CODEOWNERS` (an upstream-owned file outside §3's closed exception list) and not implementing #1428. Not restating `standards/review-requirements.md`'s corpus-review duty, per the policy template's P9.
