# Issue #1036 — layers/authorization/media-authorization.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/layers/authorization/media-authorization.md` does not exist yet, and no `launchpad/docs/corpus/layers/` directory exists at all on `origin/launchpad`. `architecture/flows/media-upload.md` and `architecture/flows/media-download.md` are already merged and document the full end-to-end HTTP flows (including their own authorization steps) — this node must not duplicate their content, only reference them. A sibling overview node, `layers-authorization-authorization` (issue #1031, PR #1796), is open but unmerged, so it is not a valid `relationships` target yet. Issue #1027 established that media upload's earlier bearer-token/`api_tokens` scope check was removed (PR #1444, merged) in favor of Blossom kind:24242 (BUD-11) signed events plus NIP-43 relay membership — current code must be verified directly, not assumed from that issue's summary.

STEP 1  Gather evidence: read the issue and its DoD; confirm the target file does not exist and record the merged corpus tree (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`). Read `launchpad/docs/corpus/AGENTS.md` and the two already-merged flow nodes (`architecture/flows/media-upload.md`, `architecture/flows/media-download.md`) as structural/content precedent — no `layers`-type template exists, so this node is written directly against `node.schema.json`. Trace the real authorization code: `crates/buzz-media/src/auth.rs` (Blossom kind:24242/BUD-11 verification, upload vs. get verb), `crates/buzz-relay/src/api/media.rs` (`AuthenticatedUpload` extractor, `authenticate_media_read`, `extract_blossom_auth`), `crates/buzz-relay/src/api/mod.rs` (`relay_members::{check_relay_membership, enforce_relay_membership}`, NIP-OA owner delegation), `crates/buzz-relay/src/config.rs` (`require_relay_membership`, `allow_nip_oa_auth`, `require_auth_token` defaults/independence), and `crates/buzz-media/src/error.rs` (`MediaError::into_response` status grouping). Confirm PR #1444's bearer-token removal directly in current code (no `X-Auth-Token`/`api_tokens` path remains in `media.rs`). ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `layers-authorization-media-authorization`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, two `references` relationships to `architecture-flows-media-upload` and `architecture-flows-media-download` — both confirmed present on `origin/launchpad`) and the body: a one-sentence definition of media authorization as the Blossom-signed-event-plus-relay-membership composition gating blob upload/download, a boundary section distinguishing it from (a) the unrelated `require_auth_token` REST-API gate, (b) the generic NIP-29 channel/community authorization layers `layers-authorization-authorization` names (unmerged, so named in prose only, no relationship edge), and (c) the removed bearer-token path PR #1444 deleted. Document the two-part composed shape (BUD-11 kind:24242 signature/verb/expiration/hash-or-server-tag verification, then NIP-43 relay membership with NIP-OA owner-delegation fallback) shared identically by upload and read, the open-relay default (`require_relay_membership=false`), and the authentication-oracle-avoiding error-status grouping. Link to the two flow nodes rather than restating their step-by-step contract.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task; the overview node (#1031/PR #1796) and other `layers/authorization/*` siblings are being authored independently in isolated worktrees and are explicitly not to be duplicated here.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK`. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here; this PR is opened as a draft for that reason.

BUDGET: small — one document, no code changes, evidence gathering scoped to `buzz-media::auth`/`error`/`config`, `buzz-relay::api::media`/`api::mod`/`config`, and the two already-merged flow nodes (roughly half a dozen source files plus two corpus nodes read).

OPEN: Whether `layers-authorization-media-authorization` should later gain a `references` (or other) edge to `layers-authorization-authorization` once #1031/PR #1796 merges is left for the batch owner's review — that overview node's own deferred-layers table does not currently list media as a fifth layer, and reconciling the two nodes' vocabulary is not this task's decision to make unilaterally.

LEFT OUT: No relationship to `layers-authorization-authorization` (unmerged; a hard validation error today). No restatement of the media-upload/media-download flow nodes' step-by-step HTTP contract, error-status table, or storage-pipeline detail — only the authorization-relevant subset is described here, with the rest linked. No claim about client-side (desktop/mobile/CLI) construction of the Blossom auth event — server-side contract only, matching the two flow nodes' own stated scope. No end-to-end/integration verification was run or claimed; only the unit/integration tests actually found and read (`crates/buzz-media/src/auth.rs` unit tests, `crates/buzz-test-client/tests/e2e_media.rs`) are cited.
