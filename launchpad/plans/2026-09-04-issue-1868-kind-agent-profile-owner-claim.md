Issue #1868 — docs(corpus): architecture-context-ai-agent.md wrongly claims KIND_AGENT_PROFILE carries an owner reference
Stated size: no `Size` line on the issue → treated as ≤30 minutes (single-file, three-statement doc correction, confirmed by RCA below) → cap: 5 steps

ALREADY TRUE  (verified against git and code, not notes)
  - Working tree is clean on branch fix/issue-1868-kind-agent-profile-owner-claim, tracking origin/launchpad, HEAD = aef93f2c2 (verified via `git status` / `git log --oneline -10`).
  - `crates/buzz-core/src/kind.rs:87` defines `KIND_AGENT_PROFILE: u32 = 10100` as a replaceable kind with no owner field in its doc comment.
  - `crates/buzz-relay/src/handlers/side_effects.rs:1237-1269` (`handle_agent_profile`) is the only handler that reads kind:10100 content; it parses only `channel_add_policy` and uses `event.pubkey` (the agent's own key) as the acted-on user. No owner field, tag, or lookup anywhere.
  - Real ownership mechanism, confirmed by code: a NIP-OA `auth` tag (`["auth", owner_pubkey, conditions, sig]`) carried on the agent's kind:0 metadata event, cryptographically verified in `crates/buzz-acp/src/lib.rs:329-370` (`check_sibling_via_profile`) against `crates/buzz-sdk/src/nip_oa.rs`. The same auth tag is presented at NIP-42 auth time and materialized into `users.agent_owner_pubkey` (`crates/buzz-relay/src/handlers/auth.rs:242-262`, `crates/buzz-db/src/store/user.rs:293-363`).
  - `crates/buzz-core/src/kind.rs:284-291` confirms `KIND_MANAGED_AGENT` (30177) genuinely IS owner-authored: "Managed-agent definition event published by the workspace owner. Addressed by (pubkey, kind, d_tag) where d_tag is the agent's pubkey" — the event's own author pubkey is the owner. This claim is correct and must be kept.
  - `launchpad/docs/corpus/architecture/context/ai-agent.md` contains the false claim in exactly three places, lines unchanged since the issue was filed:
      1. Line 78 — frontmatter `evidence` statement (entry_class FACT)
      2. Line 133 — "Human owner" row of the Actors and systems table
      3. Lines 179-180 — prose step 2 ("Identity") under "How an AI Agent's action reaches Buzz"
  - A repo-wide grep (`grep -rn "KIND_AGENT_PROFILE" launchpad/docs/corpus/`) confirms `ai-agent.md` is the only corpus file naming `KIND_AGENT_PROFILE` at all — no other node needs correction for this specific claim.
  - `launchpad/docs/corpus/README.md:26-34` documents `launchpad/project-intelligence/corpus/validate.py` (run via `just corpus-validate` with Hermit activated) as the deterministic schema/evidence check for every corpus node; CI runs it on corpus PRs (`.github/workflows/launchpad-corpus-validate.yml`).

STEP 1  Correct the three false statements in ai-agent.md                                   [independent]
        Edit `launchpad/docs/corpus/architecture/context/ai-agent.md`:
        - Line 78 (frontmatter statement): rewrite so `KIND_AGENT_PROFILE` is described as carrying only the agent's own metadata (its `channel_add_policy`), with ownership instead attributed to the NIP-OA `auth` tag on the agent's kind:0 metadata event. Keep the `KIND_MANAGED_AGENT` half of the statement (owner-published, forbidden from carrying secrets) unchanged in substance. Add evidence entries for the corrected claim: `crates/buzz-acp/src/lib.rs` and `crates/buzz-sdk/src/nip_oa.rs` (the NIP-OA verification path), alongside the existing `crates/buzz-core/src/kind.rs`.
        - Line 133 (table row): change "declared with an owning human (`KIND_MANAGED_AGENT`, `KIND_AGENT_PROFILE`)" so only `KIND_MANAGED_AGENT` is cited as declaring ownership; if `KIND_AGENT_PROFILE` is still mentioned in that row, it must be described accurately (agent's own identity/metadata, not ownership).
        - Lines 179-180 (prose step 2): rewrite so `KIND_AGENT_PROFILE` "carries the agent's own metadata" (channel-add policy) with no owner reference, and ownership is stated as established via the kind:0 metadata event's NIP-OA `auth` tag. Keep the `KIND_MANAGED_AGENT` sentence's owner-authored claim.
        done when: `grep -n "KIND_AGENT_PROFILE" launchpad/docs/corpus/architecture/context/ai-agent.md` shows no remaining text asserting `KIND_AGENT_PROFILE` carries or references an owner, and each of the three locations mentions the kind:0 NIP-OA `auth` tag as the actual ownership mechanism.

STEP 2  Re-confirm no other corpus node repeats the same false claim                         [independent]
        Re-run `grep -rln "KIND_AGENT_PROFILE" launchpad/docs/corpus/` and `grep -rn "reference to its owner\|reference to the agent's human owner" launchpad/docs/corpus/` against the current working tree (not the earlier RCA pass) to catch any drift since investigation began.
        done when: the grep output is captured and reported; any additional match outside `ai-agent.md` is reported (not silently fixed), per the task's instruction to report rather than fix findings outside the named file.

STEP 3  Validate the corrected node                                              [needs 1]  ← RUNS HERE
        Run the corpus validator: `. ./bin/activate-hermit && just corpus-validate` (or directly `python3 launchpad/project-intelligence/corpus/validate.py` if the just target needs adjustment) from the worktree root.
        done when: the validator exits 0 for `architecture-context-ai-agent`, confirming the edited frontmatter (including any new/changed `evidence` entries) still satisfies `node.schema.json`.

STEP 4  Commit and push                                                          [needs 3]
        `git add launchpad/docs/corpus/architecture/context/ai-agent.md` and `git commit -s` with a message describing the correction and referencing #1868; push the branch to origin.
        done when: `git log -1` shows a commit with a `Signed-off-by` trailer touching only `ai-agent.md`, and `git push` succeeds against `origin fix/issue-1868-kind-agent-profile-owner-claim`.

STEP 5  Open the PR                                                              [needs 4]
        Run `gh pr create` (as a standalone Bash call, no `cd` prefix — this repo's `pr-gate.sh` hook rejects a chained `cd && gh pr create`) with base `launchpad`, a body including "Closes #1868", and — after checking 2-3 recent agent-authored merged PRs' labels via `gh pr list --repo launchpad-26/buzz --search "is:merged label:by:agent" --limit 3 --json number,labels` — apply a `by:agent` label if that's the established convention. Expect the repo's `pr-gate.sh` hook to fall back to a draft PR (documented escape valve — the plan-issue/build-change pipeline doesn't produce a `review-final` ledger verdict); that is expected, not a failure to fix.
        done when: `gh pr view <n> --repo launchpad-26/buzz --json url,isDraft,body` shows the PR exists, its body contains "Closes #1868", and its base is `launchpad`.

PARALLEL  Steps 1 and 2 touch different scopes (one file's edits vs. a repo-wide read-only grep) and could run concurrently, but both are cheap enough that sequencing them costs nothing; steps 3-5 are strictly sequential (validate → commit → push → PR), each depending on the last succeeding.
GATES     No `review-code`/`review-tests`/`review-a11y` gate applies — this is a docs-only corpus-text correction with no runtime behavior, UI, or executable code touched. `qa` explore mode does not apply for the same reason (no runtime interface to exercise). The corpus validator in Step 3 is this change's functional gate (schema + evidence-citation correctness). Per the task's explicit process, this plan hands off to `serina:build-change`, which invokes its own review-gate step; expect it to fall back to the documented draft-PR escape valve since this pipeline does not produce a `review-final` ledger verdict.
BUDGET    Step 1 is the step most likely to eat the budget — getting the corrected wording exactly right in three different sentence shapes (a YAML-embedded statement, a markdown table cell, and flowing prose) without overstating the DB-side `agent_owner_pubkey` mechanism (which is a downstream materialization of the NIP-OA auth tag, not a second independent ownership channel) takes more care than the other steps.
OPEN      The issue does not specify exact replacement wording — only the direction of the correction (owner claim moves from `KIND_AGENT_PROFILE` to the kind:0 NIP-OA `auth` tag, `KIND_MANAGED_AGENT`'s claim stays). This plan leaves exact phrasing to the implementer, constrained by the done-when checks in Step 1.
LEFT OUT  Not touching `crates/buzz-core/src/kind.rs` or any other code — the issue is purely a documentation-accuracy bug, and the RCA already confirmed the code itself is correct. Not adding a new corpus statement about the `users.agent_owner_pubkey` DB column or NIP-42-auth-time materialization path — that is real and evidenced (see ALREADY TRUE) but is a level of implementation detail beyond what this node's three false statements need to be corrected; mentioning the kind:0 NIP-OA `auth` tag is sufficient and matches the issue's own stated fix.
