# Issue #646 — agents/documentation-update.md

ALREADY TRUE: `launchpad/docs/corpus/AGENTS.md` (id `corpus-agents`), `launchpad/docs/corpus/standards/provenance.md` (id `corpus-standard-provenance`, type `governance`, closes #1321), `launchpad/docs/corpus/templates/procedure.md` (id `corpus-template-procedure`, type `governance`) and `launchpad/docs/corpus/agents/invariants.md` (id `agents-invariants`, type `agent`) are all merged on `origin/launchpad` at HEAD `aef93f2c2acfe9dfe66d22d33f5abb4ac12baa90`. `launchpad/docs/corpus/agents/documentation-update.md` does not exist yet. `launchpad/project-intelligence/corpus/tests/check-plan.sh` does not exist anywhere in this repo — confirmed by an earlier batch, re-confirmed here (`find` for it returns nothing) — so this plan proceeds without that gate.

STEP 1  Gather evidence: re-read `AGENTS.md`'s "Updating a node" (6 steps) and `standards/provenance.md`'s MUST 1–5 / SHOULD 1–3 in full (already done this session — both fully read). Confirm `standards/provenance.md` really closes #1321 (`gh issue view 1321 --repo launchpad-26/buzz --json state,closed`) and that issue #646's own DoD is how-to-shaped (already fetched: goal/prerequisites, ordered executable steps, success verification, links authoritative commands — matches `templates/procedure.md`, not a policy template). ← RUNS HERE

STEP 2  [needs 1] Write the front matter: id `agents-documentation-update`, type `agent` (corpus surface is the agents/ family, per `agents-invariants`' own precedent — `governance` is reserved for `standards/`+`templates/`), status `draft`, origin `launchpad`, audiences `[agent, reviewer]` (matches `corpus-agents`), evidence entries citing only sources actually opened (`AGENTS.md`, `standards/provenance.md`, `node.schema.json`, the issue DoD, sibling issue titles for #645/#647 by title only), relationships: `implements: corpus-template-procedure`, `depends-on: corpus-agents`, `depends-on: corpus-standard-provenance`, `references: agents-invariants` — all four resolve on `origin/launchpad`.

STEP 3  [needs 2] Write the body against `templates/procedure.md`'s required sections (Overview, Before you start, one numbered task sequence, See also, Boundary, Relationships, Scope and omissions). The numbered sequence is AGENTS.md's six update steps, with step 4 (whether the recorded revision moves) built on `standards/provenance.md`'s MUST 1/3/4 rule instead of restating AGENTS.md's own stale "#1321 unsettled" caveat — cite both, do not restate either at length. Boundary section names, by title only: not `agents/documentation-creation.md` (#645, creating a brand-new node), not `agents/documentation-validation.md` (#647, running/interpreting `validate.py` — link to the command as this guide's final step only), plus the template's own reference/tutorial/concept exclusions.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the worktree root; fix and re-run until exit 0.

STEP 5  [needs 4] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the commit gate; confirm `OK`. Then, in a separate call, `git add` the plan + document and `git commit -s`. Then attempt `review-code` (skill or subagent) against the diff; if unreachable, self-review against the DoD line by line and re-run `validate.py` after any fix. Do not push or open a PR.

PARALLEL: none — single file, single task, no code changes.

GATES: `validate.py` must exit 0. The corpus unittest suite must print `OK`. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's Feature-wide integration — not run here.

BUDGET: small — one procedure document, no runtime code, evidence gathering scoped to the four already-merged sources above plus the issue body/DoD and two sibling issue titles.

OPEN: Issue #646's DoD checkbox list is the byte-identical standards-track boilerplate reused across the whole corpus batch (schema-valid front matter, one independently maintainable idea, evidence discipline, validate.py passing) layered under a how-to-specific tail (goal/prerequisites/scope, ordered steps, verification/rollback, authoritative-command links) — the how-to tail is what actually distinguishes this task from a policy-shaped one, so this plan is built against the tail's shape via `templates/procedure.md`, exactly as `templates/procedure.md`'s own "Note on Definition of Done" section describes doing for itself.

LEFT OUT: No relationships to any of the other 30 sibling `agents/*.md` / `ingestion/*.md` tasks under Feature #620 — none are merged on `origin/launchpad` at this node's authoring time, including #645 and #647, which this node's Boundary section names by title only and does not link as `relationships` targets. No attempt to run or interpret `validate.py`'s findings as a reusable skill (#647's scope) beyond linking the bare command. No new node-creation guidance (#645's scope). No change to `AGENTS.md` or `standards/provenance.md` themselves.
