# Plan — issue #1127: document `layers/networking/http.md`

Issue: `launchpad-26/buzz#1127` (task, parent Feature #609)
Branch: `task/1127-http`
Worktree: `/home/serina/Launchpad/buzz/__worktrees/task-1127-http`
Base: `origin/launchpad` at `29ca9b189bd3f639ba09c972b57c70538c0860c6`

## ALREADY TRUE

- `launchpad/docs/corpus/layers/networking/` does not exist on `origin/launchpad`;
  `find launchpad/docs/corpus/layers -type d` returns only `observability`, `data`,
  `configuration`, `compute`, `lifecycle` and the three `data/` children. This node
  creates the directory.
- `crates/buzz-relay/src/router.rs` (825 lines) has been read in full. It is the
  authoritative inventory: `build_router` composes `api_router`, `media_router`,
  `git_router`, `git_policy_router`, an optional `admin_router` and an SPA
  `fallback_service`, then applies `track_metrics`, `http_trace_layer()` and
  `build_cors_layer(...)` once over the merged whole. `build_health_router` is a
  second, separate router.
- The middleware slice has been read at source: `track_metrics`
  (`crates/buzz-relay/src/metrics.rs`), `build_cors_layer` and `make_http_span`
  (`router.rs`), `RequestBodyLimitLayer` per sub-router (`router.rs`,
  `crates/buzz-relay/src/api/git/transport.rs`, `crates/buzz-relay/src/api/git/mod.rs`).
- Listener topology read in `crates/buzz-relay/src/main.rs`: `serve()` binds
  `config.bind_addr` (TCP), optionally `config.uds_path` (Unix socket, same router
  clone), and `config.health_port` for the health-only router; `relay_metrics::install`
  opens a third listener on `config.metrics_port`.
- The repo's stated position is in `AGENTS.md` ("Nostr-first HTTP surface" and
  "Prefer Nostr events over new HTTP endpoints"). `CLAUDE.md` is a symlink to
  `AGENTS.md` — one file, cite `AGENTS.md`.
- Four merged HTTP-adjacent nodes read in full or in their boundary sections:
  `verification-contracts-http`, `architecture-flows-http-event-submission`,
  `capabilities-git-smart-http`, `capabilities-media-blossom`. Each owns one
  endpoint's contract, flow or capability. None documents the surface as a whole.
- Legal relationship targets confirmed by grepping the merged-id index: all of
  `verification-contracts-http`, `architecture-flows-http-event-submission`,
  `capabilities-git-smart-http`, `capabilities-media-blossom`,
  `implementation-crates-buzz-relay`, `architecture-containers-relay`,
  `layers-observability-metrics`, `layers-configuration-relay-configuration`
  appear there. No #609 sibling appears there — none may be targeted.
- Sibling #609 tasks enumerated via `gh issue list`: #1121–#1134. #1124
  (external-webhooks), #1126 (host-routing), #1131 (request-routing), #1133 (tls),
  #1134 (websocket) are the neighbours whose subjects abut this one.

## STEP 1 — write the front matter

Seven permitted fields only. `id: layers-networking-http`, `type: layers`,
`status: draft`, `origin: launchpad`, `audiences: [agent, developer, operator]`
(operator earned: listener ports, CORS configuration and health-probe topology are
run-the-relay facts). One commit-only FACT, the provenance entry, naming
`29ca9b189bd3f639ba09c972b57c70538c0860c6`. Every other FACT cites a bare
repo-relative path that was actually opened; no line numbers.

**Done when:** front matter parses and every citation is a path opened during
evidence gathering.

## STEP 2 — write the body

Concept-template sections: title + intro, **Definition** (one sentence first),
visual aid (Mermaid, inline — no external image), Background, Use cases,
Boundary/Scope, Scope and omissions. Content is the surface *as a surface*:

- why a Nostr-first system has HTTP at all, and the repo's own rule for adding to it;
- the inventory by category, naming route groups rather than re-specifying any
  endpoint's contract;
- the shared properties — one merged router, body limits per sub-router, one CORS
  layer, one metrics middleware, one trace span, and the separate health/metrics
  listeners that deliberately skip all of it.

**Done when:** no section restates a claim owned by the four merged nodes, and every
substantive claim maps to an evidence entry.

## STEP 3 — write the boundary section explicitly

A named split against `verification-contracts-http` (one test obligation on
`POST /events`), `architecture-flows-http-event-submission` (one endpoint's
lifecycle), `capabilities-git-smart-http` and `capabilities-media-blossom` (two
capabilities exposed over it), plus the unmerged #609 siblings by issue number.

**Done when:** the section names each neighbour and says what it owns.

## STEP 4 — validate

`python3 launchpad/project-intelligence/corpus/validate.py` from the worktree root.

**Done when:** exit 0.

## STEP 5 — stamp, then commit

Stamp: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests
-p "test_*.py"` as the sole foreground command, `timeout: 600000`. Then, separately,
`git add` + `git commit -s`.

**Done when:** the stamp run ends `OK` and the commit lands.

## GATES

- `python3 launchpad/project-intelligence/corpus/validate.py` exits 0.
- Verify-gate stamp earned by an unpiped, foreground, 600 s-timeout run.
- Self-review against #1127's DoD, line by line.

## BUDGET

One new corpus node plus this plan. No source changes, no second corpus document,
no generated-index edits.

## OPEN

- The brief names base revision `96782d1035f5bcb878edaa4b75b95ccc85ef4ce0`; the
  worktree actually branched at `29ca9b189bd3f639ba09c972b57c70538c0860c6`, which
  has `96782d1` as an ancestor. Provenance records the revision actually worked
  against, `29ca9b189b`.

## LEFT OUT

- Any per-endpoint contract, flow, or capability — owned by the four merged nodes.
- The `/hooks/{id}` middleware slice — #1124's, already committed on a sibling branch.
- TLS termination (#1133), host-to-community routing (#1126), request routing
  (#1131), the WebSocket transport (#1134).
- The admin dashboard's CSP and host gating — a security-boundary subject, not a
  networking-layer one; noted as a candidate follow-up, not folded in.
