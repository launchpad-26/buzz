# Issue #1196 — operations/administration/operator-cli.md

Stated size: no Size line on the issue; the batch dispatch brief for Feature #618 caps a single corpus-document task at 5 steps -> cap: 5 steps

ALREADY TRUE: worktree `/home/serina/Launchpad/buzz/__worktrees/task-1196-administration-operator-cli` exists on branch `task/1196-administration-operator-cli`, up to date with `origin/launchpad`, clean, at `473205a7457b208455f188847bfb27b01aa83cac`. `launchpad/docs/corpus/operations/administration/operator-cli.md` does not exist (`test -f` -> MISSING); no `operations/` directory exists under the corpus root yet. `crates/buzz-admin/` exists (`Cargo.toml`, `src/main.rs`, `src/deletions.rs`) with the clap `Command` enum `AddMember`/`RemoveMember`/`ListMembers`/`GenerateKey`/`Migrate`/`ProductFeedback`/`Deletions`/`ReconcileChannels`. `launchpad/docs/corpus/architecture/containers/cli.md` (id `architecture-containers-cli`) and `launchpad/docs/corpus/capabilities/communities/community-members.md` (id `capabilities-communities-community-members`) and `launchpad/docs/corpus/templates/procedure.md` (id `corpus-template-procedure`) are all present in `<SCRATCH>/existing-node-ids.txt`'s 204 ids resolvable on `origin/launchpad`. Governing docs read in full: `AGENTS.md`, `templates/procedure.md`, `schema/node.schema.json`, and the evidence/code-references/linking/naming standards. Source evidence already opened: `crates/buzz-admin/Cargo.toml`, `src/main.rs`, `src/deletions.rs`, `crates/buzz-deletion/src/lib.rs` (Command enum), `Dockerfile` (buzz-admin build/strip/copy stages), `Justfile` (`_ensure-migrations`), `.env.example`, `NOSTR.md` (CLI membership usage, exit codes, required env vars), `ARCHITECTURE.md` (subcommand table), `deploy/compose/README.md` and `deploy/compose/run.sh` (`./run.sh add-member`/`remove-member`/`list-members`), `TESTING.md` (build command, `generate-key` usage).

STEP 1  Write `launchpad/docs/corpus/operations/administration/operator-cli.md` — `id: operations-administration-operator-cli`, `type: operations`, `status: draft`, `origin: launchpad`, `audiences: [operator, agent]` — following `corpus-template-procedure`'s required sections (Overview, Before you start, numbered task sequences, See also, Boundary, Relationships, Scope and omissions). Cover: building `buzz-admin` (cargo build, or the pre-built binary already shipped in the relay Docker image), the environment variables that point it at Postgres/Redis/relay (`DATABASE_URL`, `REDIS_URL`, `RELAY_URL`, `BUZZ_RELAY_PRIVATE_KEY`) and how it resolves the target community from `RELAY_URL`'s host, managing relay membership (`add-member`/`remove-member`/`list-members`, both invoked directly and via `./run.sh` in the Compose deployment), and a brief non-exhaustive pointer to the remaining subcommands (`migrate`, `generate-key`, `reconcile-channels`, `product-feedback list`, `deletions`) deferring their full detail to source. Contrast explicitly with `buzz-cli` (agent-facing, different crate/audience) via `architecture-containers-cli`. One evidence entry per substantive claim; `FACT` only for sources actually opened. Declare `relationships: references -> architecture-containers-cli`, `references -> capabilities-communities-community-members`, `implements -> corpus-template-procedure`. ← RUNS HERE [independent]
  done when: the file exists at the assigned path with schema-shaped front matter and every template-required section present.

STEP 2  [needs 1] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix any reported error (path, schema, relationship-target) and re-run until exit 0. `UNVERIFIED` on the sole commit-only `FACT` is expected, not an error.
  done when: the validator exits 0.

STEP 3  [needs 2] Run, as the sole command in its own Bash call, `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` and confirm `OK`. Then, in a separate call, `git add -A && git commit -s -m "docs(corpus): document buzz-admin operator CLI procedure (#1196)"`.
  done when: the test suite reports `OK` and the commit exists with a `Signed-off-by` trailer, created without `--no-verify`.

STEP 4  [needs 3] Re-read the diff against every bullet of issue #1196's Definition of Done, including the four type-specific tail bullets (goal/prerequisites/scope; ordered executable steps; success verification/rollback; links authoritative commands rather than generic advice). Confirm no second concept was folded in — `deletions` and `product-feedback` stay a pointer, not a second procedure.
  done when: every DoD bullet is checked off or explicitly named as unsatisfied in the final report, and `validate.py` still exits 0.

PARALLEL: none — single file, single agent, no independent workstream.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0 (STEP 2). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` reports `OK`, run alone in its own Bash call (STEP 3). `git commit -s` succeeds without `--no-verify` (STEP 3).

BUDGET: small — one corpus document (~150-250 lines) plus this plan file, no code changes; evidence gathering already performed against `buzz-admin`, `buzz-deletion`, `Dockerfile`, `Justfile`, `.env.example`, `NOSTR.md`, `ARCHITECTURE.md`, `deploy/compose/`, `TESTING.md`.

OPEN: whether `buzz-admin`'s `deletions` (durable whole-community deletion control plane) and `product-feedback` subcommands deserve their own procedure/capability nodes later — flagged in the drafted node's scope-and-omissions rather than resolved here, since this task's DoD forbids folding a second concept into this document.

LEFT OUT: no second hand-authored corpus document. No editing of `architecture/containers/cli.md`, `capabilities/communities/community-members.md`, `templates/procedure.md`, or any other existing node. No changes to `crates/buzz-admin`, `crates/buzz-deletion`, or any other product code. No exhaustive flag-by-flag reference for every subcommand — deferred to source per the assignment.
