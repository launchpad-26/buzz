# Issue #1114 — layers/identity/relay-identity.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; no `launchpad/docs/corpus/layers/` directory exists yet anywhere on `origin/launchpad` (confirmed via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`), so this is the first `layers`-type node to merge — no sibling `layers/identity/*` node is a legitimate `relationships` target regardless of what unmerged sibling PRs (#1102-#1113, #1168+) currently draft in their own worktrees. Evidence already gathered by direct code reading in this session: `crates/buzz-relay/src/state.rs:670` (`AppState.relay_keypair: nostr::Keys`), `crates/buzz-relay/src/main.rs:425-446` (keypair sourced from `BUZZ_RELAY_PRIVATE_KEY`, hardcoded dev fallback when `require_auth_token=false`, panics otherwise), `crates/buzz-relay/src/nip11.rs` (NIP-11 `self` field = `relay_keypair.public_key()`, gates NIP-43 advertisement), `crates/buzz-relay/src/handlers/side_effects.rs` (relay-signed system messages kind 40099, NIP-29 group metadata, NIP-43 membership list kind 13534, git ref-state events), `crates/buzz-relay/src/handlers/push_lease.rs:488` (NIP-44 decrypt with `relay_keypair.secret_key()`), `crates/buzz-relay/src/mesh_boot.rs:415-451` (relay pubkey as inter-relay mesh identity), `crates/git-sign-nostr/README.md` (signs with a *user/agent* key via `NOSTR_PRIVATE_KEY`/`BUZZ_PRIVATE_KEY`, never the relay's own key — a boundary, not a relay-identity mechanism), and the already-merged deployment/operator nodes documenting `BUZZ_RELAY_PRIVATE_KEY` as "relay identity" and `RELAY_OWNER_PUBKEY` as a separate administrative-owner concept.

STEP 1  Re-verify the gathered evidence at current `HEAD` (`git rev-parse HEAD`) and confirm two more open questions: (a) whether `RELAY_OWNER_PUBKEY`/`relay_owner_pubkey` ever signs anything itself or is purely a stored administrative pubkey (read `crates/buzz-db` owner-bootstrap code); (b) exact wording of the mesh's use of `relay_keypair` as federation identity (read `crates/buzz-relay/src/mesh_boot.rs` in full). Record the commit SHA for provenance. ← RUNS HERE

STEP 2  [needs 1] Write front matter: `id: layers-identity-relay-identity`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships` initially — re-check against `origin/launchpad`'s current node-id list before deciding; if `architecture-containers-relay` and `architecture-context-relay-operator` are still merged (they are, confirmed this session) add `references` edges to both, since the node directly extends what those two already document about the relay process and its operator-set secrets. One `evidence` entry per substantive claim, classified honestly (FACT for opened sources, INFERENCE with confidence for reasoned claims like "rotation breaks federation trust", TEAM_KNOWLEDGE with `provided_by` for the issue's own DoD requirements).

STEP 3  [needs 2] Write the body: one-sentence definition (the relay has its own Nostr keypair, `BUZZ_RELAY_PRIVATE_KEY`, distinct from every user's or agent's), what it signs and decrypts (system messages, NIP-29 group metadata, NIP-43 membership list, git ref-state events, NIP-44 push-lease decryption), how it is advertised (NIP-11 `self`), a Boundary section distinguishing it from: `RELAY_OWNER_PUBKEY` (administrative owner, not a signing key), `git-sign-nostr` (signs with the *caller's* key, never the relay's), and per-agent/per-user keypairs (`layers-identity-agent-identity`, unmerged — do not link it). State the operational consequence of rotation (new pubkey = new identity; NIP-43/NIP-29 events signed under the old key become unverifiable; federation mesh peers stop recognizing the relay) as already documented by the merged deployment nodes.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Re-read the diff against every Definition-of-Done bullet in issue #1114 and re-open every cited source to confirm the claim it supports.

STEP 5  [needs 4] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own tool call to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK`. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here (self-review only).

BUDGET: small-to-medium — one document, no code changes, evidence gathering scoped to `buzz-relay`'s state/main/nip11/side_effects/push_lease/mesh_boot modules plus `git-sign-nostr`'s README and already-merged deployment/operator corpus nodes.

OPEN: Whether `RELAY_OWNER_PUBKEY` participates in any signing operation itself (STEP 1b) is not yet confirmed by a direct read — the node will state plainly whatever STEP 1 finds rather than assume. Whether the mesh's use of `relay_keypair` as federation identity is FACT-grade at the line level needs the STEP 1 re-read of `mesh_boot.rs` before citing it as a substantive claim rather than a general observation.

LEFT OUT: No relationships to any `layers-identity-*` sibling node — none are merged on `origin/launchpad` yet, and issue #1114's own worktree must not assume unmerged sibling PRs land first (per `launchpad/docs/corpus/AGENTS.md` step 9's explicit warning about this exact trap). No new claims about `buzz-persona` or `buzz-acp` agent-identity mechanics — those belong to `layers-identity-agent-identity` (#1103), not this node. No attempt to resolve or restate the full NIP-43/NIP-29 protocol semantics — this node covers *whose key* signs relay-authored events, not the wire protocols themselves.
