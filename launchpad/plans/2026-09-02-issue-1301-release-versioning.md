# Issue #1301 — releases/versioning.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/reference.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/releases/` does not exist yet — this is the first node in that directory, and no sibling `releases/*` task (#1291-#1300) is merged, so none is a valid `relationships` target.

STEP 1  Gather evidence: root `Cargo.toml` (`[workspace.package] version = "0.1.0"`, `rust-version = "1.88.0"`), every crate's version line (`version.workspace = true` vs hardcoded — `buzz-relay` hardcodes `0.2.1`; `buzz-persona`, `sprig`, `examples/countdown-bot` hardcode `0.1.0`), `rust-toolchain.toml` (`channel = "1.95.0"`), desktop's `package.json`/`tauri.conf.json`/`src-tauri/Cargo.toml` (synced at `0.5.20`), `mobile/pubspec.yaml` (`0.0.0+1`) cross-checked against its own git history (PR #2144 reset it from a real bumped version) and against `RELEASING.md`'s explicit statement that this is a deliberate non-release fallback, `RELEASING.md`'s full lane tables (desktop/relay/mobile version authority, tag scheme, `-block` mention only in root `AGENTS.md`'s ecosystem table, not in `RELEASING.md` itself), `Justfile`'s `get-*-version`/`bump-*-version` recipes, and a repo-wide grep for `cargo-semver-checks`/`semver` (present only as tag-format regex/GH Actions metadata parsing, never API-compatibility enforcement). Also check for an ADR explaining the MSRV/toolchain-channel relationship (none found). ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `releases-versioning`, type `release`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, no `relationships` — no merged node is a legitimate target) using `templates/reference.md`'s required sections (Reference description, structured entries table, Commands table, Boundary, Relationships, Scope and omissions) — the content is a lookup catalogue of "what version authority governs each surface," not a sequence of steps to perform, despite issue #1301's Objective/DoD using generic "procedure"/step-ordered language that batches 1-7 already established is boilerplate copied across dissimilar node shapes (see `templates/procedure.md` and `templates/reference.md`'s own "Note on Definition of Done" sections). Cross-check this call against the sibling task titles (#1293/#1295/#1296 desktop/mobile/relay-release.md are the actual step-ordered procedures; #1299 release-tags.md, #1297 release-artifacts.md are catalogue-shaped like this one).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call. Stop at the commit — no push, no PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to ~30 already-open files (Cargo.toml x-ref, Justfile, RELEASING.md, rust-toolchain.toml, mobile/pubspec.yaml + git log, .github/workflows/*.yml grep).

OPEN: Whether the MSRV floor (`rust-version = "1.88.0"`) and the pinned toolchain channel (`1.95.0`) are meant to converge, and whether CI ever actually builds against the floor rather than always against the pinned channel, is unresolved in this repository (no ADR found) — reported as an open gap in the node's body, not resolved here. Whether the `-block` desktop version suffix is applied exactly as root `AGENTS.md`'s ecosystem table states is unverifiable from this checkout (the mechanism lives in the private `squareup/buzz-releases` repo) — reported as expected-but-not-verified.

LEFT OUT: No relationships to `releases/release-tags.md`, `releases/desktop-release.md`, `releases/mobile-release.md`, `releases/relay-release.md`, or `releases/rollback.md` — none is merged on `origin/launchpad` at the recorded revision. No attempt to resolve the MSRV/toolchain divergence or to fix the mobile `pubspec.yaml` placeholder — those are implementation/decision questions, not this corpus-authoring task's to answer.
