Issue: launchpad-26/buzz#1120 — task: document layers/lifecycle/startup.md
Parent: Feature #611 (compute observability, configuration and lifecycle corpus), PRD #602

Stated size: no explicit Size line on #1120; dispatch prompt caps at 5 steps (single small document)  ->  cap: 5 steps

ALREADY TRUE

- Worktree `__worktrees/task-1120-startup` exists, branched from
  `origin/launchpad` at `338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5`, on branch
  `task/1120-startup`.
- `launchpad/docs/corpus/layers/lifecycle/startup.md` does not exist yet
  (confirmed: `test -f` fails). No BLOCKED condition.
- `launchpad/docs/corpus/layers/lifecycle/` does not exist at all yet on
  `origin/launchpad` (`git ls-tree -r --name-only origin/launchpad --
  launchpad/docs/corpus` lists no `layers/lifecycle/*` path) — this is the
  first node in that sub-surface. `layers/compute/lifecycle.md` (a sibling
  task, #1043) exists only on the unmerged branch `origin/task/611-batch-1a-compute`
  and is therefore NOT a valid `relationships` target per `AGENTS.md` step 9.
- The issue's Objective names the document's kind: "the single canonical
  **flow** node for startup" -> template `launchpad/docs/corpus/templates/flow.md`
  exists and matches.
- Per the dispatch prompt and mirroring sibling task #1043's own reasoning
  (read directly from `origin/task/611-batch-1a-compute`'s
  `layers/compute/lifecycle.md`, "A note on `type`" section): `flow.md`'s
  worked skeleton defaults an instance node to `type: architecture`, but
  that default is precedent specifically from the `architecture/flows/*`
  family (12 nodes narrating flows across the C4 static model). This task's
  target path is `layers/lifecycle/startup.md` and parent Feature #611's own
  stated surface is `layers` (cross-cutting compute/telemetry/configuration/
  runtime-lifecycle behavior), not a C4 diagram family. Per
  `standards/taxonomy.md`'s *Choosing a value* step 2 ("pick the enum member
  whose plain-English name most concretely names the node's primary
  subject... not where the node currently happens to live"), `type: layers`
  is the deliberate, disclosed choice here too. This will be stated
  explicitly in the node's own body (a "note on type" section).
- Direct investigation in this worktree has located the real startup
  sequence: `crates/buzz-relay/src/main.rs` `async fn main()` (lines
  86-1119) — crypto provider install, tracing/OTEL init, `Config::from_env`,
  Prometheus exporter, Postgres connect (`Db::new`), conditional
  `BUZZ_AUTO_MIGRATE` migration run (`db.migrate()` ->
  `crates/buzz-db/src/migration.rs::run_migrations`, `MIGRATOR =
  sqlx::migrate!("../../migrations")`, i.e. the repo-root `migrations/`
  directory), future-partition ensure, deletion-serving-catalog validation,
  NIP-43 fail-fast checks (owner pubkey / relay private key required when
  membership enforcement is on), deployment-community bootstrap + allowlist
  backfill + owner bootstrap (trust-boundary-relevant), Redis pool + pubsub
  connect with three spawned fan-out subscribers, `AppState::new`, optional
  mesh boot, optional git object-storage conformance probe (fatal if
  enabled and failing), a dozen-plus spawned background tasks (workflow
  cron, ephemeral-channel reaper, NIP-PL push matcher/worker, NIP-ER
  reminder scheduler, multi-node fan-out consumer, cache-invalidation
  consumer, community revalidator, conn-control consumer, pool-metrics
  poller, usage-metrics poller), `build_router`/`build_health_router`, then
  `serve()`: binds the health TCP listener first, spawns its `axum::serve`,
  then binds the main TCP listener (and optional UDS listener), spawns
  `axum::serve` with graceful-shutdown wiring, and spawns the SIGTERM/Ctrl-C
  watcher (`shutdown_signal`, `crates/buzz-relay/src/main.rs:1408-1422`).
  Readiness (`crates/buzz-relay/src/router.rs:374-414`) only reports `ready`
  once Postgres, Redis, and the deletion-serving catalog all check out live
  — startup is complete in the trigger-to-outcome sense only once that
  probe would return 200, not merely once the process has bound a socket.
- `desktop/src-tauri/src/main.rs` (Tauri desktop boot) was opened and
  confirmed to be a genuinely different flow — different trigger (a human
  launches the desktop app, not a deployment starting a server process),
  different actors (WebKitGTK/Tauri runtime, not Postgres/Redis/the relay's
  own background tasks), no shared code path with `buzz-relay::main`. Per
  `AGENTS.md`'s "one node is one independently maintainable idea" rule, it
  is excluded from this node rather than folded in, and named as a
  candidate follow-up in the final report (not filed as an issue here).
- Candidate `relationships` target confirmed to exist on `origin/launchpad`:
  `architecture-containers-relay` (id read directly from
  `git show origin/launchpad:launchpad/docs/corpus/architecture/containers/relay.md`)
  — it documents the relay container this flow's steps run inside, a
  natural `references` target, no ownership implied.

STEP 1 [independent]

Write `launchpad/docs/corpus/layers/lifecycle/startup.md` using the `flow.md`
template shape: front matter (`id: layers-lifecycle-startup`, `type: layers`,
`status: draft`, `origin: launchpad`, `audiences`, `evidence`,
`relationships: [{type: references, target: architecture-containers-relay}]`),
body sections A note on `type` / Flow statement / Sequence / Diagram (Mermaid
`sequenceDiagram`) / Outcome / Boundary / Relationships / Scope and
omissions, scoped to `buzz-relay`'s own process startup sequence in
`main.rs` only (explicitly excluding: `layers/compute/*` compute-provider
deploy lifecycle, a sibling Feature-#611 task; the desktop Tauri boot
sequence; the harness's own inner ACP-subprocess spawn; graceful shutdown
itself, owned by sibling task #1118).

<- RUNS HERE

done when: the file exists, front matter parses as valid YAML, and every
Sequence/Outcome step cites a real file path (and where relevant, a line or
line range) this agent directly opened in this worktree.

STEP 2 [needs 1]

Run `python3 launchpad/project-intelligence/corpus/validate.py` from the
worktree root and fix every reported error (schema violations, broken node
IDs, invalid source paths, duplicate IDs) until it exits 0.

done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0.

STEP 3 [needs 2]

Self-review: re-read the drafted node against #1120's own Definition-of-Done
checklist line by line (schema-valid front matter; one independently
maintainable node; FACT/INFERENCE/TEAM_KNOWLEDGE not conflated; links
without duplicating; checked against the recorded revision; validation
passes; states trigger/preconditions/termination; lists ordered interactions
and state movement; identifies auth/trust-boundary crossings; documents
failure/abort/rollback with representative verification links). Confirm no
second hand-authored canonical document was created.

done when: each DoD bullet is confirmed satisfied by a specific section of
the drafted file, or a gap is explicitly named in the final report as a
finding rather than silently left.

STEP 4 [needs 3]

Run the verify-gate stamp command as the sole command in its own tool call:
`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`.
Confirm `OK`. Then, in a separate tool call, commit with
`git commit -s -m "docs(corpus): document startup (#1120)"`.

done when: the unittest run reports `OK` and the commit exists on
`task/1120-startup` (`git log -1 --oneline`), with no push and no PR opened
(per the dispatch prompt's explicit instruction — this task's commit is
integrated into a shared batch PR by a separate later process).

PARALLEL

None. Steps 1-4 are a strict chain (each step's output gates the next);
there is no independent second workstream inside this single-document task.

GATES

- `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0
  before commit (Step 2).
- `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`
  must report `OK` before commit (Step 4) — this is the verify-gate stamp
  command; it must be the sole command in its own tool call, per the
  dispatch prompt.
- No push, no `gh pr create` — this task's commit lands in a later shared
  batch PR.

BUDGET

Single document. Remaining work is drafting (Step 1, the bulk of the
effort, grounded in the `main.rs`/`router.rs`/`migration.rs` reads already
done during planning), one validator fix loop (Step 2, expected 0-2
iterations), a line-by-line DoD self-review (Step 3), and the
stamp-then-commit sequence (Step 4). No code changes, no new dependencies,
no new tests beyond the existing corpus validator/test suite.

OPEN

- Whether `type: layers` (this plan's choice, disclosed in-body, mirroring
  sibling #1043) or `type: architecture` (the `flow.md` template's own
  precedent-based default for the `architecture/flows/*` family) is the
  better fit is a judgment call per `standards/taxonomy.md` step 5 ("a
  node's type MAY be revised later") — left to reviewer confirmation, not
  decided unilaterally as unrevisable. The node's own text discloses the
  reasoning either way.
- Whether every spawned background task in `main.rs` (there are roughly a
  dozen) belongs as its own numbered Sequence step, or whether most should
  be summarized as one "background tasks are spawned" step with a
  representative few cited, is a drafting judgment made in Step 1 — the
  goal is a readable flow narration, not an exhaustive line-by-line
  transcription of the function.

LEFT OUT

- Drafting a second node for `layers/compute/lifecycle.md` (the
  compute-provider deploy/stop/destroy lifecycle) — already claimed by
  sibling task #1043, out of scope here.
- Drafting nodes for the other `layers/lifecycle/*` siblings named in the
  dispatch prompt (background-workers #1115, cancellation #1116,
  concurrency #1117, graceful-shutdown #1118, resource-cleanup #1119) —
  each is its own task.
- Drafting a node for the desktop Tauri boot sequence — a genuinely
  different flow (different trigger, different actors), noted above and in
  the final report as a candidate follow-up, not filed as an issue here.
- Re-deriving or disputing any of the shutdown-timing arithmetic documented
  in `main.rs`'s own doc comments — out of scope for a startup-focused
  node; shutdown itself is sibling task #1118's subject.
- Filing a follow-up issue for any second concept discovered while
  drafting — none identified in planning that rises to "a second
  concept/contract/procedure" distinct from the one flow being narrated; if
  one surfaces during Step 1 it will be named as a candidate follow-up in
  the final report instead of being folded in, per #1120's own DoD bullet.
