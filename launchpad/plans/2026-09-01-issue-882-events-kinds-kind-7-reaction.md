Issue #882 — task: document events/kinds/kind-7-reaction.md
Stated size: none given (no `Size` label on the issue) → dispatch instructions cap this single-document corpus task at 5 steps → cap: 5 steps

ALREADY TRUE  (verified against git, not notes)
  `launchpad/docs/corpus/events/kinds/kind-7-reaction.md` does not exist yet (the whole `launchpad/docs/corpus/events/` tree is absent from `origin/launchpad`) — this is a create, not an update.
  `launchpad/docs/corpus/templates/event-kind.md` (id `corpus-template-event-kind`, type `governance`, status `active`) IS merged on `origin/launchpad` and gives the required-sections shape for an event-kind node: title/kind identity, referenced NIP, kind-range/delivery classification, tag shape, content-field semantics, access-control/storage model, worked example, versioning/supersession, relationships — this plan follows it rather than re-deriving a structure from scratch, even though the dispatch brief said no template exists.
  `node.schema.json`'s `type` enum's dedicated value for this surface is `interfaces-events` (verified: enum list is architecture, layers, capabilities, platforms, implementation, interfaces-events, verification, operations, development, release, governance, agent, ingestion) — the event-kind template itself states a real instance "would most plausibly take... interfaces-events."
  `crates/buzz-core/src/kind.rs:58` defines `pub const KIND_REACTION: u32 = 7;` with doc comment "NIP-25: Content is emoji char or `+`/`-`." It is listed in `ALL_KINDS` at line 649. It is absent from `AUTHOR_ONLY_KINDS` (129), `RESULT_GATED_KINDS` (142), `P_GATED_KINDS` (159) and `SHARED_GATED_KINDS` (215) — read directly, not inferred — so it carries no special read gate: world-readable subject to ordinary channel membership.
  `is_replaceable` (kind.rs:776-778, matches 0|3|41|10000..=19999), `is_parameterized_replaceable` (783-785, 30000-39999) and `is_ephemeral` (769-771, 20000-29999) all evaluate false for 7 — confirmed by reading the exact match arms, not by range arithmetic alone — so kind 7 is a plain regular (persistent, non-replaceable) kind.
  `crates/buzz-relay/src/handlers/ingest.rs` has a reaction-specific ingest path already read end to end: `required_scope_for_kind` (437-484) maps `KIND_REACTION` to `Scope::MessagesWrite` (470-484); `requires_h_channel_scope` (704-733) excludes `KIND_REACTION`, backed by the unit test `reactions_do_not_require_h_tag` (3705-3708); `derive_reaction_channel` (573-608) derives channel_id from the target event's stored `channel_id` via its last valid 64-hex `e` tag, returning `NoTarget` if none is found; the main dispatch branch at kind_u32 == KIND_REACTION (3016-3131) re-extracts that `e` tag (rejecting with "invalid: reaction must reference a target event via e tag" if absent, 3035-3039), defaults empty content to `"+"` (3045-3049), calls `validate_reaction_emoji` (160-191, accepts any string ≤64 chars, or a `:shortcode:`-wrapped NIP-30 custom emoji ≤66 chars with a matching `["emoji", shortcode, url]` tag), then atomically upserts via `insert_reaction_event_with_thread_metadata`, rejecting with "invalid: reaction target event not found" if the target doesn't exist (buzz-db/src/store/reaction.rs, mapped at ingest.rs:3071-3075).
  Thread counters (`reply_count`/`descendant_count`) are confirmed NOT touched by reactions: `thread_meta` is `None` whenever `requires_h_channel_scope` is false (ingest.rs:2987-2997), and `insert_event_with_thread_metadata_tx` (buzz-db/src/store/event.rs:1213-1320) only bumps the counters `if let Some(meta) = thread_meta` — reaction inserts always pass `None`. E2E proof read directly: `crates/buzz-test-client/tests/e2e_nostr_interop.rs` sends one reply and one reaction against the same root and asserts `reply_count == 1` (only the reply counted) while the reaction appears solely in the "aux closure."
  `crates/buzz-core/src/filter.rs:78-100` (test `h_tag_fallback_uses_stored_channel_id`, line 210) confirms the `#h` read-path filter falls back to `StoredEvent.channel_id` specifically for kind:7/kind:5 events that carry no `h` tag.
  `crates/buzz-sdk/src/builders.rs` has three typed builders read directly: `build_reaction` (474-483, one `e` tag + emoji content), `build_custom_emoji_reaction` (490-503, one `e` tag + one `emoji` tag, content `:shortcode:`), `build_remove_reaction` (506-509, kind:5 deletion of the reaction event id).
  `crates/buzz-cli/src/commands/reactions.rs` (full file read) implements `cmd_add_reaction`, `cmd_remove_reaction`, `cmd_get_reactions`, dispatched from `ReactionsCmd` in `crates/buzz-cli/src/lib.rs` (enum ~189-191, subcommand defs ~736-765, dispatch ~2104); a CLI test asserts subcommand names `["add","get","remove"]` (lib.rs:2357).
  `NOSTR.md` (repo root) is the primary protocol reference actually used for reactions today — lines 50, 192-198, 349, 363 — since no dedicated `docs/nips/NIP-25.md` exists (NIP-25 is an external community NIP; `docs/nips/` only holds Buzz's own custom-NIP proposals).
  `crates/buzz-search` and `crates/buzz-audit` were grepped for "reaction" (case-insensitive) with zero hits in both — reactions get no dedicated search-indexing or audit-log treatment; NIP-50 FTS is kind-agnostic for kind 7 (no P_GATED-style NULL-tsvector carve-out applies, since it isn't in that set).
  Baseline run of `python3 launchpad/project-intelligence/corpus/validate.py` on `origin/launchpad` at commit 650354eab8d41ab6ce1a71de079a6c6d95c69052 (this worktree's HEAD) exits 0 with **zero** `FAIL` lines and 360 `UNVERIFIED` notices — the dispatch brief's claim of "21 KNOWN pre-existing FAIL errors (issue #1951)" does not match current `origin/launchpad` state as actually run; whatever #1951 tracked appears already resolved upstream of this branch. This plan's bar is therefore the stricter one: zero new FAIL lines, full stop, not "≤21."
  No `events/`-typed or `interfaces-events`-typed corpus node currently exists on `origin/launchpad`, so the only legitimate `relationships` target is `corpus-template-event-kind` itself, confirmed present with that exact `id` in this worktree's copy of `origin/launchpad`.

STEP 1  Confirm the evidence ledger above is complete and re-verify the two spot-checks not yet independently re-opened in this session: `crates/buzz-relay/src/handlers/side_effects.rs` (reaction-removal-on-delete special case) and `crates/buzz-workflow/src/lib.rs` (`TriggerDef::ReactionAdded`), both reported by an earlier exploration pass but not yet personally re-read line-by-line.  [independent]  ← RUNS HERE
        done when: both files have been opened directly and either confirm or correct the earlier report; any correction is folded into STEP 2's evidence ledger before drafting.

STEP 2  [needs 1] Write `launchpad/docs/corpus/events/kinds/kind-7-reaction.md`: front matter (`id: events-kinds-kind-7-reaction`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, one `relationships` entry `{type: implements, target: corpus-template-event-kind}`, and one evidence entry per substantive claim, classified FACT/INFERENCE/TEAM_KNOWLEDGE per the rules above) plus the body, following the template's required sections: kind identity + number + constant name, referenced NIP (NIP-25, pinned to a commit SHA) with the kind-range/classification cross-check, tag shape (`e` required, `p` not required/never checked, `h` explicitly not required with the derive-from-target mechanism explained), content-field semantics (plain string ≤64 chars, or NIP-30 `:shortcode:` wrapped form ≤66 chars with the doc-comment-vs-actual-validation gap noted honestly), access control/storage (world-readable, no gating set membership, MessagesWrite scope), a worked example JSON event, producers (buzz-sdk builders, buzz-cli `reactions` subcommand), consumers/fanout (aux-closure delivery, thread-counter exclusion, filter `#h` fallback), search/audit treatment (absent — say so), and links to NIP-25, `kind.rs`, `NOSTR.md`, and the relevant tests.
        done when: the file exists, every Definition-of-done bullet from the issue body is addressed by a labeled section or an explicit stated gap, and no second hand-authored canonical corpus document is created.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; diff its error/FAIL set against the STEP-1(ALREADY TRUE) baseline (zero FAIL lines); fix and re-run until the new run also shows zero FAIL lines.
        done when: the command exits 0 and a direct comparison against the captured baseline output shows no new `FAIL` line attributable to the new node.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call; confirm it prints `OK`. Only then, in a separate call, stage and commit the plan + document with `git commit -s`.
        done when: the unittest run prints `OK` with no failures/errors, and `git log -1 --format=%H` on the worktree branch shows a new commit containing exactly the two intended files.

PARALLEL  None. Four steps, three of them chained (2→3→4) on the one file this task produces; step 1 is a short verification pass with no fan-out target.

GATES     `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no new FAIL beyond the verified-zero baseline. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must print `OK` before the commit gate will accept the commit (per this task's own dispatch instructions — if the commit hook rejects it for a missing stamp, that is reported back as a finding, never routed around with `--no-verify`). review-adjudicate, review-final and any cross-model pass are out of scope here — no PR is opened by this task, so those gates are deferred to whoever later reviews this branch for merge.

BUDGET    Small: one Markdown file, no code changes. The fiddliest part is evidence-scoping section 6 (access control) and section 5 (content semantics) honestly — the module doc comment ("Content is emoji char or +/-") is materially stricter than what `validate_reaction_emoji` actually enforces (any ≤64-char string), and the node must say so as a documented gap rather than silently repeating the doc comment as if it were the real rule.

OPEN      The dispatch brief's premise of "21 KNOWN pre-existing validate.py FAIL errors (issue #1951)" does not match this worktree's actual baseline run (zero FAIL lines) — flagged rather than silently reconciled; whether #1951 was already fixed upstream, or the baseline changed for some other reason, is not this task's to resolve, only to notice and report.
          Section 6 (access control) and the NIP-30 custom-emoji content-length allowance are the two places `standards/confidence.md`'s FACT-vs-INFERENCE distinction matters most (per the template's own "Evidence expectations" section) — this plan defers the exact FACT/INFERENCE split on each individual claim to STEP 2's drafting judgment rather than pre-deciding it here, since it depends on exactly which kind.rs comment or test backs each specific sentence.

LEFT OUT  Creating or editing any second hand-authored canonical corpus document (explicitly out of scope per the issue).
          Any change to `crates/buzz-core/src/kind.rs`, `crates/buzz-relay`, `crates/buzz-sdk`, or `crates/buzz-cli` runtime behavior — this is a documentation-only task; the doc-comment/validation-behavior gap noted above is reported, not fixed, here.
          Opening a PR, pushing, or merging — the dispatch instructions explicitly forbid all three for this task.
          Deciding whether every Buzz-proposed kind needs its own `docs/nips/NIP-XX.md` (an open gap the template itself names as unresolved) — NIP-25 already has an external community spec, so this question doesn't even arise for kind 7, but this plan does not attempt to settle it generally.
