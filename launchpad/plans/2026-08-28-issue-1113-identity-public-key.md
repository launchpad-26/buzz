# Issue #1113 — layers/identity/public-key.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and all 26 `templates/*.md` (including `concept.md`) are merged on `origin/launchpad`. No `launchpad/docs/corpus/layers/` directory exists on `origin/launchpad` or in this worktree yet — this is a fresh node in an as-yet-unmerged surface. Sibling tasks #1102 (actor), #1103, #1104, #1106, #1107, #1108 have open draft PRs (#1803, #1804, #1811, #1808, #1812, #1810) but none are merged, so none of their ids are valid `relationships` targets. #1111 (keypair) and #1112 (private-key) have no PR yet. `architecture-principles-signed-events` and `architecture-principles-humans-and-agents-are-peers` are merged and both bear directly on public-key.

STEP 1  Gather evidence: confirm `users`, `channel_members`, and `pubkey_allowlist` use `pubkey BYTEA` (`CHECK (LENGTH(pubkey) = 32)`) as part of their primary key, and `api_tokens.owner_pubkey`/`reactions.pubkey`/`events.pubkey` etc. reference or carry it as a foreign key (`migrations/0001_initial_schema.sql`). Confirm `crates/buzz-core/src/verification.rs::verify_event` uses `event.pubkey` (a `nostr::PublicKey`, BIP-340/NIP-01 Schnorr x-only key) to verify the event's Schnorr signature, and `crates/buzz-auth/src/lib.rs::AuthContext.pubkey` carries the same type as the authenticated identity of a connection post NIP-42/NIP-98. Confirm hex/npub (bech32, NIP-19) encoding: `crates/buzz-sdk/src/mentions.rs::extract_nostr_uris` decodes `nostr:npub1…` via `nostr::PublicKey::from_bech32` and re-encodes with `.to_hex()`; `buzz-cli` accepts both forms interchangeably across many flags (`crates/buzz-cli/src/lib.rs`); `NOSTR.md` documents 64-char hex as the on-disk/CLI convention and `add-member`/`buzz-admin` accepting either form. Read PR #1803's `actor.md` draft for the "actor" boundary (actor = the pubkey performing a request; role concept) to draw a clean, non-duplicating line against this node (the identifier/type itself). ← RUNS HERE

STEP 2  [needs 1] Write front matter (id `layers-identity-public-key`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, relationships: `references` → `architecture-principles-signed-events` and → `architecture-principles-humans-and-agents-are-peers`, both merged) and the body against `templates/concept.md`'s structure (definition, boundary, use cases, scope/omissions) since it's the merged template a same-surface precedent (#1811) already used for a `layers/identity` concept node. Cover: what a pubkey is (32-byte/64-hex-char secp256k1 x-only Schnorr public key per NIP-01), its role as (part of) the primary key for `users`/`channel_members`/`pubkey_allowlist` and a foreign key elsewhere, its role as the post-auth identity carried in `AuthContext`, and its two textual encodings (hex, npub/bech32 per NIP-19) with where each is used. Draw the boundary against "actor" (role, not identifier — named but not linked, PR #1803 unmerged), "keypair" and "private key" (#1111/#1112, not yet authored — name the gap), and "identity/profile" (`UserProfile`, a different concept: presentation data, not the identifier).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to `migrations/0001_initial_schema.sql`, `crates/buzz-core/src/verification.rs`, `crates/buzz-auth/src/lib.rs`, `crates/buzz-sdk/src/mentions.rs`, `crates/buzz-cli/src/lib.rs`, `NOSTR.md`.

OPEN: Whether `agent_owner_pubkey` and `revoked_by` (both bare pubkey columns with no dedicated node yet) deserve their own callouts here or belong entirely to `actor`/`human-identity`/`agent-identity` nodes — resolved by keeping this node strictly about the identifier/encoding, not about who owns which pubkey.

LEFT OUT: No relationships to `layers-identity-actor` (#1102), `layers-identity-agent-identity` (#1103), `layers-identity-community-identity` (#1104), `layers-identity-human-identity` (#1106), `layers-identity-identity-archive` (#1107), or `layers-identity-identity-invariants` (#1108) — all unmerged. No relationships to `layers-identity-keypair` (#1111) or `layers-identity-private-key` (#1112) — not yet authored. No attempt to document key generation, signing, or private-key handling (owned by #1111/#1112), nor NIP-05/profile presentation (owned by human/agent-identity nodes).
