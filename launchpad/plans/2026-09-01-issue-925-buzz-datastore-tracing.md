# Issue #925 — implementation/crates/buzz-datastore-tracing.md

Stated size: single corpus document, no code changes (issue #925 Definition of Done)  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/implementation-reference.md` are merged on `origin/launchpad`; `launchpad/docs/corpus/implementation/crates/buzz-datastore-tracing.md` does not exist yet; `git ls-tree -r --name-only HEAD -- launchpad/docs/corpus` shows no other `implementation/` node merged, and `launchpad/docs/corpus/architecture/containers/postgres.md` (id `architecture-containers-postgres`) is merged.

STEP 1  [independent] Gather evidence: read `crates/buzz-datastore-tracing/Cargo.toml`, `src/lib.rs` (the `datastore_span` proc-macro attribute), and `tests/runtime.rs` in full. Enumerate dependents (`grep -rl "buzz-datastore-tracing" --include=Cargo.toml crates/` -> buzz-audit, buzz-search, buzz-db, buzz-relay) and representative call sites (`grep -rn "datastore_span" crates/*/src -r`). Read the cross-crate policy test `crates/buzz-db/tests/observability_source.rs` (asserts the macro's generated code and `buzz-db`'s own runtime observability module never emit sensitive/unbounded fields) and the integration test `http_and_datastore_spans_are_exported_in_the_same_trace` in `crates/buzz-relay/src/router.rs`. Confirm `crates/buzz-db/src/runtime/observability.rs` is a separate module (pool-pressure metrics) that this crate does not own. Record `git rev-parse HEAD`. done when: HEAD is recorded and every file above has been opened and its relevant lines noted. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `implementation-crates-buzz-datastore-tracing`, type `implementation`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, one `references` relationship to `architecture-containers-postgres` since every current call site instruments PostgreSQL access and the macro rejects any other `system` value) and the body per the `implementation-reference` template's required sections (Realization statement, Target, Implementation surface, Divergences, Verification, Relationships, Scope and omissions): what the crate is responsible for (a `#[proc_macro_attribute]` that instruments async datastore-access functions with a privacy-policy-enforcing tracing span + operation-duration histogram + sampled slow-op log), what it deliberately does not own (span emission at call sites, metrics/tracing pipeline wiring, non-Postgres systems — the macro hard-rejects `system` values other than `"postgresql"`), its public interface (`datastore_span` attribute macro and its `name`/`system`/`fields` arguments), important dependencies (`syn`, `quote`, `proc-macro2`; dev-deps `tracing`, `tracing-opentelemetry`, `metrics`, `opentelemetry_sdk` for its own tests only), representative call sites across the four dependent crates, and where it plugs in (buzz-db store modules, buzz-audit's audit log, buzz-search's query path, buzz-relay's command executor — all Postgres-backed operations). done when: the file exists at `launchpad/docs/corpus/implementation/crates/buzz-datastore-tracing.md` with every required template section present and every substantive claim carrying an evidence entry.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root; fix and re-run until exit 0. Diff against a `git stash` of any pre-existing baseline failures if the exit code is nonzero, to confirm nothing broken is newly introduced. done when: the command exits 0, or exits nonzero solely on the pre-existing unrelated baseline failures confirmed via `git stash`.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call to earn the verification stamp; confirm `OK`. Then, in a separate tool call, `git add` the plan and the new corpus doc and `git commit -s`. Do not push, do not open a PR — this batch integrates via a later Feature-level PR. done when: the unittest run reports `OK` and a signed-off commit exists on this branch containing exactly the plan file and the new corpus document.

PARALLEL: none — single file, single task, no dependency on sibling batch documents.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 for this node (pre-existing unrelated baseline failures on `origin/launchpad` are not this task's to fix). `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK` before commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration review — not run here; a careful self-review against the issue's DoD checklist substitutes in this session.

BUDGET: small — one document, no runtime code changes, evidence gathering scoped to one crate (2 source files + 1 test file) plus representative call sites in 4 dependent crates and one cross-crate policy test.

OPEN: whether `references: architecture-containers-postgres` is the right single relationship or whether a `part-of` edge toward a future `buzz-db` implementation-reference node (not yet authored) would also be appropriate — left undeclared since no such node exists yet on `origin/launchpad`, per `AGENTS.md`'s rule that an edge may only name an already-merged id.

LEFT OUT: no claim about `crates/buzz-db/src/runtime/observability.rs` (pool-pressure instrumentation) beyond noting it is a distinct module this crate does not own — that module is out of scope for this node. No exhaustive enumeration of every one of the ~30+ call sites in `buzz-db`; the body cites representative ones per crate plus the two policy/integration tests that verify the contract holds across the whole call-site population. No change to `buzz-datastore-tracing` itself.
