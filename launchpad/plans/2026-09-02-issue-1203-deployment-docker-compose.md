# Issue #1203 — operations/deployment/docker-compose.md
Stated size: no Size line on the issue  →  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/procedure.md`, and `launchpad/docs/corpus/architecture/deployment/docker-compose.md` (`architecture-deployment-docker-compose`) are merged on `origin/launchpad`. `launchpad/docs/corpus/operations/deployment/docker-compose.md` does not exist yet — confirmed with `test -f`, and no `operations/` directory exists under the corpus root at all yet. No other `operations-*` node exists in `<SCRATCH>/existing-node-ids.txt`, so this is the first `type: operations` node.

STEP 1  Gather evidence: read `deploy/compose/{README.md,run.sh,compose.yml,compose.caddy.yml,compose.dev.yml,.env.example}`, root `docker-compose.yml`, `launchpad/deploy/{run.sh,AGENTS.md}`, `.github/workflows/docker.yml`, and `Dockerfile`, all opened directly (not taken from the architecture node's ledger). Confirm the root `docker-compose.yml` starts no relay container (local-dev-only) so the deployment/local-dev boundary can be stated explicitly rather than assumed. Exercise the real guard: build a non-secret, gitignored `deploy/compose/.env` from `.env.example` with placeholder values and run `./launchpad/deploy/run.sh check` (plain and with `BUZZ_COMPOSE_TLS=true`), plus its documented rejection paths (`ghcr.io/block/buzz`, a floating `:launchpad` tag), and `deploy/compose/run.sh`'s own `require_env` refusals (missing `.env`, unedited `CHANGE_ME` placeholders), to get executed evidence for the how-to's steps rather than only read evidence. Delete the test `.env` afterward (gitignored, never committed).  [independent] ← RUNS HERE
        done when: every listed file has been opened in this session, and each of the four `run.sh`/guard invocations above has produced observed stdout/exit-status evidence, with `deploy/compose/.env` deleted again afterward (`git status --short deploy/` reports clean).

STEP 2  Write front matter (schema-valid: id `operations-deployment-docker-compose`, type `operations`, status `draft`, origin `launchpad`, audiences `[operator, developer, agent]`, relationships `references: architecture-deployment-docker-compose` and `implements: corpus-template-procedure` — both ids confirmed present in `<SCRATCH>/existing-node-ids.txt`) and the body, following `corpus-template-procedure`'s Required sections: Overview; Before you start; numbered task sequences for configuring `.env`, first start, verifying liveness, upgrading, and rolling back; See also; Boundary; Relationships; Scope and omissions (with the two distinct parts: not-covered-with-owner, and separately what was expected but not verified). Link the architecture node for topology/why rather than restating it; do not link `operations/deployment/local-development.md` (issue #1206, not yet authored) as a path — name the boundary against local development in prose only, per the corpus's rule against linking a path this Feature has not created yet.  [needs 1]
        done when: `launchpad/docs/corpus/operations/deployment/docker-compose.md` exists, its front matter carries exactly the seven schema keys with the values above, and every one of `corpus-template-procedure`'s Required sections (Overview, Before you start, one-or-more numbered task sequences, See also, Boundary statement, Relationships, Scope and omissions) is present as a heading in the body.

STEP 3  Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repository root; fix and re-run until exit 0.  [needs 2]
        done when: the command's own exit status is `0`.

STEP 4  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own Bash call (no piping) to earn the verify-gate stamp, confirm `OK`, then commit the plan + document with `git commit -s` in a separate call.  [needs 3]
        done when: the test runner's own output ends in `OK`, and `git log -1 --stat` on this worktree's branch shows a new commit containing both the plan file and the corpus document with a `Signed-off-by` trailer.

PARALLEL: none — single file, single task, no other worktree touched.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK` and stamp the verify gate before commit. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later review — not run here. No push, no PR — commit locally only, per the dispatch brief. `qa` explore mode does not apply — this is a documentation-only change with no runtime interface of its own to exercise; the `./launchpad/deploy/run.sh` invocations exercised in STEP 1 are read-only evidence-gathering against a throwaway, gitignored `.env`, not a build under test.

BUDGET: small — one document plus this plan file, no code changes. Evidence gathering scoped to the `deploy/compose/` bundle (5 files), the root `docker-compose.yml`, the two `launchpad/deploy/` guard files, `.github/workflows/docker.yml`, and `Dockerfile` — all already read once during planning; STEP 1 re-opens them for direct citation rather than re-deriving facts from the architecture node's ledger.

OPEN: Whether `deploy/compose/`'s referenced "bootstrap script" (README.md: "should eventually replace manual `.env` editing") exists anywhere in the repository was checked with a targeted search in planning and found absent — this is stated as a scope-and-omissions gap in the node rather than invented as a step. Whether to actually run `docker compose up -d --wait` against the bundled Postgres/Redis/MinIO images (not just `config`/`check`) to get first-hand evidence for the "verify the deployment is live" section is left to STEP 1's judgment — `check`/`config` validation plus the guard's accept/reject branches were run and are enough to grade the *documented* commands as `FACT`s; a full live-stack run is not required to satisfy the DoD and is noted as not exercised if skipped.

LEFT OUT: No coverage of the Kubernetes/Helm deployment path (`deploy/charts/buzz/`) — a different node's subject, named only in the Boundary section. No coverage of local development setup (`docker-compose.yml`, `CONTRIBUTING.md`) beyond the one-paragraph boundary statement distinguishing it from this node's subject — issue #1206 owns that procedure and does not exist yet, so it is named in prose only, never linked as a path. No attempt to build or exercise the `benchmarks/harbor-buzz-orchestra` benchmark stack that reuses `deploy/compose/compose.yml` under a third project name — out of scope, per the architecture node's own scope table. No second corpus document.
