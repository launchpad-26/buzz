Issue #945 — task: create implementation reference for sprig
Stated size: no `Size` line  →  cap: 5 steps (batch-1 task brief for Feature #615)

ALREADY TRUE  (verified against git, not notes)
  Worktree `__worktrees/task-945-sprig` is on branch `task/945-sprig`, based on
    `origin/launchpad`, HEAD `76a0a4ebbe4bc4d852b0d04362ed768620da34b3`, working tree clean.
  `launchpad/docs/corpus/implementation/crates/sprig.md` does NOT exist yet (`ls` confirms).
  No `implementation/`-typed node exists anywhere in the corpus tree on `origin/launchpad`
    (`git ls-tree -r --name-only HEAD -- launchpad/docs/corpus` lists only
    `architecture/`, `schema/`, `standards/`, `templates/`, `AGENTS.md`, `README.md`) —
    this is the first implementation-reference instance.
  `launchpad/docs/corpus/architecture/containers/agent-runtime.md` (id
    `architecture-containers-agent-runtime`) IS merged on `origin/launchpad` and already
    names sprig as one of four crates composing the agent-runtime container, citing
    `crates/sprig/src/main.rs` and `crates/sprig/Cargo.toml` directly — a valid,
    already-verified `part-of` target.
  `crates/sprig/` contains exactly two files: `Cargo.toml` and `src/main.rs` (no tests,
    no README). `Cargo.toml` depends only on `buzz-acp`, `buzz-agent`, `buzz-dev-mcp`
    (path deps) and carries an independent `version = "0.1.0"` explicitly NOT inheriting
    the workspace version. `main.rs` dispatches on `argv0`'s basename: `buzz-acp` and
    `buzz-agent` call those crates' own `run()`; the bare `sprig` name prints usage/version
    and errors "invoke Sprig via a personality symlink" when given no args; every other
    name (including `rg`, `tree`, `buzz`, `git-credential-nostr`, `git-sign-nostr`) falls
    through to `buzz_dev_mcp::run()`, which owns those additional multicall names itself.
  `Dockerfile.sprig` builds sprig with `cargo build --profile sprig -p sprig`, symlinks
    all eight personality names to the one binary, and is published as
    `ghcr.io/block/buzz-sprig` by `.github/workflows/sprig-image.yml` (multi-arch,
    digest-pinned) plus a standalone musl tarball by `.github/workflows/sprig.yml`.
  `docs/remote-agents.md:22-24` names `buzz-backend-kubernetes` as the first conforming
    remote-agent provider, realizing the protocol as a bare Pod running the `sprig` image
    — this is production usage, not a dev-only convenience harness.
    `crates/buzz-backend-kubernetes/src/image.rs` parses and defaults to
    `ghcr.io/block/buzz-sprig@sha256:<digest>` references, corroborating this from the
    consumer side.
  `scripts/test-sprig-image.sh` is sprig's representative test: a Docker-contract test
    asserting the symlink layout, the entrypoint's `exec buzz-acp`, and the git-credential
    wiring inside the built image — there is no `cargo test` coverage in the crate itself
    (no `#[test]` in `main.rs`).
  `node.schema.json` requires `id, type, status, origin, audiences, evidence`; `type`'s
    enum includes `implementation`, which fits sprig (a multicall binary artifact) per
    the `implementation-reference` template's own "note on `type`" guidance.
  `relationships.schema.json` defines `part-of` as "source is a constituent
    section/child of target" with generated inverse `has-part` — the correct direction
    for `sprig` (child) pointing at `architecture-containers-agent-runtime` (parent
    container), not the reverse.
  No `implements` target exists: `docs/remote-agents.md` (the closest thing to a spec
    sprig's image realizes) has no corpus node id yet, so per `AGENTS.md` step 9 and the
    template's *Relationships* section, no `implements` edge may be declared — only named
    in prose.

STEP 1  [independent]  Draft `launchpad/docs/corpus/implementation/crates/sprig.md` body
        per the `implementation-reference` template's seven required sections
        (Realization statement, Target, Implementation surface, Divergences,
        Verification, Relationships, Scope and omissions), using only claims backed by
        source actually opened this session (`crates/sprig/{Cargo.toml,src/main.rs}`,
        `Dockerfile.sprig`, `scripts/{build-sprig.sh,test-sprig-image.sh}`,
        `.github/workflows/sprig{,-image}.yml`, `docs/remote-agents.md`,
        `crates/buzz-backend-kubernetes/src/image.rs`). Front matter: `id:
        implementation-crates-sprig`, `type: implementation`, `status: draft`, `origin:
        launchpad`, `audiences: [agent, developer, reviewer]`, one commit-citation FACT
        for provenance, and a `part-of` relationship targeting
        `architecture-containers-agent-runtime`.
        done when: the file exists, is valid YAML+Markdown, and every DoD bullet from
        issue #945's body (implementation responsibility/non-ownership, public entry
        points, owned source + representative tests, no restated domain semantics) has
        a corresponding sentence or table row in the body.

STEP 2  [needs 1]  ← RUNS HERE  Run `python3 launchpad/project-intelligence/corpus/validate.py`
        and fix anything it flags for this node specifically (pre-existing unrelated
        failures on `origin/launchpad` are not this task's to fix — confirmed by diffing
        against a stash of the new file).
        done when: `validate.py` reports zero FAIL/ERROR lines naming
        `implementation-crates-sprig`, and any nonzero exit code is shown (via
        `git stash` + re-run) to originate from pre-existing failures, not this node.

STEP 3  [needs 2]  Self-audit the finished node against issue #945's Definition of Done
        line by line, and against the template's *Evidence expectations* (every
        realization-surface row cites both the code and, where applicable, the target;
        no citation stands in for the other side).
        done when: a written line-by-line mapping exists (in the session, not
        necessarily committed) from each DoD bullet to the body text/evidence entry that
        satisfies it, with no bullet left unmapped.

STEP 4  [needs 3]  Earn the commit gate: run the corpus unit test suite as the sole
        command of its own tool call, then stage and commit with `-s`.
        done when: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests
        -p "test_*.py"` reports `OK`; `git add` stages exactly the plan file and the new
        corpus node; `git commit -s` succeeds and `git log --format=%B -1` shows a
        `Signed-off-by:` trailer.

STEP 5  [needs 4]  Final verification: re-read the committed diff against issue #945's
        DoD checklist a second time post-commit, and self-review since `corpus-review`
        may not be reachable in this session.
        done when: a written finding (clean, or a named residual concern) exists for
        every DoD bullet, and the report states plainly whether `corpus-review` ran or a
        manual self-review substituted for it.

PARALLEL  None. Steps 1-5 are strictly sequential: STEP 2 validates the exact file STEP 1
          wrote, STEP 3 audits what STEP 2 confirmed passes, STEP 4 commits what STEP 3
          audited, STEP 5 reviews what STEP 4 committed. No step is dispatched as a
          parallel subagent.

GATES     Self-review only (no `review-plan` run before STEP 1 per this task's own
          instructions, which prescribe the exact commands directly). `corpus-review`
          attempted at STEP 5 if reachable in-session; otherwise a manual self-review
          against the DoD substitutes, and the report says which happened.
          `review-code`/`review-tests` do not apply — this is a docs-only corpus node,
          no implementation or test code changes.

BUDGET    STEP 1. The hard part is not the front matter, it is writing an Implementation
          surface table whose every row is a `FACT` citing both a real source file and
          (where relevant) `docs/remote-agents.md`'s stated contract, without collapsing
          into a narrative summary the template explicitly forbids.

OPEN      Whether `implements: docs/remote-agents.md` should exist once that document
          gets a corpus node id. Planned handling: name it in the Target section's prose
          now, declare no edge, per `AGENTS.md` step 9 and the template's own rule
          against inventing an edge to a nonexistent id.

LEFT OUT  A `depends-on` edge to `buzz-acp`/`buzz-agent`/`buzz-dev-mcp` implementation
          nodes — none exist in the merged corpus yet (sibling issues #916/#918/#928 are
          unmerged and, per this task's own brief, not assumed to exist). A `references`
          edge to a verification/test-strategy node — none exists yet (`#1350`'s
          template only). Any additional hand-authored corpus document — the issue's DoD
          caps this task at exactly one canonical node.
