# Plan: issue #703 — document capabilities/agents/acp.md

Parent: Feature #613 ("agent workflow and supporting capability corpus exists"). This
plan covers issue #703 only; its DoD checklist is the spec.

## ALREADY TRUE

- `launchpad/docs/corpus/capabilities/agents/` does not exist yet on `origin/launchpad`
  (confirmed: `ls launchpad/docs/corpus/capabilities/agents/` → no such file or
  directory; `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`
  lists no `capabilities/` path at all).
- `launchpad/docs/corpus/templates/capability.md` exists and is a complete, mature
  template (required sections, skeleton, evidence expectations, industry model
  considered) — `AGENTS.md`'s "no template exists yet" line is stale for this type.
- `node.schema.json`'s `type` enum includes `capabilities` as its own dedicated value
  (distinct from `architecture`, `interfaces-events`, `operations`).
- `launchpad/docs/corpus/architecture/containers/agent-runtime.md`
  (`id: architecture-containers-agent-runtime`) already documents *how* the ACP
  harness is built (the `buzz-acp`/`buzz-agent`/`buzz-dev-mcp`/`sprig` crates), and is
  merged on `origin/launchpad`. This new node must not duplicate that content — it
  documents the product-level capability, at the level VISION_PROJECTS.md's own
  "Capability | Status" table states it ("MCP server + ACP agent harness", line 251,
  marked "✅ Ships today"), and `references` the architecture node rather than
  restating it.
- `launchpad/docs/corpus/architecture/flows/agent-turn.md`
  (`id: architecture-flows-agent-turn`) already documents the step-by-step turn
  lifecycle. Also merged; also `references`d rather than restated.
- No `interfaces-events`-typed node exists yet for the ACP protocol surface or
  `buzz-cli` — this is a real gap the new node's Boundary section names rather than
  fills.
- `origin/launchpad`'s corpus (outside `schema/`) currently has no `capabilities/`
  node at all, so there is no capability-shaped sibling to declare `part-of` or
  `implements` toward.

## STEP 1 — Write `launchpad/docs/corpus/capabilities/agents/acp.md`

Front matter: `id: capabilities-agents-acp`, `type: capabilities`, `status: draft`,
`origin: launchpad`, `audiences: [agent, developer, operator, reviewer]`. Evidence
ledger cites: this commit as provenance; `VISION_PROJECTS.md:251` for the capability
name and shipped status; `crates/buzz-acp/README.md` (top, BYOH section, "Using Any
ACP Agent" section) for what the capability lets a user/agent do and its behavioral
variants; `crates/buzz-agent/README.md` for the reference agent's loop; the two
architecture nodes above as `references` relationship targets, both confirmed present
on `origin/launchpad` at HEAD (`cad6c375fdcc590158c1456c9fc7875f0f84a844`).

Body follows the `capability.md` template skeleton exactly: capability statement,
maturity (shipped, cited), boundary (not architecture / not interface / not flow / not
operations, naming the two real neighbor nodes and the interface gap), relationships,
scope and omissions (what's covered, what's not with owners, what was expected but not
verified).

Done when: file exists, matches template shape, every claim traces to an opened
source.

## STEP 2 — Validate

Run `python3 launchpad/project-intelligence/corpus/validate.py` from repo root. Fix
any reported error (front matter, citation shape, relationship targets) and re-run
until exit 0.

Done when: exit code 0.

## STEP 3 — Earn the commit gate and commit

Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p
"test_*.py"` as the sole command in its own tool call, confirm `OK`. Then in a
separate call, `git add` the new corpus file and this plan file, and
`git commit -s -m "docs(corpus): document capabilities/agents/acp (#703)"`.

Done when: commit exists locally on `task/703-acp`; no push, no PR (per this batch's
process).

## STEP 4 — Self-review

Re-read the diff against issue #703's DoD checklist line by line; re-open every cited
source to confirm it supports its claim; confirm no second canonical document was
created; confirm `validate.py` still exits 0. Note explicitly that `review-code` /
`review-adjudicate` were not run (deferred per batch mode).

## PARALLEL

None — single small document, steps are strictly sequential (write → validate →
gate → commit → self-review).

## GATES

- `validate.py` must exit 0 before committing.
- The unittest discover command must print `OK` before committing, as its own
  isolated tool call (not chained with `git add`/`git commit`).
- No push, no PR — local commit only, per this batch's explicit process override.

## BUDGET

Single document, capped at the 5-step template above. No code changes, no generated
index changes (none exist to regenerate yet).

## OPEN

- Whether a `capabilities`-typed sibling (e.g. a broader "agent workflow" capability)
  will later want a `part-of` edge from this node — left to whoever authors that
  sibling, per `AGENTS.md`'s rule that `relationships` targets must exist in the
  branch being merged into, not be guessed at ahead of time.
- Whether an `interfaces-events` node for the ACP JSON-RPC surface / `buzz-cli` lands
  later and should be `references`d — same reasoning, left as a documented gap in
  *Scope and omissions*, not filled preemptively.

## LEFT OUT

- Any edit to `architecture/containers/agent-runtime.md` or
  `architecture/flows/agent-turn.md` — out of scope per issue #703's own "Out of
  scope" list (no second hand-authored canonical document, no restating their
  content).
- Any change to `node.schema.json`, `validate.py`, or generated indexes.
- Any resolution of open ADRs or standards gaps (e.g. #1321's revision-staying-put
  question) — this node follows the same working practice `AGENTS.md` itself uses.
