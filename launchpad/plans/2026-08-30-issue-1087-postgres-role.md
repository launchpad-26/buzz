# Issue #1087 — layers/data/postgres/role.md

Stated size: task's own instructions cap this plan at 5 steps  ->  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/datastore.md`, `launchpad/docs/corpus/architecture/containers/postgres.md` (id `architecture-containers-postgres`) and `launchpad/docs/corpus/architecture/principles/relay-is-source-of-truth.md` (id `architecture-principles-relay-is-source-of-truth`) are all merged on `origin/launchpad`. `launchpad/docs/corpus/layers/data/postgres/role.md` does not exist yet, and no `layers/data/**` file exists on `origin/launchpad` at all — the two other role.md siblings named in this Feature (`layers/data/object-storage/role.md` #1073, `layers/data/redis/role.md` #1097) are unmerged sibling-batch work and cannot be cited or linked.

STEP 1 [independent]  Gather evidence directly from source, independent of `architecture-containers-postgres.md`'s own ledger: `crates/buzz-db/src/lib.rs` (crate doc invariants, module list, `DbConfig`/`DbConfig::default`, `#[datastore_span]` call sites), `crates/buzz-db/src/migration.rs` (embedded `sqlx::migrate!`, the schema-destruction advisory lock), `crates/buzz-db/src/replica_fence.rs` (the bounded-staleness consistency proof), `migrations/0001_initial_schema.sql` (multi-tenant `community_id` row-zero invariant) and `migrations/0029_community_deletion.sql` (whole-community deletion lifecycle), `crates/buzz-relay/src/main.rs` (`BUZZ_AUTO_MIGRATE` gate, audit/search pools), `crates/buzz-audit/src/{lib,service}.rs` (hash-chain audit log), `docker-compose.yml` and `.env.example` (Postgres 17 pin, connection/pool env vars). ← RUNS HERE
  done when: every file above has been opened this session and its relevant claim noted for STEP 2's ledger.

STEP 2 [needs 1]  Write front matter (schema-valid: id `layers-data-postgres-role`, type `layers` — overriding the datastore template's own worked recommendation of `type: architecture` for a real instance, per this Feature's batch precedent set by #1073 and to be followed by #1097; disclosed explicitly in the evidence ledger per `standards/taxonomy.md`'s disclosure requirement — status `draft`, origin `launchpad`, audiences `[agent, developer, operator]`, relationships `part-of` → `architecture-containers-postgres` and `references` → `architecture-principles-relay-is-source-of-truth`, both confirmed present on `origin/launchpad`) and the body, following `templates/datastore.md`'s seven required sections (purpose & scope, technology & attachment profile, schema/namespace inventory, migration mechanism, access-pattern summary, operational characteristics, scope & omissions) while explicitly answering issue #1087's own DoD bullets not literally covered by those seven headings: authoritative-vs-derived-vs-cache-vs-transport role, owned data, lifecycle/retention, consistency semantics, tenancy/security boundary, and failure behavior. Link to schema/migration/code/test paths rather than copying DDL.
  done when: `launchpad/docs/corpus/layers/data/postgres/role.md` exists with schema-required front-matter fields present and every one of issue #1087's DoD bullets addressed by a named section or sentence.

STEP 3 [needs 2]  Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.
  done when: the command's own exit code is 0.

STEP 4 [needs 3]  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call. Do not push and do not open a PR — a later orchestration step bundles this branch with its batch siblings.
  done when: the unittest run reports OK and `git log -1` on this branch shows a new commit containing both files.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report OK, run bare and unpiped. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later bundling/review step — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to ~9 already-identified source files plus the two existing corpus nodes it links to.

OPEN: Whether `type: layers` (this Feature's batch-precedent override) or `type: architecture` (the datastore template's own worked recommendation for a real instance) is the corpus's eventual settled convention for datastore-instance nodes is not resolved by this task — it is disclosed as an explicit, evidenced override in the node's own ledger, matching the shape `corpus-standard-taxonomy.md` prescribes for an imperfect-fit `type` choice, and left for a later corpus-wide pass to reconcile across the three `role.md` siblings.

LEFT OUT: No relationship to the two sibling `layers/data/*/role.md` documents (object-storage, redis) — neither is merged on `origin/launchpad` at plan time, and declaring an edge to an unmerged id is a hard validation error once this branch lands on `launchpad`. No relationship to a `layers/data/postgres` data-entity or schema-inventory sibling node — none exists yet. No attempt to resolve the `.env.example` Typesense-variable staleness or the `BUZZ_AUTO_MIGRATE`-documentation mismatch already named as open gaps in `architecture-containers-postgres.md` — this node cites and does not re-litigate them. No change to any Rust/SQL source, and no PR — those are explicitly out of scope for this isolate/plan/build/verify/commit task.
