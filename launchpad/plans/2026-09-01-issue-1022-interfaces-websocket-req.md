Issue #1022 — document interfaces/websocket/req.md
Stated size: not stated on the issue → cap: 5 steps (per the dispatching task's explicit instruction)

ALREADY TRUE  (verified against git, not notes)
  Worktree `__worktrees/task-1022-interfaces-websocket-req` exists on branch `task/1022-interfaces-websocket-req`, `git status` clean, `HEAD` (b5dd39acb7ade0a33692edaebe674a1212111dd5) equals `origin/launchpad` after `git fetch origin launchpad`.
  `launchpad/docs/corpus/interfaces/websocket/req.md` does not exist (`ls` fails: No such file or directory).
  `launchpad/docs/corpus/schema/node.schema.json` requires `id, type, status, origin, audiences, evidence`, forbids any other top-level key, and its `type` enum is exactly 13 values including `interfaces-events` (no bare `interface` value exists).
  `launchpad/docs/corpus/templates/interface.md` (`id: corpus-template-interface`, `status: active`) is merged on `origin/launchpad` and prescribes the exact body shape for an interface-shaped node: Interface description, Operations, Contract and stability, Boundary, Relationships, Scope and omissions — plus `type: interfaces-events` for any instance node.
  `origin/launchpad`'s corpus tree (`git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`) contains `corpus-template-interface` plus draft architecture-flow nodes `architecture-flows-websocket-authentication`, `architecture-flows-websocket-connection`, `architecture-flows-historical-query`, `architecture-flows-live-fanout` — all valid `relationships` targets today.
  `crates/buzz-relay/src/handlers/req.rs` (`handle_req`) and `crates/buzz-relay/src/protocol.rs` (`ClientMessage::parse`, `RelayMessage::{event,eose,closed,notice}`) are the primary REQ implementation; `crates/buzz-core/src/filter.rs` (`filters_match`) and `crates/buzz-core/src/kind.rs` (`P_GATED_KINDS`) back the matching and p-gate claims; `crates/buzz-relay/src/nip11.rs` (`relay_limitation`) backs the advertised `max_filters`/`max_subid_length`/`max_limit` claims.

STEP 1  Re-confirm the specific line citations already gathered this session for: REQ JSON parsing and its NIP-11-advertised limits (`protocol.rs`), the p-gate 403/CLOSED behavior AGENTS.md gotcha #2 describes, EOSE/historical-delivery ordering, the auth-required CLOSED path, and `filters_match`'s AND/OR semantics — no new source classes, just pinning exact line ranges for the evidence ledger.  [independent]
        done when: every FACT statement planned for the node's evidence array has a `path:start-end` citation confirmed open in this session (already true for `req.rs:50-56`, `req.rs:82-95`, `req.rs:219-242`, `req.rs:472-479`, `protocol.rs:41-107`, `protocol.rs:180-217`, `connection.rs:548-554`, `filter.rs:10-12`, `nip11.rs` limitation constants, `kind.rs` P_GATED_KINDS).

STEP 2  [needs 1] ← RUNS HERE — Write `launchpad/docs/corpus/interfaces/websocket/req.md`: front matter (`id: interfaces-websocket-req`, `type: interfaces-events`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, evidence array classified FACT/TEAM_KNOWLEDGE per source, `relationships: implements: corpus-template-interface` plus `references` to the historical-query and websocket-authentication flow nodes) and the body following `corpus-template-interface`'s required sections, satisfying every Definition-of-done bullet on the issue (inputs/messages incl. mandatory `kinds` p-gate, outputs incl. EOSE, error/rejection, auth, versioning/NIP-11 limits, ordering, upstream NIP-01 link, one valid example, one failure example).
        done when: the file exists, front matter is valid YAML, and every Required section from `corpus-template-interface` (`## Operations`, `## Contract and stability`, `## Boundary`, `## Relationships`, `## Scope and omissions`) is present in the body.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root and fix any FAIL line it reports (UNVERIFIED notices are acceptable).
        done when: the command exits 0.

STEP 4  [needs 3] Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call, to earn the commit gate stamp.
        done when: the command prints `OK` on stderr/stdout and exits 0.

STEP 5  [needs 4] Commit the plan and the new node with `git commit -s`, in a call separate from step 4's gate command.
        done when: `git log -1 --format=%H` returns a new commit SHA and `git status` shows a clean working tree with both files tracked.

PARALLEL: none of these five can run as independent subagents — step 2 depends on step 1's citations, and steps 3-5 are a strictly sequential validate → gate → commit chain on the same file. This is a single-document, single-worktree task with no partitionable work.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 (step 3). The corpus unittest suite must print `OK` before commit (step 4) — this *is* the commit gate for this repo's corpus workflow, not a separate review skill. `review-adjudicate`, `review-final`, and any cross-model pass are explicitly out of scope per the dispatching task ("Do not open a PR. Do not push. Do not merge anything.") and are deferred to whoever later opens the PR for this branch. `qa` explore mode does not apply — this is a documentation-only change with no runtime interface to exercise.

BUDGET: Step 2 (drafting the body) is the step most likely to eat the budget — the interface template's *Required sections* and *Evidence expectations* are extensive, and every operation-table row and contract claim needs its own opened-and-cited source per `AGENTS.md`'s evidence discipline, not a restated assumption.

OPEN: The interface template itself says `implements` (its own choice) vs `references` (the sibling reference template's choice) for a node's optional self-link to its template is *not* corpus-wide-settled — this plan follows the template's own stated preference (`implements: corpus-template-interface`) but flags that as a convention, not a resolved standard. Whether the p-gate's HTTP-403 framing in root `AGENTS.md`'s gotcha #2 should be described in this WebSocket-only node (which sees a CLOSED message, not an HTTP status) or left entirely to a future HTTP `/query` interface node is resolved here by describing the WebSocket CLOSED behavior as primary and citing the shared `p_gated_filters_authorized` function as the mechanism both surfaces share, without claiming this node documents the HTTP surface.

LEFT OUT: No second corpus document — the HTTP `/query` bridge's own REQ-equivalent (`crates/buzz-relay/src/api/bridge.rs`), NIP-50 search-REQ's distinct one-shot behavior, and COUNT (NIP-45) are each `references`-able neighbors but not folded into this node's body beyond a boundary-statement mention, since REQ's own operation (register + deliver historical + EOSE) is already one complete idea. No event-kind-level documentation of any specific kind REQ can filter for (P_GATED_KINDS, SHARED_GATED_KINDS, etc.) — that is `#1337`'s event-kind template's territory per `corpus-template-interface`'s own Boundary section. No attempt to resolve the `implements`-vs-`references` self-link question noted above as a corpus-wide standard.
