# Issue #936 — implementation/crates/buzz-relay.md

Stated size: not stated on the issue  →  cap: 5 steps (corpus-batch-author task convention)

ALREADY TRUE: `launchpad/docs/corpus/templates/implementation-reference.md`, `launchpad/docs/corpus/architecture/containers/relay.md`, and all thirteen `architecture/flows/*.md` nodes buzz-relay orchestrates are merged on `origin/launchpad`. Eleven sibling implementation-reference nodes for other crates (`buzz-core`, `buzz-db`, `buzz-auth`, `buzz-acp`, `buzz-agent`, `buzz-cli`, `buzz-admin`, `buzz-audit`, `buzz-relay-mesh`, `buzz-push-gateway`, and others under `#916`-`#935`) already exist as committed-but-unmerged sibling branches in this same batch, establishing the node shape this task follows. `launchpad/docs/corpus/implementation/crates/buzz-relay.md` does not exist yet.

STEP 1  Gather evidence: read `crates/buzz-relay/Cargo.toml`, `src/lib.rs` (module list), and the module tree (`handlers/`, `api/`, `audio/`, `conformance/`, `tunnel/`) to establish ownership boundaries one layer below the container node. Read `protocol.rs`, `connection.rs`, `admission.rs`, `handlers/{auth,event,ingest,req,close}.rs`, `subscription.rs`, and `state.rs`'s `ConnectionManager`/`CommunityConnectionRegistry`/`revalidate_live_communities` for the WS message pipeline and community-lifecycle mechanics the container node does not detail. Count test functions and `#[ignore]` gates crate-wide; cross-check `Justfile`'s `test-unit` recipe, `scripts/run-tests.sh`, and `.github/workflows/ci.yml`'s `unit-tests`/`backend-integration` jobs against those counts to establish what actually runs. Record `git rev-parse HEAD`.                                    [independent]
        done when: every symbol named in STEP 2's Implementation-surface table has been opened directly, and the test-count/CI-gate claim is backed by line numbers in `Justfile`, `scripts/run-tests.sh`, and `.github/workflows/ci.yml`.

STEP 2  [needs 1] Write front matter (schema-valid: id `implementation-crates-buzz-relay`, type `implementation`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, `relationships: [{type: references, target: architecture-containers-relay}]` plus `references` to a small, personally-verified subset of the flow nodes whose citations I directly re-checked) and the body per the implementation-reference template's required sections: Target (the crate's own module-doc invariants and the conformance/TLA wire points, neither of which carry a corpus node id), Implementation surface (module/symbol table: protocol parsing, connection lifecycle, admission control, EVENT/REQ/AUTH/CLOSE handlers, subscription registry, community connection registry), Divergences (checked, not assumed — re-verify a sample of the container node's own citations), Verification (the test-count/CI-gate mechanism from STEP 1), Relationships, Scope and omissions (explicitly naming `api/git`, `api/media`, `audio/`, `push_runtime.rs`, `mesh_boot.rs` as named-but-not-read-in-depth, owned by their respective flow nodes).
        done when: `launchpad/docs/corpus/implementation/crates/buzz-relay.md` exists with all seven required template sections present and every `relationships` entry resolvable against `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Diff against the pre-existing baseline failure count on `origin/launchpad` if the exit code is nonzero, rather than assuming this node caused it.
        done when: the command exits 0, or its only FAIL lines match the pre-existing baseline confirmed by stashing this node and re-running.

STEP 4  [needs 3]  ← RUNS HERE
        Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own call to earn the verification stamp, then commit the plan + document in a separate call. Do not push and do not open a PR — this task's output is integrated into one Feature-level PR later.
        done when: the unittest suite reports `OK` and `git log -1 --stat` shows a new commit containing exactly the plan file and the corpus document.

PARALLEL: none — single file, single task, no code changes.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0 with no new FAIL entries. The corpus unittest suite must report `OK`. `review-adjudicate` and cross-model final review are deferred to the batch owner's later integration pass — not run here.

BUDGET: medium — one document, no code changes, but the crate is the repository's largest (~41k lines across top-level files plus ~29.7k in `api/`, ~23.6k in `handlers/`, ~6.4k in `audio/`), so evidence gathering is deliberately scoped to the WS protocol/connection/handler pipeline and the test-execution-gate mechanism rather than every handler; `api/git`, `api/media`, `audio/`, and `push_runtime.rs` are named but not read in depth, per the issue's own instruction not to attempt exhaustive coverage.

OPEN: Whether to declare `references` toward `architecture-containers-relay` or no relationship at all was resolved by following the already-established sibling precedent (`buzz-relay-mesh`'s unmerged draft, and `buzz-db`'s merged-shape draft toward `architecture-containers-postgres`): the container node was authored by reading buzz-relay's own code, not the reverse, so it is a sibling description at a different altitude, not a governing spec buzz-relay code was built to satisfy — `references`, not `implements`. Whether to declare `references` toward any of the thirteen flow nodes is decided per-node in STEP 2 based on which citations are actually re-opened and verified during STEP 1, not declared speculatively for all thirteen.

LEFT OUT: No `implements` edge to any target — neither the crate's own module-doc invariants nor `docs/spec/MultiTenantRelay.tla` (the conformance harness's wire-point target) carry a corpus node id at the recorded revision, so both are named by path per `AGENTS.md`'s rule against inventing an edge to a nonexistent id. No exhaustive handler-by-handler coverage of `handlers/ingest.rs` (5.2k lines) or `handlers/side_effects.rs` (3.8k lines) — the largest two files in the crate — beyond their role in the pipeline; per-kind ingestion/side-effect semantics are left to the flow nodes and to a future, narrower node if one is ever warranted. No coverage of `api/git/`, `api/media.rs`, `audio/`, `push_runtime.rs`, or `mesh_boot.rs`'s internals beyond naming them and their owning flow/implementation node — `git-push.md`, `media-upload.md`/`media-download.md`, `huddle-audio.md`, `push-notification.md`, and the (unmerged) `buzz-relay-mesh`/`buzz-push-gateway` implementation nodes already cover or will cover that ground.
