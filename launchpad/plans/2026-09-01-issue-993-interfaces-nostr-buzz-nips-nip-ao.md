Issue #993 — task: document interfaces/nostr/buzz-nips/nip-ao.md
Stated size: no `Size` line in the issue body; the dispatching brief caps this at 5 steps as a small single-document task  →  cap: 5 steps

ALREADY TRUE  (verified against git and the working tree, not notes)
  Worktree __worktrees/task-993-interfaces-nostr-buzz-nips-nip-ao exists, branched from origin/launchpad at 650354eab8d41ab6ce1a71de079a6c6d95c69052.
  launchpad/docs/corpus/interfaces/ does not exist anywhere in this worktree — confirmed by `ls`; the target file is genuinely new, not an edit.
  docs/nips/NIP-AO.md exists at repo root and was read in full — it is the authoritative NIP-AO spec text (kind 24200, Agent Observer Frame).
  node.schema.json's `type` enum has 13 members; `interfaces-events` is the only interface-shaped value, and both launchpad/docs/corpus/templates/interface.md and templates/event-kind.md (both merged on origin/launchpad already) confirm real instance nodes of either template carry `type: interfaces-events`.
  `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` lists AGENTS.md, README.md, architecture/** (36 files), standards/** (17 files), templates/** (24 files) — no `interfaces/` subtree and no other buzz-nip node anywhere, so no sibling relationship target exists yet except the templates themselves (e.g. `corpus-template-interface`).
  Implementation confirmed by direct reads, not assumed from the spec: `crates/buzz-core/src/kind.rs:469` (`KIND_AGENT_OBSERVER_FRAME = 24200`, in `P_GATED_KINDS`), `crates/buzz-core/src/observer.rs` (tag names, NIP-44 length envelope, encrypt/decrypt helpers), `crates/buzz-acp/src/observer.rs` (the `ObserverEvent` struct actually serialized into `content`), `crates/buzz-relay/src/handlers/event.rs:947-1164` (`handle_agent_observer_event`, `agent_observer_route`, rate limiter, rejection messages, tests), `crates/buzz-relay/src/handlers/req.rs` and `subscription.rs` (P-gated REQ filter enforcement), `crates/buzz-acp/src/lib.rs:1294-1325` (`cancel_turn` control handling), `crates/buzz-sdk/src/builders.rs:247-282` (`build_agent_observer_frame`), `desktop/src/shared/api/observerRelay.ts` (client subscribe/publish).
  One real spec-vs-code divergence found and confirmed by direct reads (not inferred): the spec's Decrypted Payload section lists 7 fields (`seq, timestamp, kind, agentIndex, channelId, sessionId, turnId, payload`); `crates/buzz-acp/src/observer.rs`'s `ObserverEvent` struct additionally serializes an 8th optional field, `startedAt`, that the spec text never mentions. Also the spec text says "Clients SHOULD subscribe with since=<now>"; `desktop/src/shared/api/observerRelay.ts` instead subscribes with a 300-second lookback (`OBSERVER_LIVE_LOOKBACK_SECS`), by design, per its own comment. Both go in the node as FACTs with both sources cited, not silently reconciled.

STEP 1  Draft the front matter (evidence ledger) and full body for launchpad/docs/corpus/interfaces/nostr/buzz-nips/nip-ao.md, satisfying every bullet of issue #993's Definition of Done using the evidence already gathered in this session (no new source reading required — everything cited above was opened directly).  [independent]
        done when: the file exists at that exact path, is valid YAML+Markdown, and contains sections covering inputs/messages, outputs/responses, error/rejection behavior, auth, versioning/compatibility, ordering/idempotency, a link to docs/nips/NIP-AO.md, and at least one valid + one failure example.

STEP 2  Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repo root and fix any FAIL line it reports against the new node (UNVERIFIED notices are expected and fine).  [needs 1]  ← RUNS HERE
        done when: the command exits 0 and prints no FAIL line attributable to the new node.

STEP 3  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own tool call — this is the commit gate, not a step to combine with anything else.  [needs 2]
        done when: the command's output ends with `OK`.

STEP 4  Stage exactly the node file and this plan file and commit with `git commit -s -m "docs(corpus): document Buzz NIP-AO interface (#993)"`, in a separate tool call from step 3.  [needs 3]
        done when: `git log -1 --stat` shows a new commit whose only changed files are the two named paths, and `git log -1 --pretty=%B` contains a `Signed-off-by` trailer.

STEP 5  Self-review: re-read the committed diff against issue #993's Definition-of-done checklist line by line, confirm every evidence entry's cited file/line actually supports its statement, confirm no second hand-authored canonical corpus document was created, and re-run validate.py.  [needs 4]
        done when: validate.py still exits 0 against the committed state and every DoD bullet has a corresponding section/evidence entry in the diff.

PARALLEL  None of these five steps can run as independent subagents — each depends on the file state the previous step produced (draft → validate → test-gate → commit → review), and steps 3 and 4 are explicitly required to be separate tool calls by the task brief itself, ruling out any fan-out.
GATES     Two mechanical gates only, per the task brief: `launchpad/project-intelligence/corpus/validate.py` (step 2, must exit 0) and the corpus test suite via `unittest discover` (step 3, must print `OK`, and is also this repo's commit gate — a rejected commit with no stamp found is reported as a finding, never routed around with `--no-verify`). No `review-*` skill or `qa` explore mode applies: this is a single Markdown documentation node with no runtime interface to exercise, and the task brief does not request a review pass.
BUDGET    Step 1 (drafting) is the step most likely to eat the budget — getting FACT/INFERENCE/TEAM_KNOWLEDGE classification right on ~20+ evidence entries, and writing the required sections (operations, contract/stability, boundary, relationships, scope/omissions) without restating the NIP-AO spec instead of citing it, is the substantive work; steps 2-5 are mechanical checks against what step 1 produced.
OPEN      Whether this node should be structured strictly as an "interface" node (per templates/interface.md) or would fit templates/event-kind.md's shape better is not resolved by this plan: NIP-AO's primary identity is stateable as "kind: 24200" (event-kind template's own criterion), but issue #993 and its file path (interfaces/nostr/buzz-nips/) explicitly direct an interface-shaped node at that exact path. This plan follows the issue's explicit direction and type value (`interfaces-events`, which both templates share for real instances) rather than re-litigating the template choice — flagged here, not decided silently.
LEFT OUT  No `relationships` target beyond an optional `implements: corpus-template-interface` edge (confirmed resolvable against origin/launchpad) is added — no other buzz-nip sibling node exists on origin/launchpad to link to, per AGENTS.md step 9's rule to check the merge target, not the working tree. No second corpus document, no edit to any existing corpus node, and no generated index are touched, per the issue's own out-of-scope list.
