Issue #1162 — document the `p` tag as a corpus concept node
Stated size: no `Size` line on the issue (corpus-plan:v2 document task) -> cap: 5 steps (batch brief)

ALREADY TRUE  (verified against the real code and real command output, not notes)
  Worktree `/home/serina/Launchpad/buzz/__worktrees/task-1162-p-tag`, branch
  `task/1162-p-tag`, branched from `origin/launchpad` at
  `29ca9b189bd3f639ba09c972b57c70538c0860c6` (`git rev-parse HEAD` in the worktree;
  `git cat-file -e` exits 0). `launchpad/docs/corpus/layers/protocol/` does not exist yet —
  no `layers/protocol/*` node appears in the merged-id list, so this is a new directory.

  Evidence already opened and read at that revision:
  - `crates/buzz-core/src/kind.rs` — `P_GATED_KINDS` is a six-member slice
    (`KIND_AGENT_OBSERVER_FRAME` 24200, `KIND_MEMBER_ADDED_NOTIFICATION` 44100,
    `KIND_MEMBER_REMOVED_NOTIFICATION` 44101, `KIND_GIFT_WRAP` 1059,
    `KIND_DM_VISIBILITY` 30622, `KIND_AGENT_TURN_METRIC` 44200) with a doc comment
    stating the filter-layer enforcement and the NULL `search_tsv` storage backstop.
  - `crates/buzz-relay/src/handlers/req.rs` — `p_gated_filters_authorized` uses
    `filter.kinds.as_ref().is_none_or(...)`, so a kindless filter is treated as able to
    match a p-gated kind; the `ids` exemption is withheld when the filter explicitly names
    `KIND_DM_VISIBILITY` or `KIND_AGENT_TURN_METRIC`; otherwise a non-empty `ids` passes;
    otherwise every `#p` value must equal the authenticated reader's hex pubkey.
    `filter_to_query_params` sets `p_tag_hex` only when `values.len() == 1`.
    `filter_fully_pushable` returns `false` for a `#p` with more than one value.
    `filters_match` is applied per row after the DB query (req.rs, historical delivery).
  - `crates/buzz-core/src/filter.rs` — `reader_authorized_for_event` is the result-level
    `#p`-owner check for kinds 30622 and 44200; `filter_match_one` compares tag values
    byte-exactly.
  - `crates/buzz-db/src/runtime/mod.rs` — `insert_mentions_in_transaction` is where the
    `p` tags are extracted (NOT `crates/buzz-db/src/store/event.rs`, which is where the
    dispatch prompt pointed; `store/event.rs` holds the `p_tag_hex` join instead).
    Pubkeys are validated as 64 ASCII-hex chars, lowercased, and inserted with
    `ON CONFLICT DO NOTHING` in 5,000-row chunks.
  - `migrations/0001_initial_schema.sql` — `event_mentions` primary key is
    `(community_id, pubkey_hex, event_id)`.
  - `docs/nips/NIP-AM.md`, `docs/nips/NIP-DV.md` — Buzz's own NIPs stating the `p` tag is
    the read-authorization key for 44200 and 30622 respectively.
  - Call sites: `p_gated_filters_authorized` is also called from
    `crates/buzz-relay/src/api/bridge.rs` and `crates/buzz-relay/src/handlers/count.rs`.

  Boundaries confirmed merged and owned elsewhere: `capabilities-messaging-mention`
  (@-name resolution to `p` tags), `capabilities-activity-mentions-feed` (the feed query),
  `capabilities-messaging-direct-message`, `capabilities-messaging-gift-wrap`,
  `layers-data-postgres-events-table` (the `event_mentions` schema itself).

STEP 1 — write the front-matter evidence ledger
  Exactly one commit-only FACT (the provenance entry, `29ca9b18…`). One entry per
  substantive claim, each citing a bare repo-relative path already opened above.
  TEAM_KNOWLEDGE only for #1162's own definition of done. INFERENCE, with an honest
  `confidence`, for anything reasoned rather than read.
  done-when: front matter carries only the seven schema-permitted fields, one commit-only
  FACT, and every FACT citation names a file opened in ALREADY TRUE.

STEP 2 — write the body against `templates/concept.md`
  Definition first (one sentence), then the three roles the tag plays (reference,
  delivery addressing, authorization key), the p-gate's decision order, and the query-side
  single-value pushdown asymmetry. Link — never restate — the merged capability nodes and
  the `event_mentions` schema node.
  done-when: the body defines the term in one sentence before deeper explanation, states
  what the `p` tag must not be confused with, and contains no restatement of a merged
  sibling's canonical content.

STEP 3 — write Scope and omissions with both required halves
  (a) what this node does not cover and who owns it; (b) separately, what was expected to
  be verified and could not — specifically that no unit test for `filter_fully_pushable`
  exists, and that no relay was run to observe the pushdown asymmetry at runtime.
  done-when: both halves are present as distinct, separately labelled content.

STEP 4 — add relationships, verified against the merged-id list only
  Grep each candidate id in the merged-id file before writing it. No #609 sibling id.
  done-when: every `relationships[].target` appears in the merged-id list.

STEP 5 — validate, self-review, stamp, commit
  `python3 launchpad/project-intelligence/corpus/validate.py` exits 0. Re-read every
  evidence entry against its source before stamping. Then the unittest stamp as a sole
  foreground unpiped command with `timeout: 600000`, ending `OK`. Then `git add` +
  `git commit -s`.
  done-when: validator exit 0, unittest run ends `OK`, one signed commit exists, nothing
  pushed and no PR opened.
