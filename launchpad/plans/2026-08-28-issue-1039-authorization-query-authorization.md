# Issue #1039 — layers/authorization/query-authorization.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/AGENTS.md` are merged on `origin/launchpad`; the `launchpad/docs/corpus/layers/` directory does not exist yet, so `launchpad/docs/corpus/layers/authorization/query-authorization.md` does not exist. `launchpad/docs/corpus/templates/concept.md` is the closest matching template (Diátaxis Explanation form; required sections: Definition, Use cases, Boundary, Related resources, Scope and omissions) — the issue's DoD bullets ("defines the term in one sentence," "states boundaries/non-goals," "links to related concepts," "examples clarify, don't introduce a second concept") map directly onto it. `type: layers` per the node.schema.json enum and the issue title's own `layers/authorization/` path.

STEP 1  Gather evidence directly from the read/query authorization code path, rather than trusting the sibling #1035 agent's grep-based claim by inference. Verified so far: `crates/buzz-relay/src/handlers/req.rs::handle_req` (WS REQ) and `crates/buzz-relay/src/handlers/count.rs::handle_count` (WS COUNT) both enforce auth presence, per-connection scope (`Scope::MessagesRead`), channel-membership resolution via `AppState::get_accessible_channel_ids_cached` (`crates/buzz-relay/src/state.rs`) with a DB-confirmed fallback (`Database::is_member`), the p-gate/engram-gate/author-only-gate for global (non-channel-scoped) filters (`p_gated_filters_authorized`, `engram_filters_authorized`, `author_only_filters_authorized` in `req.rs`), and a per-event visibility check (`event_visible_to_reader`). `crates/buzz-relay/src/api/bridge.rs::query_events`/`count_events` (HTTP `/query`, `/count`) mirror the same gates after host-derived tenant binding (`crate::tenant::bind_community`) and NIP-98 bridge auth (`verify_bridge_auth`). Confirmed by direct grep (`grep -rn check_read_access crates/`) that `buzz-auth`'s `check_read_access`/`ChannelAccessChecker` (`crates/buzz-auth/src/access.rs`) have **zero call sites anywhere in the workspace outside their own unit tests** — the read path does not use them either, extending #1035's write-path finding rather than contradicting it. Confirm the p-gate's "omitting kinds triggers 403" behavior traces to `p_gated_filters_authorized`'s `filter.kinds.as_ref().is_none_or(...)` treating an absent `kinds` as "can match p-gated" by default. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: `id: layers-authorization-query-authorization`, `type: layers`, `status: draft`, `origin: launchpad`, `audiences: [agent, developer, reviewer]`, no `relationships` — no merged corpus node under `layers/` or `authorization/` exists yet to be a legitimate target; re-verify this against `origin/launchpad` immediately before writing, per `AGENTS.md`'s warning against a stale "nothing to point at" justification) and the body using `templates/concept.md`'s required sections: Definition (one-sentence definition, then the REQ/`/query`/`/count` enforcement points as FACT, each cited to the exact function), Boundary (query authorization is the read-path filter/channel/event gate — explicitly not `buzz-auth`'s unused `ChannelAccessChecker` trait, not event ingestion/write authorization which is #1035's node, not channel role assignment which is #1033's node), Use cases (why an agent or reviewer needs this — e.g. explaining a `403` from `POST /query`, auditing what `kinds` omission does), Related resources (prose links to #1035/#1033's nodes once they exist — none merged yet, so no `relationships` edge), Scope and omissions (explicitly naming buzz-auth's unused checker as a known gap, and anything from STEP 1 expected but not verified).

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole command in its own tool call to earn the verification stamp, confirm `OK`, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here.

BUDGET: small — one document, no code changes, evidence gathering scoped to `req.rs`, `count.rs`, `bridge.rs`, `state.rs`, `buzz-auth/src/access.rs`, plus one grep across the workspace.

OPEN: Whether `buzz-auth`'s `check_read_access`/`ChannelAccessChecker` trait is genuinely dead code across *both* read and write paths, or reserved for a not-yet-wired future call site, is not something this node can resolve — it is reported as a verified FACT (no call sites found) with the open question named rather than answered, and any fix belongs to a separate implementation issue, not this documentation task. No `relationships` edges: #1035 (event-authorization) and #1033 (channel-roles) are sibling in-flight tasks with no merge ordering against this one, so neither is a valid target at authoring time — checked directly (`ls launchpad/docs/corpus/layers/**/*.md` returns nothing) rather than assumed.

LEFT OUT: No claim about NIP-42 WebSocket authentication itself (a separate authentication-layer node, #1028's territory) or channel role/membership *assignment* (#1033) — this node covers only what gates a filter/query once the requester is already authenticated and their channel-accessible-set is known. No attempt to fix or wire up `buzz-auth`'s unused checker — that is implementation work, not this task's. No coverage of git smart-HTTP or Blossom media read authorization — those are separate HTTP surfaces with their own gates, out of scope for "query authorization" as the issue names it.
