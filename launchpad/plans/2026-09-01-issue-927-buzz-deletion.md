# Issue #927 — implementation/crates/buzz-deletion.md
Stated size: issue #927 carries no `Size` label; the dispatching task brief explicitly caps this plan at 5 steps  →  cap: 5 steps

ALREADY TRUE: `launchpad/docs/corpus/templates/implementation-reference.md`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/schema/node.schema.json` and `launchpad/docs/corpus/schema/relationships.schema.json` are merged on `origin/launchpad`. `launchpad/docs/corpus/architecture/containers/postgres.md` (id `architecture-containers-postgres`) and `launchpad/docs/corpus/architecture/principles/subsystem-isolation.md` (id `architecture-principles-subsystem-isolation`) are also merged and already discuss `buzz-deletion` by name. `launchpad/docs/corpus/implementation/crates/buzz-deletion.md` does not exist yet. No `community-deletion-safety` ADR exists under `launchpad/decisions/` (checked by listing); the `am/community-deletion-safety-*` branch is an unmerged remote branch, not a corpus/ADR target.

STEP 1  Gather evidence: read `crates/buzz-deletion/Cargo.toml` and all of `crates/buzz-deletion/src/lib.rs` (2392 lines — public fns/types, the `Command` CLI enum, the stage-execution state machine, tests). Cross-check every crate that depends on it (`buzz-admin`, `buzz-relay`, `buzz-workflow` per `grep buzz-deletion */Cargo.toml`) and every real call site (`grep -rn buzz_deletion crates/*/src`) to determine actual integration points: `acquire_serving_write`/`ServingWriteGuard` (relay git transport, media upload, side_effects, push_runtime, tunnel/directory; workflow executor) and `store()`/`is_serving_active` (relay event.rs, ingest.rs gating writes during fencing). Read `crates/buzz-db/src/store/deletion.rs`'s `DeletionStage` enum, `DeletionStore`, `DeletionRequest`, `LeaseToken` types it wraps, and migrations `0029_community_deletion.sql`/`0030_community_deletion_recovery.sql`. Record `git rev-parse HEAD`. ← RUNS HERE  [independent]
        done when: a note exists (not necessarily committed) listing every public symbol in `lib.rs`, every dependent crate + call site, the `DeletionStage` variant list, and the recorded commit SHA.

STEP 2  Write front matter (schema-valid: id `implementation-crates-buzz-deletion`, type `implementation`, status `draft`, origin `launchpad`, audiences `[agent, developer, operator, reviewer]`, evidence array with one FACT/INFERENCE/TEAM_KNOWLEDGE entry per substantive claim, `relationships: [{type: references, target: architecture-containers-postgres}, {type: references, target: architecture-principles-subsystem-isolation}]` — both verified merged and both already discuss this crate) and the body using `implementation-reference.md`'s required sections: Realization statement, Target (no formal spec/ADR node exists — name the crate doc comment, `ARCHITECTURE.md`'s absence of this crate, and the migrations as the closest targets, and say plainly there is no corpus-id'd spec to `implements`), Implementation surface (table: `lib.rs` public fns/types + `Command` variants + which callers use them), Divergences (crate is not in `ARCHITECTURE.md`'s crate list at all — same gap #698 already recorded for other post-diagram crates), Verification (unit tests always run + many `#[ignore = "requires Postgres"/"and S3"]` integration tests), Relationships, Scope and omissions (state plainly it does not own `buzz-db`'s deletion-store persistence logic, S3/media deletion mechanics in `buzz-media`, or the CLI-argument surface already owned by `buzz-cli`/`buzz-admin` docs).  [needs 1]
        done when: `launchpad/docs/corpus/implementation/crates/buzz-deletion.md` exists on disk with every required section from the template present and no placeholder text.

STEP 3  Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0. Confirm the pre-existing ~21-failure baseline is unaffected by diffing against a stashed clean run if the exit code is nonzero.  [needs 2]
        done when: the command exits 0, or exits nonzero with only the pre-existing baseline failures (confirmed by comparison against `origin/launchpad` with this node's file stashed).

STEP 4  Run `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` as the sole command in its own call to earn the verification stamp; confirm `OK`.  [needs 3]
        done when: the command's final line reads `OK`.

STEP 5  `git add` the plan and the new corpus doc, then `git commit -s` with a `docs(corpus): ...` message referencing #927. Do not push, do not open a PR — this batch integrates via a separate Feature-level PR later.  [needs 4]
        done when: `git log -1 --format=%H` names a new commit containing both files, and `git status` shows a clean tree for them.

PARALLEL: none — single file, single task; every step depends on the previous one's output.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. The corpus unittest suite must report `OK`. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's later integration review — not run here. No `qa` explore mode — this is a docs-only change with no runtime interface to exercise.

BUDGET: small — one document, no code changes; evidence gathering scoped to one crate (~2400 lines, already read), 3 dependent Cargo.tomls, ~10 call sites, and one 4922-line wrapped module read at signature/structure level only.

OPEN: No corpus node yet exists for a "community deletion" spec/ADR/NIP this crate could `implements` toward — the crate's own doc comment and the migrations are the closest thing to a governing spec, and neither carries a corpus node id. This is recorded as an explicit gap in the Target section rather than invented as a relationship. Per `AGENTS.md` step 9, relationships are checked against `origin/launchpad`'s current corpus tree, not this worktree's — `implementation/` has no sibling nodes yet, so this is the first node under `implementation/crates/`.

LEFT OUT: No `implements` edge (no target has a corpus id). No claim about `buzz-media`'s internal deletion mechanics, `buzz-db`'s deletion-store schema/SQL, or `buzz-cli`'s CLI surface — each is a different crate/node's subject. No fix to `ARCHITECTURE.md`'s crate list omission (already flagged by #698's node) or to any other corpus node. No relationship to `architecture-principles-relay-is-source-of-truth` even though it also mentions `buzz-deletion` in passing — the two references chosen (`postgres`, `subsystem-isolation`) already substantively describe this crate's architectural position without redundant edges.
