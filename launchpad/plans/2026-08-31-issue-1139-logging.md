Issue #1139 — task: document layers/observability/logging.md
Parent PRD #611. No `Size` line on the issue; the batch-run brief caps this single-document
task at 5 steps, so that is used as the declared cap rather than guessed per-step.

Stated size: no Size line on issue -> cap: 5 steps (batch-run instruction)

ALREADY TRUE
  `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md` and
  `launchpad/docs/corpus/templates/concept.md` are merged on `origin/launchpad` (HEAD
  338b4d0cf2dd76cc43964bb717ce9f0a94a9c7a5), and
  `launchpad/docs/corpus/layers/observability/logging.md` does not exist yet. No sibling
  node under `layers/` exists on `origin/launchpad` at this revision (checked via
  `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`); #1144
  (structured-logging) and #1136 (datastore-tracing) are open tasks with no merged node to
  link to.

STEP 1  [independent]  Gather evidence for the general/umbrella logging convention across
        surfaces: `crates/buzz-relay/src/main.rs` (subscriber init, `RUST_LOG`/`EnvFilter`),
        `crates/buzz-relay/src/telemetry.rs` (JSON formatter, OTEL layer, `BUZZ_OTEL_FILTER`),
        `crates/buzz-push-gateway/src/main.rs` and `crates/buzz-test-client/src/main.rs`
        (plain `tracing_subscriber::fmt()`), `crates/buzz-admin/src/main.rs` and
        `desktop/src-tauri/src/managed_agents/*.rs` (tracing macros with no subscriber init
        found), `CONTRIBUTING.md`'s "Logging and Tracing" section, `.env.example`'s
        `RUST_LOG`/`BUZZ_OTEL_FILTER`/`OTEL_EXPORTER_OTLP_ENDPOINT` lines, desktop frontend
        console usage (`desktop/src` grep for `console.log/warn/error/debug`, `desktop/biome.json`
        checked for a console lint rule), and mobile's `debugPrint` convention
        (`AGENTS.md`/`CLAUDE.md` line ~565, `mobile/lib` grep for `debugPrint` and bare
        `print(`, `mobile/analysis_options.yaml` checked for an `avoid_print` rule).
        done when: each source above has been opened directly and its relevant lines/behavior
        recorded, not assumed from a filename.

STEP 2  [needs 1]  ← RUNS HERE  Write the node: front matter (`id: layers-observability-logging`,
        `type: layers`, `status: draft`, `origin: launchpad`, `audiences`, an `evidence` ledger
        with one entry per claim from STEP 1 classified FACT/INFERENCE/TEAM_KNOWLEDGE honestly,
        no `relationships` since no sibling node is merged) using the `concept.md` template
        shape: definition, boundaries/non-goals (explicitly excluding #1144's structured-field
        specifics and #1136's datastore-tracing-policy specifics), the per-surface picture from
        STEP 1, and a scope-and-omissions section naming both what is out of scope and what was
        expected to verify and could not.
        done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0.

STEP 3  [needs 2]  Re-run validate.py after any fix and iterate until clean; this step exists
        separately from STEP 2 because a first draft is not expected to pass on the first try
        given the citation-shape and evidence-class rules in `AGENTS.md`.
        done when: `python3 launchpad/project-intelligence/corpus/validate.py` exits 0 with no
        further edits pending.

STEP 4  [needs 3]  Earn the verify-gate stamp and commit. Run the corpus unittest suite as the
        sole command in its own tool call, confirm `OK`, then commit in a separate call.
        done when: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`
        reports `OK`, and `git commit -s -m "docs(corpus): document logging (#1139)"` succeeds.

STEP 5  [needs 4]  Self-review: re-read the diff against issue #1139's DoD checklist line by
        line, confirm no second canonical document was created, confirm `validate.py` still
        exits 0.
        done when: every DoD bullet is checked off against the actual diff and `validate.py`
        exits 0 on a final run.

PARALLEL  None — one file, five sequential steps.

GATES     `validate.py` (STEP 2/3) and the corpus unittest suite (STEP 4), run locally in this
          worktree. No push, no PR — a separate integration step cherry-picks this commit into
          a shared batch PR, so `review-adjudicate` and cross-model final review are explicitly
          deferred to that later stage, not run here.

BUDGET    STEP 1 and STEP 2 together. The hard part is scoping an umbrella "logging" concept
          node honestly against two sibling tasks (#1144 structured-logging, #1136
          datastore-tracing) that already claim the detailed territory, without either
          duplicating their content or leaving the umbrella node too thin to define the term.

OPEN      Whether `buzz-admin` and desktop-tauri's `managed_agents` module calling
          `tracing::warn!`/`tracing::info!` with no subscriber init anywhere in those crates
          means those events are silently dropped is a reasonable INFERENCE from the `tracing`
          crate's documented dispatcher behavior, not something this repository's own code
          proves directly — recorded as INFERENCE with confidence, not FACT.

LEFT OUT  Any second authored document. #1144's structured-field logging specifics and #1136's
          datastore-tracing policy-macro specifics — named as siblings and excluded by scope,
          not folded in. Any change to runtime code, `node.schema.json`, or `AGENTS.md`.
          Deciding whether the admin/desktop-tauri subscriber gap is a product bug — that is a
          design question for elsewhere, not this node's job; the node records the observation
          and its confidence, not a verdict.
