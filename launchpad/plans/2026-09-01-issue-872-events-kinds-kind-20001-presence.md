Issue #872 — task: document events/kinds/kind-20001-presence.md
Stated size: no `Size` line on issue #872; dispatch instructions explicitly cap this plan at 5 steps for a small single-document task  →  cap: 5 steps

ALREADY TRUE  (verified against git and the repository, not notes)
  - `launchpad/docs/corpus/events/kinds/kind-20001-presence.md` does not exist (`test -f` → DOES NOT EXIST; `launchpad/docs/corpus/events/` has no files at all).
  - No sibling event-kind corpus document exists yet anywhere under `launchpad/docs/corpus/`; this is the first `events/kinds/*` node.
  - `launchpad/docs/corpus/templates/event-kind.md` (id `corpus-template-event-kind`) is merged on `origin/launchpad` and is loadable by the validator (only `schema/` is excluded from validation, confirmed by reading `launchpad/project-intelligence/corpus/validate.py`'s `EXCLUDED_TOP_LEVEL_DIRS = {"schema"}`), so a `relationships: implements -> corpus-template-event-kind` edge resolves on the merge target.
  - `node.schema.json`'s `type` enum contains `interfaces-events`, and the merged event-kind template's own required-section 1 states a real instance node must carry `type: interfaces-events`.
  - `crates/buzz-core/src/kind.rs` defines `pub const KIND_PRESENCE_UPDATE: u32 = 20001;` (doc comment: "Ephemeral: user presence update (online/away/offline)."), inside the `20000–29999` ephemeral range (`EPHEMERAL_KIND_MIN`/`EPHEMERAL_KIND_MAX`), and it is not a member of `AUTHOR_ONLY_KINDS`, `P_GATED_KINDS`, `SHARED_GATED_KINDS`, `RESULT_GATED_KINDS`, or `is_relay_only_kind`/`is_command_kind`.
  - The current worktree's `HEAD` equals `origin/launchpad` at `a8b5021efb92264e724366d08b47b2a3839eb90a` (`git rev-parse HEAD` and `git rev-parse origin/launchpad` both return that SHA), so no merge-order drift risk exists for this branch.
  - `crates/buzz-relay/src/handlers/event.rs` (`handle_ephemeral_event`, `is_ephemeral` dispatch) and `crates/buzz-relay/src/handlers/ingest.rs` (`requires_h_channel_scope`, `required_scope_for_kind`, the HTTP-transport rejection at the WS-only check) and `crates/buzz-pubsub/src/presence.rs` (`set_presence`/`clear_presence`, `PRESENCE_TTL_SECS = 180`) and `crates/buzz-relay/src/api/bridge.rs` (`synthesize_presence`) have all been read directly for this task and are the evidence base for the sections below.

STEP 1  Draft `launchpad/docs/corpus/events/kinds/kind-20001-presence.md` end to end: front matter (id `events-kinds-kind-20001-presence`, type `interfaces-events`, status `draft`, origin `launchpad`, audiences, one `implements` relationship to `corpus-template-event-kind`) plus a body covering every required section from the event-kind template and every Definition-of-done bullet from issue #872: kind identity/number/name, ephemeral classification cross-checked against `kind.rs`, tag shape (none required — channel-less, no `h` tag, `requires_h_channel_scope` is false), content semantics (bare status string or legacy `{"status":...}` JSON, 128-byte truncation), access control (client-authored, WS-only transport, no scope-allowlist membership but a blanket `MessagesWrite` check for non-empty scopes), storage (Redis-only, 180s TTL, never Postgres), fanout (global ephemeral Redis pub/sub + local WS fan-out, no channel membership check), the query-bridge read path (`synthesize_presence`, relay-signed synthetic events), search/audit non-treatment, a worked example JSON event, and links to `kind.rs`, the handler files, and the named tests.                                    [independent]
        done when: the file exists at that path, `git diff --stat` shows exactly one new file under `launchpad/docs/corpus/`, and every evidence entry's `entry_class` is FACT or INFERENCE (with `confidence`) or TEAM_KNOWLEDGE (with `provided_by`), matching schema requirements.

STEP 2  Run `python3 launchpad/project-intelligence/corpus/validate.py` from the repository root and fix any reported error or schema violation, re-running until it exits 0.                        [needs 1]  ← RUNS HERE
        done when: the command's exit status is 0 and its output names no error against the new file.

STEP 3  Self-review the drafted node line by line against issue #872's Definition-of-done checklist and re-open every cited source to confirm it actually says what its statement claims (adversarial re-read per `standards/evidence.md`'s "does the cited source say this, or does it merely concern this?" test), fixing any citation or classification defect found.                        [needs 2]
        done when: every DoD bullet from the issue body has a corresponding, checkable section in the document, and no FACT entry rests on a source that was not reopened during this step.

STEP 4  Run the mandatory unit-test gate as the sole command in its own tool call: `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`.                        [needs 2]
        done when: the command's output includes `OK` and a zero exit status.

STEP 5  Stage exactly the new corpus document and this plan file, then commit with `git commit -s -m "docs(corpus): document kind 20001 presence event (#872)"`.                        [needs 3, 4]
        done when: `git log -1 --format=%H` names a new commit, and `git show --stat HEAD` lists only the two intended paths.

PARALLEL  Step 1 is the only drafting step and is independent (no prior step to wait on). Steps 2–5 are strictly sequential: validation needs the drafted file, self-review needs a passing validator run to review against, the unit-test gate and the self-review both only need the validated draft (step 2) and could in principle run in parallel with each other, but step 5's commit needs both to have completed, so this plan keeps them sequential rather than fanning out two subagents for one single-file task. Nothing here is dispatched as a subagent — this is one document, authored and checked serially by the same session.

GATES  `corpus-review` is the applicable review skill for a drafted corpus node, but the dispatching instructions for this task do not call for it as a separate step — this plan's own step 3 performs the equivalent structural/evidence/atomicity self-review inline instead, per the task's explicit "self-review" step. `review-plan`/`review-code`/`review-tests` do not apply: this is a documentation-only change with no runtime code or tests being written. `qa` explore mode does not apply: there is no runtime interface to exercise, only a Markdown document and a deterministic validator.

BUDGET  Step 1 (the draft) is the step most likely to eat the budget: it requires synthesizing a large evidence ledger from several source files (`kind.rs`, `event.rs`, `ingest.rs`, `presence.rs`, `bridge.rs`) into correctly classified FACT/INFERENCE/TEAM_KNOWLEDGE entries without over- or under-claiming what each source establishes.

OPEN  Issue #872 does not say whether `KIND_PRESENCE_SNAPSHOT` (40902, the relay-signed bulk-presence sidecar kind referenced alongside 20001 in `bridge.rs`'s `synthesize_presence`) needs its own corpus node, or whether this node may describe it in passing. This plan treats it as a boundary-only mention (named, not documented) to respect "one node, one idea," and leaves filing a separate task for kind 40902 to a human/reviewer decision rather than deciding it here.

LEFT OUT  A `docs/nips/NIP-XX.md` custom-NIP proposal document for kind 20001 is out of scope — issue #872 asks only for the corpus node, and no such proposal file exists today (confirmed: no presence-related file under `docs/nips/`); this plan does not create one. Editing `kind.rs`, the relay handlers, or any test file is out of scope — this is a documentation-only task and the issue's own "Out of scope" section forbids changing runtime product behavior. Backfilling a `relationships` edge to a future `kind-40902-presence-snapshot` node is left out because that node does not exist yet.
