Issue #1156 — task: document the Nostr event as a corpus concept node
Stated size: no `Size` line on the issue; scope stated unambiguously as exactly one
hand-authored canonical corpus document -> cap: 5 steps

ALREADY TRUE  (verified by direct read in worktree
`/home/serina/Launchpad/buzz/__worktrees/task-1156-event`, HEAD `29ca9b189bd3f639ba09c972b57c70538c0860c6`,
confirmed present with `git cat-file -e`)

  `launchpad/docs/corpus/layers/protocol/` does not exist on `origin/launchpad` — `ls
  launchpad/docs/corpus/layers/` returns `compute configuration data lifecycle
  observability` only. No sibling in Feature #609 is merged, so no `layers-protocol-*`
  id is a legal relationship target.

  `crates/buzz-core/src/verification.rs` is 71 lines and defines exactly one public
  function, `verify_event(&Event) -> Result<(), VerificationError>`, which checks
  `event.verify_id()` then `event.verify_signature()`. Its `EventId::new` reconstruction
  call takes `(&event.pubkey, &event.created_at, &event.kind, &event.tags,
  &event.content)` — five fields, with `id` and `sig` the two derived ones.

  `crates/buzz-relay/src/handlers/ingest.rs` `ingest_event_inner` runs its checks in this
  order, read directly at lines 2160-2280: community serving fence; categorical kind
  rejections (AUTH, membership notifications, HTTP-forbidden kinds, `is_relay_only_kind`);
  `verify_event` via `spawn_blocking`; `MAX_TIMESTAMP_DRIFT_SECS = 900`; then
  `MAX_EVENT_CONTENT_BYTES = 256 * 1024`; then `event.pubkey != *auth.pubkey()` identity
  binding (gift wrap excepted); then scope resolution. An in-file comment at that point
  states command kinds are routed "AFTER signature verification, timestamp check,
  pubkey/auth match, and scope validation — never before."

  That ordering is already documented as a numbered pipeline by the MERGED node
  `architecture-flows-event-ingestion`
  (`launchpad/docs/corpus/architecture/flows/event-ingestion.md`, steps 1-6). The merged
  `architecture-principles-signed-events` owns the id/sig invariant itself and its
  enforcement points. Both must be linked, not restated.

  `crates/buzz-cli/src/client.rs` `normalize_events` emits exactly `id`, `pubkey`, `kind`,
  `content`, `created_at`, `tags`, and adds `sig` only when the source value is a string —
  so the root contributor guide's "seven canonical signed Nostr event fields" claim holds,
  with the documented caveat that a non-string or absent `sig` is dropped rather than
  defaulted. That sentence lives in `AGENTS.md`; root `CLAUDE.md` is a symlink to it, and
  the phrase wraps across two lines, so a single-line grep for it misses.

  All seven names are real `nostr::Event` struct fields, reachable across the workspace:
  `event.id` 290, `event.pubkey` 142, `event.kind` 110, `event.content` 127,
  `event.created_at` 70, `event.tags` 116, `event.sig` 9 occurrences under `crates/`.

  `crates/buzz-relay/src/protocol.rs` deserializes the second array element of an `EVENT`
  frame with `serde_json::from_value::<Event>`, so a JSON object the `nostr` crate cannot
  deserialize never becomes an `Event` at all.

  `docs/nips/` contains only Buzz's own two-letter NIPs; `find . -name 'NIP-0*.md' -o -name
  'NIP-1*.md' -o -name 'NIP-4*.md'` returns nothing, so no upstream NIP-01 text is citable
  as a repo path. `docs/nips/NIP-AO.md` does carry a full seven-field event object example.

  The `nostr` 0.44 crate source is not on this machine — `~/.cargo/registry` does not
  exist and no `vendor/` directory is present — so the `Event` struct's own definition
  could not be opened.

STEP 1  Draft front matter for `launchpad/docs/corpus/layers/protocol/event.md`.  [independent]
        `id: layers-protocol-event`, `type: layers`, `status: draft`, `origin: launchpad`,
        `audiences: [agent, developer]`. Ledger: exactly one commit-only FACT recording
        `29ca9b189bd3f639ba09c972b57c70538c0860c6`; one FACT per substantive body claim,
        each citing a bare repo path opened during ALREADY TRUE above; the upstream-NIP
        absence as a FACT on a `find(...) -> ...` tool-result citation in
        `identifier(args) -> result` shape; anything resting only on issue text as
        TEAM_KNOWLEDGE with `provided_by`.
        done when: every FACT's citation is a path read during ALREADY TRUE, there is
        exactly one commit-only FACT, and no `confidence` sits on a FACT or
        TEAM_KNOWLEDGE entry.

STEP 2  Write the body against `templates/concept.md`'s required sections.       [needs 1]
        Definition first (one sentence, then the seven fields as one object and how the
        five authored fields determine the two derived ones), then Use cases, then a
        boundary section, then Scope and omissions. Explain the object as a whole; do not
        state the id preimage serialization (#1152), the Schnorr scheme (#1166), the kind
        integer registry (#1153), tag grammar (#1155), the `["EVENT", ...]` frame (#1154),
        or ephemeral/replaceable lifecycles (#1151/#1163/#1164).
        done when: the body contains no numbered id-computation algorithm, no kind-number
        table, no tag-letter table, and no restatement of `architecture-flows-event-ingestion`'s
        numbered pipeline — it links there instead.

STEP 3  Add relationships, each grepped from the merged-id list first.           [needs 2]
        Candidate targets: `architecture-principles-signed-events`,
        `architecture-flows-event-ingestion`, `layers-data-postgres-events-table`,
        `implementation-crates-buzz-core`, `implementation-crates-buzz-relay`,
        `architecture-context-nostr-network`, `verification-contracts-nostr`. Type
        `references` unless a stronger edge is genuinely true.
        done when: every `relationships[].target` appears in
        `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus` output as a
        merged node's id, and no `layers-protocol-*` target is present.

STEP 4  Write Scope and omissions carrying both required halves.                 [needs 2]
        Half one: what this node does not cover and who owns it — one row per sibling
        issue by subject. Half two: what was expected and could not be verified — the
        `nostr` crate `Event` struct definition (not on disk), the absence of upstream NIP
        text in the repository, and any test asserting the seven-field normalization
        contract that was not located.
        done when: the section has two clearly separated parts and names at least the
        unopened `nostr` crate as a verification gap.

STEP 5  Validate, self-review, stamp, commit.                                    [needs 1,2,3,4]
        Run `python3 launchpad/project-intelligence/corpus/validate.py` to exit 0. Re-read
        every evidence entry against its cited file BEFORE stamping. Then run
        `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p
        "test_*.py"` as the sole foreground command with a 600000 ms timeout, confirm the
        final line is `OK`, and only then `git add` + `git commit -s`.
        done when: validate.py exits 0, the unittest run ends `OK`, and the commit exists
        on `task/1156-event` with no push and no PR.
