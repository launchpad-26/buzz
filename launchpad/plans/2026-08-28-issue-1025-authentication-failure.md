# Issue #1025 — layers/authentication/authentication-failure.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, and `launchpad/docs/corpus/templates/concept.md` are merged on `origin/launchpad`. `launchpad/docs/corpus/layers/authentication/authentication-failure.md` does not exist yet — confirmed via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`. The issue's DoD tail bullets (define the term in one sentence, state boundaries/non-goals, link to related concepts/implementation/verification, examples clarify without introducing a second concept) match `templates/concept.md`'s required-sections list exactly, so this is a `concept`-template node placed under the `layers` corpus surface (per the node's path). Two sibling nodes already merged are directly relevant and must not be duplicated: `architecture-flows-websocket-authentication` (the full NIP-42 handshake procedure, success and failure) and `architecture-principles-fail-closed-boundaries` (the fail-closed convention this node's failure paths follow).

STEP 1  Gather evidence: read `crates/buzz-auth/src/error.rs` (`AuthError` enum), `crates/buzz-auth/src/nip42.rs` (`verify_nip42_event`), `crates/buzz-auth/src/nip98.rs` (`verify_nip98_event`), `crates/buzz-relay/src/handlers/auth.rs` (`handle_auth`'s ban/allowlist/relay-membership gates and their denial messages), `crates/buzz-relay/src/handlers/event.rs` and `crates/buzz-relay/src/handlers/count.rs` (the "auth-required: not authenticated" gate on unauthenticated writes/reads), `crates/buzz-relay/src/connection.rs` (`AuthState` enum), and the HTTP/NIP-98 call sites in `crates/buzz-relay/src/api/bridge.rs` and `crates/buzz-relay/src/api/git/transport.rs` (401 responses). ← RAN HERE

STEP 2  [needs 1] Write front matter: id `layers-authentication-authentication-failure`, type `layers`, status `draft`, origin `launchpad`, audiences `[agent, developer, reviewer]`, evidence ledger with the provenance FACT plus one entry per substantive claim (classified honestly — FACT for opened code, no INFERENCE/TEAM_KNOWLEDGE unless genuinely reasoned/attributed), and `relationships: [references -> architecture-flows-websocket-authentication, references -> architecture-principles-fail-closed-boundaries]` (both confirmed present on `origin/launchpad`).

STEP 3  [needs 2] Write the body per `templates/concept.md`'s required sections: a one-sentence definition of authentication failure, its boundary against authorization failure (allowlist/relay-membership/ban/insufficient-scope, which run only *after* identity is established) and against the sibling flow node (procedural detail lives there, not here), a short "use cases" section (why a reader needs this distinction), 2-3 concrete illustrative examples (challenge mismatch, expired timestamp, NIP-98 signature failure) used only to clarify — not a second catalogue — and the required scope-and-omissions section.

STEP 4  [needs 3] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 5  [needs 4] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole prior command to earn the verification stamp, then commit the plan + document in a separate call, push, and open a draft PR.

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"` must report `OK`. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner's review — not run here; this PR opens as a draft and says so explicitly.

BUDGET: small — one document, no code changes, evidence gathering scoped to the `buzz-auth` crate plus a handful of `buzz-relay` handler/API call sites already located via RepoQL.

OPEN: Whether the relay's current conflation of authentication-failure and authorization-failure into the same `AuthState::Failed` state and overlapping `"auth-required:"`/`"restricted:"` message prefixes deserves its own corpus node (e.g. a sibling "authorization-failure" concept) is not this task's call — it is named as a boundary/gap in the body's scope section, not resolved or filed as a new issue, since the parent Feature (#607) already tracks a fixed slate of child issues and this task's DoD does not ask for new issue filing.

LEFT OUT: No new `relationships` beyond the two confirmed-merged sibling nodes above. No claim about the desktop/mobile/CLI client-side handling of an auth failure (out of scope for a relay-side authentication-failure concept; the client's `WsClientError::AuthFailed` is already covered by the flow node's own evidence ledger). No attempt to add or change relay code, error messages, or metrics — this task documents current behavior only.
