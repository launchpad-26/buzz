# Issue #961 — ingestion/invariant-extraction.md

ALREADY TRUE: `launchpad/docs/corpus/schema/node.schema.json`, `launchpad/docs/corpus/AGENTS.md`, `launchpad/docs/corpus/templates/procedure.md` (id `corpus-template-procedure`) and `launchpad/docs/corpus/agents/invariants.md` (id `agents-invariants`) are merged on `origin/launchpad`. No `ingestion/` directory exists yet on `origin/launchpad` (verified via `git ls-tree -r --name-only origin/launchpad -- launchpad/docs/corpus`) — this is the first node in that family to land. Sibling `ingestion/concept-extraction.md` (#955) exists only as a local unmerged commit in a sibling worktree and is not a valid relationship target.

STEP 1  Gather evidence: re-read `agents/invariants.md` (merged) as the flagship worked precedent — it states outright that it extracted I1-I10 out of `AGENTS.md`'s connected prose, giving each a stable id. Confirm real, current worked examples of the other three source shapes the issue names: a code comment claiming an invariant (`crates/buzz-core/src/tenant.rs`'s "## The fence" module doc), a test encoding a MUST (`crates/buzz-audit/src/hash.rs`'s `storage_precision_timestamps_survive_a_database_round_trip`), and a schema constraint (`launchpad/docs/corpus/schema/node.schema.json`'s `entry_class` conditional `allOf`/`if`/`then` rules). Read `templates/invariant.md` in full for the downstream template this node's output feeds, and read #955's local content in full for the boundary. ← RUNS HERE

STEP 2  [needs 1] Write front matter (schema-valid: id `ingestion-invariant-extraction`, type `ingestion`, status `draft`, origin `launchpad`, audiences `[agent, reviewer]`, relationships: `references` → `corpus-agents` and `agents-invariants`, `implements` → `corpus-template-procedure`) and the body per `templates/procedure.md`'s required sections: Overview, Before you start, one numbered task sequence (recognize a MUST-shaped constraint, branch by source shape with the four real worked examples, assign a stable citable id, hand off to either a new `templates/invariant.md`-shaped node or an evidence entry), See also, an explicit Boundary against #955 (general concepts, not MUST-shaped constraints) and against `templates/invariant.md` (documents one already-identified invariant; this node is the step before that), Relationships, and Scope and omissions.

STEP 3  [needs 2] Run `python3 launchpad/project-intelligence/corpus/validate.py`; fix and re-run until exit 0.

STEP 4  [needs 3] Run the corpus unittest suite (`python3 -m unittest discover -s launchpad/project-intelligence/corpus/tests -p "test_*.py"`) as the sole prior command to earn the verification stamp, then commit in a separate call. No push, no PR (per batch instructions — stop after commit).

PARALLEL: none — single file, single task.

GATES: `python3 launchpad/project-intelligence/corpus/validate.py` must exit 0. `review-adjudicate` and the cross-model final review pass are deferred to the batch owner — this task runs its own self-review (`review-code` skill if reachable) before committing.

BUDGET: small — one document, no code changes; evidence gathering scoped to one merged corpus node, one template, four already-located source files, and the sibling #955 worktree file.

OPEN: Whether `agents-invariants` (a policy-shaped node) is an unusual but legitimate `references` target for a procedure-shaped node — `relationships.schema.json`'s `references` directionality ("source cites target as supporting context") does not restrict target shape, and this node genuinely leans on `agents-invariants` as its worked precedent throughout, the same way `ingestion/concept-extraction.md` (#955, unmerged) declared `references: corpus-agents` despite `corpus-agents` not being reference/concept-shaped either.

LEFT OUT: No relationship edge to `ingestion/concept-extraction.md` (#955) — not merged on `origin/launchpad`, so not a valid target regardless of how closely related the two subjects are. No relationship edge to `templates/invariant.md` — it is discussed at length in prose (Boundary, See also) but the procedure this node documents does not `implement` or structurally `depend-on` it; the downstream authoring step is free-standing, per the same reasoning `agents/invariants.md` and `ingestion/concept-extraction.md` both used for their own unlinked-but-discussed neighbors. No attempt to decide, for either of the four worked examples, whether it should become its own corpus node — that decision belongs to whoever picks up the candidate, not this extraction-procedure task.
