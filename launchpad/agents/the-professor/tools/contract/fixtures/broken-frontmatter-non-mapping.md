---
- just
- a
- list
---

## Frontmatter that parses but isn't a mapping

This page's frontmatter block is valid YAML, but it parses to a list rather
than a mapping -- a distinct failure branch from both "no frontmatter block"
and "not valid YAML" (opinion).
