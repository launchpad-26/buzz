---
id: "specifications-normative-documents"
type: "governance"
status: "draft"
origin: "launchpad"
audiences:
  - "agent"
  - "developer"
  - "reviewer"
evidence:
  - statement: "This document was generated by launchpad/project-intelligence/corpus/indexes.py from 205 canonical corpus node(s); input digest sha256:224e78b5d4dcbb6f840c2c3da2bd815237b37a523b4108199aba621ffe9a7301."
    entry_class: "FACT"
    evidence:
      - "launchpad/project-intelligence/corpus/indexes.py"
  - statement: "The subject-specific listing was produced by builder 'normative-documents'; the framework rendered the front matter and body skeleton."
    entry_class: "FACT"
    evidence:
      - "launchpad/project-intelligence/corpus/index_defs/normative_documents.py"
  - statement: "At input digest sha256:224e78b5d4dcbb6f840c2c3da2bd815237b37a523b4108199aba621ffe9a7301, exactly 0 canonical node(s) in `ctx.valid_nodes` have a corpus-root-relative path beginning `specifications/`; of those, 0 carry front-matter `type: governance` and are excluded by this builder's rule (b); of the remainder, exactly 0 case-insensitively name RFC 2119, RFC 8174, or BCP 14 in their own file text and are listed here. The candidate count is 0 today not because rule (b) or (c) excluded anything, but because `indexes.py`'s own framework excludes every registered builder's output path -- including `specifications/INDEX.md` (#1302) and this builder's own output -- from `ctx.valid_nodes` before any builder's generate(ctx) runs; walking `validate.load_nodes` directly (before that exclusion) still finds `specifications/INDEX.md` on disk."
    entry_class: "FACT"
    evidence:
      - "launchpad/project-intelligence/corpus/validate.py"
  - statement: "templates/specification.md's own evidence ledger reports that of Buzz's 16 docs/nips/*.md files, 7 (NIP-AA, NIP-CW, NIP-DV, NIP-IA, NIP-MP, NIP-PL, NIP-WP) cite RFC 2119 alone, 9 (including NIP-AM.md) declare no normative-language convention at all, and zero cite RFC 8174 or 'BCP 14' by name -- the same keyword set and methodology this builder's rule (c) reuses, applied to actual specifications/ corpus nodes rather than to docs/nips/*.md prose."
    entry_class: "FACT"
    evidence:
      - "launchpad/docs/corpus/templates/specification.md"
relationships:
  - type: "references"
    target: "corpus-agents"
  - type: "implements"
    target: "corpus-template-generated-index"
  - type: "references"
    target: "corpus-template-specification"
---

# Normative documents index: specifications declaring a normative-language convention

> **Generated -- do not edit by hand.** Produced by `launchpad/project-intelligence/corpus/indexes.py`
> (builder `normative-documents`), from the canonical corpus nodes under the
> corpus root, excluding every registered generated output. Edits made
> directly to this file are overwritten on the next regeneration; change
> the generator or its inputs instead.

## Generator

- **Script**: `launchpad/project-intelligence/corpus/indexes.py` (builder `normative-documents`, `launchpad/project-intelligence/corpus/index_defs/normative_documents.py`)
- **Inputs**: the 205 canonical corpus node(s) discovered by `validate.py`'s
  `discover_markdown_files` contract (sorted walk, `schema/` excluded, symlinks resolved),
  minus the 29 registered generated output path(s)
- **Ordering**: listing rows sorted lexicographically by corpus-root-relative path
- **Source revision**: input digest `sha256:224e78b5d4dcbb6f840c2c3da2bd815237b37a523b4108199aba621ffe9a7301` over the
  sorted (relative path, bytes) of the canonical inputs

## Inclusion and exclusion rules

This index includes:
- every canonical corpus node whose corpus-root-relative path begins with the literal prefix `specifications/` (the same base set #1302's specifications-index builder walks), AND whose front-matter `type` is not `governance` (excludes meta/index content living in that path, e.g. `specifications/INDEX.md` itself), AND whose raw file text (front matter or body) case-insensitively contains one of `RFC 2119`, `RFC2119`, `RFC 8174`, `RFC8174`, `BCP 14`, `BCP14` -- the identical keyword set `templates/specification.md`'s own evidence ledger greped `docs/nips/*.md` for

This index deliberately excludes:
- any node outside the `specifications/` path prefix, regardless of type or content
- `governance`-typed nodes under `specifications/` (meta/index content, e.g. `specifications/INDEX.md`), even when their text happens to mention RFC 2119, RFC 8174, or BCP 14 while discussing the convention rather than declaring it for themselves
- specification-shaped nodes under `specifications/` that use MUST/SHOULD/MAY without naming which normative-language convention they follow -- using the words is not the same as declaring the convention, per `templates/specification.md` required section 4
- The `schema/` subtree, for the same reason `validate.py` excludes it: schema-testing
  infrastructure, not corpus content
- Every registered generated output path (`GLOSSARY.md`, `INDEX.md`, `decisions/INDEX.md`, `generated/api-index.md`, `generated/capability-index.md`, `generated/code-to-doc-map.md`, `generated/concept-index.md`, `generated/configuration-index.md`, `generated/corpus-index.md`, `generated/coverage.md`, `generated/crate-index.md`, `generated/database-index.md`, `generated/decision-index.md`, `generated/dependency-graph.md`, `generated/doc-to-code-map.md`, `generated/documentation-graph.md`, `generated/event-kind-index.md`, `generated/layer-index.md`, `generated/nip-index.md`, `generated/orphaned-docs.md`, `generated/provenance-index.md`, `generated/stale-docs.md`, `generated/test-index.md`, `generated/test-to-doc-map.md`, `specifications/INDEX.md`, `specifications/draft-documents.md`, `specifications/implemented-documents.md`, `specifications/normative-documents.md`, `specifications/superseded-documents.md`), so no generated
  view feeds itself

## Interpreting "normative"

`templates/specification.md`'s own scope paragraph calls a specification node
"the complete normative definition of one protocol" -- read on its own, that
would make every node under `specifications/` normative by definition, which
would make this document a duplicate of #1302's `specifications/INDEX.md`
(every canonical node under that prefix, regardless of type or content).

**This document does not use that reading.** It narrows "normative" to
"declares a normative-language (RFC 2119 / RFC 8174 / BCP 14) convention" --
a checkable content claim, not a type label -- following the exact methodology
`templates/specification.md`'s own evidence ledger already used when it greped
`docs/nips/*.md` for the same keyword set and found 7 of 16 files citing RFC
2119 alone, 9 citing nothing, and none citing the current RFC 8174-amended BCP
14 boilerplate.

**What #1302's `specifications/INDEX.md` already covers**: every canonical
corpus node whose path lives under `specifications/`, regardless of type or
content -- the complete membership index of that subtree.

**What this document adds**: the narrower subset of that same membership which
is both specification-shaped (excludes `governance`-typed meta/index content,
such as `INDEX.md` itself) and which makes a checkable claim, in its own file
text, about which normative-language convention its MUST/SHOULD/MAY usage
follows. A reader wanting the full subtree listing reads #1302's `INDEX.md`; a
reader wanting only the documents that have actually declared a
normative-language convention reads this document instead.

## Documents declaring a normative-language convention

No canonical corpus node currently qualifies. At the time of
writing, no node reaches rules (b) or (c) at all: the only
on-disk file under `specifications/` besides this document's own
output is `INDEX.md` (#1302's `specifications-index` builder),
and the generator framework itself already excludes every
registered builder's output path from canonical inputs before
any builder's inclusion rule runs -- so `INDEX.md` is gone from
the candidate set before this builder's own `type: governance`
exclusion (rule b) or normative-keyword check (rule c) ever
sees it. This is an honest empty listing, not an omission. It
populates automatically once a real, non-`governance`-typed
specification node is authored under that prefix and declares
its own normative-language convention, per
`launchpad/docs/corpus/templates/specification.md`
(`corpus-template-specification`) required section 4.

## Relationships

- references: corpus-agents
- implements: corpus-template-generated-index
- references: corpus-template-specification

## Scope and omissions

**This node covers** a generated listing of canonical corpus nodes under the specifications/ prefix that declare a normative-language (RFC 2119 / RFC 8174 / BCP 14) convention in their own text, as of input
digest `sha256:224e78b5d4dcbb6f840c2c3da2bd815237b37a523b4108199aba621ffe9a7301`.

**It does not cover, and these are gaps rather than silence:**

- Hand-authored corpus content and policy -- the canonical nodes themselves own that.
- The full membership of the `specifications/` subtree regardless of type or content -- that is #1302's `specifications/INDEX.md`, not this document.
- Whether a listed document's normative body actually uses its declared convention correctly -- this listing only checks that a convention is named in the node's own text, not that its MUST/SHOULD/MAY usage is internally consistent with it.

**Expected but not verified when this node was written:**

- Whether this builder's three-part rule -- path prefix, non-`governance` type, then a case-insensitive RFC 2119/8174/BCP 14 keyword match -- correctly discriminates a real specification node has not been observed against any real specification content, because none exists in this corpus yet outside this builder's own test fixtures. The rule's behavior on an actual specification body (rather than a fixture built to exercise it) is untested.
