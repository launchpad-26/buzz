---
id: bad-missing-evidence-field-node
type: governance
status: active
origin: launchpad
audiences:
  - agent
---

Invalid fixture: no top-level `evidence` field at all (distinct from
missing-evidence-fact.md and missing-evidence-inference.md, which supply an `evidence`
array whose one entry is missing its own citations). Confirms the top-level `evidence`
array itself is enforced as required, not merely its per-entry contents.
